# Context and Guidelines — caramello-web

> Not yet implemented. This module contains no code. The stack decision is recorded in the root [`docs/architecture.md`](../../docs/architecture.md) ("The frontend is a server-rendered web application; native packaging is out of scope"). The code standards and invariants for this module will be filled in as it is built.

Repository-wide rules (language, commits, configuration and environment variables, monorepo scope) live in the root `AGENTS.md` and must never be duplicated here.
