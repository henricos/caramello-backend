# Development setup — caramello-web

> Not yet implemented. This module contains no code, so there is nothing to install or run yet.

Once the module exists, this document will cover prerequisites, installation, the OIDC provider used in development, configuration through the committed `.env.development` (see "Configuration and environment variables" in the root `AGENTS.md`), how to run the dev server, how to verify the login flow, tests, and troubleshooting.
