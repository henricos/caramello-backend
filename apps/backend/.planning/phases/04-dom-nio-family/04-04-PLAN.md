---
phase: 04-dom-nio-family
plan: "04"
type: execute
wave: 4
depends_on: ["03"]
files_modified:
  - src/caramello/families/operations.py
  - src/caramello/shared/auth.py
autonomous: true
requirements: [FAMILY-01, FAMILY-02, FAMILY-03, FAMILY-07]
requirements_deferred: [FAMILY-04, FAMILY-05, FAMILY-06]  # Informativo (D-04) — diferimento para M2; rastreabilidade no frontmatter. Nenhum task deste plano (ou de qualquer outro da Phase 4) implementa estes requisitos.

must_haves:
  truths:
    - "src/caramello/families/operations.py primeira linha == # CARAMELLO-GENERATED: implemented (era stub no plano 04-03)"
    - "src/caramello/families/operations.py implementa as 6 operações de D-07 com lógica real (não NotImplementedError)"
    - "D-13: POST /families/registry cria FamilyMember com role='owner' para o criador; pré-cadastro via pre-register cria FamilyMember com role='member' no auto-join; validações verificam role == 'owner' via query"
    - "POST /families/registry: cria Family + FamilyMember(role='owner') na mesma transação (FAMILY-01)"
    - "GET /families/families: retorna apenas famílias do usuário autenticado via JOIN com family_member (FAMILY-02)"
    - "GET /families/families/{family_uuid}: retorna 200 se membro, 403 se não membro (FAMILY-03)"
    - "POST /families/families/{family_uuid}/pre-register: cria FamilyInvitation com status='pending_login' se owner, 403 caso contrário (D-07)"
    - "GET /families/families/{family_uuid}/members: lista todos os membros se o requisitante é membro, 403 caso contrário"
    - "DELETE /families/families/{family_uuid}/members/{user_uuid}: deleta FamilyMember se o requisitante é owner, 403 caso contrário (FAMILY-07)"
    - "src/caramello/shared/auth.py estende get_current_user: após JIT do User, busca FamilyInvitation pendente por email; se existe, cria FamilyMember(role='member') e atualiza invitation.status='joined' (D-02)"
    - "Todos os 5 testes funcionais de tests/test_family_operations.py (test_registry_..., test_list_..., test_get_family_detail_..., test_pre_register_..., test_remove_member_...) PASSAM (XFAIL removido)"
    - "tests/test_auth.py::test_auto_join_on_login PASSA (XFAIL ou importorskip removido)"
    - "tests/test_family_operations.py::test_operations_annotation_is_implemented PASSA (XFAIL removido)"
    - "Suite pytest completa verde sem novos FAILs"
    - "ruff + mypy strict passam em src/caramello/families/operations.py e src/caramello/shared/auth.py"
  artifacts:
    - path: "src/caramello/families/operations.py"
      provides: "6 operações de negócio do domínio families implementadas (FAMILY-01, 02, 03, 07)"
      exports: ["router (APIRouter)", "registry_family", "list_my_families", "get_family_detail", "pre_register_member", "list_members", "remove_member"]
      contains: "# CARAMELLO-GENERATED: implemented"
    - path: "src/caramello/shared/auth.py"
      provides: "get_current_user com auto-join transparente após JIT (D-02)"
      contains: "FamilyInvitation"
  key_links:
    - from: "POST /families/registry"
      to: "Family.id (via session.flush) → FamilyMember.family_id"
      via: "session.add(Family) → session.flush() → session.add(FamilyMember(role='owner')) → session.commit()"
      pattern: 'role="owner"'
    - from: "shared/auth.py get_current_user"
      to: "FamilyMember (auto-join)"
      via: "SELECT FamilyInvitation WHERE email AND status='pending_login' → INSERT FamilyMember(role='member') + UPDATE invitation.status='joined'"
      pattern: 'role="member"'
    - from: "Endpoints pre-register e remove-member"
      to: "HTTPException 403 quando role != 'owner'"
      via: "_require_owner helper consultando FamilyMember"
      pattern: "status_code=403"
---

<objective>
Implementar a camada de operações de negócio do domínio `families` (`src/caramello/families/operations.py`) sobrescrevendo o stub gerado pelo plano 04-03, e estender `src/caramello/shared/auth.py` com a lógica de auto-join (D-02) no fluxo `get_current_user`. Após este plano, os 6 endpoints de D-07 ficam funcionais e os requisitos FAMILY-01, FAMILY-02, FAMILY-03 e FAMILY-07 estão entregues.

**Sobre FAMILY-04, FAMILY-05, FAMILY-06 (deferidos):** os requisitos são apenas **marcados como deferidos para M2** em ROADMAP.md e REQUIREMENTS.md (conforme D-04). Eles NÃO são implementados aqui e por isso NÃO aparecem no campo `requirements` deste plano. Para rastreabilidade formal, são listados no campo `requirements_deferred` do frontmatter — **esse campo é informativo, não implica implementação.**

Purpose: este é o "GREEN" final da Phase 4. Os testes do plano 04-01 que estavam XFAIL/SKIP durante os planos 02 e 03 destrabam aqui. A anotação `CARAMELLO-GENERATED` muda de `stub` para `implemented` — o generator passa a respeitar este arquivo dali em diante.

Output:
- `src/caramello/families/operations.py` reescrito completo (anotação `implemented`)
- `src/caramello/shared/auth.py` com bloco de auto-join inserido após JIT
- Suite pytest 100% verde para a Phase 4
- Requisitos FAMILY-01/02/03/07 fechados; FAMILY-04/05/06 marcados como deferidos M2 (D-04) em ROADMAP.md e REQUIREMENTS.md — registro documental apenas, sem implementação. Rastreabilidade formal via `requirements_deferred` no frontmatter deste plano.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-dom-nio-family/04-CONTEXT.md
@.planning/phases/04-dom-nio-family/04-RESEARCH.md
@.planning/phases/04-dom-nio-family/04-PATTERNS.md
@.planning/phases/04-dom-nio-family/04-01-PLAN.md
@.planning/phases/04-dom-nio-family/04-02-PLAN.md
@.planning/phases/04-dom-nio-family/04-03-PLAN.md
@src/caramello/families/models.py
@src/caramello/families/operations.py
@src/caramello/families/router.py
@src/caramello/users/models.py
@src/caramello/users/operations.py
@src/caramello/shared/auth.py
@src/caramello/shared/database.py
@tests/test_family_operations.py
@tests/test_auth.py

<interfaces>
**Models (regenerados no plano 04-03 — referência):**

Em `src/caramello/families/models.py` devem estar declarados (gerados pelo DSL):
- `class Family(SQLModel, table=True)`: id, uuid, name, description, status, created_at, updated_at
- `class FamilyMember(SQLModel, table=True)`: user_id (PK, FK user.id), family_id (PK, FK family.id), role (str default "member"), joined_at
- `class FamilyInvitation(SQLModel, table=True)`: id, uuid, family_id (FK), inviter_id (FK), email (str), status (str default "pending_login"), created_at
- `class FamilyRead(SQLModel)`: uuid, name, description, status, created_at, updated_at
- `class FamilyCreate(SQLModel)`: name, description (opt), status (opt)
- `class FamilyInvitationRead(SQLModel)`: uuid, family_id, inviter_id, email, status, created_at
- `class FamilyInvitationCreate(SQLModel)`: family_id, inviter_id, email, status (opt)

**Schema de Read e Create para auxiliar a implementação:**

O endpoint POST /families/registry recebe `FamilyCreate` (somente name + description opcional). O retorno é `FamilyRead`.

O endpoint POST /families/{uuid}/pre-register recebe um body com apenas `email`. Como o DSL gerou `FamilyInvitationCreate` exigindo family_id e inviter_id, precisamos OU definir um Pydantic schema próprio dentro de operations.py OU aceitar um dict simples. Solução recomendada (alinhada com PATTERNS.md):

```python
from pydantic import BaseModel, EmailStr

class PreRegisterBody(BaseModel):
    email: EmailStr
```

Inteiramente local ao arquivo operations.py.

**O endpoint GET /members retorna a lista de FamilyMember. Como FamilyMember tem `user_id` e `family_id` como PKs, e não há FamilyMemberRead gerado (é link model — D-12), criar schema próprio em operations.py:**

```python
class FamilyMemberRead(BaseModel):
    user_uuid: UUID
    role: str
    joined_at: datetime
```

(A consulta vira um JOIN User x FamilyMember para resolver user_uuid a partir de user_id.)

**Padrão final de src/caramello/families/operations.py (a escrever via Write):**

Ver "Sub-task 1B" no bloco action abaixo para o conteúdo exato.

**Extensão shared/auth.py (auto-join):**

Inserir o bloco de auto-join APÓS o `SELECT User WHERE idp_sub` (linha ~188 do arquivo atual — que é o último SELECT antes do `return user`). Bloco esperado:

```python
# AUTO-JOIN (Phase 4 D-02): se existe FamilyInvitation pendente com este email,
# adicionar o usuário automaticamente como FamilyMember(role="member").
from caramello.families.models import FamilyInvitation, FamilyMember  # noqa: PLC0415 — import lazy para evitar ciclo

inv_result = await session.exec(
    select(FamilyInvitation).where(
        FamilyInvitation.email == email,
        FamilyInvitation.status == "pending_login",
    )
)
pending_inv = inv_result.first()
if pending_inv is not None:
    new_member = FamilyMember(
        user_id=user.id,
        family_id=pending_inv.family_id,
        role="member",
    )
    session.add(new_member)
    pending_inv.status = "joined"
    session.add(pending_inv)
    await session.commit()
```

Não usar `pg_insert ON CONFLICT` no FamilyMember aqui — o auto-join só roda no primeiro login (status == "pending_login"). Após o UPDATE para "joined", o SELECT seguinte não encontra mais a invitation. Se houver concorrência (mesmo user fazendo 2 requests simultâneas), o constraint composto (user_id, family_id) PRIMARY KEY garante que o INSERT duplicado falhe com erro distinguível — aceitar essa falha como race rara (cobertura defensiva pode ser adicionada depois).

**Verificação de ownership — helper privado em operations.py:**

```python
async def _require_owner(
    family_uuid: UUID,
    current_user: User,
    session: AsyncSession,
) -> tuple[Family, FamilyMember]:
    """Verifica que o current_user é owner da família com o uuid dado.
    Retorna (family, member) se sim; raise 403 se não.
    """
    result = await session.exec(
        select(Family, FamilyMember)
        .join(FamilyMember, FamilyMember.family_id == Family.id)
        .where(
            Family.uuid == family_uuid,
            FamilyMember.user_id == current_user.id,
            FamilyMember.role == "owner",
        )
    )
    row = result.first()
    if row is None:
        raise HTTPException(
            status_code=403,
            detail="Apenas owner pode realizar esta operação",
        )
    family, member = row
    return family, member
```

E um helper análogo para verificar membership (sem exigir owner) usado em GET detail e GET members:

```python
async def _require_member(
    family_uuid: UUID,
    current_user: User,
    session: AsyncSession,
) -> Family:
    result = await session.exec(
        select(Family)
        .join(FamilyMember, FamilyMember.family_id == Family.id)
        .where(
            Family.uuid == family_uuid,
            FamilyMember.user_id == current_user.id,
        )
    )
    family = result.first()
    if family is None:
        raise HTTPException(
            status_code=403,
            detail="Você não é membro desta família",
        )
    return family
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implementar src/caramello/families/operations.py com as 6 operações + helpers de owner/member</name>
  <files>src/caramello/families/operations.py, tests/test_family_operations.py</files>
  <read_first>
    - src/caramello/families/operations.py (stub gerado pelo plano 04-03 — primeira linha # CARAMELLO-GENERATED: stub)
    - src/caramello/families/models.py (verificar nomes exatos das classes geradas: Family, FamilyMember, FamilyInvitation, FamilyRead, FamilyCreate, FamilyInvitationRead, FamilyInvitationCreate)
    - src/caramello/users/operations.py (referência do padrão de operations.py com APIRouter próprio + Depends)
    - src/caramello/shared/auth.py (referência de padrão de Depends(get_current_user) e session async)
    - src/caramello/shared/database.py (assinatura de get_session)
    - tests/test_family_operations.py (contratos que vão destrabar — usar como guia das asserções)
    - .planning/phases/04-dom-nio-family/04-PATTERNS.md (seção src/caramello/families/operations.py — padrões exatos de helpers e endpoints)
    - .planning/phases/04-dom-nio-family/04-RESEARCH.md (Code Examples + Pattern 3 — verificação de ownership)
    - .planning/phases/04-dom-nio-family/04-CONTEXT.md (D-07 — lista exata dos 6 endpoints; D-13 — role "owner" vs "member")
  </read_first>
  <behavior>
    - POST /families/registry: aceita FamilyCreate → cria Family + FamilyMember(role="owner", user_id=current_user.id) na mesma sessão. Retorna FamilyRead (201).
    - **Nota RESOLVED (Open Question 1 do 04-RESEARCH.md):** `session.flush()` com SQLModel + asyncpg É seguro para obter `db_family.id` (autoincrement PK) antes do commit. Este é o padrão estabelecido em todo o codebase Caramello (ver `src/caramello/shared/auth.py` para padrão análogo de provisioning). Não há quirk especial — usar `await session.flush()` antes de criar o FamilyMember dependente.
    - GET /families/families: retorna list[FamilyRead] das famílias do usuário (JOIN com FamilyMember filtrado por user_id).
    - GET /families/families/{family_uuid}: retorna FamilyRead se user é membro; 403 caso contrário (helper _require_member).
    - POST /families/families/{family_uuid}/pre-register: aceita {email} → 403 se não-owner; senão cria FamilyInvitation(family_id, inviter_id=current_user.id, email, status="pending_login"). Retorna FamilyInvitationRead.
    - GET /families/families/{family_uuid}/members: 403 se não-membro; senão retorna list[FamilyMemberRead] (com user_uuid resolvido via JOIN).
    - DELETE /families/families/{family_uuid}/members/{user_uuid}: 403 se não-owner; senão deleta FamilyMember. Retorna {"ok": True} ou 204.
    - Anotação primeira linha: `# CARAMELLO-GENERATED: implemented` (não mais stub).
    - Testes que estavam XFAIL no plano 04-03 destrabam: remover os `pytest.xfail(...)` adicionados ao plano 04-03 para os 5 testes funcionais + test_operations_annotation_is_implemented.
  </behavior>
  <action>
    **Sub-task 1A — Confirmar nomes exatos dos schemas em families/models.py:**

    Antes de escrever operations.py, ler `src/caramello/families/models.py` (gerado no plano 04-03) e verificar:
    - Que `FamilyCreate`, `FamilyRead`, `FamilyInvitationCreate`, `FamilyInvitationRead` existem
    - Que `FamilyInvitation` tem o campo `email` (não `invitee_email`) e `status` com default "pending_login"
    - Se os schemas Read/Create foram gerados como classes ou se estão ausentes para link model (FamilyMember é link, sem FamilyMemberRead)

    Se algum nome ESPERADO ESTIVER DIFERENTE (ex: o generator emite `FamilyInvitationCreate` com nome ligeiramente outro), ajustar os imports e nomes no código abaixo conforme o que existe de fato. O código abaixo assume nomes canônicos do DSL.

    **Sub-task 1B — Reescrever src/caramello/families/operations.py via Write tool:**

    Conteúdo EXATO (substituir o stub completo):

    ```python
    # CARAMELLO-GENERATED: implemented
    """Operações de negócio do domínio families — Phase 4.

    Cobre:
      - FAMILY-01: POST /families/registry (criar família + tornar-se owner)
      - FAMILY-02: GET  /families/families (listar minhas famílias)
      - FAMILY-03: GET  /families/families/{uuid} (detalhe se membro, 403 senão)
      - D-07:      POST /families/families/{uuid}/pre-register (owner pré-registra email)
      - D-07:      GET  /families/families/{uuid}/members (lista membros se membro)
      - FAMILY-07: DELETE /families/families/{uuid}/members/{user_uuid} (owner remove)

    NÃO implementado nesta fase (D-04 — deferidos para M2):
      - FAMILY-04: POST /families/families/{uuid}/invitations (código convite reutilizável)
      - FAMILY-05: POST /families/invitations/{code}/join (solicitação de entrada)
      - FAMILY-06: PATCH /families/invitations/{id} (aprovar/rejeitar)
    """
    from __future__ import annotations

    from datetime import datetime
    from uuid import UUID

    from fastapi import APIRouter, Depends, HTTPException, status
    from pydantic import BaseModel, EmailStr
    from sqlmodel import select
    from sqlmodel.ext.asyncio.session import AsyncSession

    from caramello.families.models import (
        Family,
        FamilyCreate,
        FamilyInvitation,
        FamilyInvitationRead,
        FamilyMember,
        FamilyRead,
    )
    from caramello.shared.auth import get_current_user
    from caramello.shared.database import get_session
    from caramello.users.models import User

    router = APIRouter(prefix="/families", tags=["Family"])


    # ---------------------------------------------------------------------------
    # Schemas locais (não fazem parte da geração DSL — específicos das operações)
    # ---------------------------------------------------------------------------


    class PreRegisterBody(BaseModel):
        """Body de POST /families/{uuid}/pre-register — só precisa do email do convidado."""

        email: EmailStr


    class FamilyMemberRead(BaseModel):
        """Retorno de GET /families/{uuid}/members. Inclui user_uuid resolvido via JOIN."""

        user_uuid: UUID
        role: str
        joined_at: datetime


    # ---------------------------------------------------------------------------
    # Helpers de autorização (D-13 — verificação de role)
    # ---------------------------------------------------------------------------


    async def _require_owner(
        family_uuid: UUID,
        current_user: User,
        session: AsyncSession,
    ) -> tuple[Family, FamilyMember]:
        """Garante que o usuário é OWNER da família com o uuid dado.

        Retorna (family, member) se sim; raise 403 caso contrário.
        """
        result = await session.exec(
            select(Family, FamilyMember)
            .join(FamilyMember, FamilyMember.family_id == Family.id)
            .where(
                Family.uuid == family_uuid,
                FamilyMember.user_id == current_user.id,
                FamilyMember.role == "owner",
            )
        )
        row = result.first()
        if row is None:
            raise HTTPException(
                status_code=status.HTTP_403_FORBIDDEN,
                detail="Apenas owner pode realizar esta operação",
            )
        family, member = row
        return family, member


    async def _require_member(
        family_uuid: UUID,
        current_user: User,
        session: AsyncSession,
    ) -> Family:
        """Garante que o usuário é MEMBRO (qualquer role) da família com o uuid dado.

        Retorna Family se sim; raise 403 caso contrário.
        """
        result = await session.exec(
            select(Family)
            .join(FamilyMember, FamilyMember.family_id == Family.id)
            .where(
                Family.uuid == family_uuid,
                FamilyMember.user_id == current_user.id,
            )
        )
        family = result.first()
        if family is None:
            raise HTTPException(
                status_code=status.HTTP_403_FORBIDDEN,
                detail="Você não é membro desta família",
            )
        return family


    # ---------------------------------------------------------------------------
    # FAMILY-01: POST /families/registry
    # ---------------------------------------------------------------------------


    @router.post("/registry", response_model=FamilyRead, status_code=status.HTTP_201_CREATED)
    async def registry_family(
        family_in: FamilyCreate,
        session: AsyncSession = Depends(get_session),
        current_user: User = Depends(get_current_user),
    ) -> Family:
        """FAMILY-01: cria família e registra o usuário autenticado como owner."""
        db_family = Family.model_validate(family_in)
        session.add(db_family)
        # flush para obter db_family.id sem commitar — necessário para o FK do FamilyMember.
        # RESOLVED (Open Question 1 do 04-RESEARCH.md): session.flush() com SQLModel+asyncpg
        # é seguro para obter PK autoincrement antes do commit (padrão estabelecido).
        await session.flush()

        owner_member = FamilyMember(
            user_id=current_user.id,
            family_id=db_family.id,
            role="owner",
        )
        session.add(owner_member)
        await session.commit()
        await session.refresh(db_family)
        return db_family


    # ---------------------------------------------------------------------------
    # FAMILY-02: GET /families/families
    # ---------------------------------------------------------------------------


    @router.get("/families", response_model=list[FamilyRead])
    async def list_my_families(
        session: AsyncSession = Depends(get_session),
        current_user: User = Depends(get_current_user),
    ) -> list[Family]:
        """FAMILY-02: lista famílias onde o usuário autenticado é membro."""
        result = await session.exec(
            select(Family)
            .join(FamilyMember, FamilyMember.family_id == Family.id)
            .where(FamilyMember.user_id == current_user.id)
        )
        return list(result.all())


    # ---------------------------------------------------------------------------
    # FAMILY-03: GET /families/families/{family_uuid}
    # ---------------------------------------------------------------------------


    @router.get("/families/{family_uuid}", response_model=FamilyRead)
    async def get_family_detail(
        family_uuid: UUID,
        session: AsyncSession = Depends(get_session),
        current_user: User = Depends(get_current_user),
    ) -> Family:
        """FAMILY-03: retorna detalhes da família se o usuário é membro; 403 senão."""
        return await _require_member(family_uuid, current_user, session)


    # ---------------------------------------------------------------------------
    # D-07: POST /families/families/{family_uuid}/pre-register
    # ---------------------------------------------------------------------------


    @router.post(
        "/families/{family_uuid}/pre-register",
        response_model=FamilyInvitationRead,
        status_code=status.HTTP_201_CREATED,
    )
    async def pre_register_member(
        family_uuid: UUID,
        body: PreRegisterBody,
        session: AsyncSession = Depends(get_session),
        current_user: User = Depends(get_current_user),
    ) -> FamilyInvitation:
        """D-07: owner pré-registra um email para adesão automática (D-02 auto-join)."""
        family, _ = await _require_owner(family_uuid, current_user, session)

        invitation = FamilyInvitation(
            family_id=family.id,
            inviter_id=current_user.id,
            email=str(body.email),
            status="pending_login",
        )
        session.add(invitation)
        await session.commit()
        await session.refresh(invitation)
        return invitation


    # ---------------------------------------------------------------------------
    # D-07: GET /families/families/{family_uuid}/members
    # ---------------------------------------------------------------------------


    @router.get(
        "/families/{family_uuid}/members",
        response_model=list[FamilyMemberRead],
    )
    async def list_members(
        family_uuid: UUID,
        session: AsyncSession = Depends(get_session),
        current_user: User = Depends(get_current_user),
    ) -> list[FamilyMemberRead]:
        """D-07: lista todos os membros se o requisitante é membro da família."""
        family = await _require_member(family_uuid, current_user, session)

        result = await session.exec(
            select(User, FamilyMember)
            .join(FamilyMember, FamilyMember.user_id == User.id)
            .where(FamilyMember.family_id == family.id)
        )
        rows = list(result.all())
        return [
            FamilyMemberRead(
                user_uuid=user.uuid,
                role=member.role,
                joined_at=member.joined_at,
            )
            for user, member in rows
        ]


    # ---------------------------------------------------------------------------
    # FAMILY-07: DELETE /families/families/{family_uuid}/members/{user_uuid}
    # ---------------------------------------------------------------------------


    @router.delete(
        "/families/{family_uuid}/members/{user_uuid}",
        status_code=status.HTTP_200_OK,
    )
    async def remove_member(
        family_uuid: UUID,
        user_uuid: UUID,
        session: AsyncSession = Depends(get_session),
        current_user: User = Depends(get_current_user),
    ) -> dict[str, bool]:
        """FAMILY-07: owner remove membro da família; 403 caso contrário."""
        family, _ = await _require_owner(family_uuid, current_user, session)

        # Localizar o user-alvo
        target_result = await session.exec(
            select(User).where(User.uuid == user_uuid)
        )
        target_user = target_result.first()
        if target_user is None:
            raise HTTPException(status_code=404, detail="Usuário não encontrado")

        # Localizar o membership
        member_result = await session.exec(
            select(FamilyMember).where(
                FamilyMember.family_id == family.id,
                FamilyMember.user_id == target_user.id,
            )
        )
        target_member = member_result.first()
        if target_member is None:
            raise HTTPException(status_code=404, detail="Usuário não é membro desta família")

        await session.delete(target_member)
        await session.commit()
        return {"ok": True}
    ```

    **Sub-task 1C — Atualizar tests/test_family_operations.py removendo XFAILs do plano 04-03:**

    Os 5 testes funcionais + `test_operations_annotation_is_implemented` foram marcados com `pytest.xfail(...)` no plano 04-03. Remover essas linhas xfail agora — os testes devem PASSAR. Manter `pytest.importorskip("caramello.families.operations")` no início de cada teste (defesa em profundidade — não atrapalha quando o módulo existe).

    Especificamente, remover as linhas:
    ```python
    pytest.xfail("Plano 04-04 ainda não rodou — operations.py ainda é stub (NotImplementedError)")
    ```
    e
    ```python
    pytest.xfail("Plano 04-04 ainda não rodou — anotação ainda é stub")
    ```

    de tests/test_family_operations.py.

    **Sub-task 1D — Verificar:**

    ```bash
    cd /home/claude/work/caramello-api
    uv run ruff check src/caramello/families/operations.py tests/test_family_operations.py
    uv run mypy src/caramello/families/operations.py 2>&1 | tail -20
    uv run pytest tests/test_family_operations.py -v
    ```

    Esperado: todos os 7 testes de test_family_operations.py PASSAM e mypy retorna sem erros em families/operations.py.
  </action>
  <verify>
    <automated>cd /home/claude/work/caramello-api &amp;&amp; uv run ruff check src/caramello/families/operations.py tests/test_family_operations.py &amp;&amp; uv run mypy src/caramello/families/operations.py &amp;&amp; head -1 src/caramello/families/operations.py | grep -q "implemented" &amp;&amp; uv run pytest tests/test_family_operations.py -v 2>&amp;1 | tail -25 &amp;&amp; uv run pytest tests/test_family_operations.py -q 2>&amp;1 | tail -3 | grep -q "passed"</automated>
  </verify>
  <acceptance_criteria>
    - `head -1 src/caramello/families/operations.py` retorna `# CARAMELLO-GENERATED: implemented`
    - `grep -c "@router.post(\"/registry\"" src/caramello/families/operations.py` == 1
    - `grep -c "@router.get(\"/families\"" src/caramello/families/operations.py` == 1
    - `grep -c "@router.get(\"/families/{family_uuid}\"" src/caramello/families/operations.py` == 1
    - `grep -c "/pre-register" src/caramello/families/operations.py` >= 2 (decorator + comentário)
    - `grep -c "@router.get(\"/families/{family_uuid}/members\"" src/caramello/families/operations.py` == 1
    - `grep -c "@router.delete(\"/families/{family_uuid}/members/{user_uuid}\"" src/caramello/families/operations.py` == 1
    - `grep -c 'role="owner"' src/caramello/families/operations.py` >= 2 (registry_family + _require_owner)
    - `grep -c "_require_owner" src/caramello/families/operations.py` >= 4 (def + 2 chamadas + docstring)
    - `grep -c "_require_member" src/caramello/families/operations.py` >= 4 (def + 2 chamadas + docstring)
    - `grep -c "status_code=403\|HTTP_403" src/caramello/families/operations.py` >= 2 (em _require_owner e _require_member)
    - `grep -c "NotImplementedError" src/caramello/families/operations.py` == 0 (stub removido)
    - `grep -c "pytest.xfail" tests/test_family_operations.py` == 0 (xfails removidos do plano 04-03)
    - `uv run ruff check src/caramello/families/operations.py tests/test_family_operations.py` retorna sem erros
    - **NOVO (mypy strict):** `uv run mypy src/caramello/families/operations.py` exit 0 (sem erros mypy em strict mode)
    - `uv run pytest tests/test_family_operations.py -q` mostra todos PASS (sem FAIL nem XFAIL persistente)
  </acceptance_criteria>
  <done>
    operations.py reescrito com 6 endpoints funcionais; helpers de owner/member em uso; testes do plano 04-01 destrabados e passando; ruff + mypy strict limpos.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Estender shared/auth.py com auto-join + ROADMAP/REQUIREMENTS marcando FAMILY-04/05/06 como deferidos</name>
  <files>src/caramello/shared/auth.py, tests/test_auth.py, .planning/ROADMAP.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - src/caramello/shared/auth.py (estado atual completo — onde inserir o bloco auto-join)
    - src/caramello/families/models.py (confirmar nomes: FamilyInvitation, FamilyMember; campos: email, status)
    - tests/test_auth.py (verificar estado do test_auto_join_on_login criado no plano 04-01)
    - .planning/phases/04-dom-nio-family/04-RESEARCH.md (Pattern 4 — Auto-join em get_current_user; Pitfall 3 — import circular)
    - .planning/phases/04-dom-nio-family/04-CONTEXT.md (D-02 — fluxo exato do auto-join; D-04 — FAMILY-04/05/06 deferidos M2)
    - .planning/REQUIREMENTS.md (formato atual das tabelas de requirements/traceability)
    - .planning/ROADMAP.md (formato do bloco Phase 4)
  </read_first>
  <behavior>
    - shared/auth.py: após o `SELECT User WHERE idp_sub` retornar `user` (linha ~188-196 atual), inserir bloco que:
      - Importa lazy FamilyInvitation e FamilyMember de caramello.families.models
      - SELECT FamilyInvitation WHERE email == token_email AND status == "pending_login"
      - Se existir: cria FamilyMember(user_id=user.id, family_id=inv.family_id, role="member") + UPDATE inv.status = "joined" + commit
      - Se NÃO existir: segue sem modificar nada
    - Mantém o `return user` no final inalterado
    - O bloco DEVE passar mypy strict — o import lazy (PLC0415) é necessário para evitar ciclo entre shared/ e families/, e mypy strict precisa enxergar os tipos corretamente. Verificação explícita: `uv run mypy src/caramello/shared/auth.py` exit 0.
    - **Nota RESOLVED (Open Question 3 do 04-RESEARCH.md):** O auto-join em login subsequente é seguro — após a primeira execução, `pending_inv.status` foi atualizado para `"joined"`, então o SELECT seguinte (`status == "pending_login"`) retorna None e o bloco de auto-join é simplesmente pulado. Não há criação duplicada de FamilyMember.
    - test_auto_join_on_login do plano 04-01 passa (remove qualquer skip/importorskip se necessário; mantém pytest.importorskip("caramello.families.models") como defesa)
    - ROADMAP.md Phase 4: substituir a linha `**Requirements**: FAMILY-01, FAMILY-02, FAMILY-03, FAMILY-04, FAMILY-05, FAMILY-06, FAMILY-07` por uma versão que explicita o deferimento:
      ```
      **Requirements**: FAMILY-01, FAMILY-02, FAMILY-03, FAMILY-07 (implementados); FAMILY-04, FAMILY-05, FAMILY-06 (deferidos para M2 — ver D-04 do 04-CONTEXT.md)
      ```
    - REQUIREMENTS.md Traceability: para FAMILY-04, FAMILY-05, FAMILY-06, mudar Status de "Pendente" para "Deferred → M2 (D-04)"
  </behavior>
  <action>
    **Sub-task 2A — Inserir bloco de auto-join em shared/auth.py:**

    Ler o arquivo atual. Localizar o bloco final de get_current_user:

    ```python
        # 6. SELECT — ON CONFLICT DO NOTHING não retorna a linha (pitfall #5)
        result = await session.exec(select(User).where(User.idp_sub == idp_sub))
        user = result.first()
        if user is None:
            # Estado inesperado: insert aconteceu mas select falhou — caso raro de race
            raise HTTPException(
                status_code=status.HTTP_500_INTERNAL_SERVER_ERROR,
                detail="Falha ao provisionar usuário",
            )
        return user
    ```

    Inserir ENTRE a checagem `if user is None: raise HTTPException(...)` e o `return user` (ou seja, logo antes do `return user`) o bloco:

    ```python
        # 7. AUTO-JOIN (Phase 4 D-02): se existe FamilyInvitation pendente com este email,
        # adicionar o usuário automaticamente como FamilyMember(role="member").
        # Import lazy para evitar ciclo entre shared/ e families/ (pitfall #3 RESEARCH.md Phase 4).
        from caramello.families.models import FamilyInvitation, FamilyMember  # noqa: PLC0415

        inv_result = await session.exec(
            select(FamilyInvitation).where(
                FamilyInvitation.email == email,
                FamilyInvitation.status == "pending_login",
            )
        )
        pending_inv = inv_result.first()
        if pending_inv is not None:
            new_member = FamilyMember(
                user_id=user.id,
                family_id=pending_inv.family_id,
                role="member",
            )
            session.add(new_member)
            pending_inv.status = "joined"
            session.add(pending_inv)
            await session.commit()
    ```

    Ajustar a docstring de `get_current_user` para mencionar o passo 7 (auto-join) — adicionar à lista de Fluxo:

    Antes:
    ```python
    Fluxo (D-12):
      1. Extrair kid do JWT header (sem verificar assinatura).
      2. Lookup no _jwks_cache; re-busca JWKS uma vez se kid desconhecido.
      3. jwt.decode com algorithms=['RS256'] (bloqueia downgrade explicitamente).
      4. Extrair claims (sub, email, name | preferred_username) — D-03.
      5. INSERT ON CONFLICT DO NOTHING — operação atômica (D-12, AUTO-02).
      6. SELECT do User para retornar (ON CONFLICT DO NOTHING não retorna
         a linha — pitfall #5).
    ```

    Adicionar:
    ```
      7. AUTO-JOIN (Phase 4 D-02): busca FamilyInvitation pendente por email;
         se existe, cria FamilyMember(role="member") + atualiza invitation.status="joined".
    ```

    **Sub-task 2B — Verificar mypy strict em shared/auth.py:**

    O import lazy `from caramello.families.models import FamilyInvitation, FamilyMember` dentro da função pode gerar erro em mypy strict mode se as anotações não forem corretas. Validar:

    ```bash
    uv run mypy src/caramello/shared/auth.py
    ```

    Esperado: exit 0 (sem erros). Se houver erro de tipo (ex: `error: Module has no attribute "FamilyInvitation"`), investigar:
    - O módulo `caramello.families.models` realmente exporta `FamilyInvitation` e `FamilyMember` (gerados no plano 04-03)?
    - `# noqa: PLC0415` é específico para ruff; mypy não respeita esse comentário. Se mypy strict reclamar do import dentro da função, usar TYPE_CHECKING como alternativa (mas isso quebra o uso em runtime — neste caso o import dentro da função É necessário porque o objeto é instanciado).

    Se mypy continuar reclamando, adicionar `# type: ignore[import-not-found]` no final da linha de import, mas isso deve ser último recurso. O ideal é que o import funcione sem ignores.

    **Sub-task 2C — Verificar test_auto_join_on_login passa:**

    O teste do plano 04-01 usa `pytest.importorskip("caramello.families.models")` — agora o módulo existe, então o teste roda. Verificar que passa:

    ```bash
    uv run pytest tests/test_auth.py::test_auto_join_on_login -v
    ```

    Se falhar por algum detalhe (ex: o teste mocka 2 SELECTs em ordem mas o auth.py faz só 1, ou vice-versa), depurar — pode ser necessário ajustar o teste ou o bloco em auth.py para alinhar a ordem dos SELECTs. A ordem esperada após este plano:
    1. SELECT User WHERE idp_sub == sub → retorna provisioned_user (mock 1)
    2. SELECT FamilyInvitation WHERE email AND status='pending_login' → retorna pending_inv (mock 2)

    Que é exatamente o que o teste `select_results = iter([provisioned_user, pending_inv])` esperava.

    **Sub-task 2D — Atualizar ROADMAP.md Phase 4 — Requirements line:**

    Localizar em `.planning/ROADMAP.md` a linha da Phase 4:
    ```
    **Requirements**: FAMILY-01, FAMILY-02, FAMILY-03, FAMILY-04, FAMILY-05, FAMILY-06, FAMILY-07
    ```

    Substituir por:
    ```
    **Requirements**: FAMILY-01, FAMILY-02, FAMILY-03, FAMILY-07 (implementados); FAMILY-04, FAMILY-05, FAMILY-06 (deferidos para M2 — ver D-04 em 04-CONTEXT.md)
    ```

    Também atualizar os "Success Criteria" da Phase 4 — atualmente listam o fluxo antigo (código de convite). Substituir pelos critérios do CONTEXT.md (válidos para esta fase):

    Substituir o bloco inteiro:
    ```markdown
    **Success Criteria** (what must be TRUE):
      1. `POST /family/families` cria família e torna o usuário autenticado owner automaticamente
      2. `GET /family/families` lista apenas famílias das quais o usuário é membro
      3. `POST /family/families/{id}/invitations` gera código de convite reutilizável — apenas owner consegue; não-owner recebe 403
      4. `POST /family/invitations/{code}/join` registra solicitação pendente para o usuário autenticado
      5. `PATCH /family/invitations/{id}` permite owner aprovar ou rejeitar — solicitação aprovada adiciona membro; solicitação rejeitada não adiciona
      6. `DELETE /family/families/{id}/members/{user_id}` remove membro — apenas owner consegue; todos os endpoints retornam 401 sem token
    ```

    Por:
    ```markdown
    **Success Criteria** (what must be TRUE — atualizado em Phase 4 para refletir CONTEXT.md):
      1. `POST /families/registry` cria família e torna o usuário autenticado owner (role="owner") (FAMILY-01)
      2. `GET /families/families` lista apenas famílias das quais o usuário é membro (FAMILY-02)
      3. `GET /families/families/{uuid}` retorna 200 se o usuário é membro; 403 caso contrário (FAMILY-03)
      4. `POST /families/families/{uuid}/pre-register` registra email para auto-join — apenas owner consegue; não-owner recebe 403 (D-07)
      5. Primeiro login de email pré-registrado adiciona automaticamente como FamilyMember(role="member") — auto-join transparente em `get_current_user()` (D-02)
      6. `GET /families/families/{uuid}/members` lista membros se o requisitante é membro; 403 senão
      7. `DELETE /families/families/{uuid}/members/{user_uuid}` remove membro — apenas owner consegue (FAMILY-07); endpoints sem token retornam 401
      8. FAMILY-04, FAMILY-05, FAMILY-06 (código convite reutilizável + join request + aprovação) DEFERIDOS para M2 conforme D-04
    ```

    Adicionar/atualizar entrada Plans no bloco Phase 4 do ROADMAP.md:
    ```
    **Plans**: 4 planos
    Plans:
    - [ ] 04-01-PLAN.md — Wave 0: stubs de testes para FAMILY-01/02/03/07 + auto-join + generator
    - [ ] 04-02-PLAN.md — Wave 1: evoluir generator (URL plural + hifens + generate_operations com DOMAIN_TO_ENTITY_NAME) + atualizar YAMLs (domains plurais + redesign FamilyInvitation + dsl/operations/family.yaml)
    - [ ] 04-03-PLAN.md — Wave 2: regenerar código (users/ + families/), deletar diretórios antigos (user/, family/), atualizar main.py + alembic/env.py + criar migration Alembic
    - [ ] 04-04-PLAN.md — Wave 3: implementar families/operations.py + estender shared/auth.py com auto-join + finalizar ROADMAP/REQUIREMENTS
    ```

    **Sub-task 2E — Atualizar REQUIREMENTS.md Traceability:**

    Localizar a tabela de Traceability em `.planning/REQUIREMENTS.md`. Para as linhas:
    ```
    | FAMILY-04 | Phase 4 | Pendente |
    | FAMILY-05 | Phase 4 | Pendente |
    | FAMILY-06 | Phase 4 | Pendente |
    ```

    Substituir por:
    ```
    | FAMILY-04 | Phase 4 → M2 | Deferred (D-04) |
    | FAMILY-05 | Phase 4 → M2 | Deferred (D-04) |
    | FAMILY-06 | Phase 4 → M2 | Deferred (D-04) |
    ```

    Adicionar uma nota no rodapé da seção Domínio Family ou imediatamente após (antes da seção MCP):

    ```markdown
    > **Nota Phase 4 (D-04):** Os requisitos FAMILY-04, FAMILY-05 e FAMILY-06 (fluxo de código de convite reutilizável + join request + aprovação manual) foram deferidos para o Milestone 2 — eles caracterizam um produto público com múltiplas famílias de terceiros, fora do escopo do M1 (grupo fechado 1-5 usuários). Em substituição, a Phase 4 implementa o fluxo de **pré-cadastro por email** (D-01, D-02) que é equivalente em valor para o grupo familiar e mais simples operacionalmente.
    ```

    **Sub-task 2F — Verificar suite completa:**

    ```bash
    cd /home/claude/work/caramello-api
    uv run ruff check src/caramello tests scripts/generate_code.py alembic/
    uv run mypy src/caramello/shared/auth.py
    uv run pytest -q
    ```

    Esperado: tudo verde, exit 0.

    Confirmar que test_auto_join_on_login passa:
    ```bash
    uv run pytest tests/test_auth.py::test_auto_join_on_login -v
    ```
  </action>
  <verify>
    <automated>cd /home/claude/work/caramello-api &amp;&amp; uv run ruff check src/caramello/shared/auth.py tests/test_auth.py &amp;&amp; uv run mypy src/caramello/shared/auth.py &amp;&amp; grep -c "AUTO-JOIN" src/caramello/shared/auth.py &amp;&amp; grep -c "from caramello.families.models import" src/caramello/shared/auth.py &amp;&amp; grep -c 'role="member"' src/caramello/shared/auth.py &amp;&amp; grep -F "FAMILY-04, FAMILY-05, FAMILY-06 (deferidos para M2" .planning/ROADMAP.md &amp;&amp; grep -F "Deferred (D-04)" .planning/REQUIREMENTS.md &amp;&amp; uv run pytest tests/test_auth.py -v 2>&amp;1 | tail -15 &amp;&amp; uv run pytest -q 2>&amp;1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "AUTO-JOIN" src/caramello/shared/auth.py` >= 1
    - `grep -c "from caramello.families.models import" src/caramello/shared/auth.py` >= 1
    - `grep -c 'role="member"' src/caramello/shared/auth.py` >= 1 (no INSERT FamilyMember do auto-join)
    - `grep -c 'pending_inv.status = "joined"' src/caramello/shared/auth.py` >= 1
    - `grep -c "FamilyInvitation.status == \"pending_login\"" src/caramello/shared/auth.py` >= 1
    - `uv run ruff check src/caramello/shared/auth.py` retorna sem erros
    - **NOVO (mypy strict):** `uv run mypy src/caramello/shared/auth.py` exit 0 (sem erros mypy em strict mode — o import lazy com `# noqa: PLC0415` deve passar; se mypy reclamar, ver Sub-task 2B para alternativas)
    - `uv run pytest tests/test_auth.py -q` mostra todos PASS (incluindo test_auto_join_on_login que era importorskip)
    - `grep -F "FAMILY-04, FAMILY-05, FAMILY-06 (deferidos para M2" .planning/ROADMAP.md` retorna >= 1 linha
    - `grep -F "Deferred (D-04)" .planning/REQUIREMENTS.md` retorna >= 3 linhas (FAMILY-04, FAMILY-05, FAMILY-06)
    - `grep -F "/families/registry" .planning/ROADMAP.md` retorna >= 1 linha (Success Criteria atualizado)
    - `uv run pytest -q` retorna exit 0 (suite verde)
  </acceptance_criteria>
  <done>
    shared/auth.py estendido com auto-join (D-02); mypy strict limpo em shared/auth.py; test_auto_join_on_login passa; ROADMAP.md Phase 4 atualizado com novos Success Criteria e marca FAMILY-04/05/06 como deferidos; REQUIREMENTS.md Traceability atualizado. Suite completa verde.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Cliente → POST /families/registry | Body validado por Pydantic (FamilyCreate); current_user vem de JWT validado (shared/auth.py). |
| Cliente → POST /families/{uuid}/pre-register | Body validado (PreRegisterBody com EmailStr); current_user validado; ownership verificado via _require_owner. |
| Cliente → DELETE /families/{uuid}/members/{user_uuid} | Path params são UUIDs (validados por FastAPI); ownership verificado. |
| Keycloak JWT (email claim) → auto-join | Email vem de token assinado pelo Keycloak — não pode ser injetado pelo cliente. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-11 | Spoofing | Cliente forja role para acessar pre-register / remove-member | mitigate | `_require_owner` consulta o banco em cada request (FamilyMember WHERE role='owner'); não confia em claims do JWT além do email/sub. Testado em test_pre_register_member_non_owner_returns_403 e test_remove_member_non_owner_returns_403. |
| T-04-12 | Spoofing | Auto-join com email falsificado (cliente inventando token) | mitigate | Email vem do payload JWT validado em shared/auth.py com algorithms=['RS256'] e chave do JWKS Keycloak. Sem assinatura válida, não passa pelo decode em get_current_user. |
| T-04-13 | Information Disclosure | GET /families/{uuid}/members vaza membros para não-membros | mitigate | `_require_member` valida membership antes de listar. Testado implicitamente via padrão idêntico ao FAMILY-03 (mesma helper). |
| T-04-14 | Elevation of Privilege | Auto-join cria FamilyMember com role="owner" indevidamente | mitigate | Bloco em shared/auth.py hardcoda `role="member"`. Apenas o fluxo de POST /families/registry pode criar role="owner" — e só para o próprio criador. Cobertura: test_registry_creates_family_and_owner verifica `members_added[0].role == "owner"`. |
| T-04-15 | Tampering | Cliente envia FamilyInvitation com status diferente de "pending_login" | mitigate | pre_register_member hardcoda `status="pending_login"` no INSERT — não aceita esse campo do body. PreRegisterBody só tem `email`. |
| T-04-16 | Denial of Service | Auto-join consulta FamilyInvitation a cada login mesmo para users sem invitations | accept | Query é `WHERE email AND status='pending_login'` — usa índice implícito via filtro de status (que tem cardinalidade baixa). Custo é uma SELECT trivial por login; aceitável para 1-5 usuários no Caramello. Otimização (cache local de "já passou pelo auto-join") fica para M2 se virar gargalo. |
| T-04-17 | Repudiation | Mudança de role/membership não tem audit log | accept | Out-of-scope no M1 (Caramello é grupo familiar com 1-5 usuários, sem necessidade de compliance). Logging estruturado (OPS-02) e audit log são deferidos para milestones futuros. |
| T-04-18 | Race condition | Concorrência: 2 logins simultâneos do mesmo email causam INSERT duplicado de FamilyMember | accept | PRIMARY KEY composto (user_id, family_id) garante que o segundo INSERT falha com IntegrityError. Em Phase 4 essa exceção propaga como 500 — aceitável dado que é race rara e o cliente pode retentar. Tratamento defensivo com try/except IntegrityError é melhoria para M2. |
</threat_model>

<verification>
1. `uv run ruff check src/caramello tests scripts/generate_code.py alembic/` — sem erros.
2. `uv run mypy src/caramello/families/operations.py src/caramello/shared/auth.py` — exit 0 (mypy strict limpo em ambos os arquivos modificados nesta fase).
3. `uv run pytest -q` — todos PASS, sem FAIL nem XFAIL persistente.
4. `head -1 src/caramello/families/operations.py` retorna `# CARAMELLO-GENERATED: implemented`.
5. `grep -c "AUTO-JOIN" src/caramello/shared/auth.py` >= 1.
6. `.planning/ROADMAP.md` Phase 4 tem Success Criteria atualizado e marca FAMILY-04/05/06 como deferidos.
7. `.planning/REQUIREMENTS.md` Traceability tem FAMILY-04/05/06 com status "Deferred (D-04)".
8. Frontmatter deste plano declara `requirements_deferred: [FAMILY-04, FAMILY-05, FAMILY-06]` para rastreabilidade.
</verification>

<success_criteria>
- src/caramello/families/operations.py com as 6 operações implementadas (não mais stub)
- src/caramello/shared/auth.py estendido com auto-join (D-02)
- Anotação `# CARAMELLO-GENERATED: implemented` na primeira linha de families/operations.py — generator passa a respeitar este arquivo
- Suite pytest 100% verde
- ruff + mypy strict limpos em src/caramello/families/operations.py e src/caramello/shared/auth.py (acceptance_criteria explícita em ambas as tasks)
- ROADMAP.md Phase 4 atualizado: Success Criteria refletem o fluxo de pré-cadastro (CONTEXT.md), FAMILY-04/05/06 marcados como deferidos para M2
- REQUIREMENTS.md Traceability: FAMILY-04/05/06 com status "Deferred (D-04)"
- FAMILY-01, FAMILY-02, FAMILY-03, FAMILY-07 entregues (todos os testes funcionais passam)
- FAMILY-04, FAMILY-05, FAMILY-06 deferidos (rastreabilidade preservada em frontmatter `requirements_deferred` + ROADMAP/REQUIREMENTS, sem implementação)
</success_criteria>

<output>
Após completar, criar `.planning/phases/04-dom-nio-family/04-04-SUMMARY.md` listando:
- Resumo dos 6 endpoints implementados (path + status code + comportamento de auth)
- Bloco de auto-join adicionado em shared/auth.py (D-02)
- Confirmação de que suite pytest está verde
- Confirmação de que mypy strict passa em families/operations.py e shared/auth.py
- Atualizações em ROADMAP.md e REQUIREMENTS.md (deferimento de FAMILY-04/05/06)
- Rastreabilidade formal: frontmatter `requirements_deferred: [FAMILY-04, FAMILY-05, FAMILY-06]`
- Próximo passo: `/gsd-verify-work` para verificar a Phase 4 contra os Success Criteria atualizados
</output>
</content>
</invoke>