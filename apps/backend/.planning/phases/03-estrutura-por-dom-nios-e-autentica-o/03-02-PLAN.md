---
phase: 03-estrutura-por-dom-nios-e-autentica-o
plan: 02
type: execute
wave: 1
depends_on: [01]
files_modified:
  - pyproject.toml
  - uv.lock
  - src/caramello/core/config.py
  - dsl/entities/user.yaml
  - dsl/entities/family.yaml
  - dsl/entities/family_member.yaml
  - dsl/entities/family_invitation.yaml
  - dsl/operations/user.yaml
  - dsl/schema.yaml
autonomous: true
requirements: [STRUCT-02, AUTH-01, USER-01]

user_setup:
  - service: keycloak
    why: "shared/auth.py valida tokens JWT contra realm 'caramello' já provisionado na infra existente"
    env_vars:
      - name: KEYCLOAK_URL
        source: "URL pública do Keycloak (ex: https://auth.caramello.cloud) — confirmar com o operador"
      - name: KEYCLOAK_REALM
        source: "Valor fixo: 'caramello' (D-01)"
      - name: KEYCLOAK_CLIENT_ID
        source: "Client ID de dev provisionado no Keycloak — confirmar com o operador"

must_haves:
  truths:
    - "PyJWT[crypto] está em project.dependencies do pyproject.toml"
    - "httpx está em project.dependencies do pyproject.toml (movido de dependency-groups.dev)"
    - "Settings expõe KEYCLOAK_URL, KEYCLOAK_REALM, KEYCLOAK_CLIENT_ID como campos obrigatórios"
    - "Cada YAML de entidade em dsl/entities/ contém o campo `domain`"
    - "dsl/operations/user.yaml define a operação get_me em GET /user/me"
    - "dsl/schema.yaml documenta o campo `domain` como propriedade reconhecida"
  artifacts:
    - path: "pyproject.toml"
      provides: "Dependências PyJWT e httpx em project.dependencies"
      contains: "PyJWT"
    - path: "src/caramello/core/config.py"
      provides: "Campos Keycloak no Settings"
      contains: "KEYCLOAK_URL: str"
    - path: "dsl/operations/user.yaml"
      provides: "Definição da operação GET /user/me"
      contains: "get_me"
    - path: "dsl/entities/user.yaml"
      provides: "Campo domain: user no YAML de entidade"
      contains: "domain: user"
  key_links:
    - from: "src/caramello/core/config.py"
      to: ".env"
      via: "pydantic-settings model_config"
      pattern: "KEYCLOAK_URL"
    - from: "scripts/generate_code.py"
      to: "dsl/entities/*.yaml"
      via: "yaml.safe_load — leitura do campo domain"
      pattern: "domain"
---

<objective>
Preparar todos os inputs necessários para a evolução do generator (Plan 03) e para a implementação de auth (Plan 04): adicionar dependências PyJWT[crypto] e httpx ao pyproject, expor as três env vars do Keycloak no Settings, anotar todas as entidades YAML com o campo `domain`, e criar o primeiro arquivo de operação de negócio (`dsl/operations/user.yaml`).

Purpose: Estes são pré-requisitos paralelizáveis que não dependem entre si nem têm overlap de arquivos. Centralizá-los em um plano evita criação artificial de waves.

Output: pyproject.toml atualizado (uv.lock regerado), config.py com 3 campos novos, 4 YAMLs de entidade com campo domain, dsl/operations/user.yaml criado, dsl/schema.yaml documentando o novo campo.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-estrutura-por-dom-nios-e-autentica-o/03-CONTEXT.md
@.planning/phases/03-estrutura-por-dom-nios-e-autentica-o/03-RESEARCH.md
@.planning/phases/03-estrutura-por-dom-nios-e-autentica-o/03-PATTERNS.md

<interfaces>
# Settings atual (src/caramello/core/config.py — referência) — NÃO editar fora do escopo:
class Settings(BaseSettings):
    model_config = SettingsConfigDict(env_file=".env", env_ignore_empty=True, extra="ignore")
    ENVIRONMENT: str = "development"
    DATABASE_URL: str | None = None
    DB_HOST: str
    DB_PORT: int
    DB_USER: str
    DB_PASSWORD: str
    DB_NAME: str
    CORS_ORIGINS: list[str] = ["http://localhost:3000", "http://localhost:5173"]
    def model_post_init(self, __context: object) -> None: ...

# pyproject.toml atual — dependencies em [project] (sem PyJWT); httpx em [dependency-groups].dev

# YAMLs de entidade atuais — `name:` é o primeiro campo, `domain` precisa entrar logo após
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adicionar PyJWT[crypto] e mover httpx para project.dependencies</name>
  <files>pyproject.toml, uv.lock</files>
  <read_first>
    - pyproject.toml (estado atual: PyJWT ausente; httpx em dependency-groups.dev linha 40)
    - .planning/phases/03-estrutura-por-dom-nios-e-autentica-o/03-RESEARCH.md (seção "Standard Stack" e "Code Examples — Instalação de PyJWT[crypto]")
  </read_first>
  <action>
    1. Executar `uv add "PyJWT[crypto]"` na raiz do repo. Isso adiciona `PyJWT[crypto]` ao bloco `[project] dependencies` do pyproject.toml e atualiza `uv.lock`. Versão mínima alvo: `>=2.10` (research verificou 2.13.0 no PyPI).

    2. Executar `uv add httpx` (sem `--dev`). Isso adiciona httpx ao `[project] dependencies`. Em seguida, **editar manualmente `pyproject.toml`** removendo a linha `"httpx>=0.28.1",` do bloco `[dependency-groups] dev` (mantendo pytest, ruff, mypy).

    3. Verificar visualmente o resultado: o bloco `[project] dependencies` deve conter uma entrada começando com `"PyJWT[crypto]` e outra com `"httpx`. O bloco `[dependency-groups] dev` deve conter exatamente três itens: `pytest>=9.0.1`, `ruff>=0.9.0`, `mypy>=1.0.0`.

    4. Rodar `uv sync` para garantir que o venv reflete o estado novo.

    5. Validar que os imports funcionam:
       ```bash
       uv run python -c "import jwt; import httpx; from jwt.algorithms import RSAAlgorithm; print(jwt.__version__, httpx.__version__)"
       ```
       Deve imprimir as duas versões sem erro.

    Notas:
    - NÃO editar `uv.lock` manualmente — apenas via comandos `uv add`/`uv sync`.
    - NÃO mexer em outras dependências (fastapi, sqlmodel, asyncpg etc.).
  </action>
  <verify>
    <automated>cd /home/claude/work/caramello-api &amp;&amp; uv run python -c "import jwt, httpx; from jwt.algorithms import RSAAlgorithm; print('OK', jwt.__version__, httpx.__version__)" &amp;&amp; grep -E '"PyJWT\[crypto\]' pyproject.toml &amp;&amp; [ "$(grep -c '"httpx' pyproject.toml)" = "1" ]</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E '"PyJWT\[crypto\]' pyproject.toml` retorna pelo menos uma linha
    - `grep -c '"httpx' pyproject.toml` retorna exatamente 1 (apenas em [project] dependencies, não em [dependency-groups] dev)
    - `uv run python -c "import jwt; from jwt.algorithms import RSAAlgorithm"` exit code 0
    - `uv run python -c "import httpx; print(httpx.__version__)"` exit code 0
    - `git diff uv.lock | wc -l` > 0 (lock atualizado)
  </acceptance_criteria>
  <done>
    PyJWT[crypto] e httpx estão em project.dependencies; uv.lock atualizado; imports funcionam no venv.
  </done>
</task>

<task type="auto">
  <name>Task 2: Adicionar campos KEYCLOAK_* ao Settings em config.py</name>
  <files>src/caramello/core/config.py</files>
  <read_first>
    - src/caramello/core/config.py (estado atual: 37 linhas; sem campos Keycloak)
    - .env.example (placeholders KEYCLOAK_URL, KEYCLOAK_REALM, KEYCLOAK_CLIENT_ID já existem nas linhas 17-19 — não editar)
    - .planning/phases/03-estrutura-por-dom-nios-e-autentica-o/03-PATTERNS.md (seção src/caramello/core/config.py)
  </read_first>
  <action>
    Editar `src/caramello/core/config.py` para adicionar três campos obrigatórios ao Settings, seguindo exatamente o padrão dos campos DB_* (sem default, lidos do .env).

    Inserir o seguinte bloco entre a linha `CORS_ORIGINS: list[str] = ["http://localhost:3000", "http://localhost:5173"]` e a linha `def model_post_init(self, __context: object) -> None:`:

    ```python

        # Keycloak Configuration (Required) — provisioned na infra existente
        # JWKS URL será construída em shared/auth.py como
        # f"{KEYCLOAK_URL}/realms/{KEYCLOAK_REALM}/protocol/openid-connect/certs"
        KEYCLOAK_URL: str
        KEYCLOAK_REALM: str
        KEYCLOAK_CLIENT_ID: str
    ```

    Preservar exatamente:
    - O `model_config = SettingsConfigDict(env_file=".env", env_ignore_empty=True, extra="ignore")` (linhas atuais 5-9)
    - O `model_post_init` construindo `DATABASE_URL` (linhas atuais 28-34)
    - A instância final `settings = Settings()  # type: ignore[call-arg] ...` (linha 37)

    Não usar valores default — campos obrigatórios. Pydantic-settings irá levantar `ValidationError` clara se .env não tiver os valores.
  </action>
  <verify>
    <automated>cd /home/claude/work/caramello-api &amp;&amp; grep -q "KEYCLOAK_URL: str" src/caramello/core/config.py &amp;&amp; grep -q "KEYCLOAK_REALM: str" src/caramello/core/config.py &amp;&amp; grep -q "KEYCLOAK_CLIENT_ID: str" src/caramello/core/config.py &amp;&amp; uv run ruff check src/caramello/core/config.py &amp;&amp; uv run mypy src/caramello/core/config.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "KEYCLOAK_URL: str$" src/caramello/core/config.py` retorna 1
    - `grep -c "KEYCLOAK_REALM: str$" src/caramello/core/config.py` retorna 1
    - `grep -c "KEYCLOAK_CLIENT_ID: str$" src/caramello/core/config.py` retorna 1
    - Nenhum dos três tem `= "default"` ou `= None` na mesma linha (campos obrigatórios sem default)
    - `uv run ruff check src/caramello/core/config.py` exit 0
    - `uv run mypy src/caramello/core/config.py` exit 0
  </acceptance_criteria>
  <done>
    Settings aceita as três env vars Keycloak; ruff e mypy passam; padrão dos campos espelha DB_*.
  </done>
</task>

<task type="auto">
  <name>Task 3: Adicionar campo `domain` nos 4 YAMLs de entidade + atualizar dsl/schema.yaml</name>
  <files>dsl/entities/user.yaml, dsl/entities/family.yaml, dsl/entities/family_member.yaml, dsl/entities/family_invitation.yaml, dsl/schema.yaml</files>
  <read_first>
    - dsl/entities/user.yaml (estado atual: 65 linhas; sem campo `domain`; `name:` é o primeiro campo)
    - dsl/entities/family.yaml (estado atual: 65 linhas; sem campo `domain`)
    - dsl/entities/family_member.yaml (40 linhas, `is_link_model: true`)
    - dsl/entities/family_invitation.yaml (63 linhas)
    - dsl/schema.yaml (esquema JSON-Schema-like que valida os YAMLs)
    - .planning/phases/03-estrutura-por-dom-nios-e-autentica-o/03-RESEARCH.md (seção "Novo Campo domain no YAML")
  </read_first>
  <action>
    Mapeamento (D-06):
    - `user.yaml` → `domain: user`
    - `family.yaml` → `domain: family`
    - `family_member.yaml` → `domain: family`
    - `family_invitation.yaml` → `domain: family`

    Em cada um dos 4 arquivos, inserir uma linha `domain: <valor>` IMEDIATAMENTE após a linha `name: <Entity>`. Exemplo concreto para `user.yaml`:

    Antes:
    ```yaml
    name: User
    description: Represents a system user, provisioned on first authentication via Keycloak.
    table_name: user
    ```

    Depois:
    ```yaml
    name: User
    domain: user
    description: Represents a system user, provisioned on first authentication via Keycloak.
    table_name: user
    ```

    Aplicar o mesmo padrão (`domain: family`) em family.yaml, family_member.yaml e family_invitation.yaml.

    Em `dsl/schema.yaml`, adicionar o campo `domain` como propriedade reconhecida no schema. Inserir entre a propriedade `name:` (linhas atuais 6-8) e a propriedade `description:` (linhas 9-11):

    ```yaml
      domain:
        type: string
        description: "Domínio de negócio para output do generator (ex: user, family)."
    ```

    E adicionar `domain` à lista `required` no final do schema. Alterar:
    ```yaml
    required:
      - name
      - table_name
      - fields
    ```
    para:
    ```yaml
    required:
      - name
      - domain
      - table_name
      - fields
    ```

    NÃO alterar outras partes dos YAMLs (campos de entidade, relationships, descrições existentes permanecem intactos).
  </action>
  <verify>
    <automated>cd /home/claude/work/caramello-api &amp;&amp; uv run python -c "
import yaml
from pathlib import Path
expected = {'user.yaml': 'user', 'family.yaml': 'family', 'family_member.yaml': 'family', 'family_invitation.yaml': 'family'}
for fname, dom in expected.items():
    data = yaml.safe_load((Path('dsl/entities') / fname).read_text())
    assert data.get('domain') == dom, f'{fname}: expected {dom}, got {data.get(\"domain\")!r}'
schema = yaml.safe_load(Path('dsl/schema.yaml').read_text())
assert 'domain' in schema['properties'], 'schema.yaml deve listar domain em properties'
assert 'domain' in schema['required'], 'schema.yaml deve listar domain em required'
print('OK')
" &amp;&amp; uv run pytest tests/test_generator.py::test_user_yaml_has_domain_field tests/test_generator.py::test_family_yamls_have_domain_field -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^domain: user$" dsl/entities/user.yaml` retorna 1
    - `grep -c "^domain: family$" dsl/entities/family.yaml` retorna 1
    - `grep -c "^domain: family$" dsl/entities/family_member.yaml` retorna 1
    - `grep -c "^domain: family$" dsl/entities/family_invitation.yaml` retorna 1
    - `grep -A2 "^  domain:" dsl/schema.yaml | grep -c "type: string"` retorna 1
    - `grep -A5 "^required:" dsl/schema.yaml | grep -c "  - domain"` retorna 1
    - `uv run pytest tests/test_generator.py::test_user_yaml_has_domain_field tests/test_generator.py::test_family_yamls_have_domain_field -x -q` passa (verde)
  </acceptance_criteria>
  <done>
    Todos os 4 YAMLs declaram `domain`; schema.yaml reconhece o campo como obrigatório; testes em test_generator.py passam (verde, não xfail).
  </done>
</task>

<task type="auto">
  <name>Task 4: Criar dsl/operations/user.yaml com operação get_me</name>
  <files>dsl/operations/user.yaml</files>
  <read_first>
    - dsl/manifest.yaml (formato YAML do projeto; o generator irá varrer `dsl/operations/*.yaml` via glob, sem registrar no manifest)
    - .planning/phases/03-estrutura-por-dom-nios-e-autentica-o/03-RESEARCH.md (seção "Novo Conceito: dsl/operations/{domain}.yaml")
    - .planning/phases/03-estrutura-por-dom-nios-e-autentica-o/03-CONTEXT.md (D-08 e D-10)
  </read_first>
  <action>
    Criar o diretório `dsl/operations/` se não existir.

    Criar o arquivo `dsl/operations/user.yaml` com este conteúdo exato:

    ```yaml
    # dsl/operations/user.yaml
    # Operações de negócio do domínio user.
    # Gera src/caramello/user/operations.py (stub que será implementado nesta fase — D-10).

    domain: user
    operations:
      - name: get_me
        method: GET
        path: /user/me
        description: "Retorna o perfil do usuário autenticado."
    ```

    Não modificar `dsl/manifest.yaml` — o generator descobrirá `dsl/operations/*.yaml` via `Path.glob()` quando for atualizado no Plan 03.

    Não criar `dsl/operations/family.yaml` nesta fase — é deferido para a Phase 4.
  </action>
  <verify>
    <automated>cd /home/claude/work/caramello-api &amp;&amp; uv run python -c "
import yaml
from pathlib import Path
p = Path('dsl/operations/user.yaml')
assert p.exists(), 'arquivo deve existir'
data = yaml.safe_load(p.read_text())
assert data['domain'] == 'user', f'domain deve ser user; foi {data.get(\"domain\")!r}'
ops = data['operations']
assert len(ops) == 1, f'deve ter 1 operação; tem {len(ops)}'
op = ops[0]
assert op['name'] == 'get_me'
assert op['method'] == 'GET'
assert op['path'] == '/user/me'
assert op['description']
print('OK')
" &amp;&amp; uv run pytest tests/test_generator.py::test_operations_user_yaml_exists -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `test -f dsl/operations/user.yaml` exit 0
    - `grep -q "^domain: user$" dsl/operations/user.yaml` exit 0
    - `grep -q "name: get_me" dsl/operations/user.yaml` exit 0
    - `grep -q "path: /user/me" dsl/operations/user.yaml` exit 0
    - `grep -q "method: GET" dsl/operations/user.yaml` exit 0
    - `uv run pytest tests/test_generator.py::test_operations_user_yaml_exists -x -q` passa (verde, não xfail)
  </acceptance_criteria>
  <done>
    `dsl/operations/user.yaml` existe com a operação get_me em /user/me; teste correspondente verde.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `.env` → Settings | Env vars KEYCLOAK_URL/REALM/CLIENT_ID lidas em runtime — se .env vazar, expõe URL do IdP (não credencial) |
| uv add → PyPI | Resolução de PyJWT[crypto] e httpx busca pacotes do PyPI — vetor de supply chain |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-T03 | Tampering | PyJWT[crypto] supply chain via uv add | mitigate | uv.lock hash-pins as versões; revisar `git diff uv.lock` antes de commitar. PyJWT é mantenedor conhecido (jpadilla). |
| T-3-T04 | Information Disclosure | KEYCLOAK_URL em Settings (lido do .env) | accept | URL do Keycloak não é segredo — é endpoint público. CLIENT_ID idem (audience claim já é público no token). KEYCLOAK_REALM idem. Nenhuma credencial fica em código. |
| T-3-T05 | Denial of Service | Settings sem default para KEYCLOAK_* | mitigate | Sem default = pydantic-settings levanta ValidationError no boot se .env incompleto. Fail-fast preferível a degraded auth. |
| T-3-T06 | Tampering | dsl/operations/user.yaml lido pelo generator | accept | Generator roda apenas em ambiente dev (bin/generate_code) — não em produção. YAMLs versionados no git. |
</threat_model>

<verification>
- `uv run python -c "import jwt; import httpx"` exit 0
- `uv run ruff check src/caramello/core/config.py` exit 0
- `uv run mypy src/caramello/core/config.py` exit 0
- `uv run pytest tests/test_generator.py::test_user_yaml_has_domain_field tests/test_generator.py::test_family_yamls_have_domain_field tests/test_generator.py::test_operations_user_yaml_exists -x -q` todos verdes
- `grep -c '"httpx' pyproject.toml` retorna 1 (apenas em [project] dependencies)
- `grep -E '"PyJWT\[crypto\]' pyproject.toml` retorna pelo menos uma linha
</verification>

<success_criteria>
Wave 1 entregue: dependências PyJWT[crypto] e httpx no project.dependencies; Settings com KEYCLOAK_URL/REALM/CLIENT_ID; todos os YAMLs de entidade com `domain`; `dsl/operations/user.yaml` criado; `dsl/schema.yaml` atualizado. Esses são os inputs que o Plan 03 (generator) e o Plan 04 (auth) consumirão.
</success_criteria>

<output>
Após completion, criar `.planning/phases/03-estrutura-por-dom-nios-e-autentica-o/03-02-SUMMARY.md`
</output>
