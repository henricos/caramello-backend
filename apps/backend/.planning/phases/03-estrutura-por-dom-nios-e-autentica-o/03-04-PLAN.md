---
phase: 03-estrutura-por-dom-nios-e-autentica-o
plan: 04
type: execute
wave: 2
depends_on: [02]
files_modified:
  - src/caramello/shared/auth.py
autonomous: true
requirements: [AUTH-01, AUTH-02, AUTH-03]

must_haves:
  truths:
    - "shared/auth.py existe em src/caramello/shared/"
    - "Módulo expõe fetch_jwks(), get_current_user(), e _jwks_cache: dict[str, Any]"
    - "get_current_user valida JWT com algorithms=['RS256'] explícito (rejeita 'none')"
    - "Em kid não encontrado, re-busca JWKS antes de retornar 401"
    - "JIT provisioning usa pg_insert + on_conflict_do_nothing seguido de SELECT"
    - "Sem token Authorization, HTTPBearer levanta 401/403 automaticamente"
    - "jwt.decode usa options={'verify_aud': False} inicialmente (D-02)"
    - "Mapeamento de claims: sub→idp_sub, email→email, name (ou preferred_username)→name (D-03)"
    - "tests/test_auth.py::test_auth_module passa (verde, sem xfail)"
  artifacts:
    - path: "src/caramello/shared/auth.py"
      provides: "fetch_jwks(), get_current_user(), http_bearer, _jwks_cache"
      exports: ["fetch_jwks", "get_current_user", "http_bearer"]
      min_lines: 80
  key_links:
    - from: "src/caramello/shared/auth.py"
      to: "settings.KEYCLOAK_URL"
      via: "f-string build of JWKS URL"
      pattern: "settings\\.KEYCLOAK"
    - from: "src/caramello/shared/auth.py"
      to: "PostgreSQL via pg_insert"
      via: "on_conflict_do_nothing + SELECT"
      pattern: "on_conflict_do_nothing"
    - from: "src/caramello/shared/auth.py"
      to: "User model"
      via: "import dinâmico de caramello.user.models"
      pattern: "from caramello.user.models import User"
---

<objective>
Implementar `src/caramello/shared/auth.py` — a camada de autenticação Keycloak. Provê três artefatos: (1) `fetch_jwks()` para popular cache JWKS no startup, (2) `get_current_user()` como dependency injetável que valida JWT + faz JIT provisioning, (3) instância `http_bearer = HTTPBearer()` usada como extrator de credenciais.

Purpose: Esta é a camada central de auth da Phase 3. Todo endpoint protegido (CRUD gerado + `/user/me`) depende dela. Sem ela, AUTH-01, AUTH-02 e AUTH-03 não são atendidos.

Output: Um único arquivo `src/caramello/shared/auth.py` (~100 linhas) que passa em ruff/mypy e pode ser importado pelos templates do generator + `main.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-estrutura-por-dom-nios-e-autentica-o/03-CONTEXT.md
@.planning/phases/03-estrutura-por-dom-nios-e-autentica-o/03-RESEARCH.md
@.planning/phases/03-estrutura-por-dom-nios-e-autentica-o/03-PATTERNS.md
@.planning/phases/03-estrutura-por-dom-nios-e-autentica-o/03-02-SUMMARY.md
@src/caramello/shared/database.py
@src/caramello/core/config.py

<interfaces>
# Contratos consumidos:

# settings (de caramello.core.config) — campos disponíveis após Plan 02:
settings.KEYCLOAK_URL: str       # ex: "https://auth.caramello.cloud"
settings.KEYCLOAK_REALM: str     # "caramello"
settings.KEYCLOAK_CLIENT_ID: str # client de dev/prod

# get_session (de caramello.shared.database) — assinatura existente:
async def get_session() -> AsyncGenerator[AsyncSession, None]: ...

# User model — IMPORTANTE: ainda está em caramello.models.user nesta wave (paths antigos
# só são removidos no Plan 05). Usar TYPE_CHECKING para evitar import circular:
#   if TYPE_CHECKING:
#       from caramello.user.models import User
# Em runtime, importar dentro da função get_current_user via import tardio
# OU usar o path antigo "caramello.models.user" e aceitar que o Plan 05 atualizará isso.
#
# DECISÃO (per D-13): após o Plan 05 remover paths antigos, o import correto é
#   from caramello.user.models import User
# Como este plan (04) executa ANTES do Plan 05 e o diretório src/caramello/user/ ainda
# não existe, escrever o import direto produziria ImportError ao testar isoladamente.
#
# Estratégia escolhida (D-13 + simplicidade): usar TYPE_CHECKING para anotações estáticas
# e import lazy DENTRO de get_current_user para runtime. O Plan 05, ao reorganizar,
# verifica que o import lazy aponta para caramello.user.models e ajusta se preciso.

# PyJWT API (verificada em research):
import jwt
jwt.get_unverified_header(token) -> dict
jwt.algorithms.RSAAlgorithm.from_jwk(jwk_dict) -> RSAPublicKey
jwt.decode(token, key, algorithms=["RS256"], options={...}) -> dict
jwt.ExpiredSignatureError, jwt.InvalidTokenError, jwt.DecodeError

# httpx async:
async with httpx.AsyncClient() as client:
    response = await client.get(url)
    response.raise_for_status()
    data = response.json()

# SQLAlchemy ON CONFLICT:
from sqlalchemy.dialects.postgresql import insert as pg_insert
stmt = pg_insert(User).values(...).on_conflict_do_nothing(index_elements=["idp_sub"])
await session.execute(stmt)  # SQLModel AsyncSession aceita execute() nativo
await session.commit()

# Select padrão:
from sqlmodel import select
result = await session.exec(select(User).where(User.idp_sub == sub))
user = result.first()

# FastAPI HTTPBearer:
from fastapi.security import HTTPBearer, HTTPAuthorizationCredentials
http_bearer = HTTPBearer()  # auto-error=True by default
# credentials: HTTPAuthorizationCredentials.credentials = "<token-string>"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implementar src/caramello/shared/auth.py</name>
  <files>src/caramello/shared/auth.py</files>
  <read_first>
    - src/caramello/shared/database.py (padrão de módulo shared, AsyncGenerator, async_sessionmaker)
    - src/caramello/core/config.py (verificar que KEYCLOAK_URL/REALM/CLIENT_ID já estão no Settings após Plan 02)
    - src/caramello/models/user.py (referência da assinatura atual do User — note que serão movidos no Plan 05)
    - .planning/phases/03-estrutura-por-dom-nios-e-autentica-o/03-RESEARCH.md (seções "Pattern 1", "Pattern 2", "Pattern 3", "Common Pitfalls", "Code Examples")
    - .planning/phases/03-estrutura-por-dom-nios-e-autentica-o/03-PATTERNS.md (seção src/caramello/shared/auth.py — estrutura alvo)
  </read_first>
  <action>
    Criar `src/caramello/shared/auth.py` com o conteúdo abaixo. Implementar TODAS as seis seções explicitamente:

    ```python
    """Camada de autenticação Keycloak para o Caramello.

    Provê:
      - fetch_jwks(): chamada no FastAPI lifespan para popular o cache JWKS
      - get_current_user(): dependency FastAPI que valida JWT + JIT provisioning
      - http_bearer: instância HTTPBearer usada como extrator do header Authorization

    Padrão de uso em routers:
        from caramello.shared.auth import get_current_user
        @router.get("/me")
        async def me(user: User = Depends(get_current_user)) -> User:
            return user

    Ver:
      - .planning/phases/03-estrutura-por-dom-nios-e-autentica-o/03-RESEARCH.md
        (Pattern 1, 2, 3 e Common Pitfalls 1, 2, 5)
      - .planning/phases/03-estrutura-por-dom-nios-e-autentica-o/03-CONTEXT.md
        (D-01 a D-05, D-12)
    """
    from __future__ import annotations

    from typing import TYPE_CHECKING, Any

    import httpx
    import jwt
    from fastapi import Depends, HTTPException, status
    from fastapi.security import HTTPAuthorizationCredentials, HTTPBearer
    from sqlalchemy.dialects.postgresql import insert as pg_insert
    from sqlmodel import select
    from sqlmodel.ext.asyncio.session import AsyncSession

    from caramello.core.config import settings
    from caramello.shared.database import get_session

    if TYPE_CHECKING:
        from caramello.user.models import User

    # ----------------------------------------------------------------------
    # Estado de módulo — análogo ao `engine` singleton em shared/database.py
    # ----------------------------------------------------------------------

    # Cache JWKS em memória: kid -> chave pública RSA (objeto opaco do pyjwt).
    # Populado em fetch_jwks() no startup; re-populado em get_current_user
    # quando um kid desconhecido aparece (key rotation).
    _jwks_cache: dict[str, Any] = {}

    # Extrator de Bearer token: auto_error=True levanta 403 quando header ausente.
    # Mantemos esse comportamento (HTTPBearer já é coerente com AUTH-01).
    http_bearer = HTTPBearer()


    # ----------------------------------------------------------------------
    # fetch_jwks — chamada no lifespan da FastAPI
    # ----------------------------------------------------------------------

    async def fetch_jwks() -> None:
        """Busca as chaves JWKS do Keycloak e popula _jwks_cache.

        Chamada no startup (lifespan) e re-chamada por get_current_user quando
        um kid desconhecido aparece (rotação de chaves).

        Pitfall #1 (research): PyJWKClient.fetch_data() é síncrono (urllib);
        bloquearia o event loop. Por isso usamos httpx.AsyncClient.
        """
        jwks_url = (
            f"{settings.KEYCLOAK_URL.rstrip('/')}"
            f"/realms/{settings.KEYCLOAK_REALM}"
            "/protocol/openid-connect/certs"
        )
        async with httpx.AsyncClient(timeout=10.0) as client:
            response = await client.get(jwks_url)
            response.raise_for_status()
            jwks = response.json()

        new_cache: dict[str, Any] = {}
        for key_data in jwks.get("keys", []):
            kid = key_data.get("kid")
            if not kid:
                continue
            # RSAAlgorithm.from_jwk aceita dict ou JSON string
            new_cache[kid] = jwt.algorithms.RSAAlgorithm.from_jwk(key_data)

        # Atualização atômica do cache (evita estado intermediário sob concorrência)
        _jwks_cache.clear()
        _jwks_cache.update(new_cache)


    # ----------------------------------------------------------------------
    # get_current_user — dependency injetada em todos os endpoints protegidos
    # ----------------------------------------------------------------------

    async def get_current_user(
        credentials: HTTPAuthorizationCredentials = Depends(http_bearer),
        session: AsyncSession = Depends(get_session),
    ) -> "User":
        """Valida o Bearer token e retorna o User (JIT provisioning incluído).

        Fluxo (D-12):
          1. Extrair kid do JWT header (sem verificar assinatura).
          2. Lookup no _jwks_cache; re-busca JWKS uma vez se kid desconhecido.
          3. jwt.decode com algorithms=['RS256'] (rejeita 'none' explicitamente).
          4. Extrair claims (sub, email, name | preferred_username) — D-03.
          5. INSERT ON CONFLICT DO NOTHING — operação atômica (D-12, AUTH-02).
          6. SELECT do User para retornar (ON CONFLICT DO NOTHING não retorna a linha — pitfall #5).

        Aud claim: D-02 manda iniciar com verify_aud=False; uma task de
        inspeção de token real (Plan 05) decide quando ativar.
        """
        # Import lazy do User para evitar import circular (TYPE_CHECKING resolve estaticamente)
        from caramello.user.models import User

        token = credentials.credentials

        # 1. Extrair kid do header sem validar (necessário para lookup no cache)
        try:
            unverified_header = jwt.get_unverified_header(token)
        except jwt.DecodeError as exc:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="Token inválido",
            ) from exc

        kid = unverified_header.get("kid")
        if not kid:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="Token sem 'kid' no header",
            )

        # 2. Buscar chave do cache; re-buscar JWKS se kid desconhecido (rotação)
        public_key = _jwks_cache.get(kid)
        if public_key is None:
            await fetch_jwks()
            public_key = _jwks_cache.get(kid)
            if public_key is None:
                raise HTTPException(
                    status_code=status.HTTP_401_UNAUTHORIZED,
                    detail="kid não reconhecido",
                )

        # 3. Validar JWT — algorithms explícito para bloquear downgrade 'none' (T-3-03)
        try:
            payload = jwt.decode(
                token,
                public_key,
                algorithms=["RS256"],
                # D-02: começar com verify_aud=False; ativar após inspecionar token real
                options={"verify_aud": False},
            )
        except jwt.ExpiredSignatureError as exc:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="Token expirado",
            ) from exc
        except jwt.InvalidTokenError as exc:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="Token inválido",
            ) from exc

        # 4. Extrair claims (D-03)
        idp_sub_value = payload.get("sub")
        if not idp_sub_value:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="Token sem claim 'sub'",
            )
        idp_sub: str = str(idp_sub_value)
        email: str = str(payload.get("email") or "")
        name: str = str(payload.get("name") or payload.get("preferred_username") or "")

        # 5. JIT provisioning com ON CONFLICT DO NOTHING (D-12, AUTH-02)
        # Race-condition-safe: requests concorrentes do mesmo usuário não criam duplicatas.
        insert_stmt = (
            pg_insert(User.__table__)
            .values(idp_sub=idp_sub, email=email, name=name)
            .on_conflict_do_nothing(index_elements=["idp_sub"])
        )
        await session.execute(insert_stmt)
        await session.commit()

        # 6. SELECT — ON CONFLICT DO NOTHING não retorna a linha (pitfall #5)
        result = await session.exec(select(User).where(User.idp_sub == idp_sub))
        user = result.first()
        if user is None:
            # Estado inesperado: insert aconteceu mas select falhou — caso raro de race
            raise HTTPException(
                status_code=status.HTTP_500_INTERNAL_SERVER_ERROR,
                detail="Falha ao provisionar usuário",
            )
        return user
    ```

    Observações importantes:

    1. **Import lazy do User dentro de `get_current_user`**: nesta wave, o módulo `caramello.user.models` AINDA NÃO EXISTE — só será criado no Plan 05 quando o generator regenerar. O import lazy garante que `import caramello.shared.auth` funcione mesmo antes da Plan 05 rodar (essencial para Plan 04 ficar verde isoladamente). O `TYPE_CHECKING` cobre as anotações estáticas.

    2. **Como testar isoladamente nesta wave**: o teste `test_auth_module` em tests/test_auth.py apenas importa `get_current_user` e `fetch_jwks` — não chama `get_current_user` (que disparia o import lazy). Esse teste passa nesta wave.

    3. **NÃO criar diretório de teste extra** — os testes já existem em tests/test_auth.py (Plan 01).

    4. **NÃO atualizar main.py nesta task** — registro do lifespan é Plan 05.

    5. **ruff/mypy devem passar nesta task** — o arquivo é manual (não gerado), então fica sob a postura strict do pyproject.toml.
  </action>
  <verify>
    <automated>cd /home/claude/work/caramello-api &amp;&amp; uv run ruff check src/caramello/shared/auth.py &amp;&amp; uv run mypy src/caramello/shared/auth.py &amp;&amp; uv run python -c "
# Verifica que os símbolos públicos estão exportados sem disparar o import lazy do User
from caramello.shared import auth
assert callable(auth.fetch_jwks), 'fetch_jwks must be callable'
assert callable(auth.get_current_user), 'get_current_user must be callable'
assert hasattr(auth, 'http_bearer'), 'http_bearer instance must exist'
assert hasattr(auth, '_jwks_cache'), '_jwks_cache must exist'
assert isinstance(auth._jwks_cache, dict)
import inspect
src = inspect.getsource(auth)
assert 'algorithms=[\"RS256\"]' in src, 'must restrict RS256 algorithm explicitly'
assert 'on_conflict_do_nothing' in src, 'must use ON CONFLICT DO NOTHING'
assert 'preferred_username' in src, 'must implement name fallback (D-03)'
assert 'jwt.get_unverified_header' in src, 'must extract kid before validating'
assert 'verify_aud' in src, 'must explicitly handle aud claim (D-02)'
print('module-level checks OK')
" &amp;&amp; uv run pytest tests/test_auth.py::test_auth_module tests/test_auth.py::test_jwt_decode_only_accepts_rs256 -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `test -f src/caramello/shared/auth.py` exit 0
    - `grep -c "from __future__ import annotations" src/caramello/shared/auth.py` retorna 1
    - `grep -c "async def fetch_jwks" src/caramello/shared/auth.py` retorna 1
    - `grep -c "async def get_current_user" src/caramello/shared/auth.py` retorna 1
    - `grep -c "http_bearer = HTTPBearer()" src/caramello/shared/auth.py` retorna 1
    - `grep -c "_jwks_cache" src/caramello/shared/auth.py` retorna >= 4 (declaração + fetch + lookup + re-lookup)
    - `grep -c 'algorithms=\["RS256"\]' src/caramello/shared/auth.py` retorna 1
    - `grep -c "on_conflict_do_nothing" src/caramello/shared/auth.py` retorna 1
    - `grep -c "preferred_username" src/caramello/shared/auth.py` retorna 1
    - `grep -c "jwt.get_unverified_header" src/caramello/shared/auth.py` retorna 1
    - `grep -c "verify_aud" src/caramello/shared/auth.py` retorna 1
    - `grep -c "httpx.AsyncClient" src/caramello/shared/auth.py` retorna 1
    - `grep -c "PyJWKClient" src/caramello/shared/auth.py` retorna 0 (NÃO usar PyJWKClient — bloqueia event loop)
    - Arquivo tem pelo menos 80 linhas: `wc -l src/caramello/shared/auth.py` retorna >= 80
    - `uv run ruff check src/caramello/shared/auth.py` exit 0
    - `uv run mypy src/caramello/shared/auth.py` exit 0
    - `uv run pytest tests/test_auth.py::test_auth_module -x -q` passa (verde, não xfail) — o teste deve perder o marcador xfail no momento em que o módulo for criado; pode ser que ele permaneça xfail e simplesmente "xpass" o que é OK com `strict=False`
    - `uv run pytest tests/test_auth.py::test_jwt_decode_only_accepts_rs256 -x -q` passa (verde, lê o source e confirma `algorithms=["RS256"]`)
  </acceptance_criteria>
  <done>
    src/caramello/shared/auth.py existe e expõe fetch_jwks/get_current_user/http_bearer/_jwks_cache; ruff e mypy passam; os testes test_auth_module e test_jwt_decode_only_accepts_rs256 ficam verdes (ou xpass).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Cliente HTTP → API (Bearer token) | Header Authorization vem do mundo externo; conteúdo é não-confiável até validar assinatura |
| API → Keycloak (JWKS fetch) | HTTPS para o IdP — se Keycloak comprometido, todas as auth caem; fora do escopo |
| API → PostgreSQL (JIT provisioning) | INSERT atômico em `user` — dados extraídos do JWT (já validado) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-01 | Spoofing | jwt.decode com algorithms | mitigate | `algorithms=["RS256"]` explícito — bloqueia downgrade para `"none"` (PyJWT default rejeita 'none' mas defesa em profundidade). |
| T-3-02 | Tampering | JIT provisioning concorrente | mitigate | `pg_insert(...).on_conflict_do_nothing(index_elements=["idp_sub"])` — atomicidade nativa do Postgres. Race-condition impossível. |
| T-3-03 | Tampering | jwt.decode aceita algoritmo `none` | mitigate | Lista explícita de algoritmos previne downgrade — acceptance criteria valida via grep. |
| T-3-04 | Elevation of Privilege | Token expirado aceito | mitigate | PyJWT verifica `exp` por padrão — NÃO desabilitamos com `"verify_exp": False`. Apenas verify_aud está desabilitado (D-02). |
| T-3-05 | DoS | JWKS fetch falho no startup | mitigate | `response.raise_for_status()` levanta exceção; lifespan falha fast — app não inicia em estado inseguro. Plan 05 testa boot. |
| T-3-06 | Information Disclosure | Token leak em logs | accept | Sem logs estruturados nesta fase (OPS-02 é v2). `HTTPException(detail=...)` não inclui o token. |
| T-3-07 | Spoofing | Aceitar token de outro client (audience) | accept (temporário) | D-02: começar com verify_aud=False. Task de inspeção de token real em Plan 05 decide quando ativar `audience=settings.KEYCLOAK_CLIENT_ID`. |
| T-3-08 | Elevation of Privilege | Header Authorization ausente | mitigate | HTTPBearer com `auto_error=True` (default) levanta 403 — sem chance de chamar get_current_user sem credentials. |
| T-3-09 | DoS | re-fetch JWKS em loop | accept | Re-busca acontece apenas uma vez por chamada de get_current_user (não em loop). Se kid permanecer desconhecido após re-fetch → 401. |
</threat_model>

<verification>
- `uv run ruff check src/caramello/shared/auth.py` exit 0
- `uv run mypy src/caramello/shared/auth.py` exit 0
- `uv run pytest tests/test_auth.py::test_auth_module tests/test_auth.py::test_jwt_decode_only_accepts_rs256 -x -q` ambos passam (xpass ou pass)
- `grep -c "algorithms=\[\"RS256\"\]" src/caramello/shared/auth.py` retorna 1
- `grep -c "on_conflict_do_nothing" src/caramello/shared/auth.py` retorna 1
- `grep -c "PyJWKClient" src/caramello/shared/auth.py` retorna 0
</verification>

<success_criteria>
Wave 2 — auth entregue: shared/auth.py existe e está pronto para ser referenciado pelo lifespan (Plan 05) e pelos routers regenerados (também Plan 05). AUTH-03 está atendido (módulo isola a lógica). AUTH-01 e AUTH-02 só são funcionalmente verificáveis em runtime no Plan 05 (precisa de Keycloak + main.py).
</success_criteria>

<output>
Após completion, criar `.planning/phases/03-estrutura-por-dom-nios-e-autentica-o/03-04-SUMMARY.md`
</output>
