---
phase: 06-funda-o-dsl-schema
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/generate_code.py
  - dsl/schema.yaml
  - tests/test_generator.py
autonomous: true
requirements:
  - "Nenhum requisito funcional direto — habilita SC-7 e SC-8 do ROADMAP Phase 6"
must_haves:
  truths:
    - "O gerador mapeia o tipo DSL `Decimal` para anotação Python `Decimal` + coluna `Column(Numeric(15, 2))`"
    - "O gerador emite `__table_args__` com `Index(...)` para entidades que declaram bloco `filters:`"
    - "models.py gerado com campo Decimal contém `from decimal import Decimal` e `from sqlalchemy import Column, ..., Numeric`"
    - "`_run_ruff_fix` aplica ruff em qualquer domínio novo (incluindo `finances`) via descoberta dinâmica"
  artifacts:
    - path: "scripts/generate_code.py"
      provides: "Gerador estendido: Decimal, filters→__table_args__, DOMAIN_TO_ENTITY_NAME[finances], ruff dinâmico"
      contains: "Numeric(15, 2)"
    - path: "tests/test_generator.py"
      provides: "Testes unitários do gerador para Decimal e filters"
      contains: "def test_generator_decimal_emits_numeric"
  key_links:
    - from: "scripts/generate_code.py get_field_definition"
      to: "Column(Numeric(15, 2))"
      via: "ramo ftype == Decimal"
      pattern: "Numeric\\(15, 2\\)"
    - from: "scripts/generate_code.py generate_models"
      to: "__table_args__"
      via: "_build_table_args(entity_data)"
      pattern: "__table_args__"
---

<objective>
Estender o gerador DSL (`scripts/generate_code.py`) para suportar o tipo monetário `Decimal` (D-01/D-02/D-03), o bloco `filters:` que gera `Index` em `__table_args__` (D-11), o registro do domínio `finances` em `DOMAIN_TO_ENTITY_NAME`, e corrigir `_run_ruff_fix` para descoberta dinâmica de domínios. Criar os testes unitários do gerador (Wave 0) que provam esse comportamento sem precisar de banco.

Purpose: O gerador é o pré-requisito de todo o restante da fase — os 5 YAMLs financeiros só podem ser gerados corretamente depois que o gerador conhecer `Decimal` e `filters:`. Gerar antes produziria `FLOAT` (perda de precisão, pitfall P1) e nenhum índice.
Output: `scripts/generate_code.py` estendido, `dsl/schema.yaml` documentando `filters:`, e testes unitários novos em `tests/test_generator.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-funda-o-dsl-schema/06-CONTEXT.md
@.planning/phases/06-funda-o-dsl-schema/06-RESEARCH.md
@.planning/phases/06-funda-o-dsl-schema/06-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — testes unitários do gerador para Decimal e filters</name>
  <files>tests/test_generator.py</files>
  <read_first>
    - tests/test_generator.py (padrão de testes existentes: import dinâmico de scripts.generate_code via sys.path, padrão `test_router_url_has_domain_prefix_and_hyphens` linhas 184-229)
    - scripts/generate_code.py (funções `map_type_to_python` linhas 50-75, `get_field_definition` linhas 78-115, `generate_models` linhas 192-351 — assinaturas atuais)
    - .planning/phases/06-funda-o-dsl-schema/06-RESEARCH.md (Pattern 1 e Pattern 2 — output esperado verificado localmente)
  </read_first>
  <behavior>
    - test_generator_decimal_emits_numeric: chamar `generate_models` (ou `_consolidate_models`) com uma entidade contendo um campo `{name: amount, type: Decimal, nullable: false}` produz string contendo `Column(Numeric(15, 2)` e `nullable=False`, e o cabeçalho importa `from decimal import Decimal`.
    - test_generator_filters_emits_table_args: chamar `generate_models` com uma entidade que tem `filters: [{fields: [account_id]}, {fields: [competencia_year, competencia_month]}]` produz string contendo `__table_args__ = (`, `Index("ix_<table>_account_id", "account_id")` e `Index("ix_<table>_competencia_year_competencia_month", "competencia_year", "competencia_month")`.
    - Ambos os testes usam o mesmo padrão de import dinâmico de `scripts.generate_code` já presente no arquivo (sys.path insert/remove de REPO_ROOT).
  </behavior>
  <action>
    Adicionar duas funções de teste a `tests/test_generator.py` seguindo o padrão de import dinâmico já existente (`sys.path.insert(0, str(REPO_ROOT))` em try/finally, como em `test_router_url_has_domain_prefix_and_hyphens`). Os testes chamam `generate_models(entity_data, entity_domain={})` com `entity_data` mínimo (campos `id`, `uuid`, `amount: Decimal` para o primeiro; campos + bloco `filters:` para o segundo). Asserir os strings literais descritos em <behavior>. Estes testes devem FALHAR inicialmente (RED) porque o gerador ainda não suporta `Decimal` nem `filters:`. NÃO criar ainda os arquivos de finances — estes testes operam apenas sobre a saída em memória do gerador.
  </action>
  <verify>
    <automated>uv run pytest tests/test_generator.py::test_generator_decimal_emits_numeric tests/test_generator.py::test_generator_filters_emits_table_args -v</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_generator.py contém `def test_generator_decimal_emits_numeric`
    - tests/test_generator.py contém `def test_generator_filters_emits_table_args`
    - Antes da Task 2 os dois testes FALHAM (RED): saída do comando mostra 2 failed
    - Os testes referenciam `Numeric(15, 2)` e `__table_args__` como strings esperadas
  </acceptance_criteria>
  <done>Dois testes novos existem e falham com a mensagem esperada (gerador ainda não emite Decimal/filters).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Estender o gerador — Decimal, filters→__table_args__, finances, ruff dinâmico</name>
  <files>scripts/generate_code.py, dsl/schema.yaml</files>
  <read_first>
    - scripts/generate_code.py (INTEGRAL das funções: `DOMAIN_TO_ENTITY_NAME` linhas 32-37, `map_type_to_python` linhas 50-75, `get_field_definition` linhas 78-115, `generate_models` linhas 192-351 — em especial bloco de imports linhas 247-267 e linha `__tablename__` 279, `_run_ruff_fix` linhas 909-929)
    - .planning/phases/06-funda-o-dsl-schema/06-RESEARCH.md (Pattern 1, Pattern 2, exemplos de código `map_type_to_python`/`get_field_definition`/`_build_table_args` — todos verificados localmente; Pitfall 3 sa_column + unique; Pitfall 5 __table_args__ só na classe table=True)
    - .planning/phases/06-funda-o-dsl-schema/06-PATTERNS.md (seção `scripts/generate_code.py` — pontos exatos de injeção; seção `dsl/schema.yaml` — chave `filters:`)
  </read_first>
  <action>
    Aplicar cinco modificações concretas em `scripts/generate_code.py`:
    (1) Em `map_type_to_python`, adicionar a entrada `"decimal": "Decimal"` ao dict `type_map`.
    (2) Em `get_field_definition`, adicionar um ramo: quando `ftype == "Decimal"`, retornar a linha `Field(sa_column=Column(Numeric(15, 2), nullable=<False|True>))` em vez do `Field(...)` padrão — `nullable=False` quando o campo não é nullable, `nullable=True` caso contrário. NÃO combinar `unique=True` com `sa_column` (Pitfall 3): campos Decimal nesta fase não têm `unique`.
    (3) Em `generate_models`, calcular `needs_decimal = any(map_type_to_python(f["type"]) == "Decimal" for f in fields)` e `needs_table_args = bool(entity_data.get("filters"))`. Quando `needs_decimal`, emitir no bloco de imports stdlib `from decimal import Decimal`; quando `needs_decimal or needs_table_args`, emitir `from sqlalchemy import Column, Index, Numeric` (incluir apenas os símbolos efetivamente usados — `Numeric`/`Column` para Decimal, `Index` para filters). Adicionar um novo helper `_build_table_args(entity_data)` que retorna o bloco `__table_args__ = ( Index("ix_{table}_{'_'.join(fields)}", "<f1>", "<f2>"), ... )` ou `None` quando não há `filters:`. Injetar a saída de `_build_table_args` imediatamente após a linha `code += f'    __tablename__ = "{table_name}"\n\n'` (linha 279) — APENAS na classe `table=True`, nunca nas classes Read/Create/Update.
    (4) Em `DOMAIN_TO_ENTITY_NAME`, adicionar `"finances": "Account"` (A1 do RESEARCH — âncora do stub de operations).
    (5) Em `_run_ruff_fix`, substituir a lista hardcoded `("user", "family", "users", "families")` por descoberta dinâmica: iterar `src_dir.iterdir()`, incluir diretórios cujo nome não começa com `_` e não está em `{"shared", "core"}`.
    Em `dsl/schema.yaml`, adicionar a chave de topo `filters:` documentada como array de objetos com `fields: [string]` (seguir o padrão da seção `dsl/schema.yaml` em 06-PATTERNS.md). Verificar que os dois testes da Task 1 passam (GREEN) e que a suite existente (16 testes) continua verde.
  </action>
  <verify>
    <automated>uv run pytest tests/test_generator.py -v</automated>
  </verify>
  <acceptance_criteria>
    - scripts/generate_code.py contém `"decimal": "Decimal"` no type_map
    - scripts/generate_code.py contém `Numeric(15, 2)` no ramo de `get_field_definition`
    - scripts/generate_code.py contém `def _build_table_args`
    - scripts/generate_code.py contém `"finances": "Account"` em DOMAIN_TO_ENTITY_NAME
    - `grep -n 'user", "family", "users", "families"' scripts/generate_code.py` retorna vazio (lista hardcoded removida de _run_ruff_fix)
    - dsl/schema.yaml contém a chave `filters:`
    - `uv run pytest tests/test_generator.py -v` sai 0 com test_generator_decimal_emits_numeric e test_generator_filters_emits_table_args passando, e todos os testes preexistentes verdes
  </acceptance_criteria>
  <done>Gerador suporta Decimal e filters; os 2 testes novos passam (GREEN); suite preexistente intacta.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| YAML DSL → código gerado | Input de definição confiável (autoria do desenvolvedor); processado em build-time, não em runtime de produção |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | Campo monetário gerado como FLOAT em vez de NUMERIC | mitigate | Ramo `Decimal → Column(Numeric(15,2))` em `get_field_definition`; teste `test_generator_decimal_emits_numeric` bloqueia regressão |
| T-06-02 | Information Disclosure | Geração de código não expõe dados de usuário | accept | Fase build-time; sem dados em runtime, sem superfície de ataque |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | Nenhum pacote externo novo instalado nesta fase (RESEARCH §Package Legitimacy Audit: não aplicável) |
</threat_model>

<verification>
- `uv run pytest tests/test_generator.py -v` verde (incluindo os 2 testes novos + 16 preexistentes)
- Nenhum pacote novo instalado (sem alteração em pyproject.toml/uv.lock)
</verification>

<success_criteria>
SC-7 (gerador emite `Numeric(15, 2)` para `Decimal`) e SC-8 (`__table_args__` para `filters:`) do ROADMAP §Phase 6 verificados por teste unitário. Gerador pronto para gerar o domínio finances no Plano 02.
</success_criteria>

<output>
Create `.planning/phases/06-funda-o-dsl-schema/06-01-SUMMARY.md` when done
</output>
