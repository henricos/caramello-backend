---
phase: 08-movimenta-es-importa-o
plan: "01"
type: execute
wave: 0
depends_on: []
files_modified:
  - pyproject.toml
  - tests/test_finances_operations.py
  - tests/test_services/test_finances_service.py
autonomous: false
requirements: [MOV-01, MOV-02, MOV-03, MOV-04, MOV-05]
user_setup: []

must_haves:
  truths:
    - "Toda função de teste descrita no Test Map de RESEARCH.md existe como teste skipado/red executável"
    - "ofxparse e openpyxl estão declarados em pyproject.toml dependencies"
    - "tests/test_services/test_finances_service.py existe e cobre os parsers"
  artifacts:
    - path: "tests/test_finances_operations.py"
      provides: "Stubs de teste para MOV-01..05, D-15, AUTH-FIN em endpoints de Movement"
      contains: "def test_create_movement"
    - path: "tests/test_services/test_finances_service.py"
      provides: "Stubs de teste puros para _parse_csv, _parse_ofx, _parse_xlsx, hash, threshold"
      contains: "def test_parse_csv"
    - path: "pyproject.toml"
      provides: "Declaração das deps ofxparse e openpyxl"
      contains: "ofxparse"
  key_links:
    - from: "tests/test_services/test_finances_service.py"
      to: "caramello.finances.services"
      via: "import dentro do corpo do teste (lazy)"
      pattern: "from caramello.finances.services import"
---

<objective>
Criar a malha de testes (Nyquist) para toda a Phase 8 ANTES de qualquer código de produção, e adicionar as dependências `ofxparse` e `openpyxl` ao `pyproject.toml`. Os testes começam red/skipados e passam à medida que os planos 08-02, 08-03 e 08-04 entregam a implementação.

Purpose: Garantir que cada requisito (MOV-01..05, D-15, AUTH-FIN-01/02) tenha um `<automated>` correspondente antes da implementação, evitando código sem verificação.
Output: `tests/test_finances_operations.py` estendido, `tests/test_services/test_finances_service.py` criado, deps instaladas.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-movimenta-es-importa-o/08-CONTEXT.md
@.planning/phases/08-movimenta-es-importa-o/08-RESEARCH.md
@.planning/phases/08-movimenta-es-importa-o/08-PATTERNS.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verificar legitimidade de ofxparse e openpyxl antes de instalar</name>
  <read_first>
    - .planning/phases/08-movimenta-es-importa-o/08-RESEARCH.md (§Package Legitimacy Audit — ambos marcados [ASSUMED] porque slopcheck não rodou)
  </read_first>
  <files>pyproject.toml</files>
  <action>
    Checkpoint humano (sem automação). Verificar manualmente a legitimidade de `ofxparse` e `openpyxl` no PyPI conforme `<how-to-verify>` antes de liberar a instalação no Task 2. Gate não auto-aprovável: ambos marcados [ASSUMED] no RESEARCH.md.
  </action>
  <what-built>
    Nada ainda automatizado. Este checkpoint precede a instalação porque o RESEARCH.md marcou ambos os pacotes como [ASSUMED] (slopcheck bloqueado pelo sandbox). Pacotes [ASSUMED] não podem ser instalados sem verificação humana (gate não auto-aprovável).
  </what-built>
  <how-to-verify>
    1. Abrir https://pypi.org/project/ofxparse/ — confirmar repo source github.com/jseutter/ofxparse, versão 0.21 publicada, projeto real (não typosquatting).
    2. Abrir https://pypi.org/project/openpyxl/ — confirmar versão 3.1.5, mantenedor oficial, milhões de downloads.
    3. Confirmar que nenhum dos nomes é variação suspeita de pacote conhecido.
  </how-to-verify>
  <acceptance_criteria>
    - Operador confirmou explicitamente que ofxparse e openpyxl são pacotes legítimos no PyPI
  </acceptance_criteria>
  <resume-signal>Digite "approved" para liberar a instalação, ou descreva o problema encontrado</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Adicionar ofxparse e openpyxl ao pyproject.toml e instalar</name>
  <read_first>
    - pyproject.toml (lista `dependencies` nas linhas 6-19 — adicionar ao final dessa lista)
    - .planning/phases/08-movimenta-es-importa-o/08-RESEARCH.md (§Standard Stack — versões: ofxparse>=0.21, openpyxl>=3.1.5; python-multipart já presente via uv.lock 0.0.29)
  </read_first>
  <files>pyproject.toml</files>
  <action>
    Adicionar `"ofxparse>=0.21"` e `"openpyxl>=3.1.5"` à lista `dependencies` em `pyproject.toml` (`[project]`, linhas 6-19). NÃO adicionar python-multipart — já presente transitivamente (uv.lock 0.0.29). Rodar `uv add ofxparse>=0.21 openpyxl>=3.1.5` para sincronizar `uv.lock`. Confirmar import: `uv run python -c "import ofxparse, openpyxl"`.
  </action>
  <verify>
    <automated>uv run python -c "import ofxparse, openpyxl; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - pyproject.toml contém `ofxparse>=0.21` na lista dependencies
    - pyproject.toml contém `openpyxl>=3.1.5` na lista dependencies
    - `uv run python -c "import ofxparse, openpyxl"` sai com código 0
    - uv.lock atualizado com ambos os pacotes
  </acceptance_criteria>
  <done>Ambas as libs importáveis no ambiente uv; pyproject.toml e uv.lock atualizados.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Estender test_finances_operations.py e criar test_finances_service.py com stubs Nyquist</name>
  <read_first>
    - tests/test_finances_operations.py (arquivo existente, 730 linhas — ESTENDER, não substituir; ver helpers `_make_fake_user` linha 43, `_skip_if_stub` linha 24, padrão TestClient + dependency_overrides linhas 117-200, padrão call_count linhas 148-163, padrão auth linhas 275-368)
    - tests/test_services/test_family_service.py (analog para o novo arquivo de serviço — header linhas 1-14, @pytest.mark.asyncio, AsyncMock session)
    - .planning/phases/08-movimenta-es-importa-o/08-RESEARCH.md (§Validation Architecture — Phase Requirements → Test Map: lista nomes exatos de cada função de teste)
    - .planning/phases/08-movimenta-es-importa-o/08-PATTERNS.md (§tests/test_finances_operations.py e §tests/test_services/test_finances_service.py — padrões de upload multipart, mock de session.execute para hash pre-check, parser puro sem session)
  </read_first>
  <files>tests/test_finances_operations.py, tests/test_services/test_finances_service.py</files>
  <behavior>
    Cada teste abaixo deve existir e estar red/skipado (pula via `_skip_if_stub` enquanto operations não tem os endpoints, ou import lazy que falha em ImportError). Nenhum teste deve estar verde ainda — a implementação chega nos planos 08-02/03/04.

    Em tests/test_finances_operations.py (request-response, TestClient):
    - test_create_movement: POST /finances/accounts/{uuid}/movements cria movimentação, retorna 201 + uuid (MOV-01)
    - test_create_movement_409_duplicate: POST com hash já existente retorna 409 + existing_uuid (MOV-01, D-17)
    - test_import_csv: POST /import?format=csv retorna inserted + movements[] (MOV-02)
    - test_import_ofx: POST /import?format=ofx funciona com sample OFX (MOV-03)
    - test_import_xlsx: POST /import?format=xlsx funciona com BytesIO XLSX (MOV-03)
    - test_import_deduplication: reimportar mesmo arquivo não duplica (MOV-04)
    - test_import_potential_duplicates: CSV/XLSX com hash match retorna potential_duplicates[] (MOV-05, D-05)
    - test_import_confirm: POST /import/confirm insere confirmadas sem colisão de hash (MOV-05, D-08)
    - test_list_movements: GET /accounts/{uuid}/movements retorna lista paginada (D-15)
    - test_movements_require_auth: 401 sem token, 403 para família alheia (AUTH-FIN-01/02)

    Em tests/test_services/test_finances_service.py (unit puro, parsers):
    - test_parse_csv: _parse_csv detecta separador `;` e `,` via Sniffer (MOV-02, D-10)
    - test_parse_csv_error_lines: linha com amount inválido vai para error_lines[] sem abortar (MOV-02, D-13)
    - test_parse_csv_abort_threshold: >50% de linhas inválidas levanta ValueError (MOV-02, D-13)
    - test_compute_hash: hash de (account_id|date|amount|desc_norm) é determinístico; FITID gera hash distinto (D-04, D-07)
    - test_normalize_description: "  PIX  RECEBIDO " -> "pix recebido" (D-06)
  </behavior>
  <action>
    Estender `tests/test_finances_operations.py` adicionando as 10 funções de teste listadas no behavior, reaproveitando `_make_fake_user`, `_skip_if_stub`, o padrão TestClient + `dependency_overrides` (cleanup em `finally`), e o padrão `call_count` para mock multi-step. Para uploads usar `files={"file": ("test.csv", io.BytesIO(bytes), "text/csv")}` e query `?format=csv`. Mockar `mock_session.execute = AsyncMock()` para o hash pre-check além de `mock_session.exec`. Criar `tests/test_services/test_finances_service.py` (novo) seguindo o header e os padrões de `test_family_service.py`, com import lazy `from caramello.finances.services import ...` dentro de cada teste para que o arquivo colete mesmo antes de services.py existir (ImportError → red explícito, não erro de coleta: envolver import em try/skip ou usar `pytest.importorskip("caramello.finances.services")`). Os nomes das funções DEVEM ser idênticos aos do Test Map de RESEARCH.md para casar com os comandos `<automated>` dos planos seguintes.
  </action>
  <verify>
    <automated>uv run python -m pytest tests/test_finances_operations.py tests/test_services/test_finances_service.py --collect-only -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run python -m pytest tests/test_finances_operations.py tests/test_services/test_finances_service.py --collect-only -q` lista todas as 15 funções de teste do behavior
    - tests/test_services/test_finances_service.py existe e contém `def test_parse_csv`
    - tests/test_finances_operations.py contém `def test_create_movement` e `def test_import_csv`
    - `uv run python -m pytest tests/test_services/test_finances_service.py -q` não quebra a coleta (skip ou red, nunca erro de import na coleta)
    - Os testes existentes de Account/Category/Subcategory (11 passing) continuam coletáveis sem alteração
  </acceptance_criteria>
  <done>Todas as 15 funções de teste do Test Map existem, coletáveis, red/skipadas; nenhuma verde ainda.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev → PyPI/uv | Instalação de pacotes externos cruza fronteira de supply chain |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-SC | Tampering | uv add ofxparse/openpyxl | mitigate | checkpoint:human-verify (Task 1) verifica ambos no pypi.org antes de instalar; pacotes [ASSUMED] no RESEARCH.md exigem gate humano não auto-aprovável |
| T-08-01 | Repudiation | Cobertura de teste ausente | mitigate | Nyquist: toda função do Test Map existe como `<automated>` antes da implementação |
</threat_model>

<verification>
- `uv run python -c "import ofxparse, openpyxl"` → ok
- `uv run python -m pytest tests/test_finances_operations.py tests/test_services/test_finances_service.py --collect-only -q` lista 15 funções novas
- Suíte existente continua coletável (52 passed pré-Phase 8 não afetados)
</verification>

<success_criteria>
- ofxparse e openpyxl declarados em pyproject.toml e importáveis
- 15 funções de teste (10 em operations, 5 em service) criadas, red/skipadas, nomes idênticos ao Test Map de RESEARCH.md
- Nenhum teste verde ainda (implementação vem nos planos seguintes)
</success_criteria>

<output>
Create `.planning/phases/08-movimenta-es-importa-o/08-01-SUMMARY.md` when done
</output>
