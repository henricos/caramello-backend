---
phase: 08-movimenta-es-importa-o
plan: "03"
type: execute
wave: 2
depends_on: ["08-01", "08-02"]
files_modified:
  - src/caramello/finances/services.py
autonomous: true
requirements: [MOV-02, MOV-03, MOV-04, MOV-05]
user_setup: []

must_haves:
  truths:
    - "_parse_csv detecta separador ; e , automaticamente e respeita header case-insensitive"
    - "_parse_ofx extrai FITID e usa fallback ISO-8859-1 para bancos BR"
    - "_parse_xlsx usa read_only=True e fecha o workbook"
    - "Linhas inválidas vão para error_lines[] sem abortar; >50% aborta com ValueError"
    - "Hash de OFX usa FITID; hash de CSV/XLSX usa (account_id|date|amount|desc_norm)"
    - "import_movements deduplica via pre-check em lote + on_conflict_do_nothing"
  artifacts:
    - path: "src/caramello/finances/services.py"
      provides: "import_movements() + parsers privados + hash + dedup (lógica de negócio sem FastAPI)"
      contains: "async def import_movements"
      min_lines: 120
  key_links:
    - from: "src/caramello/finances/services.py"
      to: "caramello.finances.models.Movement"
      via: "import e select(Movement.import_hash)"
      pattern: "from caramello.finances.models import Movement"
    - from: "src/caramello/finances/services.py"
      to: "movement (banco)"
      via: "pg_insert + on_conflict_do_nothing"
      pattern: "on_conflict_do_nothing"
---

<objective>
Implementar `src/caramello/finances/services.py`: a camada de lógica de negócio (sem dependências FastAPI) com `import_movements()` orquestrando os parsers privados `_parse_csv`, `_parse_ofx`, `_parse_xlsx`, o cálculo de hash, a normalização de descrição, o parse de datas com fallback BR, a deduplicação em lote (pre-check + on_conflict_do_nothing safety net) e o tratamento de linhas inválidas (error_lines[] + abort >50%).

Purpose: Centralizar parsing e deduplicação fora dos routers (testável sem framework). É consumido pelos endpoints do plano 08-04.
Output: `src/caramello/finances/services.py` (novo arquivo) implementado e passando nos testes de parser do plano 08-01.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-movimenta-es-importa-o/08-CONTEXT.md
@.planning/phases/08-movimenta-es-importa-o/08-RESEARCH.md
@.planning/phases/08-movimenta-es-importa-o/08-PATTERNS.md
@src/caramello/families/services.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implementar parsers, hash e normalização em services.py (parte pura)</name>
  <read_first>
    - src/caramello/families/services.py (analog de estrutura/header de módulo de serviço — docstring, `from __future__ import annotations`, recebe AsyncSession por parâmetro)
    - tests/test_services/test_finances_service.py (testes red criados em 08-01 — nomes e asserts que esta task deve satisfazer)
    - .planning/phases/08-movimenta-es-importa-o/08-PATTERNS.md (§src/caramello/finances/services.py — ParsedRow dataclass, _normalize_description, _compute_hash, _parse_date, _parse_csv, _parse_ofx, _parse_xlsx, threshold abort)
    - .planning/phases/08-movimenta-es-importa-o/08-RESEARCH.md (§Code Examples e §Architecture Patterns Padrão 2/3/4; §Common Pitfalls P5 wb.close, P6 encoding OFX, P7 csv.Sniffer fallback)
  </read_first>
  <files>src/caramello/finances/services.py</files>
  <behavior>
    - ParsedRow dataclass com campos: date (datetime), amount (Decimal), description (str), fitid (str | None = None)
    - _normalize_description("  PIX  RECEBIDO ") == "pix recebido" (D-06: strip().lower() + colapsar espaços via re.sub(r'\s+',' '))
    - _compute_hash com fitid presente usa "fitid:{fitid}" (D-04); sem fitid usa "{account_id}|{date.isoformat}|{amount}|{norm_desc}" (D-07). sha256 hexdigest. -100 e +100 geram hashes distintos.
    - _parse_date tenta "%Y-%m-%d" depois "%d/%m/%Y" (D-12); data inválida levanta ValueError com número da linha
    - _parse_csv: csv.Sniffer no sample (D-10), fallback csv.excel em csv.Error (P7); DictReader; headers case-insensitive por nome (D-11); amount via Decimal(str(...)) nunca float (P1); linha inválida → não adiciona row, registra em error_lines
    - _parse_ofx: OfxParser.parse(BytesIO); em Exception, fallback decode iso-8859-1 + StringIO (P6); txn.id como fitid; txn.amount como Decimal(str(...))
    - _parse_xlsx: openpyxl.load_workbook(read_only=True); header case-insensitive; try/finally wb.close() (P5); Decimal(str(...))
    - threshold: se len(error_lines)/total > 0.5 levanta ValueError (D-13)
  </behavior>
  <action>
    Criar `src/caramello/finances/services.py` com `from __future__ import annotations` e imports: csv, hashlib, io, re, dataclass, datetime/timezone, Decimal, Any, select (sqlmodel), AsyncSession (sqlmodel.ext.asyncio.session), pg_insert (sqlalchemy.dialects.postgresql import insert as pg_insert), Movement (caramello.finances.models). Implementar: `ParsedRow` dataclass; `_normalize_description` (D-06); `_compute_hash(account_id, row)` (D-04/D-07); `_parse_date(value, line)` (D-12); `_parse_csv(content: bytes) -> tuple[list[ParsedRow], list[dict]]` retornando rows e error_lines (D-10/D-11/D-13 — aplicar o threshold de 50% aqui levantando ValueError); `_parse_ofx` (D-04/P6) e `_parse_xlsx` (P5) com mesma assinatura de retorno. Sempre `Decimal(str(valor))`, nunca `float` (P1). Importar `ofxparse`/`openpyxl` dentro das funções de parser (lazy) para não acoplar o módulo às libs quando só CSV é usado.
  </action>
  <verify>
    <automated>uv run python -m pytest tests/test_services/test_finances_service.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - src/caramello/finances/services.py existe e contém `class ParsedRow`, `def _normalize_description`, `def _compute_hash`, `def _parse_date`, `def _parse_csv`, `def _parse_ofx`, `def _parse_xlsx`
    - `grep -c "float(" src/caramello/finances/services.py` retorna 0 (sem float em parsing monetário)
    - services.py contém `Decimal(str(` em pelo menos um parser
    - services.py contém `wb.close()` dentro de bloco finally
    - services.py contém `iso-8859-1` (fallback OFX)
    - `uv run python -m pytest tests/test_services/test_finances_service.py -q` → todos verdes (test_parse_csv, test_parse_csv_error_lines, test_parse_csv_abort_threshold, test_compute_hash, test_normalize_description)
  </acceptance_criteria>
  <done>Parsers, hash e normalização implementados; testes de service verdes; zero float em campo monetário.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implementar import_movements() com deduplicação em lote</name>
  <read_first>
    - src/caramello/finances/services.py (estado após Task 1 — parsers e hash disponíveis)
    - src/caramello/shared/auth.py (linhas ~191-199 — padrão pg_insert + on_conflict_do_nothing já em uso no projeto; padrão session.execute para queries com IN)
    - .planning/phases/08-movimenta-es-importa-o/08-PATTERNS.md (§Batch hash pre-check, §on_conflict_do_nothing safety net, §import_movements public interface)
    - .planning/phases/08-movimenta-es-importa-o/08-CONTEXT.md (D-04 OFX dedup definitiva, D-05 CSV/XLSX duplicata suspeita, D-14 shape da resposta)
    - .planning/phases/08-movimenta-es-importa-o/08-RESEARCH.md (§Common Pitfalls P4 import_hash UNIQUE, P8 session.execute vs exec)
  </read_first>
  <files>src/caramello/finances/services.py</files>
  <behavior>
    - import_movements(content, format, account_id, session) -> dict no shape D-14: {inserted, duplicates_skipped, potential_duplicates[], error_lines[], movements[]}
    - Faz pre-check em lote: select(Movement.import_hash).where(import_hash.in_(all_hashes)) via session.execute (P8), retorna set de hashes existentes
    - OFX (rows com fitid): hash existente → duplicates_skipped (D-04, não pergunta, não insere)
    - CSV/XLSX (rows sem fitid): hash existente → potential_duplicates[] com {new_row, existing_movement_uuid, hash} (D-05), NÃO insere
    - Rows com hash ausente → inseridas; usa pg_insert(...).on_conflict_do_nothing(index_elements=["import_hash"]) como safety net (P4); commit
    - movements[] traz as inseridas com sucesso (uuid, date, amount, description, created_at) para display imediato (D-14)
    - error_lines[] propagado do parser
  </behavior>
  <action>
    Adicionar `async def import_movements(content: bytes, format: str, account_id: int, session: AsyncSession) -> dict[str, Any]` em `services.py`. Dispatch por `format` ("csv"/"ofx"/"xlsx") para o parser correspondente, obtendo (rows, error_lines). Computar hash de cada row via `_compute_hash`. Pre-check em lote com `session.execute(select(Movement.import_hash).where(Movement.import_hash.in_(hashes)))` e montar `existing = {row[0] for row in result.fetchall()}` (P8). Separar: rows com fitid e hash em existing → `duplicates_skipped` (D-04); rows sem fitid e hash em existing → `potential_duplicates[]` com `existing_movement_uuid` resolvido via consulta dos uuids dos hashes existentes (D-05/D-14); demais → inserir. Inserir via `pg_insert(Movement.__table__).values([...]).on_conflict_do_nothing(index_elements=["import_hash"])` + `await session.commit()` (P4 safety net). Refazer um select das movimentações inseridas (por uuid gerado ou por hash) para popular `movements[]`. Retornar dict no shape D-14. Usar `session.execute` (não `session.exec`) para todas as queries com `.in_()`/projeção (P8).
  </action>
  <verify>
    <automated>uv run python -c "import inspect; from caramello.finances.services import import_movements; assert inspect.iscoroutinefunction(import_movements); sig=inspect.signature(import_movements); assert set(sig.parameters) >= {'content','format','account_id','session'}, sig; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - services.py contém `async def import_movements(` com parâmetros content, format, account_id, session
    - import_movements é coroutine (inspect.iscoroutinefunction True)
    - services.py contém `on_conflict_do_nothing(index_elements=["import_hash"])`
    - services.py contém `select(Movement.import_hash)` com `.in_(` (pre-check em lote)
    - services.py usa `session.execute(` (não `session.exec(`) para as queries de hash
    - O retorno de import_movements contém as chaves: inserted, duplicates_skipped, potential_duplicates, error_lines, movements (shape D-14)
    - `grep -v '^#' src/caramello/finances/services.py | grep -c "session.exec("` retorna 0 (sem session.exec em queries de lote)
  </acceptance_criteria>
  <done>import_movements implementado com dedup em lote e shape D-14; assinatura verificável.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| arquivo carregado (bytes) → parser | conteúdo não confiável de extrato cruza para parsing em memória |
| parser → banco movement | inserção em lote sobre dados parseados |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-04 | Tampering | _parse_xlsx (XML/zip bomb) | mitigate | openpyxl read_only=True limita carga em memória; wb.close() em finally evita leak de file handle (P5) |
| T-08-05 | Tampering | precisão monetária | mitigate | Decimal(str(valor)) em todos os parsers; zero float em campo de valor (P1); verificado por grep |
| T-08-06 | Spoofing | colisão de hash de dedup | accept | SHA-256 sobre (account_id|date|amount|desc_norm); colisão criptográfica improvável; FITID para OFX é dedup definitiva (D-04) |
| T-08-07 | Denial of Service | flooding por importação | mitigate | threshold de 50% de linhas inválidas aborta o lote com ValueError (D-13) |
| T-08-08 | Tampering | UnicodeDecodeError OFX banco BR | mitigate | try/except com fallback decode iso-8859-1 + StringIO (P6) |
</threat_model>

<verification>
- `uv run python -m pytest tests/test_services/test_finances_service.py -q` → todos verdes
- `grep -v '^#' src/caramello/finances/services.py | grep -c "session.exec("` → 0
- `grep -c "float(" src/caramello/finances/services.py` → 0
- import_movements é coroutine com assinatura (content, format, account_id, session)
</verification>

<success_criteria>
- Parsers CSV/OFX/XLSX implementados com fallbacks (Sniffer, ISO-8859-1, read_only+close)
- Hash determinístico: FITID para OFX, composto para CSV/XLSX
- import_movements deduplica em lote (pre-check + on_conflict_do_nothing) e retorna shape D-14
- Zero float em campo monetário; session.execute (não exec) para queries com IN
</success_criteria>

<output>
Create `.planning/phases/08-movimenta-es-importa-o/08-03-SUMMARY.md` when done
</output>
