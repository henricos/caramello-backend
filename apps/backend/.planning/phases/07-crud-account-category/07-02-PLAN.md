---
phase: 07-crud-account-category
plan: 02
type: execute
wave: 1
depends_on: ["07-01"]
files_modified:
  - src/caramello/shared/auth.py
  - src/caramello/finances/operations.py
  - src/caramello/main.py
autonomous: true
requirements: [ACC-01, ACC-02, ACC-03, AUTH-FIN-01, AUTH-FIN-02]

must_haves:
  truths:
    - "POST /finances/accounts cria conta (name, type, currency) e resposta inclui uuid sem expor id/family_id (ACC-01)"
    - "GET /finances/accounts?family_uuid=xxx retorna apenas contas da família do usuário; 403 sem token; 403 para família alheia (ACC-02, AUTH-FIN-01, AUTH-FIN-02)"
    - "PATCH /finances/accounts/{uuid} com is_active=false arquiva conta sem deletar dados (ACC-03)"
    - "GET /finances/accounts/{uuid} retorna detalhe da conta se usuário é membro"
    - "Router finances_operations registrado em main.py ANTES de mcp.mount_http() (pitfall P7)"
  artifacts:
    - path: "src/caramello/shared/auth.py"
      provides: "Helper _require_family_access(family_id, current_user, session) reutilizável em Phases 7/8/9"
      contains: "_require_family_access"
    - path: "src/caramello/finances/operations.py"
      provides: "CRUD de Account scoped por família com schemas públicos *Public"
      contains: "AccountReadPublic"
      min_lines: 120
    - path: "src/caramello/main.py"
      provides: "Registro do router finances_operations antes do MCP"
      contains: "finances_operations.router"
  key_links:
    - from: "src/caramello/finances/operations.py"
      to: "caramello.shared.auth._require_family_access"
      via: "import + chamada após resolver family_uuid"
      pattern: "_require_family_access\\("
    - from: "src/caramello/main.py"
      to: "finances_operations.router"
      via: "app.include_router antes de mcp.mount_http()"
      pattern: "include_router\\(finances_operations\\.router\\)"
---

<objective>
Implementar o controle de acesso por família e o CRUD completo de Account no domínio finances, e registrar o router em `main.py`.

Esta é a base reutilizável da fase: o helper `_require_family_access` (D-04) em `shared/auth.py` será usado por Account (aqui), Category/Subcategory (Plan 03) e pelas Phases 8/9. O CRUD de Account usa schemas públicos `*Public` (D-07/D-08/D-09) que expõem `family_uuid` em vez de `family_id` interno.

Purpose: Entregar ACC-01/02/03 e AUTH-FIN-01/02 — usuário autenticado cria, lista, detalha, atualiza e arquiva contas da sua família, com 403 para acesso a famílias alheias.
Output: `shared/auth.py` estendido, `finances/operations.py` com schemas + CRUD de Account, `main.py` com router registrado antes do MCP.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-crud-account-category/07-CONTEXT.md
@.planning/phases/07-crud-account-category/07-RESEARCH.md
@.planning/phases/07-crud-account-category/07-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adicionar helper _require_family_access em shared/auth.py (D-04)</name>
  <files>src/caramello/shared/auth.py</files>
  <read_first>
    - src/caramello/shared/auth.py — arquivo a estender. Ver `get_current_user` (estilo async, raise HTTPException) e o padrão de import LAZY de `FamilyMember` já usado dentro de funções (07-PATTERNS.md cita linhas 201-205). `select`, `AsyncSession`, `status`, `HTTPException` já importados no topo (linhas 27-31).
    - .planning/phases/07-crud-account-category/07-PATTERNS.md — seção `shared/auth.py` com a assinatura exata do helper e o import lazy.
    - src/caramello/families/models.py — modelo `FamilyMember` (campos `family_id`, `user_id`).
  </read_first>
  <action>
    Adicionar a função `async def _require_family_access(family_id: int, current_user: "User", session: AsyncSession) -> None` APÓS as funções existentes (`fetch_jwks`, `get_current_user`). Comportamento: import LAZY `from caramello.families.models import FamilyMember` DENTRO do corpo da função (com `# noqa: PLC0415`) para evitar ciclo shared/ ↔ families/ (pitfall #6 do 07-RESEARCH.md — mesmo padrão de `get_current_user`). Executar `await session.exec(select(FamilyMember).where(FamilyMember.family_id == family_id, FamilyMember.user_id == current_user.id))`; se `result.first() is None`, levantar `HTTPException(status_code=status.HTTP_403_FORBIDDEN, detail="Você não é membro desta família")`; caso contrário retornar `None`. Usar `session.exec` (query simples em uma tabela, sem JOIN — pitfall #3). `User` deve estar disponível como forward ref (já em `TYPE_CHECKING` no topo). Não alterar nada mais no arquivo.
  </action>
  <verify>
    <automated>uv run python -c "import ast,sys; src=open('src/caramello/shared/auth.py').read(); assert '_require_family_access' in src; ast.parse(src); print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - src/caramello/shared/auth.py contém `async def _require_family_access(`.
    - O corpo da função contém `from caramello.families.models import FamilyMember` (import lazy, dentro da função, não no topo do módulo).
    - A função levanta `status.HTTP_403_FORBIDDEN` quando `result.first() is None`.
    - `uv run python -c "import caramello.shared.auth"` importa sem ImportError (sem ciclo de import).
  </acceptance_criteria>
  <done>Helper `_require_family_access` existe, usa import lazy de FamilyMember, levanta 403 para não-membros, e o módulo importa sem ciclo.</done>
</task>

<task type="auto">
  <name>Task 2: Implementar schemas públicos + CRUD de Account em finances/operations.py</name>
  <files>src/caramello/finances/operations.py</files>
  <read_first>
    - src/caramello/finances/operations.py — stub atual (será substituído integralmente). Primeira linha do novo arquivo deve ser `# CARAMELLO-GENERATED: implemented`.
    - src/caramello/families/operations.py — analog EXATO de estilo: APIRouter com prefix, schemas locais BaseModel, helpers, padrão de POST/GET/PATCH, resolução UUID→objeto, raise 404/403.
    - .planning/phases/07-crud-account-category/07-PATTERNS.md — seção `finances/operations.py`: cabeçalho/imports, schemas `*Public`, POST com resolução UUID→ID, GET listagem, GET detail, PATCH com `updated_at` manual.
    - src/caramello/finances/models.py — `Account` (campos `family_id`, `name`, `type`, `currency`, `is_active`, `uuid`, `created_at`, `updated_at`).
    - .planning/phases/07-crud-account-category/07-CONTEXT.md — D-07/D-08/D-09/D-11 (schemas públicos, Literal de type), especifics (currency default "BRL", is_active via PATCH).
  </read_first>
  <action>
    Substituir o conteúdo de finances/operations.py. Primeira linha: `# CARAMELLO-GENERATED: implemented`. Imports: `APIRouter, Depends, HTTPException, status` de fastapi; `BaseModel` de pydantic; `select` de sqlmodel; `AsyncSession` de sqlmodel.ext.asyncio.session; `Account` de caramello.finances.models; `Family` de caramello.families.models; `get_current_user, _require_family_access` de caramello.shared.auth; `get_session` de caramello.shared.database; `User` de caramello.users.models; `datetime, timezone`, `Literal`, `UUID`. Definir `router = APIRouter(prefix="/finances", tags=["Finances"])`.
    Definir os schemas públicos (D-08/D-09/D-11):
    - `AccountCreatePublic`: `family_uuid: UUID`, `name: str`, `type: Literal["corrente", "poupanca", "cartao", "investimento"]`, `currency: str = "BRL"`.
    - `AccountReadPublic`: `uuid: UUID`, `family_uuid: UUID`, `name: str`, `type: str`, `currency: str`, `is_active: bool`, `created_at: datetime`, `updated_at: datetime` (NUNCA `id` nem `family_id`).
    - `AccountUpdatePublic`: `name`, `type` (Literal | None), `currency`, `is_active` — todos `| None = None`.
    Implementar 4 endpoints. Em TODOS injetar `current_user: User = Depends(get_current_user)`:
    - `POST /accounts` (status 201, response_model=AccountReadPublic): resolver `family_uuid`→Family (404 "Família não encontrada" se ausente) → `await _require_family_access(family.id, current_user, session)` → criar `Account(family_id=family.id, name=..., type=..., currency=...)` → add/commit/refresh → retornar `AccountReadPublic` com `family_uuid=account_in.family_uuid`.
    - `GET /accounts` (response_model=list[AccountReadPublic]) com `family_uuid: UUID` query param obrigatório (07-RESEARCH.md Open Question 1 — recomendação: obrigatório): resolver Family (404 se ausente) → `_require_family_access` → `select(Account).where(Account.family_id == family.id)` → mapear cada Account para AccountReadPublic com `family_uuid` do query param.
    - `GET /accounts/{account_uuid}` (response_model=AccountReadPublic): `select(Account).where(Account.uuid == account_uuid)` (404 "Conta não encontrada") → resolver `Family` por `Account.family_id` (para obter `family.uuid`) → `_require_family_access(db_account.family_id, ...)` → retornar AccountReadPublic com `family_uuid=family.uuid`.
    - `PATCH /accounts/{account_uuid}` (response_model=AccountReadPublic): lookup Account por uuid (404) → resolver Family → `_require_family_access` → aplicar `account_in.model_dump(exclude_none=True)` via setattr → definir `db_account.updated_at = datetime.now(timezone.utc)` MANUALMENTE (pitfall #4 — sem onupdate) → add/commit/refresh → retornar AccountReadPublic. ACC-03 (arquivar) é PATCH com `is_active=false` — NÃO usar `session.delete`.
    Usar `session.exec` para queries simples em uma tabela. NÃO expor `id`/`family_id` em nenhuma resposta. NÃO registrar o `finances/router.py` gerado.
  </action>
  <verify>
    <automated>uv run python -m pytest tests/test_finances_operations.py::test_create_account_returns_uuid tests/test_finances_operations.py::test_list_accounts_scoped_to_family tests/test_finances_operations.py::test_accounts_require_auth tests/test_finances_operations.py::test_accounts_403_non_member tests/test_finances_operations.py::test_archive_account -x</automated>
  </verify>
  <acceptance_criteria>
    - Primeira linha de finances/operations.py == `# CARAMELLO-GENERATED: implemented`.
    - finances/operations.py contém `class AccountReadPublic` e NÃO contém `family_id` nos campos de AccountReadPublic (verificável: o bloco de AccountReadPublic não declara `family_id`).
    - finances/operations.py contém `await _require_family_access(` em create/list/get/patch de Account.
    - finances/operations.py contém `db_account.updated_at = datetime.now(timezone.utc)` no handler PATCH.
    - `test_create_account_returns_uuid`, `test_list_accounts_scoped_to_family`, `test_accounts_require_auth`, `test_accounts_403_non_member`, `test_archive_account` passam (não mais skipados).
  </acceptance_criteria>
  <done>CRUD de Account implementado com schemas públicos, controle de acesso 403, arquivamento via PATCH, e os 5 testes de Account verdes.</done>
</task>

<task type="auto">
  <name>Task 3: Registrar finances_operations.router em main.py antes do MCP (pitfall P7)</name>
  <files>src/caramello/main.py</files>
  <read_first>
    - src/caramello/main.py — arquivo atual. Ver imports de domínio (linhas 22-25) e ordem de `include_router` (linhas 55-58) ANTES do bloco `mcp = FastApiMCP(...)` / `mcp.mount_http()` (linhas 60-71). Ler o comentário pitfall sobre MCP.
    - .planning/phases/07-crud-account-category/07-PATTERNS.md — seção `main.py` com ponto de inserção exato.
    - .planning/phases/07-crud-account-category/07-CONTEXT.md — D-01/D-02 (registrar `operations`, NÃO `router` gerado).
  </read_first>
  <action>
    Adicionar `from caramello.finances import operations as finances_operations  # noqa: E402` junto aos imports de domínio (após a linha de `families_router`). Adicionar `app.include_router(finances_operations.router)` na sequência de `include_router`, APÓS `app.include_router(families_router.router)` e ANTES da linha `mcp = FastApiMCP(...)` / `mcp.mount_http()` (pitfall P7 — RESEARCH.md Pitfall 1). NÃO importar nem registrar `caramello.finances.router` (router gerado — D-01/D-02). Não alterar a configuração do MCP nesta fase (ferramentas MCP de finances chegam na Phase 9).
  </action>
  <verify>
    <automated>uv run python -c "from caramello.main import app; paths={r.path for r in app.routes}; assert '/finances/accounts' in paths, paths; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - src/caramello/main.py contém `from caramello.finances import operations as finances_operations`.
    - src/caramello/main.py contém `app.include_router(finances_operations.router)`.
    - A linha `include_router(finances_operations.router)` aparece ANTES de `mcp.mount_http()` (verificável: número da linha do include < número da linha de `mount_http`).
    - src/caramello/main.py NÃO contém `from caramello.finances import router` (router gerado não registrado).
    - `from caramello.main import app` expõe `/finances/accounts` em `app.routes`.
  </acceptance_criteria>
  <done>Router finances_operations registrado antes do MCP; router gerado não registrado; app importa e expõe as rotas /finances/accounts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → API (HTTP) | Payloads não confiáveis (`family_uuid`, `name`, `type`); Bearer token validado em get_current_user |
| API → PostgreSQL | family_id interno resolvido no backend, nunca aceito do cliente |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Information Disclosure | AccountReadPublic | mitigate | Schema público omite `id`/`family_id`; só expõe `uuid`/`family_uuid` (D-07/D-09) |
| T-07-02 | Elevation of Privilege | GET/PATCH /finances/accounts | mitigate | `_require_family_access(family.id, current_user, session)` após resolver UUID → 403 para não-membro (IDOR mitigado; UUID v4 espaço 2^122) |
| T-07-03 | Tampering | AccountCreatePublic | mitigate | Schema aceita só `family_uuid` (UUID); `family_id` resolvido no backend; `type` validado por `Literal[...]` (422 para inválido) |
| T-07-04 | Spoofing | todos os endpoints de Account | mitigate | `Depends(get_current_user)` em todos os handlers; Bearer ausente → 403 (HTTPBearer auto_error) |
| T-07-SC | Tampering | npm/pip/cargo installs | accept | Nenhum pacote instalado nesta fase (07-RESEARCH.md §Package Legitimacy Audit: não aplicável) |
</threat_model>

<verification>
- `uv run python -m pytest tests/test_finances_operations.py -q` → testes de Account verdes (paths test ainda pode falhar até Plan 03 adicionar categories/subcategory)
- `uv run python -c "from caramello.main import app"` → importa sem erro, /finances/accounts presente
- `uv run python -m pytest -q` → sem regressões na suite existente
</verification>

<success_criteria>
- ACC-01: POST /finances/accounts cria conta e retorna `uuid` sem `id`/`family_id`
- ACC-02: GET filtra por família; 403 sem token; 403 para família alheia
- ACC-03: PATCH `is_active=false` arquiva sem deletar
- AUTH-FIN-01/02: Bearer obrigatório + membership obrigatório
- Router registrado antes do MCP (P7)
</success_criteria>

<output>
Create `.planning/phases/07-crud-account-category/07-02-SUMMARY.md` when done
</output>
