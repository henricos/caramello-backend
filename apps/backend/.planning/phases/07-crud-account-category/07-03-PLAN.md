---
phase: 07-crud-account-category
plan: 03
type: execute
wave: 2
depends_on: ["07-02"]
files_modified:
  - src/caramello/finances/operations.py
autonomous: true
requirements: [CAT-01, CAT-02, CAT-03, CAT-04]

must_haves:
  truths:
    - "POST /finances/categories cria categoria pai (family_uuid, name) e retorna uuid (CAT-01)"
    - "POST /finances/subcategory com category_uuid válido cria subcategoria de nível 2 (CAT-02)"
    - "Estrutura de duas tabelas impede criação de nível 3 — não existe endpoint de sub-subcategoria (CAT-03)"
    - "GET e PATCH de Category e Subcategory funcionam scoped por família (CAT-04)"
    - "Router finances expõe todos os 6 paths esperados (accounts/categories/subcategory + detalhe por uuid)"
  artifacts:
    - path: "src/caramello/finances/operations.py"
      provides: "CRUD de Category e Subcategory scoped por família, anexado ao módulo de Account"
      contains: "SubcategoryReadPublic"
  key_links:
    - from: "src/caramello/finances/operations.py (subcategory)"
      to: "caramello.shared.auth._require_family_access"
      via: "resolve category_uuid → category.family_id → _require_family_access"
      pattern: "_require_family_access\\("
    - from: "src/caramello/finances/operations.py (subcategory)"
      to: "Category"
      via: "resolve category_uuid → category_id antes de persistir Subcategory"
      pattern: "Category\\.uuid =="
---

<objective>
Implementar o CRUD de Category (nível 1) e Subcategory (nível 2) em `finances/operations.py`, anexando ao módulo já criado no Plan 02. Reutiliza o helper `_require_family_access` e o padrão de schemas públicos `*Public`.

A regra "máximo 2 níveis" (CAT-03) é enforced ESTRUTURALMENTE pelo schema de duas tabelas — `Subcategory.category_id` aponta para `Category`, não para outra `Subcategory`. Não há lógica de validação adicional nem endpoint de "sub-subcategoria" (07-RESEARCH.md §Don't Hand-Roll). Subcategory usa rotas planas (D-12): `category_uuid` no payload/query (D-13), resolvido para `category_id` no backend.

Purpose: Entregar CAT-01/02/03/04 — usuário autenticado cria categorias pai, subcategorias vinculadas, e lista/atualiza ambas, scoped por família.
Output: `finances/operations.py` estendido com schemas e endpoints de Category e Subcategory; 6 paths de router completos.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-crud-account-category/07-CONTEXT.md
@.planning/phases/07-crud-account-category/07-RESEARCH.md
@.planning/phases/07-crud-account-category/07-PATTERNS.md
@.planning/phases/07-crud-account-category/07-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implementar CRUD de Category (CAT-01, CAT-04) em finances/operations.py</name>
  <files>src/caramello/finances/operations.py</files>
  <read_first>
    - src/caramello/finances/operations.py — estado pós-Plan 02 (Account CRUD + schemas + `_require_family_access` importado). Anexar Category sem alterar o que já existe; reutilizar imports e o `router`.
    - src/caramello/finances/models.py — `Category` (campos `family_id`, `name`, `uuid`, `created_at`, `updated_at`).
    - .planning/phases/07-crud-account-category/07-PATTERNS.md — schemas `CategoryCreatePublic`/`CategoryReadPublic`/`CategoryUpdatePublic` e padrões POST/GET/PATCH (idênticos ao de Account).
    - .planning/phases/07-crud-account-category/07-CONTEXT.md — D-10 (schemas locais com family_uuid para Category).
  </read_first>
  <action>
    Adicionar a `finances/operations.py` os schemas: `CategoryCreatePublic(family_uuid: UUID, name: str)`; `CategoryReadPublic(uuid: UUID, family_uuid: UUID, name: str, created_at: datetime, updated_at: datetime)` (sem `id`/`family_id`); `CategoryUpdatePublic(name: str | None = None)`. Importar `Category` de caramello.finances.models (adicionar ao import existente). Implementar endpoints (todos com `current_user: User = Depends(get_current_user)`):
    - `POST /categories` (status 201, response_model=CategoryReadPublic) — CAT-01: resolver `family_uuid`→Family (404 se ausente) → `_require_family_access(family.id, ...)` → criar `Category(family_id=family.id, name=...)` → add/commit/refresh → retornar CategoryReadPublic com `family_uuid=category_in.family_uuid`.
    - `GET /categories` (response_model=list[CategoryReadPublic]) com `family_uuid: UUID` query param obrigatório — CAT-04: resolver Family (404) → `_require_family_access` → `select(Category).where(Category.family_id == family.id)` → mapear para CategoryReadPublic.
    - `GET /categories/{category_uuid}` (response_model=CategoryReadPublic): lookup por uuid (404 "Categoria não encontrada") → resolver Family por `category.family_id` → `_require_family_access` → retornar com `family_uuid=family.uuid`.
    - `PATCH /categories/{category_uuid}` (response_model=CategoryReadPublic) — CAT-04: lookup (404) → resolver Family → `_require_family_access` → aplicar `model_dump(exclude_none=True)` via setattr → `updated_at = datetime.now(timezone.utc)` manual (pitfall #4) → add/commit/refresh → retornar.
    Usar `session.exec` para queries de uma tabela. NÃO expor `id`/`family_id`.
  </action>
  <verify>
    <automated>uv run python -m pytest tests/test_finances_operations.py::test_create_category tests/test_finances_operations.py::test_list_update_categories -x</automated>
  </verify>
  <acceptance_criteria>
    - finances/operations.py contém `class CategoryReadPublic` sem campo `family_id`.
    - finances/operations.py contém handlers para `POST /categories`, `GET /categories`, `GET /categories/{category_uuid}`, `PATCH /categories/{category_uuid}`.
    - Cada handler de Category contém `await _require_family_access(`.
    - `test_create_category` e `test_list_update_categories` passam.
  </acceptance_criteria>
  <done>CRUD de Category implementado com schemas públicos e controle de acesso; testes de Category verdes.</done>
</task>

<task type="auto">
  <name>Task 2: Implementar CRUD de Subcategory com rotas planas (CAT-02, CAT-04) e validar paths (CAT-03)</name>
  <files>src/caramello/finances/operations.py</files>
  <read_first>
    - src/caramello/finances/operations.py — estado pós-Task 1 (Account + Category prontos). Anexar Subcategory reutilizando router e imports.
    - src/caramello/finances/models.py — `Subcategory` (campos `category_id`, `name`, `uuid`); `Category` (para resolver `category.family_id`).
    - .planning/phases/07-crud-account-category/07-CONTEXT.md — D-12 (rotas planas), D-13 (`category_uuid` público resolvido para `category_id`; acesso via `category.family_id`).
    - .planning/phases/07-crud-account-category/07-RESEARCH.md — §Don't Hand-Roll e Anti-Patterns: CAT-03 enforced estruturalmente, NÃO criar validação de nível nem endpoint de sub-subcategoria.
    - tests/test_finances_operations.py — `test_finances_router_paths` (os 6 paths exatos esperados) e `test_create_subcategory`.
  </read_first>
  <action>
    Adicionar a `finances/operations.py` os schemas: `SubcategoryCreatePublic(category_uuid: UUID, name: str)`; `SubcategoryReadPublic(uuid: UUID, category_uuid: UUID, name: str, created_at: datetime, updated_at: datetime)`; `SubcategoryUpdatePublic(name: str | None = None)`. Importar `Subcategory` de caramello.finances.models. Implementar rotas PLANAS (D-12), todas com `current_user: User = Depends(get_current_user)`:
    - `POST /subcategory` (status 201, response_model=SubcategoryReadPublic) — CAT-02: resolver `category_uuid`→Category (404 "Categoria não encontrada" se ausente) → `_require_family_access(category.family_id, current_user, session)` (D-13) → criar `Subcategory(category_id=category.id, name=...)` → add/commit/refresh → retornar com `category_uuid=subcategory_in.category_uuid`.
    - `GET /subcategory` (response_model=list[SubcategoryReadPublic]) com `category_uuid: UUID` query param OBRIGATÓRIO (D-12): resolver Category (404) → `_require_family_access(category.family_id, ...)` → `select(Subcategory).where(Subcategory.category_id == category.id)` → mapear para SubcategoryReadPublic com `category_uuid` do query param.
    - `GET /subcategory/{subcategory_uuid}` (response_model=SubcategoryReadPublic): lookup por uuid (404 "Subcategoria não encontrada") → resolver Category por `subcategory.category_id` (para `category.uuid` e `category.family_id`) → `_require_family_access` → retornar com `category_uuid=category.uuid`.
    - `PATCH /subcategory/{subcategory_uuid}` (response_model=SubcategoryReadPublic) — CAT-04: lookup (404) → resolver Category → `_require_family_access(category.family_id, ...)` → aplicar `model_dump(exclude_none=True)` via setattr → `updated_at = datetime.now(timezone.utc)` manual → add/commit/refresh → retornar.
    NÃO criar endpoint de "sub-subcategoria" nem qualquer validação de profundidade — CAT-03 é garantido pela estrutura (Subcategory só referencia Category). Após implementar, o router deve expor exatamente os 6 paths: `/finances/accounts`, `/finances/accounts/{account_uuid}`, `/finances/categories`, `/finances/categories/{category_uuid}`, `/finances/subcategory`, `/finances/subcategory/{subcategory_uuid}`.
  </action>
  <verify>
    <automated>uv run python -m pytest tests/test_finances_operations.py::test_create_subcategory tests/test_finances_operations.py::test_finances_router_paths -x</automated>
  </verify>
  <acceptance_criteria>
    - finances/operations.py contém `class SubcategoryReadPublic` com campo `category_uuid` e sem `category_id`/`id`.
    - finances/operations.py contém handlers para `POST /subcategory`, `GET /subcategory`, `GET /subcategory/{subcategory_uuid}`, `PATCH /subcategory/{subcategory_uuid}`.
    - Handlers de Subcategory resolvem `category_uuid` → Category e chamam `_require_family_access(category.family_id, ...)`.
    - finances/operations.py NÃO contém endpoint de "sub-subcategoria" nem validação de `parent_id` de nível (CAT-03 estrutural).
    - `test_create_subcategory` e `test_finances_router_paths` passam (6 paths presentes).
  </acceptance_criteria>
  <done>CRUD de Subcategory com rotas planas implementado; acesso via category.family_id; CAT-03 garantido estruturalmente; os 6 paths do router presentes e testes verdes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → API (HTTP) | Payloads não confiáveis (`family_uuid`, `category_uuid`, `name`); Bearer validado em get_current_user |
| API → PostgreSQL | `family_id`/`category_id` internos resolvidos no backend, nunca aceitos do cliente |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-03 | Information Disclosure | CategoryReadPublic / SubcategoryReadPublic | mitigate | Schemas públicos omitem `id`/`family_id`/`category_id`; expõem só `uuid`/`family_uuid`/`category_uuid` (D-10) |
| T-07-05 | Elevation of Privilege | endpoints de Subcategory via category_uuid | mitigate | Resolve `category_uuid` → `category.family_id` → `_require_family_access` (D-13); 403 para não-membro (IDOR mitigado) |
| T-07-06 | Tampering | hierarquia de categorias | mitigate | CAT-03 enforced estruturalmente: schema de duas tabelas impede nível 3 (nenhuma rota de sub-subcategoria) |
| T-07-SC | Tampering | npm/pip/cargo installs | accept | Nenhum pacote instalado nesta fase (07-RESEARCH.md §Package Legitimacy Audit: não aplicável) |
</threat_model>

<verification>
- `uv run python -m pytest tests/test_finances_operations.py -q` → TODOS os testes verdes (Account + Category + Subcategory + router_paths)
- `uv run python -m pytest -q` → suite completa verde antes de /gsd-verify-work
</verification>

<success_criteria>
- CAT-01: POST /finances/categories cria categoria pai e retorna uuid
- CAT-02: POST /finances/subcategory com category_uuid válido cria subcategoria nível 2
- CAT-03: nenhum endpoint/validação de nível 3 — garantido pela estrutura de duas tabelas
- CAT-04: GET e PATCH de Category e Subcategory scoped por família
- Router expõe os 6 paths esperados
</success_criteria>

<output>
Create `.planning/phases/07-crud-account-category/07-03-SUMMARY.md` when done
</output>
