---
phase: 07-crud-account-category
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/test_finances_operations.py
autonomous: true
requirements: [ACC-01, ACC-02, ACC-03, CAT-01, CAT-02, CAT-03, CAT-04, AUTH-FIN-01, AUTH-FIN-02]

must_haves:
  truths:
    - "Arquivo tests/test_finances_operations.py existe e é coletável pelo pytest"
    - "Cada teste começa com pytest.importorskip('caramello.finances.operations') — skip limpo até a implementação chegar"
    - "Suite roda verde (todos skipados) antes da implementação de operations.py"
  artifacts:
    - path: "tests/test_finances_operations.py"
      provides: "Scaffold de testes Wave 0 cobrindo os 9 requisitos da fase"
      contains: "pytest.importorskip"
      min_lines: 120
  key_links:
    - from: "tests/test_finances_operations.py"
      to: "caramello.finances.operations"
      via: "pytest.importorskip"
      pattern: "importorskip\\(.caramello\\.finances\\.operations."
---

<objective>
Criar o scaffold de testes unitários `tests/test_finances_operations.py` ANTES da implementação (Nyquist Rule — Wave 0). Cada `<verify><automated>` das fases de implementação aponta para um teste deste arquivo; ele precisa existir primeiro.

Os testes começam skipados via `pytest.importorskip("caramello.finances.operations")` — falham limpo (skip) enquanto `operations.py` é apenas stub, e passam a executar automaticamente quando a implementação (Plans 02 e 03) chega. Segue exatamente o padrão de `tests/test_family_operations.py`.

Purpose: Estabelecer feedback automatizado contínuo para todos os requisitos da fase (ACC-01/02/03, CAT-01/02/03/04, AUTH-FIN-01/02) com latência < 15s.
Output: `tests/test_finances_operations.py` com testes nomeados conforme o Test Map do 07-VALIDATION.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-crud-account-category/07-VALIDATION.md
@.planning/phases/07-crud-account-category/07-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar scaffold tests/test_finances_operations.py com testes skipados</name>
  <files>tests/test_finances_operations.py</files>
  <read_first>
    - tests/test_family_operations.py — analog EXATO. Copiar `_make_fake_user`, padrão de `AsyncMock` + `MagicMock` para sessão, override de `dependency_overrides[get_current_user]` e `[get_session]`, `TestClient(app)` SEM context manager, e o padrão `test_*_router_paths`.
    - .planning/phases/07-crud-account-category/07-VALIDATION.md — Test Map (nomes exatos das funções de teste e comandos automatizados).
    - .planning/phases/07-crud-account-category/07-PATTERNS.md — seção `tests/test_finances_operations.py` (linhas com `_make_fake_user`, mock de sessão, override + TestClient, `test_finances_router_paths`, padrão 403 non-member).
    - src/caramello/finances/models.py — campos de Account, Category, Subcategory para construir mocks coerentes.
  </read_first>
  <action>
    Criar tests/test_finances_operations.py importando lazy de `caramello.finances.operations` via `pytest.importorskip("caramello.finances.operations")` no início de CADA teste. Copiar `_make_fake_user(user_id=42)` idêntico ao analog. Definir as seguintes funções de teste com os nomes EXATOS exigidos pelo Test Map do 07-VALIDATION.md:
    - `test_finances_module_exists` — só `importorskip`.
    - `test_finances_operations_annotation_is_implemented` — primeira linha de operations.py == `# CARAMELLO-GENERATED: implemented` (padrão do analog, com `pytest.skip` se arquivo ainda não implementado).
    - `test_finances_router_paths` — verifica que `router.routes` contém os paths completos: `/finances/accounts`, `/finances/accounts/{account_uuid}`, `/finances/categories`, `/finances/categories/{category_uuid}`, `/finances/subcategory`, `/finances/subcategory/{subcategory_uuid}`.
    - `test_create_account_returns_uuid` (ACC-01) — POST /finances/accounts com payload `{family_uuid, name, type, currency}`; mock de sessão retorna Family por uuid; assert status 201, body contém `uuid` e `family_uuid`, body NÃO contém `id` nem `family_id`.
    - `test_list_accounts_scoped_to_family` (ACC-02) — GET /finances/accounts?family_uuid=xxx retorna apenas contas da família; assert 200 e lista.
    - `test_accounts_require_auth` (ACC-02/AUTH-FIN-01) — sem override de `get_current_user`, requisição a /finances/accounts retorna 403 (HTTPBearer auto_error=True; comportamento documentado do projeto — NÃO 401, ver 07-RESEARCH.md Open Question 2).
    - `test_accounts_403_non_member` (AUTH-FIN-02) — usuário autenticado mas não-membro: mock retorna Family existente porém `FamilyMember` ausente (first()=None na query de membership) → assert 403.
    - `test_archive_account` (ACC-03) — PATCH /finances/accounts/{uuid} com `{is_active: false}`; assert 200 e body `is_active` false; nenhuma chamada de `session.delete`.
    - `test_create_category` (CAT-01) — POST /finances/categories com `{family_uuid, name}`; assert 201 e `uuid`.
    - `test_list_update_categories` (CAT-04) — GET /finances/categories?family_uuid=xxx (200) e PATCH /finances/categories/{uuid} (200).
    - `test_create_subcategory` (CAT-02) — POST /finances/subcategory com `{category_uuid, name}`; mock resolve Category por uuid; assert 201 e `uuid`.
    Use o padrão de mock de sessão do analog: função `_exec(_stmt)` async retornando `MagicMock` com `.first()` e `.all()` configuráveis por teste; `mock_session.exec.side_effect = _exec`; `mock_session.commit/refresh/add` como `AsyncMock`/`MagicMock`. Use `TestClient(app)` SEM context manager para não disparar lifespan/fetch_jwks. Sempre limpar `app.dependency_overrides` em `finally`.
    NÃO escrever a implementação de operations.py aqui — apenas os testes. Todos devem ficar SKIPPED (importorskip) nesta etapa porque operations.py ainda é stub.
  </action>
  <verify>
    <automated>uv run python -m pytest tests/test_finances_operations.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run python -m pytest tests/test_finances_operations.py -q` sai com código 0 (todos skipados, nenhum erro de coleta/sintaxe).
    - tests/test_finances_operations.py contém `pytest.importorskip("caramello.finances.operations")`.
    - tests/test_finances_operations.py define as funções: `test_create_account_returns_uuid`, `test_list_accounts_scoped_to_family`, `test_accounts_require_auth`, `test_accounts_403_non_member`, `test_archive_account`, `test_create_category`, `test_list_update_categories`, `test_create_subcategory`, `test_finances_router_paths` (verificável via `grep -c 'def test_' tests/test_finances_operations.py` >= 9).
    - tests/test_finances_operations.py contém `_make_fake_user` e usa `TestClient(app)`.
  </acceptance_criteria>
  <done>Arquivo de testes existe, é coletável, roda verde com todos os testes skipados, e cobre os 9 requisitos da fase com nomes de função exatos do Test Map.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → app | Testes exercitam endpoints via TestClient com `dependency_overrides`; nenhum dado real cruza para banco |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | tests/test_finances_operations.py | mitigate | Testes validam que resposta pública NÃO expõe `id`/`family_id` (assert ausência no body de `test_create_account_returns_uuid`) |
| T-07-SC | Tampering | npm/pip/cargo installs | accept | Nenhum pacote instalado nesta fase (07-RESEARCH.md §Package Legitimacy Audit: não aplicável) |
</threat_model>

<verification>
- `uv run python -m pytest tests/test_finances_operations.py -q` → exit 0, todos skipados
- `uv run python -m pytest -q` → suite completa permanece verde (nenhuma regressão)
</verification>

<success_criteria>
- tests/test_finances_operations.py existe com >= 9 funções de teste nomeadas conforme o Test Map
- Todos os testes skipados via `pytest.importorskip` enquanto operations.py é stub
- Suite completa verde
</success_criteria>

<output>
Create `.planning/phases/07-crud-account-category/07-01-SUMMARY.md` when done
</output>
