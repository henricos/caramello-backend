---
phase: 05-mcp-testes-e-docker
plan: 06
type: execute
wave: 5
depends_on: [01, 03, 05]
files_modified:
  - .env.example
  - docs/apps-platform.md
  - CLAUDE.md
  - .planning/REQUIREMENTS.md
autonomous: false
requirements: [MODEL-03]
user_setup:
  - service: postgresql
    why: "Renomeação de bancos (D-NAMING-01) pode exigir operação manual se o banco local ainda se chama familia_dev"
    env_vars:
      - name: DB_NAME
        source: ".env — verificar se já aponta para caramello_dev; atualizar se ainda estiver como familia_dev"
    dashboard_config:
      - task: "Garantir que caramello_dev existe e tem schema atualizado (bin/manage_db upgrade)"
        location: "PostgreSQL — verificar com psql -l"
must_haves:
  truths:
    - "Documentação e configs referenciam caramello/caramello_dev — sem resquícios de familia_dev/familia_prod"
    - ".env.example documenta a nova nomenclatura de banco"
    - "Operador confirmou que caramello_dev existe com schema atualizado e a suite de integração roda"
  artifacts:
    - path: ".env.example"
      provides: "DB_NAME=caramello_dev e comentário com nova convenção"
      contains: "caramello_dev"
    - path: "docs/apps-platform.md"
      provides: "Tabela §5 com os 2 bancos caramello*"
      contains: "caramello_dev"
  key_links:
    - from: ".env.example"
      to: "convenção de nomenclatura D-NAMING-01"
      via: "DB_NAME=caramello_dev"
      pattern: "DB_NAME=caramello_dev"
---

<objective>
Corrigir a divergência de nomenclatura de banco (D-NAMING-01): atualizar `.env.example`, `docs/apps-platform.md §5`, o bloco `## Constraints`/`## Technology Stack` do `CLAUDE.md` e a `REQUIREMENTS.md` (MODEL-03) para a convenção real — `caramello` (prod) e `caramello_dev` (dev, também usado para testes via rollback). Inclui um checkpoint para o operador confirmar que `caramello_dev` existe com schema atualizado e rodar a suite completa.

Purpose: Operadores que seguem a documentação criavam bancos com nome errado (`familia_dev`). Esta correção fecha o gap entre docs e realidade. Não há banco separado `caramello_test` (D-NAMING-01, D-TEST-01).
Output: 4 arquivos de doc/config atualizados + confirmação do operador de que caramello_dev está pronto.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-mcp-testes-e-docker/05-CONTEXT.md
@.planning/phases/05-mcp-testes-e-docker/05-RESEARCH.md
@.planning/phases/05-mcp-testes-e-docker/05-PATTERNS.md

<interfaces>
<!-- Ocorrências atuais de familia_* a corrigir -->
- .env.example line 5 (comentário) e line 10 (DB_NAME=familia_dev)
- docs/apps-platform.md lines 165-166 (tabela familia_prod/familia_dev), line 87 (familia-backend), line 194, 235, 297 (referências em prosa)
- CLAUDE.md §Constraints + §Technology Stack: "familia_dev (dev) e familia_prod (prod)"
- .planning/REQUIREMENTS.md MODEL-03 (line 22) e Phase 1 SC2 do ROADMAP referenciam familia_dev/familia_prod
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Atualizar .env.example e CLAUDE.md</name>
  <files>.env.example, CLAUDE.md</files>
  <read_first>
    - .env.example (atual: line 5 comentário "familia_dev (dev) e familia_prod (prod)", line 10 DB_NAME=familia_dev)
    - CLAUDE.md (seções com "familia_dev"/"familia_prod" — §Constraints e §Technology Stack/Infrastructure)
    - .planning/phases/05-mcp-testes-e-docker/05-PATTERNS.md §.env.example (texto novo)
  </read_first>
  <action>
    Em `.env.example`, substituir o bloco de banco:
    ```bash
    # Banco de dados PostgreSQL
    # Nomenclatura: caramello (prod), caramello_dev (dev — também usado para testes de integração via rollback)
    DB_HOST=localhost
    DB_PORT=5432
    DB_USER=postgres
    DB_PASSWORD=postgres
    DB_NAME=caramello_dev
    ```
    (line 5 comentário e line 10 DB_NAME passam a caramello_dev; demais linhas mantidas). Não há banco caramello_test — remover qualquer menção a ele neste arquivo.

    Em `CLAUDE.md`, localizar todas as ocorrências de `familia_dev` e `familia_prod` e substituir pela nova convenção. No §Constraints, trocar:
    `DB naming: familia_dev (dev) e familia_prod (prod) — convenção definida em docs/apps-platform.md §5`
    por:
    `DB naming: caramello (prod), caramello_dev (dev) — convenção definida em docs/apps-platform.md §5`
    Substituir quaisquer outras menções a `familia_dev`/`familia_prod` no CLAUDE.md pela forma `caramello`/`caramello_dev` mantendo o sentido da frase em pt-BR. NÃO editar AGENTS.md (CLAUDE.md inclui-o por referência; AGENTS.md não menciona os bancos).

    Atenção: CLAUDE.md tem seções geradas automaticamente (Developer Profile). NÃO tocar nessas — apenas as menções a nomenclatura de banco.
  </action>
  <verify>
    <automated>grep -q "DB_NAME=caramello_dev" .env.example && ! grep -q "familia_dev\|familia_prod" .env.example && ! grep -q "familia_dev (dev) e familia_prod" CLAUDE.md && echo "OK"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "DB_NAME=caramello_dev" .env.example` retorna match
    - `grep "familia_dev\|familia_prod" .env.example` retorna VAZIO
    - `grep "caramello_dev" CLAUDE.md` retorna match
    - `grep "familia_dev (dev) e familia_prod" CLAUDE.md` retorna VAZIO
  </acceptance_criteria>
  <done>.env.example e CLAUDE.md usam a convenção caramello/caramello_dev; sem resquícios de familia_* e sem referências a caramello_test.</done>
</task>

<task type="auto">
  <name>Task 2: Atualizar docs/apps-platform.md §5 e REQUIREMENTS.md</name>
  <files>docs/apps-platform.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - docs/apps-platform.md lines 160-200 (tabela §5 de bancos + prosa com familia_prod/familia_dev)
    - .planning/REQUIREMENTS.md line 22 (MODEL-03) e quaisquer outras menções a familia_*
    - .planning/phases/05-mcp-testes-e-docker/05-PATTERNS.md §docs/apps-platform.md (nova tabela)
  </read_first>
  <action>
    Em `docs/apps-platform.md §5`, substituir a tabela de bancos (lines ~165-166) por 2 bancos (sem caramello_test — D-NAMING-01):
    ```markdown
    | Database | Propósito |
    |---|---|
    | `caramello` | Produção do Grupo Família |
    | `caramello_dev` | Desenvolvimento e testes de integração (rollback por teste) |
    ```
    Atualizar também as menções em prosa a `familia_prod`/`familia_dev` no mesmo documento (ex: line 194 "tabela users vive no database familia_prod", line 235, line 297 que lista bancos a subir) para `caramello`/`caramello_dev`. A line 87 `familia-backend/` refere-se a um nome de diretório de exemplo histórico — atualizar para `caramello-api/` se for o nome do diretório de backend, mantendo coerência com o repo atual.

    Em `.planning/REQUIREMENTS.md`, atualizar MODEL-03 (line 22): trocar `familia_dev (dev) e familia_prod (prod)` por `caramello_dev (dev) e caramello (prod)`. Adicionar nota de que a convenção foi finalizada na Phase 5 (D-NAMING-01). Marcar MODEL-03 como `[x]` na lista e atualizar a Traceability se a linha de MODEL-03 ainda constar como Pendente — registrar "Implementado (05-06)".
  </action>
  <verify>
    <automated>grep -q "caramello_dev" docs/apps-platform.md && grep -q "caramello_dev" .planning/REQUIREMENTS.md && ! grep -q "familia_prod (prod)" docs/apps-platform.md && echo "OK"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "caramello_dev" docs/apps-platform.md` retorna match (tabela §5)
    - `grep "familia_prod\|familia_dev" docs/apps-platform.md` retorna VAZIO (ou só em contexto histórico documentado explicitamente — preferir VAZIO)
    - `grep "caramello_dev" .planning/REQUIREMENTS.md` retorna match na MODEL-03
    - `grep "familia_dev (dev) e familia_prod" .planning/REQUIREMENTS.md` retorna VAZIO
    - `grep -v "caramello_test" docs/apps-platform.md` — sem menção a caramello_test na tabela §5
  </acceptance_criteria>
  <done>docs/apps-platform.md §5 e REQUIREMENTS.md MODEL-03 refletem caramello/caramello_dev — sem caramello_test.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3 (checkpoint): Operador confirma caramello_dev pronto e roda a suite completa</name>
  <action>Checkpoint humano — o operador verifica que caramello_dev existe com schema atualizado e roda a suite de testes completa.</action>
  <what-built>
    Docs e configs atualizados para a nomenclatura caramello/caramello_dev (D-NAMING-01).
    As Tasks 1-2 corrigiram .env.example, CLAUDE.md, docs/apps-platform.md e REQUIREMENTS.md.
    Os testes de integração (plano 05-03) rodam contra caramello_dev com rollback por teste (D-TEST-01).
  </what-built>
  <how-to-verify>
    O operador deve confirmar que o ambiente local está alinhado com a nova nomenclatura:

    1. Verificar que o banco `caramello_dev` existe:
       ```
       psql -l | grep caramello_dev
       ```
       Se o banco ainda se chama `familia_dev`:
       - Sem dados para preservar: criar novo banco via `bin/setup_db` (com DB_NAME=caramello_dev no .env).
       - Com dados para preservar: `ALTER DATABASE familia_dev RENAME TO caramello_dev;` (requer nenhuma conexão ativa).

    2. Garantir que o schema está atualizado:
       ```
       bin/manage_db upgrade
       ```

    3. Rodar a suite completa de testes:
       ```
       uv run pytest
       ```
       Resultado esperado: testes de integração passando (caramello_dev disponível com rollback), unit tests passando.
  </how-to-verify>
  <verify>Operador confirma via `psql -l` que caramello_dev existe; `uv run pytest` (suite completa) verde.</verify>
  <done>caramello_dev confirmado no PostgreSQL com schema atualizado; suite completa verde.</done>
  <resume-signal>Digite "ok banco confirmado" após verificar caramello_dev no PostgreSQL e a suite completa verde, ou descreva problemas encontrados.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs → operador | Documentação incorreta leva a banco mal-nomeado; correção fecha o gap |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-13 | Tampering | renomeação de banco | accept | Operação manual sob controle do operador; checkpoint bloqueante exige confirmação antes de prosseguir; ALTER DATABASE preserva dados se necessário |
</threat_model>

<verification>
- `grep -r "familia_dev\|familia_prod" .env.example CLAUDE.md docs/apps-platform.md .planning/REQUIREMENTS.md` retorna vazio
- Operador confirma caramello_dev criado e com schema atualizado (checkpoint)
- `uv run pytest` (suite completa) verde após confirmação do operador
</verification>

<success_criteria>
- Nomenclatura caramello/caramello_dev consistente em todos os docs/configs (MODEL-03 finalizado, D-NAMING-01)
- Sem referências a caramello_test nos docs (não existe banco separado de testes)
- Operador confirmou caramello_dev pronto e a suite de integração roda verde
</success_criteria>

<output>
After completion, create `.planning/phases/05-mcp-testes-e-docker/05-06-SUMMARY.md`
</output>
