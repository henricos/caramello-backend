---
phase: 09-concilia-o-relat-rios-mcp
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - pyproject.toml
  - uv.lock
  - tests/test_finances_operations.py
  - tests/test_services/test_finances_service.py
autonomous: false
requirements: [LAN-01, LAN-02, LAN-03, LAN-04, LAN-05, REL-01, REL-02, REL-03, REL-04, REL-05]
user_setup: []

must_haves:
  truths:
    - "Suite de testes roda com stubs skipados para todos os endpoints/serviços da fase 9 (verde)"
    - "rapidfuzz importável após instalação"
  artifacts:
    - path: "tests/test_finances_operations.py"
      provides: "Stubs Nyquist para LAN-01..05, REL-01..05 + test_finances_router_paths atualizado"
      contains: "test_reconcile_movement"
    - path: "tests/test_services/test_finances_service.py"
      provides: "Stubs para suggest_category, account_balance, family_balance, monthly_breakdown, by_member_breakdown"
      contains: "test_suggest_category_service"
    - path: "pyproject.toml"
      provides: "rapidfuzz>=3.14.5 como dependência"
      contains: "rapidfuzz"
  key_links:
    - from: "tests/test_services/test_finances_service.py"
      to: "rapidfuzz"
      via: "importorskip de caramello.finances.services.suggest_category"
      pattern: "suggest_category"
---

<objective>
Estabelecer a infraestrutura de validação Nyquist da fase 9 (Wave 0) e instalar a única dependência nova, `rapidfuzz`. Todos os comportamentos da fase 9 (conciliação, sugestão de categoria, saldos, relatórios) recebem testes-stub que ficam skipados até a implementação chegar, garantindo que cada task subsequente tenha um `<automated>` verde para validar contra.

Purpose: Sem os stubs e sem `rapidfuzz` instalado, os planos 03 e 04 não têm como provar conclusão. Esta é a fundação de feedback da fase.
Output: Stubs de teste em `test_finances_operations.py` e `test_finances_service.py`, `test_finances_router_paths` atualizado com os 8 novos paths, e `rapidfuzz>=3.14.5` adicionado ao `pyproject.toml`/`uv.lock`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-concilia-o-relat-rios-mcp/09-CONTEXT.md
@.planning/phases/09-concilia-o-relat-rios-mcp/09-RESEARCH.md
@.planning/phases/09-concilia-o-relat-rios-mcp/09-PATTERNS.md
@.planning/phases/09-concilia-o-relat-rios-mcp/09-VALIDATION.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verificação de legitimidade do pacote rapidfuzz (T-09-SC)</name>
  <files>pyproject.toml</files>
  <what-built>Nenhum código ainda — esta é uma checagem obrigatória de supply chain antes de instalar `rapidfuzz`. A auditoria de legitimidade da RESEARCH.md marcou `rapidfuzz` como [ASSUMED] porque o slopcheck não pôde rodar no sandbox.</what-built>
  <action>NÃO executar `uv add` ainda. Pausar para verificação humana de legitimidade do pacote `rapidfuzz` (pacote [ASSUMED] na auditoria — slopcheck indisponível no sandbox). Apresentar ao operador os links de verificação e aguardar aprovação explícita antes de qualquer instalação. Este é um gate bloqueante: não auto-aprovável independente de `workflow.auto_advance`.</action>
  <how-to-verify>
    1. Abrir https://pypi.org/project/RapidFuzz/ — confirmar que o pacote existe, está em `rapidfuzz` 3.14.5, license MIT, com histórico de releases longo (~6 anos).
    2. Confirmar o repositório oficial https://github.com/rapidfuzz/RapidFuzz — organização dedicada, não um fork pessoal.
    3. Confirmar que o nome digitado é exatamente `rapidfuzz` (sem typosquat tipo `rapid-fuzz`, `rapidfuzzy`, `rapidfuz`).
    4. Aprovar somente se os três pontos conferem.
  </how-to-verify>
  <verify>
    <human-check>Operador confirmou em pypi.org/project/RapidFuzz e github.com/rapidfuzz/RapidFuzz que o pacote é legítimo, MIT, nome exato `rapidfuzz`.</human-check>
  </verify>
  <done>Operador respondeu "approved" liberando o `uv add rapidfuzz>=3.14.5`.</done>
  <resume-signal>Digite "approved" para liberar o `uv add rapidfuzz>=3.14.5`, ou descreva o problema encontrado.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Instalar rapidfuzz e adicionar stubs de serviço (test_finances_service.py)</name>
  <files>pyproject.toml, uv.lock, tests/test_services/test_finances_service.py</files>
  <read_first>
    - pyproject.toml (estado atual das dependências `[project.dependencies]`)
    - tests/test_services/test_finances_service.py (padrão de stub existente: lazy import + `pytest.importorskip` + `getattr(...) is None → pytest.skip`)
    - .planning/phases/09-concilia-o-relat-rios-mcp/09-PATTERNS.md (seção "tests/test_services/test_finances_service.py" — padrão de teste unitário com AsyncMock para funções que recebem session)
    - .planning/phases/09-concilia-o-relat-rios-mcp/09-RESEARCH.md (Pattern 5: assinatura de `suggest_category`; Pattern 3: `account_balance`)
  </read_first>
  <action>
    Instalar a dependência com `uv add rapidfuzz>=3.14.5` (somente após aprovação do checkpoint Task 1) — isso atualiza `pyproject.toml` e `uv.lock`.

    Adicionar stubs de teste a `tests/test_services/test_finances_service.py` seguindo o padrão de stub existente (lazy import via `pytest.importorskip("caramello.finances.services")` + `getattr(services, "<func>", None)` + `pytest.skip` se None). Criar funções de teste, cada uma cobrindo uma assinatura de service definida em 09-CONTEXT.md/09-RESEARCH.md:
    - `test_suggest_category_service` — chama `suggest_category(movement_uuid, family_id, session)` com `AsyncMock`; histórico não-vazio retorna lista de dicts com chaves `subcategory_uuid`, `subcategory_name`, `category_uuid`, `category_name`, `score` (LAN-03, D-CAT-01/02).
    - `test_suggest_category_empty_history` — sem histórico (`execute().fetchall()` retorna `[]`) → função retorna `[]` (D-CAT-03).
    - `test_account_balance_empty` — `account_balance(account_id, session)` com `scalar_one_or_none()` retornando `None` → resultado `Decimal("0.00")` (REL-01, pitfall P6).
    - `test_family_balance` — `family_balance(family_id, session)` retorna `Decimal` consolidado (REL-02).
    - `test_monthly_breakdown` — `monthly_breakdown(family_id, year, month, session)` retorna estrutura de rows (REL-03/04).
    - `test_by_member_breakdown` — `by_member_breakdown(family_id, year, month, session)` retorna rows incluindo grupo `user_uuid=None` (D-REP-02).

    Cada teste DEVE chamar `pytest.importorskip` e `pytest.skip` quando a função não existe, de modo que a suite fique verde antes da implementação. Usar `AsyncMock` para `session` e `MagicMock` para o resultado de `session.execute()` conforme padrão de 09-PATTERNS.md. NÃO importar `rapidfuzz` no topo do módulo de teste — depender do import lazy de `services` (que importa rapidfuzz) para o skip funcionar.
  </action>
  <verify>
    <automated>uv run python -c "import rapidfuzz; print(rapidfuzz.__version__)" && uv run pytest tests/test_services/test_finances_service.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'rapidfuzz' pyproject.toml` retorna 0 (dependência presente)
    - `uv run python -c "import rapidfuzz"` sai com código 0
    - `tests/test_services/test_finances_service.py` contém `def test_suggest_category_service`
    - `tests/test_services/test_finances_service.py` contém `def test_account_balance_empty`
    - `tests/test_services/test_finances_service.py` contém `def test_by_member_breakdown`
    - `uv run pytest tests/test_services/test_finances_service.py -x` sai com código 0 (stubs skipados, nenhum erro)
  </acceptance_criteria>
  <done>rapidfuzz instalado e importável; 6 stubs de serviço adicionados e skipando corretamente; suite de serviço verde.</done>
</task>

<task type="auto">
  <name>Task 3: Adicionar stubs de endpoint + atualizar router paths (test_finances_operations.py)</name>
  <files>tests/test_finances_operations.py</files>
  <read_first>
    - tests/test_finances_operations.py (padrões: `_skip_if_stub`, `test_finances_router_paths` linhas 81-112 com set `expected`, padrão de mock session + `dependency_overrides` do `test_create_movement` linhas 745-835)
    - .planning/phases/09-concilia-o-relat-rios-mcp/09-PATTERNS.md (seção "tests/test_finances_operations.py" — mock session com `rollback`, dependency override, set `expected` atualizado)
    - .planning/phases/09-concilia-o-relat-rios-mcp/09-CONTEXT.md (lista de endpoints da fase + schema rico D-REC-02)
  </read_first>
  <action>
    Adicionar stubs Nyquist a `tests/test_finances_operations.py` seguindo o padrão existente (`_skip_if_stub()` no início + mock session via `dependency_overrides`). Os stubs devem skipar enquanto o endpoint correspondente não existe (verificar via `client.post(...)`/`client.get(...)` com expectativa, mas guardados por `_skip_if_stub` ou por checagem de 404 de rota inexistente — usar o mesmo mecanismo dos stubs da fase 8).

    Criar:
    - `test_reconcile_movement` — POST `/finances/movements/{movement_uuid}/reconcile` retorna 201 + corpo com `uuid`, `movement`, `subcategory_uuid`, `competencia_year`, `is_recorrente` (LAN-01, LAN-04, D-REC-02).
    - `test_reconcile_409_duplicate` — segundo POST reconcile no mesmo movement com `session.commit` levantando `IntegrityError` retorna 409 (LAN-02). Mock session precisa de `rollback = AsyncMock()`.
    - `test_suggest_category` — GET `/finances/movements/{movement_uuid}/suggest-category` retorna lista ordenada por `score` desc, cada item com `subcategory_uuid`, `subcategory_name`, `category_uuid`, `category_name`, `score` (LAN-03).
    - `test_update_entry` — PATCH `/finances/entries/{entry_uuid}` atualiza `subcategory_uuid`, `competencia_year`, `notes` e retorna schema rico (LAN-05).
    - `test_entry_responsible_user_uuid` — PATCH com `responsible_user_uuid` atribui; PATCH com `responsible_user_uuid: null` limpa (D-ATTR + sentinela `model_fields_set`).
    - `test_account_balance` — GET `/finances/accounts/{account_uuid}/balance` retorna `{account_uuid, balance, currency}` com `balance` string (REL-01, D-BAL-01).
    - `test_family_balance` — GET `/finances/families/{family_uuid}/balance` retorna `{family_uuid, total_balance, accounts}` (REL-02, D-BAL-02).
    - `test_monthly_report` — GET `/finances/reports/monthly?family_uuid=&year=&month=` retorna `{period, total, rows}` com `category_uuid`/`subcategory_uuid` por row (REL-03/04, D-REP-01).
    - `test_report_uses_competencia` — verifica que o relatório aceita query params `year`/`month` (competência) e não params de data de movimentação (REL-05, D-REP-03).
    - `test_movement_entry_uuid_field` — GET movements inclui campo `entry_uuid` em cada item (D-MOV-01).
    - `test_movement_reconciled_filter` — GET `/finances/accounts/{account_uuid}/movements?reconciled=false` retorna apenas pendentes (D-MOV-02).

    Atualizar o set `expected` em `test_finances_router_paths` adicionando exatamente estes paths: `/finances/movements/{movement_uuid}/reconcile`, `/finances/movements/{movement_uuid}/suggest-category`, `/finances/entries/{entry_uuid}`, `/finances/entries`, `/finances/accounts/{account_uuid}/balance`, `/finances/families/{family_uuid}/balance`, `/finances/reports/monthly`, `/finances/reports/by-member`. Como o router ainda não tem essas rotas, este teste específico deve usar `_skip_if_stub()` no topo OU ser guardado para só comparar quando as rotas existem — manter consistência com o mecanismo de skip usado nos stubs da fase 8 (não deixar `test_finances_router_paths` falhar enquanto operations.py não tem as rotas). O plano 04 Task 3 remove esse guard.
  </action>
  <verify>
    <automated>uv run pytest tests/test_finances_operations.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_finances_operations.py` contém `def test_reconcile_movement`
    - `tests/test_finances_operations.py` contém `def test_reconcile_409_duplicate`
    - `tests/test_finances_operations.py` contém `def test_suggest_category`
    - `tests/test_finances_operations.py` contém `def test_account_balance`
    - `tests/test_finances_operations.py` contém `def test_monthly_report`
    - `tests/test_finances_operations.py` contém `def test_movement_reconciled_filter`
    - `tests/test_finances_operations.py` contém a string `/finances/movements/{movement_uuid}/reconcile`
    - `tests/test_finances_operations.py` contém a string `/finances/reports/by-member`
    - `uv run pytest tests/test_finances_operations.py -x` sai com código 0 (stubs skipando, nenhum erro)
  </acceptance_criteria>
  <done>11 stubs de endpoint adicionados e skipando; set `expected` de `test_finances_router_paths` inclui os 8 paths novos; suite de operations verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev machine → PyPI | `uv add rapidfuzz` baixa código de terceiros que executa no ambiente de build/test |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-SC | Tampering | install de `rapidfuzz` via uv/PyPI | mitigate | Checkpoint blocking-human (Task 1) verifica pacote em pypi.org/project/RapidFuzz e github.com/rapidfuzz/RapidFuzz antes do `uv add`; nome exato `rapidfuzz` confere contra typosquat; `[ASSUMED]` na auditoria de legitimidade exige aprovação manual |
| T-09-01 | Tampering | uv.lock | accept | `uv.lock` é committed e fixa hash do pacote — pinning previne troca silenciosa de versão pós-aprovação |
</threat_model>

<verification>
- `uv run python -c "import rapidfuzz"` sai 0
- `uv run pytest tests/test_finances_operations.py tests/test_services/test_finances_service.py -x` sai 0 (todos os stubs da fase 9 skipados)
- `uv run pytest` (suite completa) permanece verde
</verification>

<success_criteria>
- rapidfuzz>=3.14.5 em `pyproject.toml` e `uv.lock`, importável
- Stubs Nyquist para todos os 10 requisitos (LAN-01..05, REL-01..05) + D-MOV-01/02 + D-ATTR presentes e skipando
- `test_finances_router_paths.expected` contém os 8 paths novos
- Suite completa verde
</success_criteria>

<output>
Create `.planning/phases/09-concilia-o-relat-rios-mcp/09-01-SUMMARY.md` when done
</output>
