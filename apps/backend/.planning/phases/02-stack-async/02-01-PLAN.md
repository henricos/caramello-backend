---
phase: 02-stack-async
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - uv.lock
autonomous: true
requirements:
  - INFRA-01
tags:
  - python
  - dependencies
  - asyncpg
  - sqlmodel
must_haves:
  truths:
    - "D-01: asyncpg instalado e psycopg2-binary removido das dependências"
    - "D-02: SQLModel atualizado para 0.0.38 (ou superior compatível)"
    - "asyncpg está instalado no projeto"
    - "psycopg2-binary não está mais nas dependências do projeto"
    - "SQLModel está na versão 0.0.38 (ou superior compatível)"
    - "uv.lock reflete a remoção de psycopg2 e a presença de asyncpg"
  artifacts:
    - path: "pyproject.toml"
      provides: "Lista declarativa de dependências sem psycopg2-binary e com asyncpg"
      contains: "asyncpg"
    - path: "uv.lock"
      provides: "Lockfile resolvido sem psycopg2 e com asyncpg + sqlmodel 0.0.38"
      contains: "asyncpg"
  key_links:
    - from: "pyproject.toml"
      to: "uv.lock"
      via: "uv sync resolve dependencies"
      pattern: "asyncpg"
---

<objective>
Trocar o driver de banco de psycopg2-binary (sync) por asyncpg (async) e atualizar SQLModel para a versão 0.0.38 (D-01, D-02). Esta é a fundação que desbloqueia toda a Phase 2 — sem asyncpg instalado, nem shared/database.py nem alembic async podem ser implementados.

Purpose: Remover o driver síncrono que impede operações async no event loop e adicionar o driver nativo async para PostgreSQL.
Output: pyproject.toml e uv.lock refletindo asyncpg como driver e SQLModel atualizado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-stack-async/02-CONTEXT.md
@.planning/phases/02-stack-async/02-RESEARCH.md

@pyproject.toml
@uv.lock
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Substituir psycopg2-binary por asyncpg e atualizar sqlmodel em pyproject.toml e uv.lock</name>
  <files>pyproject.toml, uv.lock</files>
  <read_first>
    - pyproject.toml — bloco `[project] dependencies` (linhas 6-16) precisa ser editado
    - uv.lock — confirmar entradas atuais de psycopg2-binary e sqlmodel após a operação
    - .planning/phases/02-stack-async/02-RESEARCH.md §Standard Stack — versões alvo (asyncpg 0.31.0, sqlmodel 0.0.38)
    - .planning/phases/02-stack-async/02-CONTEXT.md §Decisions D-01 e D-02
  </read_first>
  <action>
    Esta é uma operação em duas etapas, sempre via `uv`, NUNCA editando `uv.lock` à mão.

    **Etapa A — remover psycopg2-binary (per D-01):**

    Executar (na raiz do repo, com `.env` presente):
    ```bash
    uv remove psycopg2-binary
    ```

    Resultado esperado:
    - Linha `"psycopg2-binary",` removida de `pyproject.toml` no array `[project] dependencies` (ela está hoje na linha 15)
    - Entradas relacionadas a `psycopg2-binary` removidas de `uv.lock`

    **Etapa B — adicionar asyncpg e atualizar sqlmodel (per D-01, D-02):**

    Executar:
    ```bash
    uv add asyncpg
    uv add "sqlmodel>=0.0.38"
    ```

    Resultado esperado em `pyproject.toml`:
    - Nova entrada `"asyncpg",` no array `[project] dependencies`
    - Entrada existente `"sqlmodel",` substituída por `"sqlmodel>=0.0.38",`

    Resultado esperado em `uv.lock`:
    - Bloco `[[package]] name = "asyncpg"` presente, versão >=0.31.0
    - Bloco `[[package]] name = "sqlmodel"` com `version = "0.0.38"` (ou superior compatível)
    - Bloco `[[package]] name = "psycopg2-binary"` AUSENTE

    **Importante:**
    - NÃO editar `uv.lock` manualmente — apenas os comandos `uv remove` e `uv add` acima podem mexer no lockfile.
    - O resto de `pyproject.toml` (configurações de ruff, mypy, build-system, dependency-groups) NÃO deve ser tocado. Apenas o array `[project] dependencies` muda.
    - Caso `uv add asyncpg` falhe por erro de rede em sandbox, executar fora do sandbox e revalidar.
  </action>
  <verify>
    <automated>grep -c '"asyncpg"' pyproject.toml | grep -q '^1$' &amp;&amp; ! grep -q "psycopg2-binary" pyproject.toml &amp;&amp; grep -q "sqlmodel&gt;=0.0.38" pyproject.toml &amp;&amp; ! grep -q '^name = "psycopg2-binary"' uv.lock &amp;&amp; grep -q '^name = "asyncpg"' uv.lock</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '"asyncpg"' pyproject.toml` retorna `1` (entrada exata `"asyncpg",` presente em `[project] dependencies`)
    - `grep "psycopg2-binary" pyproject.toml` não retorna nenhuma linha (exit 1 / vazio)
    - `grep "sqlmodel>=0.0.38" pyproject.toml` exits 0 (versão mínima explícita declarada)
    - `grep '^name = "psycopg2-binary"' uv.lock` não retorna nenhuma linha (exit 1 / vazio) — confirma remoção do lockfile
    - `grep '^name = "asyncpg"' uv.lock` exits 0 — confirma presença no lockfile
    - `grep -A1 '^name = "sqlmodel"' uv.lock | grep -E 'version = "0\.0\.(38|39|[4-9][0-9])"'` exits 0 — versão é 0.0.38 ou superior
    - `uv run python -c "import asyncpg; print(asyncpg.__version__)"` retorna versão >= 0.31.0
    - `uv run python -c "from sqlmodel.ext.asyncio.session import AsyncSession; print('ok')"` imprime `ok` — import funciona, ambiente está consistente
    - `uv run python -c "import psycopg2" 2>&amp;1 | grep -q "ModuleNotFoundError"` — confirma que psycopg2 não está mais instalado
    - Bloco `[tool.ruff]`, `[tool.mypy]`, `[build-system]` e `[dependency-groups]` em `pyproject.toml` permanecem inalterados — `git diff pyproject.toml | grep -E '^[-+]' | grep -vE '(asyncpg|psycopg2|sqlmodel)'` não retorna linhas relevantes
  </acceptance_criteria>
  <done>
    `pyproject.toml` declara `asyncpg` e `sqlmodel>=0.0.38` no array `[project] dependencies`, sem `psycopg2-binary`. `uv.lock` reflete a mudança: bloco `asyncpg` presente, bloco `psycopg2-binary` ausente, `sqlmodel` em 0.0.38+. Imports `import asyncpg` e `from sqlmodel.ext.asyncio.session import AsyncSession` funcionam via `uv run python -c`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Build/install pipeline → runtime | Resolução de dependências define quais drivers entram no container/process |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2-01 | Information Disclosure | `pyproject.toml` / `uv.lock` | accept | Sem segredos no manifesto; apenas nomes de pacotes públicos. Plano não introduz superfície sensível. |
| T-2-02 | Tampering | `uv.lock` | mitigate | Atualização SEMPRE via `uv add` / `uv remove` — nunca editar lockfile à mão (evita downgrade silencioso ou pinagem maliciosa). Acceptance critério grep contra `uv.lock` valida nomes/versões resultantes. |
</threat_model>

<verification>
- `uv sync --frozen` deve concluir sem erro (lockfile coerente com pyproject.toml)
- `uv run python -c "import asyncpg, sqlmodel; from sqlmodel.ext.asyncio.session import AsyncSession; print(asyncpg.__version__, sqlmodel.__version__)"` imprime versões esperadas
- Nenhum arquivo `.py` em `src/` ou `scripts/` foi tocado por este plano
</verification>

<success_criteria>
- pyproject.toml + uv.lock refletem stack async (asyncpg presente, psycopg2 ausente, sqlmodel >= 0.0.38)
- Imports async (`asyncpg`, `sqlmodel.ext.asyncio.session.AsyncSession`) funcionam
- Wave 2 (Plans 02 e 03) está desbloqueada
</success_criteria>

<output>
After completion, create `.planning/phases/02-stack-async/02-01-SUMMARY.md`
</output>
