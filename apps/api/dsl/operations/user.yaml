# dsl/operations/user.yaml
# Business operations of the users domain.
# Generates src/caramello_api/users/operations.py (a stub, then implemented).

domain: users
operations:
  - name: get_me
    method: GET
    path: /users/me
    description: "Returns the profile of the authenticated user."
