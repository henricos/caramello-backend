# dsl/operations/user.yaml
# Operações de negócio do domínio users.
# Gera src/caramello_api/users/operations.py (stub que será implementado na Phase 3 — D-10).

domain: users
operations:
  - name: get_me
    method: GET
    path: /users/me
    description: "Retorna o perfil do usuário autenticado."
