# dsl/operations/user.yaml
# Business operations of the users domain.
# Generates src/caramello_api/users/operations.py (the stub implemented in Phase 3 — D-10).

domain: users
operations:
  - name: get_me
    method: GET
    path: /users/me
    description: "Returns the profile of the authenticated user."
