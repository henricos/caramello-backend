---
phase: 05-mcp-testes-e-docker
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - src/caramello/families/services.py
  - src/caramello/families/operations.py
  - tests/test_services/test_family_service.py
autonomous: true
requirements: [MCP-01]
must_haves:
  truths:
    - "A lógica de listagem de famílias vive em families/services.py como função pura"
    - "O endpoint GET /families/families chama o service e expõe operation_id='list_my_families'"
    - "list_my_families é chamável com (session, user) sem dependências FastAPI"
  artifacts:
    - path: "src/caramello/families/services.py"
      provides: "Service list_my_families(session, user) extraído de operations.py"
      contains: "async def list_my_families"
    - path: "src/caramello/families/operations.py"
      provides: "Endpoint GET /families/families com operation_id explícito chamando o service"
      contains: "operation_id=\"list_my_families\""
  key_links:
    - from: "src/caramello/families/operations.py"
      to: "src/caramello/families/services.py"
      via: "import + chamada ao service"
      pattern: "from caramello.families.services import"
---

<objective>
Extrair a lógica de listagem de famílias de `families/operations.py` para um novo `families/services.py` (função pura que recebe `AsyncSession` + `User`), refatorar o endpoint `GET /families/families` para chamar o service, e adicionar `operation_id="list_my_families"` ao decorator — pré-requisito para a integração MCP do plano 05-04 (D-MCP-03, D-MCP-04).

Purpose: O `operation_id` explícito é obrigatório para `include_operations=["list_my_families"]` no FastApiMCP. A extração para services.py dá testabilidade independente do router (TEST infra) e prepara o padrão para M2.
Output: families/services.py (novo), operations.py refatorado, teste unitário do service.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-mcp-testes-e-docker/05-CONTEXT.md
@.planning/phases/05-mcp-testes-e-docker/05-RESEARCH.md
@.planning/phases/05-mcp-testes-e-docker/05-PATTERNS.md

<interfaces>
<!-- Lógica atual a extrair (src/caramello/families/operations.py lines 160-171) -->
```python
@router.get("/families", response_model=list[FamilyRead])
async def list_my_families(
    session: AsyncSession = Depends(get_session),
    current_user: User = Depends(get_current_user),
) -> list[Family]:
    """FAMILY-02: lista famílias onde o usuário autenticado é membro."""
    result = await session.exec(
        select(Family)
        .join(FamilyMember, FamilyMember.family_id == Family.id)  # type: ignore[arg-type]
        .where(FamilyMember.user_id == current_user.id)
    )
    return list(result.all())
```

Modelos disponíveis (de caramello.families.models): Family, FamilyMember, FamilyRead
Modelo de usuário (de caramello.users.models): User
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Criar families/services.py com list_my_families</name>
  <files>src/caramello/families/services.py, tests/test_services/test_family_service.py</files>
  <behavior>
    - list_my_families(session, user) retorna list[Family] das famílias onde user é membro
    - Não importa fastapi, APIRouter, Depends nem HTTPException
    - test_family_service.py: com AsyncMock de session, exec().all() retornando 2 famílias → service retorna lista de 2
  </behavior>
  <read_first>
    - src/caramello/families/operations.py lines 17-37 (imports) e lines 160-171 (lógica a extrair)
    - tests/test_family_operations.py (padrão AsyncMock de session.exec)
    - .planning/phases/05-mcp-testes-e-docker/05-PATTERNS.md §families/services.py (imports e core pattern)
  </read_first>
  <action>
    Criar `src/caramello/families/services.py` SEM anotação `# CARAMELLO-GENERATED` (é código manual). Imports (de 05-PATTERNS.md lines 36-50, sem fastapi):
    ```python
    from __future__ import annotations

    from sqlmodel import select
    from sqlmodel.ext.asyncio.session import AsyncSession

    from caramello.families.models import Family, FamilyMember
    from caramello.users.models import User
    ```
    Função:
    ```python
    async def list_my_families(session: AsyncSession, user: User) -> list[Family]:
        """Retorna as famílias onde o usuário autenticado é membro."""
        result = await session.exec(
            select(Family)
            .join(FamilyMember, FamilyMember.family_id == Family.id)  # type: ignore[arg-type]
            .where(FamilyMember.user_id == user.id)
        )
        return list(result.all())
    ```
    O service NÃO levanta HTTPException — erros de domínio são erros Python; o caller (operations.py) trata.

    Criar `tests/test_services/test_family_service.py` (arquivo atualmente vazio em tests/test_services/test_user_service.py é o análogo de localização). Teste unitário com AsyncMock seguindo o padrão de test_family_operations.py: mockar `session.exec` para retornar um objeto cujo `.all()` devolve duas instâncias Family, chamar `await list_my_families(mock_session, fake_user)`, assertar `len(result) == 2`. Não marcar como integration (usa mock, sem banco real).
  </action>
  <verify>
    <automated>uv run python -c "from caramello.families.services import list_my_families" && uv run pytest tests/test_services/test_family_service.py -x -m "not integration" 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep "async def list_my_families" src/caramello/families/services.py` retorna match
    - `grep -c "fastapi\|APIRouter\|HTTPException" src/caramello/families/services.py` retorna 0 (service puro, sem fastapi)
    - `grep "CARAMELLO-GENERATED" src/caramello/families/services.py` retorna vazio (código manual)
    - `uv run pytest tests/test_services/test_family_service.py -x -m "not integration"` exits 0
    - `uv run ruff check src/caramello/families/services.py` exits 0
    - `uv run mypy src/caramello/families/services.py` exits 0
  </acceptance_criteria>
  <done>services.py criado com list_my_families puro; teste unitário verde; ruff/mypy limpos.</done>
</task>

<task type="auto">
  <name>Task 2: Refatorar operations.py para chamar o service + operation_id</name>
  <files>src/caramello/families/operations.py</files>
  <read_first>
    - src/caramello/families/operations.py lines 160-171 (endpoint atual a refatorar)
    - src/caramello/families/services.py (service criado na Task 1)
    - .planning/phases/05-mcp-testes-e-docker/05-PATTERNS.md §operations.py após extração (lines 68-80)
  </read_first>
  <action>
    Substituir o endpoint `list_my_families` em operations.py (lines 160-171) por:
    ```python
    @router.get(
        "/families",
        response_model=list[FamilyRead],
        operation_id="list_my_families",
    )
    async def list_my_families(
        session: AsyncSession = Depends(get_session),
        current_user: User = Depends(get_current_user),
    ) -> list[Family]:
        """FAMILY-02: lista famílias onde o usuário autenticado é membro."""
        from caramello.families.services import list_my_families as svc
        return await svc(session, current_user)
    ```
    O `operation_id="list_my_families"` é OBRIGATÓRIO — sem ele FastAPI gera um id automático (ex: `list_my_families_families_families_get`) que quebra o filtro `include_operations` do MCP em 05-04 (RESEARCH.md Critical Decision 2). O import do service é local (dentro da função) para evitar ciclo de import, seguindo o padrão de 05-PATTERNS.md.

    Não alterar os demais endpoints. Manter a anotação `# CARAMELLO-GENERATED: implemented` no topo do arquivo (operations.py continua sendo o arquivo de operações; a extração para services.py não muda esse status).
  </action>
  <verify>
    <automated>grep -q 'operation_id="list_my_families"' src/caramello/families/operations.py && grep -q "from caramello.families.services import list_my_families as svc" src/caramello/families/operations.py && uv run pytest tests/test_family_operations.py -x -m "not integration" 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep 'operation_id="list_my_families"' src/caramello/families/operations.py` retorna match
    - `grep "from caramello.families.services import list_my_families as svc" src/caramello/families/operations.py` retorna match
    - `uv run pytest tests/test_family_operations.py -x -m "not integration"` exits 0 (testes unitários existentes continuam verdes)
    - `uv run ruff check src/caramello/families/operations.py` exits 0
    - `uv run mypy src/caramello/families/operations.py` exits 0
  </acceptance_criteria>
  <done>Endpoint GET /families/families chama o service e expõe operation_id="list_my_families"; testes existentes verdes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → GET /families/families | Endpoint protegido por Depends(get_current_user); service filtra por user.id |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03 | Elevation of Privilege | list_my_families service | mitigate | Filtro `.where(FamilyMember.user_id == user.id)` garante que o usuário só vê famílias das quais é membro; o user vem de get_current_user (JWT validado) |
| T-05-04 | Information Disclosure | response_model | mitigate | `response_model=list[FamilyRead]` — schema Read separado evita vazar campos internos da tabela Family |
</threat_model>

<verification>
- `uv run pytest -m "not integration"` verde (service + operations unit tests)
- `uv run ruff check src/ && uv run mypy src/` sem erros
- operation_id presente para o MCP do plano 05-04
</verification>

<success_criteria>
- families/services.py com list_my_families puro e testado (MCP-01 — lógica reutilizável)
- operations.py refatorado chamando o service com operation_id="list_my_families"
- Nenhuma regressão nos testes unitários existentes
</success_criteria>

<output>
After completion, create `.planning/phases/05-mcp-testes-e-docker/05-02-SUMMARY.md`
</output>
