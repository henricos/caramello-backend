---
phase: 05-mcp-testes-e-docker
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - uv.lock
  - tests/conftest.py
  - tests/test_api/test_families_integration.py
  - tests/test_api/test_mcp.py
  - tests/test_api/test_version.py
autonomous: true
requirements: [TEST-01, TEST-02, TEST-03, MCP-02, DEPLOY-03]
must_haves:
  truths:
    - "fastapi-mcp 0.4.0 está instalado como dependência de produção"
    - "pytest-asyncio 1.4.0 está instalado como dependência dev"
    - "uv run pytest -m 'not integration' executa sem erro de coleta"
    - "Arquivos stub de teste existem e são coletáveis pelo pytest"
  artifacts:
    - path: "pyproject.toml"
      provides: "Dependências fastapi-mcp + pytest-asyncio e asyncio_mode=auto"
      contains: "asyncio_mode"
    - path: "tests/conftest.py"
      provides: "Fixtures async test_engine, db_session, async_client"
      contains: "join_transaction_mode"
    - path: "tests/test_api/test_families_integration.py"
      provides: "Stubs dos testes de integração family"
    - path: "tests/test_api/test_mcp.py"
      provides: "Smoke tests de auth do /mcp"
    - path: "tests/test_api/test_version.py"
      provides: "Verificação de APP_VERSION na OpenAPI spec"
  key_links:
    - from: "tests/conftest.py"
      to: "caramello_test database"
      via: "TEST_DB_URL via create_async_engine"
      pattern: "caramello_test"
---

<objective>
Wave 0 da Phase 5: instalar as duas dependências faltantes (`fastapi-mcp`, `pytest-asyncio`), configurar `asyncio_mode = "auto"` no pyproject, e criar os arquivos de teste (conftest async + 3 arquivos de teste com stubs/xfail) para que as waves seguintes tenham infraestrutura de verificação automatizada disponível desde o início (Nyquist).

Purpose: Sem `fastapi-mcp` e `pytest-asyncio` instalados, nenhuma das waves seguintes pode ser executada nem verificada. Este plano destrava todas as demais.
Output: pyproject.toml atualizado, uv.lock regenerado, conftest.py com fixtures async, 3 arquivos de teste coletáveis.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-mcp-testes-e-docker/05-CONTEXT.md
@.planning/phases/05-mcp-testes-e-docker/05-RESEARCH.md
@.planning/phases/05-mcp-testes-e-docker/05-PATTERNS.md
@.planning/phases/05-mcp-testes-e-docker/05-VALIDATION.md

<interfaces>
<!-- Padrões existentes que os stubs de teste devem replicar -->

Fake User (de tests/test_family_operations.py lines 22-36):
```python
from caramello.users.models import User
from uuid import uuid4
from datetime import datetime, timezone

User(
    id=1, uuid=uuid4(), idp_sub="test-sub",
    email="test@example.com", name="Test User",
    created_at=datetime.now(timezone.utc),
    updated_at=datetime.now(timezone.utc),
)
```

Dependências a sobrescrever em testes:
- `caramello.shared.database.get_session` — injeta AsyncSession
- `caramello.shared.auth.get_current_user` — injeta User autenticado

Construção da URL de banco (de src/caramello/core/config.py):
```python
f"postgresql+asyncpg://{DB_USER}:{DB_PASSWORD}@{DB_HOST}:{DB_PORT}/{DB_NAME}"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Instalar dependências e configurar pytest</name>
  <files>pyproject.toml, uv.lock</files>
  <read_first>
    - pyproject.toml (estado atual: deps em [project] dependencies, dev em [dependency-groups] dev, [tool.pytest.ini_options] já tem markers)
    - .planning/phases/05-mcp-testes-e-docker/05-RESEARCH.md §Standard Stack (versões verificadas)
  </read_first>
  <action>
    Executar exatamente:
    ```
    uv add fastapi-mcp
    uv add --group dev pytest-asyncio
    ```
    Isso adiciona `fastapi-mcp>=0.4.0` em `[project] dependencies` e `pytest-asyncio>=1.4.0` em `[dependency-groups] dev`, e regenera `uv.lock`.

    Em seguida editar `[tool.pytest.ini_options]` no pyproject.toml. O bloco atual é:
    ```toml
    [tool.pytest.ini_options]
    markers = [
        "integration: testes que requerem banco real e Keycloak — não rodam em CI padrão",
    ]
    ```
    Substituir por:
    ```toml
    [tool.pytest.ini_options]
    asyncio_mode = "auto"
    markers = [
        "integration: testes que requerem banco real — não rodam sem caramello_test",
    ]
    ```
    `asyncio_mode = "auto"` é obrigatório para pytest-asyncio 1.4.0 — sem ele as fixtures async geram ScopeMismatch (RESEARCH.md Critical Decision 4).
  </action>
  <verify>
    <automated>uv run python -c "import fastapi_mcp; print(fastapi_mcp.__version__)" && uv run python -c "import pytest_asyncio" && grep -q 'asyncio_mode = "auto"' pyproject.toml && grep -q "fastapi-mcp" pyproject.toml</automated>
  </verify>
  <acceptance_criteria>
    - `uv run python -c "import fastapi_mcp"` exits 0
    - `uv run python -c "import pytest_asyncio"` exits 0
    - `grep 'asyncio_mode = "auto"' pyproject.toml` retorna match
    - `grep "fastapi-mcp" pyproject.toml` retorna match em [project] dependencies
    - `grep "pytest-asyncio" pyproject.toml` retorna match em [dependency-groups] dev
  </acceptance_criteria>
  <done>fastapi-mcp e pytest-asyncio instalados; asyncio_mode=auto configurado; uv.lock atualizado.</done>
</task>

<task type="auto">
  <name>Task 2: Evoluir conftest.py com fixtures async</name>
  <files>tests/conftest.py</files>
  <read_first>
    - tests/conftest.py (atual: 14 linhas, fixture `client` síncrono TestClient — MANTER)
    - tests/test_family_operations.py (padrão de fake_user e dependency_overrides)
    - src/caramello/core/config.py (padrão de construção de DATABASE_URL)
    - .planning/phases/05-mcp-testes-e-docker/05-PATTERNS.md §tests/conftest.py (código completo das fixtures)
  </read_first>
  <action>
    Manter o fixture `client` existente intacto. Adicionar ao final do arquivo as fixtures async, exatamente conforme o padrão em 05-PATTERNS.md (lines 156-220). Adicionar no topo os imports necessários:
    ```python
    import os
    import pytest_asyncio
    from sqlalchemy.ext.asyncio import create_async_engine
    from sqlmodel.ext.asyncio.session import AsyncSession
    from httpx import AsyncClient, ASGITransport
    ```
    Adicionar a constante TEST_DB_URL apontando para `caramello_test`:
    ```python
    TEST_DB_URL = (
        f"postgresql+asyncpg://{os.getenv('DB_USER', 'postgres')}"
        f":{os.getenv('DB_PASSWORD', 'postgres')}"
        f"@{os.getenv('DB_HOST', 'localhost')}"
        f":{os.getenv('DB_PORT', '5432')}"
        f"/caramello_test"
    )
    ```
    Adicionar 3 fixtures:
    - `test_engine` (scope="session"): `create_async_engine(TEST_DB_URL, echo=False, future=True)`, yield, `await engine.dispose()`.
    - `db_session(test_engine)`: abre `test_engine.connect()`, `await conn.begin()`, cria `AsyncSession(bind=conn, join_transaction_mode="create_savepoint")`, yield session, `await session.close()`, `await conn.rollback()`. O `join_transaction_mode="create_savepoint"` é OBRIGATÓRIO para rollback funcionar com asyncpg (RESEARCH.md Pitfall 4).
    - `async_client(db_session)`: importa app de `caramello.main`, cria `fake_user` (User com id=1, uuid=uuid4(), idp_sub="test-sub", email="test@example.com", name="Test User", created_at/updated_at com datetime.now(timezone.utc)), define `app.dependency_overrides[get_session]` para yield db_session e `app.dependency_overrides[get_current_user] = lambda: fake_user`, instancia `AsyncClient(transport=ASGITransport(app=app), base_url="http://test")`, yield client, e ao final `app.dependency_overrides.clear()`.

    Usar o código completo de 05-PATTERNS.md como fonte literal — não inventar variações.
  </action>
  <verify>
    <automated>uv run python -c "import tests.conftest" && grep -q "join_transaction_mode" tests/conftest.py && grep -q "caramello_test" tests/conftest.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep "def client" tests/conftest.py` ainda presente (fixture síncrono mantido)
    - `grep 'join_transaction_mode="create_savepoint"' tests/conftest.py` retorna match
    - `grep "caramello_test" tests/conftest.py` retorna match
    - `grep "async def test_engine\|async def db_session\|async def async_client" tests/conftest.py` retorna 3 matches
    - `uv run python -c "import tests.conftest"` exits 0
  </acceptance_criteria>
  <done>conftest.py mantém o fixture client síncrono e adiciona test_engine, db_session (com savepoint rollback) e async_client.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Criar arquivos de teste (stubs verificáveis)</name>
  <files>tests/test_api/test_families_integration.py, tests/test_api/test_mcp.py, tests/test_api/test_version.py</files>
  <behavior>
    - test_version.py: GET /openapi.json → spec["info"]["version"] == os.getenv("APP_VERSION", "0.0.0"). Este teste DEVE passar imediatamente (não-integration, sem banco) pois main.py atualmente tem version="0.1.0" hardcoded — então o teste FALHA até o plano 05-04 corrigir. Marcar como esperado-vermelho até 05-04.
    - test_mcp.py (test_mcp_requires_auth): GET /mcp sem Bearer → status_code in (401, 403). FALHA até 05-04 montar o MCP (404 hoje). Não-integration.
    - test_families_integration.py: 4 testes marcados @pytest.mark.integration cobrindo criar família, listar famílias, pré-registrar membro, listar membros. Não rodam sem caramello_test.
  </behavior>
  <read_first>
    - tests/test_family_operations.py (padrão dependency_overrides + fake_user)
    - .planning/phases/05-mcp-testes-e-docker/05-PATTERNS.md §test_families_integration.py §test_mcp.py §test_version.py (código de cada arquivo)
    - src/caramello/families/operations.py (assinaturas dos endpoints: POST /families/registry, GET /families/families, POST /families/families/{uuid}/pre-register, GET /families/families/{uuid}/members)
  </read_first>
  <action>
    Criar os 3 arquivos conforme 05-PATTERNS.md. Os diretórios `tests/test_api/` e `tests/test_services/` já existem com `__init__.py`.

    `tests/test_api/test_version.py` — sem marca integration, usa fixture `client`.
    Os testes `test_openapi_version_field` e `test_mcp_requires_auth` e `test_mcp_with_valid_token_returns_tools`
    falham ativamente até o plano 05-04 corrigir APP_VERSION e montar o MCP. Marcar com
    `@pytest.mark.xfail(reason="Requer 05-04: MCP não montado / APP_VERSION não dinâmico", strict=False)`
    para que não causem falhas ativas na suite nas Waves 1-3:
    ```python
    """Verifica que APP_VERSION aparece no campo version da OpenAPI spec (DEPLOY-03)."""
    from __future__ import annotations
    import os
    import pytest

    @pytest.mark.xfail(
        reason="Requer 05-04: APP_VERSION não dinâmico ainda",
        strict=False,
    )
    def test_openapi_version_field(client):
        response = client.get("/openapi.json")
        assert response.status_code == 200
        spec = response.json()
        assert spec["info"]["version"] == os.getenv("APP_VERSION", "0.0.0")
    ```

    `tests/test_api/test_mcp.py` — sem marca integration, usa fixture `client`.
    Ambas as funções recebem `@pytest.mark.xfail(reason="Requer 05-04: MCP não montado", strict=False)`:
    - `test_mcp_requires_auth(client)`: `response = client.get("/mcp")`; `assert response.status_code in (401, 403)`.
    - `test_mcp_with_valid_token_returns_tools(client)`: override `get_current_user` com fake_user, `client.get("/mcp")`, `assert response.status_code in (200, 307)`, finally `app.dependency_overrides.clear()`. Usar o código de 05-PATTERNS.md lines 296-318.

    `tests/test_api/test_families_integration.py` — todos com `@pytest.mark.integration`, recebem fixture `async_client`, são `async def`:
    - `test_create_family`: POST /families/registry json={"name": "Familia Integração"} → 201, data["name"] == "Familia Integração", "uuid" in data.
    - `test_list_my_families`: GET /families/families → 200, isinstance(response.json(), list).
    - `test_pre_register_member`: cria família, depois POST /families/families/{uuid}/pre-register json={"email": "novo@example.com"} → 201.
    - `test_list_members`: cria família, depois GET /families/families/{uuid}/members → 200, lista contém o owner.

    Os endpoints reais já existem em operations.py (Phase 4) — os testes de integração devem passar quando rodados contra caramello_test real.

    IMPORTANTE: O decorator `@pytest.mark.xfail` deve ser REMOVIDO dos testes test_version.py e test_mcp.py
    pelo plano 05-04 assim que APP_VERSION for dinâmico e o MCP for montado.
  </action>
  <verify>
    <automated>uv run pytest tests/test_api/test_families_integration.py tests/test_api/test_mcp.py tests/test_api/test_version.py --collect-only -q -m "not integration" 2>&1 | grep -q "test_" && echo COLLECTED</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_api/ --collect-only -q` lista os testes sem erro de import
    - `grep "@pytest.mark.integration" tests/test_api/test_families_integration.py` retorna 4 matches
    - `grep "def test_mcp_requires_auth" tests/test_api/test_mcp.py` retorna match
    - `grep "def test_openapi_version_field" tests/test_api/test_version.py` retorna match
    - `grep "xfail" tests/test_api/test_version.py` retorna match (stub marcado como esperado-vermelho)
    - `grep "xfail" tests/test_api/test_mcp.py` retorna match (stubs marcados como esperado-vermelho)
    - `grep "async_client" tests/test_api/test_families_integration.py` retorna match (usa o fixture, não client síncrono)
    - `uv run pytest -m "not integration" --tb=no -q` termina com 0 falhas ativas (xfail reportado como xfail, não FAILED)
  </acceptance_criteria>
  <done>3 arquivos de teste criados e coletáveis; testes de integração marcados; testes de versão e MCP com @pytest.mark.xfail(strict=False) — não causam falhas ativas até 05-04.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pytest → caramello_test DB | Fixtures conectam a um banco real; rollback por savepoint garante isolamento |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | conftest db_session | mitigate | `join_transaction_mode="create_savepoint"` + `conn.rollback()` por teste — caramello_test nunca acumula estado; caramello_dev nunca tocado (URL hardcoded para caramello_test) |
| T-05-02 | Information Disclosure | TEST_DB_URL credentials | accept | Credenciais lidas de env vars (DB_USER/DB_PASSWORD); sem secrets hardcoded no arquivo de teste — fallback "postgres" é só para ambiente local |
</threat_model>

<verification>
- `uv run pytest -m "not integration"` coleta e roda sem erro de import (testes de versão/mcp reportados como xfail até 05-04 — esperado)
- `grep "asyncio_mode" pyproject.toml` confirma config
- `uv run python -c "import fastapi_mcp, pytest_asyncio"` exits 0
</verification>

<success_criteria>
- fastapi-mcp 0.4.0 e pytest-asyncio 1.4.0 instalados (TEST-01 infra)
- conftest.py com fixtures async + rollback por teste (TEST-01)
- 3 arquivos de teste coletáveis cobrindo TEST-02/TEST-03 (integration), MCP-02 (smoke) e DEPLOY-03 (version)
- pytest configurado com asyncio_mode=auto
- Stubs test_version.py e test_mcp.py com @pytest.mark.xfail(strict=False) — suite não quebra nas Waves 1-3
</success_criteria>

<output>
After completion, create `.planning/phases/05-mcp-testes-e-docker/05-01-SUMMARY.md`
</output>
