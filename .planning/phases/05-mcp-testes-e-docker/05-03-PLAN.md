---
phase: 05-mcp-testes-e-docker
plan: 03
type: execute
wave: 3
depends_on: [01, 02]
files_modified:
  - tests/test_api/test_families_integration.py
  - bin/manage_db
autonomous: true
requirements: [TEST-01, TEST-02, TEST-03]
must_haves:
  truths:
    - "Testes de integração do domínio family cobrem criar família, listar famílias, pré-registrar membro, listar membros"
    - "Testes usam dependency_overrides para simular usuário autenticado sem Keycloak real"
    - "bin/manage_db aceita --env test e opera sobre caramello_test"
    - "Testes contra caramello_test fazem rollback por teste — banco caramello_dev nunca é tocado"
  artifacts:
    - path: "tests/test_api/test_families_integration.py"
      provides: "Testes de integração family completos (4 casos de sucesso)"
      contains: "@pytest.mark.integration"
    - path: "bin/manage_db"
      provides: "Flag --env test para gerenciar caramello_test"
      contains: "caramello_test"
  key_links:
    - from: "tests/test_api/test_families_integration.py"
      to: "caramello_test via async_client fixture"
      via: "fixture async_client de conftest.py"
      pattern: "async_client"
    - from: "bin/manage_db"
      to: "caramello_test database"
      via: "export DB_NAME=caramello_test quando --env test"
      pattern: "DB_NAME=\"caramello_test\""
---

<objective>
Implementar os testes de integração do domínio family (criar família, listar, pré-registrar, listar membros) contra o banco real `caramello_test` com rollback por teste, e evoluir `bin/manage_db` para aceitar `--env test` (gerenciar `caramello_test`). Cobre TEST-01 (banco isolado com rollback), TEST-02 (cobertura de casos de sucesso) e TEST-03 (dependency_overrides sem Keycloak real).

Purpose: Garantir que o domínio family tem cobertura automatizada contra banco real, e que o operador tem comando para preparar/resetar o banco de testes.
Output: test_families_integration.py implementado (substitui os stubs do 05-01), bin/manage_db com --env test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-mcp-testes-e-docker/05-CONTEXT.md
@.planning/phases/05-mcp-testes-e-docker/05-RESEARCH.md
@.planning/phases/05-mcp-testes-e-docker/05-PATTERNS.md
@.planning/phases/05-mcp-testes-e-docker/05-VALIDATION.md

<interfaces>
<!-- Endpoints reais do domínio family (de families/operations.py — Phase 4) -->
- POST /families/registry  — body FamilyCreate {"name": str} → 201 FamilyRead {uuid, name, ...}
- GET  /families/families  — → 200 list[FamilyRead]
- POST /families/families/{family_uuid}/pre-register — body {"email": EmailStr} → 201 FamilyInvitationRead
- GET  /families/families/{family_uuid}/members — → 200 list[FamilyMemberRead] {user_uuid, role, joined_at}

<!-- Fixture disponível (conftest.py do 05-01) -->
async_client: AsyncClient com get_session→db_session (caramello_test, rollback) e get_current_user→fake_user (id=1)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implementar testes de integração do domínio family</name>
  <files>tests/test_api/test_families_integration.py</files>
  <behavior>
    - test_create_family: POST /families/registry {"name": "Familia Integração"} → 201; data["name"] == "Familia Integração"; "uuid" in data
    - test_list_my_families: cria 1 família, GET /families/families → 200; lista contém a família criada
    - test_pre_register_member: cria família, POST /families/families/{uuid}/pre-register {"email": "novo@example.com"} → 201; data["email"] == "novo@example.com"
    - test_list_members: cria família, GET /families/families/{uuid}/members → 200; lista contém 1 membro com role "owner" (o fake_user owner)
  </behavior>
  <read_first>
    - tests/test_api/test_families_integration.py (stubs criados no 05-01)
    - tests/conftest.py (fixtures async_client, db_session — criados no 05-01)
    - src/caramello/families/operations.py (assinaturas e status codes dos 4 endpoints; registry torna o usuário owner automaticamente)
    - .planning/phases/05-mcp-testes-e-docker/05-PATTERNS.md §test_families_integration.py (padrão de teste)
  </read_first>
  <action>
    Substituir os stubs por implementações completas. Todos os testes:
    - são `async def`
    - marcados com `@pytest.mark.integration`
    - recebem o fixture `async_client` (NÃO o `client` síncrono) — o override de auth e session já está encapsulado em async_client
    - usam `await async_client.post(...)` / `await async_client.get(...)`

    Sequência de cada teste que precisa de uma família existente: primeiro POST /families/registry para criar e capturar o `uuid` do response, depois usar esse uuid no endpoint subsequente.

    test_create_family:
    ```python
    @pytest.mark.integration
    async def test_create_family(async_client):
        response = await async_client.post("/families/registry", json={"name": "Familia Integração"})
        assert response.status_code == 201
        data = response.json()
        assert data["name"] == "Familia Integração"
        assert "uuid" in data
    ```
    test_list_my_families: cria família, depois `GET /families/families`, assert 200 e que a família criada aparece (comparar pelo uuid retornado no registry).
    test_pre_register_member: cria família, captura uuid, `POST /families/families/{uuid}/pre-register` json={"email": "novo@example.com"}, assert 201 e data["email"] == "novo@example.com".
    test_list_members: cria família, captura uuid, `GET /families/families/{uuid}/members`, assert 200 e que a lista tem 1 item com role == "owner".

    Cada teste roda numa transação isolada (db_session faz rollback ao final) — não há contaminação entre testes nem no caramello_dev.
  </action>
  <verify>
    <automated>uv run pytest tests/test_api/test_families_integration.py -m integration -x 2>&1 | tail -10 || echo "REQUER caramello_test — se PG indisponível, validar via --collect-only"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "@pytest.mark.integration" tests/test_api/test_families_integration.py` retorna 4
    - `grep -c "async def test_" tests/test_api/test_families_integration.py` retorna 4
    - `grep "async_client" tests/test_api/test_families_integration.py` presente em todos os testes (sem uso de `client` síncrono)
    - `uv run pytest tests/test_api/test_families_integration.py --collect-only -q` lista 4 testes sem erro de import
    - Quando caramello_test disponível: `uv run pytest tests/test_api/test_families_integration.py -m integration -x` exits 0
  </acceptance_criteria>
  <done>4 testes de integração implementados cobrindo os casos de sucesso do domínio family via async_client.</done>
</task>

<task type="auto">
  <name>Task 2: Evoluir bin/manage_db com flag --env test</name>
  <files>bin/manage_db</files>
  <read_first>
    - bin/manage_db (atual: parse de COMMAND na line 24, carregamento de .env só no branch reset lines 53-55)
    - .planning/phases/05-mcp-testes-e-docker/05-PATTERNS.md §bin/manage_db (padrão de parse --env + carregamento .env)
    - docs/dev.md (onde documentar o novo comando — checar se existe seção de banco)
  </read_first>
  <action>
    Modificar bin/manage_db para aceitar `--env {dev|test}` ANTES do comando. Inserir, logo após a checagem `if [ -z "$1" ]` (antes de `COMMAND=$1`):
    ```bash
    # Parse --env flag (deve vir ANTES da leitura de COMMAND)
    ENV_FLAG="dev"  # default não muda comportamento atual

    while [[ "$#" -gt 0 ]]; do
      case "$1" in
        --env)
          ENV_FLAG="$2"
          shift 2
          ;;
        *) break ;;
      esac
    done

    # Carregar .env para todos os comandos (antes era só no reset)
    if [ -f .env ]; then
        export $(grep -v '^#' .env | xargs)
    fi

    # Se --env test, sobrescreve DB_NAME para caramello_test (APÓS carregar .env)
    if [[ "$ENV_FLAG" == "test" ]]; then
      export DB_NAME="caramello_test"
    fi
    ```
    Mover o carregamento de .env do branch `reset` (lines 53-55) para essa seção comum (evita duplicação; o reset não precisa mais recarregar). O `export DB_NAME` sobrescreve o valor do .env, então alembic (que lê settings.DATABASE_URL construído de DB_NAME) opera sobre caramello_test.

    Atualizar a função `usage()` adicionando:
    ```
    echo "Options:"
    echo "  --env {dev|test}  Target environment (default: dev)"
    echo ""
    echo "Examples:"
    echo "  $0 upgrade --env test     Apply migrations to caramello_test"
    echo "  $0 reset --env test       Reset caramello_test (DATA LOSS WARNING)"
    ```
    Documentar o comando `bin/manage_db reset --env test` e `bin/manage_db upgrade --env test` em docs/dev.md na seção de banco (se não houver seção, adicionar uma linha sob o tópico de operações de banco existente).
  </action>
  <verify>
    <automated>grep -q 'ENV_FLAG' bin/manage_db && grep -q 'DB_NAME="caramello_test"' bin/manage_db && bash -n bin/manage_db && echo "SYNTAX_OK"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "ENV_FLAG" bin/manage_db` retorna match
    - `grep 'DB_NAME="caramello_test"' bin/manage_db` retorna match
    - `bash -n bin/manage_db` exits 0 (sintaxe válida)
    - `grep -- "--env" bin/manage_db` aparece na função usage
    - `grep "caramello_test\|--env test" docs/dev.md` retorna match (documentação atualizada)
  </acceptance_criteria>
  <done>bin/manage_db aceita --env test, sobrescreve DB_NAME para caramello_test, e o comando está documentado em docs/dev.md.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pytest → caramello_test | Testes operam em banco isolado; rollback garante não-contaminação |
| bin/manage_db → caramello_test vs caramello_dev | Flag --env determina o banco; default dev preserva comportamento atual |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05 | Tampering | bin/manage_db reset | mitigate | reset mantém o prompt de confirmação (y/N) antes de DROP; --env test isola o alvo em caramello_test |
| T-05-06 | Spoofing | dependency_overrides[get_current_user] | accept | Override só existe no escopo de teste (async_client fixture); a app de produção usa get_current_user real com validação JWT — sem bypass em runtime |
</threat_model>

<verification>
- `uv run pytest tests/test_api/test_families_integration.py --collect-only` sem erro de import
- `bash -n bin/manage_db` válido; `bin/manage_db upgrade --env test` aplica migrations em caramello_test (requer PG)
- Com caramello_test disponível: suite de integração verde
</verification>

<success_criteria>
- 4 testes de integração do domínio family cobrindo casos de sucesso (TEST-02) com auth simulada via dependency_overrides (TEST-03)
- Rollback por teste garante isolamento de caramello_test (TEST-01)
- bin/manage_db --env test operacional e documentado
</success_criteria>

<output>
After completion, create `.planning/phases/05-mcp-testes-e-docker/05-03-SUMMARY.md`
</output>
