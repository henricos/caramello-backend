---
phase: 01-infra-base
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - dsl/entities/user.yaml
  - scripts/generate_code.py
  - src/caramello/models/user.py
  - src/caramello/api/generated/user_router.py
  - tests/generated/test_user.py
autonomous: true
requirements: [MODEL-01, MODEL-02, MODEL-03]

must_haves:
  truths:
    - "Tabela `user` contém `idp_sub` como TEXT NOT NULL UNIQUE e não contém `hashed_password`, `google_id`, `phone_number`, `is_active`"
    - "Campo `name` substitui `full_name` no modelo User"
    - "Código gerado usa `datetime.now(timezone.utc)` em vez de `datetime.utcnow` (sem warning no Python 3.12+)"
  artifacts:
    - path: "dsl/entities/user.yaml"
      provides: "Definição canônica do User com campos Keycloak-aligned"
      contains: "idp_sub"
    - path: "scripts/generate_code.py"
      provides: "Gerador DSL com datetime moderno"
      contains: "datetime.now(timezone.utc)"
    - path: "src/caramello/models/user.py"
      provides: "Modelo ORM User regenerado"
      contains: "idp_sub"
  key_links:
    - from: "dsl/entities/user.yaml"
      to: "src/caramello/models/user.py"
      via: "bin/generate_code"
      pattern: "idp_sub"
    - from: "scripts/generate_code.py"
      to: "src/caramello/models/user.py"
      via: "geração automática"
      pattern: "timezone.utc"
---

<objective>
Corrigir a definição do modelo User no YAML da DSL para alinhamento com Keycloak (sem campos de auth local, com `idp_sub`), corrigir o gerador DSL para emitir `datetime.now(timezone.utc)` em vez do `datetime.utcnow` deprecated, e regenerar todos os artefatos derivados.

Propósito: A Phase 3 precisará de JIT provisioning via `idp_sub` — o campo deve existir no modelo desde o início. O warning de `utcnow` deprecation polui logs em Python 3.12+.

Saída: `dsl/entities/user.yaml` corrigido, `scripts/generate_code.py` com datetime moderno, modelos e routers regenerados.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-infra-base/01-CONTEXT.md
@docs/dsl_rules.md

<interfaces>
<!-- Estrutura atual de user.yaml — executor parte desta base para aplicar as correções -->
<!-- Campos a REMOVER: full_name, phone_number, hashed_password, google_id, avatar_url, is_active -->
<!-- Campos a ADICIONAR: idp_sub (str, unique, nullable: false) -->
<!-- Campo a RENOMEAR: full_name → name -->
<!-- Campos a MANTER: id, uuid, email, created_at, updated_at, relationships -->

<!-- generate_code.py linha 96 (atual): -->
<!-- field_args.append("default_factory=datetime.utcnow") -->
<!-- Deve ser: -->
<!-- field_args.append("default_factory=lambda: datetime.now(timezone.utc)") -->

<!-- generate_code.py linha 138 (atual): -->
<!-- "from datetime import datetime" -->
<!-- Deve ser: -->
<!-- "from datetime import datetime, timezone" -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Corrigir dsl/entities/user.yaml — modelo alinhado com Keycloak</name>
  <files>dsl/entities/user.yaml</files>
  <action>
Reescrever `dsl/entities/user.yaml` com os campos corretos para integração Keycloak (sem auth local).

Campos finais em ordem:
1. `id` — int, primary_key: true (manter)
2. `uuid` — UUID, unique: true, default_factory: uuid4, nullable: false (manter)
3. `idp_sub` — str, unique: true, nullable: false — NOVO campo para o subject do Keycloak
4. `email` — EmailStr, unique: true, nullable: false (manter, remover menção a login)
5. `name` — str, max_length: 100, nullable: false — RENOMEAR de full_name
6. `created_at` — datetime, default_factory: now_utc, nullable: false (manter)
7. `updated_at` — datetime, default_factory: now_utc, on_update: now_utc, nullable: false (manter)

REMOVER os campos: `full_name`, `phone_number`, `hashed_password`, `google_id`, `avatar_url`, `is_active`

Relacionamentos: manter `families` e `sent_invitations` sem alteração.

Atualizar description do campo email para "Endereço de e-mail único do usuário." (sem menção a login/senha).
  </action>
  <verify>grep -c "idp_sub" dsl/entities/user.yaml && grep -c "hashed_password" dsl/entities/user.yaml | grep -q "^0$" && echo "OK"</verify>
  <done>`idp_sub` presente no YAML; campos `hashed_password`, `phone_number`, `google_id`, `avatar_url`, `is_active` ausentes; campo `name` presente no lugar de `full_name`</done>
</task>

<task type="auto">
  <name>Task 2: Corrigir scripts/generate_code.py — datetime moderno e imports</name>
  <files>scripts/generate_code.py</files>
  <action>
Aplicar duas correções no gerador DSL para eliminar o uso de `datetime.utcnow` (deprecated no Python 3.12+):

**Correção 1 — linha 96:** Substituir o default_factory gerado para campos datetime.

Antes:
```python
field_args.append("default_factory=datetime.utcnow")
```

Depois:
```python
field_args.append("default_factory=lambda: datetime.now(timezone.utc)")
```

**Correção 2 — função `generate_models`, lista `imports` (linha ~138):** Atualizar o import de datetime nos arquivos gerados.

Antes:
```python
"from datetime import datetime",
```

Depois:
```python
"from datetime import datetime, timezone",
```

Nenhuma outra alteração no arquivo. O gerador deve continuar funcionando para todos os outros campos e entidades.
  </action>
  <verify>grep -c "timezone.utc" scripts/generate_code.py | grep -qv "^0$" && grep "utcnow" scripts/generate_code.py | grep -v "^#" | wc -l | grep -q "^0$" && echo "OK"</verify>
  <done>`datetime.now(timezone.utc)` presente em scripts/generate_code.py; `datetime.utcnow` ausente em código não-comentado; import `timezone` incluído na lista de imports gerados</done>
</task>

<task type="auto">
  <name>Task 3: Regenerar artefatos — executar bin/generate_code</name>
  <files>
    src/caramello/models/user.py,
    src/caramello/api/generated/user_router.py,
    tests/generated/test_user.py
  </files>
  <action>
Executar o gerador DSL para produzir os artefatos derivados atualizados:

```bash
uv run python scripts/generate_code.py
```

Após a geração, verificar manualmente que:
- `src/caramello/models/user.py` contém `idp_sub` e NÃO contém `hashed_password`
- `src/caramello/models/user.py` contém `from datetime import datetime, timezone`
- `src/caramello/models/user.py` usa `lambda: datetime.now(timezone.utc)` nos fields de timestamp
- O arquivo de modelo NÃO contém `password` no `UserCreate` (não há mais `hashed_password` no YAML)

Se o gerador falhar (erro de importação ou YAML inválido), corrigir o erro antes de prosseguir — não avançar com artefatos desatualizados.
  </action>
  <verify>grep -c "idp_sub" src/caramello/models/user.py && python -c "import ast; ast.parse(open('src/caramello/models/user.py').read()); print('syntax OK')"</verify>
  <done>`src/caramello/models/user.py` gerado sem erros de sintaxe, contém `idp_sub`, não contém `hashed_password`; artefatos de router e tests também regenerados</done>
</task>

</tasks>

<threat_model>
## Limites de Confiança

| Limite | Descrição |
|--------|-----------|
| DSL YAML → código gerado | Entrada controlada pelo dev; sem risco externo nesta fase |
| Código gerado → banco de dados | A migration (Plano 03) valida o schema final |

## Registro de Ameaças STRIDE

| ID | Categoria | Componente | Disposição | Plano de Mitigação |
|----|-----------|------------|------------|-------------------|
| T-01-01 | Tampering | dsl/entities/user.yaml | aceitar | Arquivo versionado em git; alterações rastreáveis por histórico de commit |
| T-01-02 | Information Disclosure | UserRead schema gerado | mitigar | `hashed_password` removido do YAML — não pode ser emitido no schema Read |
| T-01-03 | Elevation of Privilege | Campo `is_active` removido | aceitar | Controle de acesso delegado ao Keycloak; campo local de ativação é risco desnecessário |
</threat_model>

<verification>
Após as três tasks, verificar o estado final:

```bash
# 1. YAML contém os campos corretos
grep "idp_sub" dsl/entities/user.yaml
grep -v "hashed_password\|phone_number\|google_id\|avatar_url\|is_active" dsl/entities/user.yaml | grep -c "name:"

# 2. Gerador usa datetime moderno
grep "timezone.utc" scripts/generate_code.py

# 3. Modelo gerado está correto
grep "idp_sub" src/caramello/models/user.py
grep "timezone" src/caramello/models/user.py
python -c "import ast; ast.parse(open('src/caramello/models/user.py').read()); print('syntax OK')"
```
</verification>

<success_criteria>
- `dsl/entities/user.yaml`: contém `idp_sub`, `email`, `name`, `created_at`, `updated_at` — sem `hashed_password`, `phone_number`, `google_id`, `avatar_url`, `is_active`
- `scripts/generate_code.py`: usa `datetime.now(timezone.utc)` para campos datetime; zero ocorrências de `utcnow` em código não-comentado
- `src/caramello/models/user.py`: sintaxe válida, contém `idp_sub`, não contém `hashed_password`; import inclui `timezone`
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/01-infra-base/01-01-SUMMARY.md` com:
- Campos finais do modelo User
- Confirmação do fix de datetime no gerador
- Quaisquer desvios ou ajustes aplicados durante a execução
</output>
