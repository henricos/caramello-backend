---
phase: 01-infra-base
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - src/caramello/services/user.py
  - src/caramello/repositories/user.py
  - src/caramello/exceptions.py
  - src/caramello/http_errors.py
  - src/caramello/api/v1/routes.py
  - src/caramello/api/v1/users.py
  - src/caramello/schemas/user.py
  - tests/generated/test_user.py
  - tests/generated/test_family.py
  - tests/generated/test_familyinvitation.py
  - tests/test_generated_api.py
autonomous: true
requirements: [INFRA-02]

must_haves:
  truths:
    - "Nenhum arquivo vazio ou desconectado existe nos diretórios src/caramello/services/, src/caramello/repositories/, src/caramello/api/v1/"
    - "Diretório src/caramello/schemas/ não existe"
    - "Diretório tests/generated/ não existe"
    - "Arquivo tests/test_generated_api.py não existe"
    - "Aplicação FastAPI ainda sobe sem ImportError após as remoções"
  artifacts:
    - path: "src/caramello/main.py"
      provides: "Ponto de entrada FastAPI funcional sem imports quebrados"
      contains: "app = FastAPI"
  key_links:
    - from: "src/caramello/main.py"
      to: "src/caramello/api/generated/"
      via: "include_router"
      pattern: "include_router"
---

<objective>
Remover todos os artefatos obsoletos que foram criados por ferramentas anteriores (datamodel-codegen, código gerado sem uso) e que agora representam ruído ou conflito: arquivos vazios em services/repositories, diretório api/v1, diretório schemas, testes sem fixtures isoladas.

Propósito: Artefatos mortos causam falhas no linting (mypy acusa imports não utilizados, ruff acusa arquivos sem conteúdo útil) e confundem o executor de fases futuras sobre o que é "real" no codebase.

Saída: Codebase limpo — apenas os arquivos que fazem parte da arquitetura atual (DSL gerado + core + main.py).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-infra-base/01-CONTEXT.md
@.planning/phases/01-infra-base/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remover arquivos vazios de services, repositories, exceptions e api/v1</name>
  <files>
    src/caramello/services/user.py,
    src/caramello/repositories/user.py,
    src/caramello/exceptions.py,
    src/caramello/http_errors.py,
    src/caramello/api/v1/routes.py,
    src/caramello/api/v1/users.py,
    src/caramello/api/v1/__init__.py
  </files>
  <action>
Deletar os seguintes arquivos e o diretório `src/caramello/api/v1/`:

```bash
rm -f src/caramello/services/user.py
rm -f src/caramello/repositories/user.py
rm -f src/caramello/exceptions.py
rm -f src/caramello/http_errors.py
rm -rf src/caramello/api/v1/
```

Os diretórios `src/caramello/services/` e `src/caramello/repositories/` podem permanecer se contiverem apenas `__init__.py` (os diretórios existem para a arquitetura futura). Se os diretórios estiverem completamente vazios após a remoção do `user.py`, removê-los também.

Verificar se `src/caramello/main.py` importa algum destes arquivos — se sim, remover apenas as linhas de import afetadas (sem quebrar a aplicação). Com base na leitura atual do main.py, NÃO há imports de v1, services, repositories, exceptions ou http_errors — mas confirmar antes de prosseguir.
  </action>
  <verify>
    test ! -f src/caramello/exceptions.py &&
    test ! -f src/caramello/http_errors.py &&
    test ! -d src/caramello/api/v1/ &&
    echo "artefatos removidos OK"
  </verify>
  <done>Arquivos e diretório v1 removidos; `src/caramello/main.py` ainda importa apenas os routers gerados em `api/generated/`</done>
</task>

<task type="auto">
  <name>Task 2: Remover src/caramello/schemas/ e tests/generated/ e tests/test_generated_api.py</name>
  <files>
    src/caramello/schemas/user.py,
    src/caramello/schemas/__init__.py,
    tests/generated/test_user.py,
    tests/generated/test_family.py,
    tests/generated/test_familyinvitation.py,
    tests/generated/__init__.py,
    tests/test_generated_api.py
  </files>
  <action>
Deletar o diretório `src/caramello/schemas/` inteiro e o diretório `tests/generated/` inteiro, mais o arquivo avulso `tests/test_generated_api.py`:

```bash
rm -rf src/caramello/schemas/
rm -rf tests/generated/
rm -f tests/test_generated_api.py
```

Motivo:
- `src/caramello/schemas/` foi gerado pelo `datamodel-codegen` e não é usado por nenhum router ou modelo atual — é um artefato desconectado.
- `tests/generated/` contém testes que dependem de fixtures inexistentes e de um banco de dados de teste não configurado — causariam falhas em `pytest` sem valor de cobertura.
- `tests/test_generated_api.py` usa paths de API incorretos (legado pré-DSL).

Após a remoção, confirmar que `tests/` ainda contém `conftest.py` e os outros subdiretórios legítimos.
  </action>
  <verify>
    test ! -d src/caramello/schemas/ &&
    test ! -d tests/generated/ &&
    test ! -f tests/test_generated_api.py &&
    test -f tests/conftest.py &&
    echo "limpeza OK"
  </verify>
  <done>Diretórios `schemas/` e `tests/generated/` removidos; `tests/test_generated_api.py` removido; `tests/conftest.py` intacto</done>
</task>

<task type="auto">
  <name>Task 3: Verificar que a aplicação ainda sobe após as remoções</name>
  <files>src/caramello/main.py</files>
  <action>
Verificar que a aplicação FastAPI ainda importa e inicializa sem erros após todas as remoções:

```bash
uv run python -c "from caramello.main import app; print('app OK:', app.title)"
```

Se houver ImportError, identificar qual import está quebrado e corrigir o `main.py` (remover a linha de import inválida). Não reconstituir arquivos removidos.

Também verificar que o pytest não tenta coletar os arquivos removidos:

```bash
uv run pytest --collect-only 2>&1 | head -30
```

O objetivo é que `pytest --collect-only` não retorne erros de importação — pode retornar "no tests ran" se os únicos testes eram os que foram removidos.
  </action>
  <verify>uv run python -c "from caramello.main import app; print('OK')" 2>&1 | grep -q "OK"</verify>
  <done>`from caramello.main import app` executa sem erro; `pytest --collect-only` não retorna ImportError</done>
</task>

</tasks>

<threat_model>
## Limites de Confiança

| Limite | Descrição |
|--------|-----------|
| Filesystem → aplicação | Remoção de arquivos pode quebrar imports em main.py |

## Registro de Ameaças STRIDE

| ID | Categoria | Componente | Disposição | Plano de Mitigação |
|----|-----------|------------|------------|-------------------|
| T-02-01 | Tampering | src/caramello/main.py | mitigar | Verificar imports antes de remover; Task 3 valida que app ainda sobe |
| T-02-02 | Denial of Service | pytest collect | aceitar | Testes gerados removidos; coleção vazia é estado válido nesta fase |
</threat_model>

<verification>
Estado esperado após este plano:

```bash
# Diretórios obsoletos não existem
test ! -d src/caramello/schemas/ && echo "schemas: OK"
test ! -d tests/generated/ && echo "tests/generated: OK"
test ! -d src/caramello/api/v1/ && echo "api/v1: OK"
test ! -f src/caramello/exceptions.py && echo "exceptions: OK"
test ! -f src/caramello/http_errors.py && echo "http_errors: OK"

# Aplicação ainda inicializa
uv run python -c "from caramello.main import app; print('app: OK')"
```
</verification>

<success_criteria>
- Arquivos `exceptions.py`, `http_errors.py`, `services/user.py`, `repositories/user.py` não existem
- Diretório `src/caramello/api/v1/` não existe
- Diretório `src/caramello/schemas/` não existe
- Diretório `tests/generated/` não existe
- Arquivo `tests/test_generated_api.py` não existe
- `uv run python -c "from caramello.main import app"` executa sem erro
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/01-infra-base/01-02-SUMMARY.md` com:
- Lista de arquivos/diretórios removidos
- Confirmação de que a aplicação ainda sobe
- Qualquer ajuste feito em main.py (se necessário)
</output>
