---
phase: 02-stack-async
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - src/caramello/shared/__init__.py
  - src/caramello/shared/database.py
  - src/caramello/core/config.py
autonomous: true
requirements:
  - INFRA-01
tags:
  - python
  - async
  - sqlmodel
  - asyncpg
  - session
must_haves:
  truths:
    - "Existe um módulo shared/database.py que expõe engine async, session factory async e get_session() async"
    - "AsyncSession é importado de sqlmodel.ext.asyncio.session (NÃO de sqlalchemy.ext.asyncio)"
    - "async_sessionmaker é criado com expire_on_commit=False (obrigatório para evitar MissingGreenlet)"
    - "DATABASE_URL construída em config.py usa o prefixo postgresql+asyncpg://"
    - "shared/database.py passa em ruff e mypy strict (disallow_untyped_defs=true)"
  artifacts:
    - path: "src/caramello/shared/__init__.py"
      provides: "Marca shared/ como pacote Python"
      contains: ""
    - path: "src/caramello/shared/database.py"
      provides: "engine async, async_session_factory, get_session async generator"
      contains: "create_async_engine"
    - path: "src/caramello/core/config.py"
      provides: "DATABASE_URL com prefixo postgresql+asyncpg://"
      contains: "postgresql+asyncpg"
  key_links:
    - from: "src/caramello/shared/database.py"
      to: "src/caramello/core/config.py"
      via: "settings.DATABASE_URL fornece a URL async já formatada"
      pattern: "from caramello.core.config import settings"
    - from: "src/caramello/shared/database.py"
      to: "sqlmodel.ext.asyncio.session.AsyncSession"
      via: "import explícito do AsyncSession do SQLModel"
      pattern: "from sqlmodel.ext.asyncio.session import AsyncSession"
---

<objective>
Criar o módulo `src/caramello/shared/database.py` como substituto async de `src/caramello/database/session.py`, e ajustar `src/caramello/core/config.py` para construir o `DATABASE_URL` com prefixo `postgresql+asyncpg://`. Esta entrega implementa D-03, D-04, D-05, D-13 e parte de D-12 (config.py prefix).

Purpose: Estabelecer a camada de session async que os routers gerados e o Alembic vão consumir, e fazer o `Settings` produzir uma URL compatível com `create_async_engine`.
Output: módulo `shared/` criado com `__init__.py` e `database.py` (engine + factory + get_session async); `config.py` com prefixo `postgresql+asyncpg://`.

**Nota importante sobre o arquivo legado `src/caramello/database/session.py`:** ele NÃO é deletado neste plano. Os 4 routers em `src/caramello/api/generated/` ainda importam dele. A deleção acontece no Plan 04, depois que o generator for atualizado e os routers regenerados. Esta separação evita quebrar o app entre waves.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-stack-async/02-CONTEXT.md
@.planning/phases/02-stack-async/02-RESEARCH.md
@.planning/phases/02-stack-async/02-PATTERNS.md

@src/caramello/database/session.py
@src/caramello/core/config.py
@pyproject.toml

<interfaces>
<!-- API alvo que este plano cria. Outros planos (03 alembic, 04 generator) consomem estes símbolos. -->

```python
# src/caramello/shared/database.py — interface pública
engine: AsyncEngine                                     # singleton criado via create_async_engine
async_session_factory: async_sessionmaker[AsyncSession] # factory para criar AsyncSession
async def get_session() -> AsyncGenerator[AsyncSession, None]:  # dependency para FastAPI
    ...
```

```python
# src/caramello/core/config.py — interface pública (inalterada externamente)
settings: Settings                  # singleton já existente
settings.DATABASE_URL: str          # agora com prefixo postgresql+asyncpg://
```

Import path canônico para AsyncSession (CRÍTICO):
```python
from sqlmodel.ext.asyncio.session import AsyncSession  # SQLModel — tem session.exec() async
# NÃO usar:
# from sqlalchemy.ext.asyncio import AsyncSession  # SQLAlchemy puro — só tem session.execute()
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Atualizar prefixo de DATABASE_URL para postgresql+asyncpg:// em config.py</name>
  <files>src/caramello/core/config.py</files>
  <read_first>
    - src/caramello/core/config.py (37 linhas) — confirmar o estado atual do método `model_post_init` na linha 28
    - .planning/phases/02-stack-async/02-PATTERNS.md §"src/caramello/core/config.py" — diff exato (linhas 100-103 do PATTERNS)
    - .planning/phases/02-stack-async/02-RESEARCH.md §"Pitfall 4: URL sem prefixo +asyncpg" — explica o erro `ArgumentError: Could not parse rfc1738 URL` se o prefixo estiver errado
  </read_first>
  <action>
    Editar APENAS a linha 33 de `src/caramello/core/config.py`.

    Estado atual (linhas 28-34):
    ```python
    def model_post_init(self, __context: object) -> None:
        """Constrói DATABASE_URL a partir dos campos individuais."""
        password = f":{self.DB_PASSWORD}" if self.DB_PASSWORD else ""
        port = f":{self.DB_PORT}" if self.DB_PORT else ""
        self.DATABASE_URL = (
            f"postgresql://{self.DB_USER}{password}@{self.DB_HOST}{port}/{self.DB_NAME}"
        )
    ```

    Estado alvo — substituir o prefixo da f-string de `postgresql://` para `postgresql+asyncpg://`:
    ```python
    def model_post_init(self, __context: object) -> None:
        """Constrói DATABASE_URL a partir dos campos individuais."""
        password = f":{self.DB_PASSWORD}" if self.DB_PASSWORD else ""
        port = f":{self.DB_PORT}" if self.DB_PORT else ""
        self.DATABASE_URL = (
            f"postgresql+asyncpg://{self.DB_USER}{password}@{self.DB_HOST}{port}/{self.DB_NAME}"
        )
    ```

    **NADA MAIS muda neste arquivo.** Em particular:
    - Linhas 1-27 (imports, declaração da classe `Settings`, campos `ENVIRONMENT`, `DATABASE_URL`, `DB_HOST`, `DB_PORT`, `DB_USER`, `DB_PASSWORD`, `DB_NAME`, `CORS_ORIGINS`, `model_config`) permanecem idênticas.
    - Linha 37 (`settings = Settings()`) permanece idêntica.
    - O comentário `# type: ignore[call-arg]` na linha 37 permanece idêntico.

    Per D-04 / D-13: o tipo de `DATABASE_URL: str | None = None` (linha 16) já está corretamente anotado — nada a fazer aqui.
  </action>
  <verify>
    <automated>grep -q 'postgresql+asyncpg://' src/caramello/core/config.py &amp;&amp; ! grep -q '"postgresql://' src/caramello/core/config.py &amp;&amp; ! grep -q "f.postgresql://" src/caramello/core/config.py &amp;&amp; uv run ruff check src/caramello/core/config.py &amp;&amp; uv run mypy src/caramello/core/config.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'postgresql+asyncpg://' src/caramello/core/config.py` retorna `1` (exatamente uma ocorrência do novo prefixo)
    - `grep -c 'postgresql://' src/caramello/core/config.py` retorna `0` quando se exclui o token `postgresql+asyncpg://` — confirmação: `grep "postgresql://" src/caramello/core/config.py | grep -v "postgresql+asyncpg://" | wc -l` retorna `0`
    - `uv run ruff check src/caramello/core/config.py` exits 0 (sem violações de lint)
    - `uv run mypy src/caramello/core/config.py` exits 0 (passa em `disallow_untyped_defs=true`)
    - `uv run python -c "from caramello.core.config import settings; assert settings.DATABASE_URL.startswith('postgresql+asyncpg://'), settings.DATABASE_URL; print('ok')"` imprime `ok` (executar com `.env` presente — pula se `.env` ausente)
    - `git diff src/caramello/core/config.py` mostra EXATAMENTE uma linha removida (`postgresql://...`) e uma linha adicionada (`postgresql+asyncpg://...`); nenhuma outra mudança no arquivo
  </acceptance_criteria>
  <done>
    `src/caramello/core/config.py` constrói `DATABASE_URL` com o prefixo `postgresql+asyncpg://`, mantendo o resto do arquivo inalterado. ruff e mypy passam.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Criar diretório shared/ com __init__.py vazio e database.py async (engine + factory + get_session)</name>
  <files>src/caramello/shared/__init__.py, src/caramello/shared/database.py</files>
  <read_first>
    - src/caramello/database/session.py (17 linhas) — arquivo de referência (analog sync) que mostra a forma atual
    - src/caramello/core/config.py — confirma que `settings.DATABASE_URL` é o ponto de consumo (mesmo padrão deve ser mantido)
    - .planning/phases/02-stack-async/02-PATTERNS.md §"src/caramello/shared/database.py" — código alvo exato (linhas 52-77 do PATTERNS)
    - .planning/phases/02-stack-async/02-RESEARCH.md §"Pattern 1" — fonte canônica do padrão; §"Common Pitfalls" 1, 2, 3 (import errado, await ausente, expire_on_commit)
    - pyproject.toml §[tool.ruff] e §[tool.mypy] — confirma que `src/caramello/shared/` está NO ESCOPO de ruff/mypy (não está nos `exclude` patterns)
  </read_first>
  <action>
    **Criar dois arquivos novos. NÃO tocar em `src/caramello/database/session.py` neste plano — deletar é Plan 04.**

    **Arquivo 1 — `src/caramello/shared/__init__.py`:**

    Criar arquivo vazio (0 bytes). Existe apenas para marcar `shared/` como pacote Python.

    **Arquivo 2 — `src/caramello/shared/database.py`:**

    Conteúdo exato (per D-03, D-04, D-05, D-13 e RESEARCH Pattern 1):
    ```python
    from collections.abc import AsyncGenerator

    from sqlalchemy.ext.asyncio import async_sessionmaker, create_async_engine
    from sqlmodel.ext.asyncio.session import AsyncSession

    from caramello.core.config import settings

    engine = create_async_engine(
        str(settings.DATABASE_URL),
        echo=False,
        future=True,
    )

    async_session_factory = async_sessionmaker(
        bind=engine,
        class_=AsyncSession,
        expire_on_commit=False,
    )


    async def get_session() -> AsyncGenerator[AsyncSession, None]:
        async with async_session_factory() as session:
            yield session
    ```

    **Pontos críticos (NÃO desviar):**
    1. `AsyncSession` deve ser importado de `sqlmodel.ext.asyncio.session` — NÃO de `sqlalchemy.ext.asyncio` (RESEARCH Pitfall 1). O `AsyncSession` do SQLModel é o que possui `session.exec()` async.
    2. `expire_on_commit=False` é OBRIGATÓRIO (RESEARCH Pitfall 3) — sem isso, `MissingGreenlet` após commit.
    3. `echo=False` por design (mitigação T-2-01: evitar leak de connection string em logs).
    4. `str(settings.DATABASE_URL)` é obrigatório — `settings.DATABASE_URL` é tipado como `str | None`, então o `str(...)` apaziguzando mypy (`disallow_untyped_defs=true`).
    5. Type hint de retorno de `get_session()` é `AsyncGenerator[AsyncSession, None]` — required por mypy strict.
    6. `create_db_and_tables()` NÃO existe neste arquivo (per D-05) — schema é responsabilidade exclusiva do Alembic.

    O arquivo deve estar livre de:
    - `Session` (sqlmodel sync) — não importar nem usar
    - `create_engine` (sync) — não importar nem usar
    - `create_db_and_tables` — não definir nem chamar
    - `SQLModel.metadata.create_all` — não chamar
  </action>
  <verify>
    <automated>test -f src/caramello/shared/__init__.py &amp;&amp; test ! -s src/caramello/shared/__init__.py &amp;&amp; test -f src/caramello/shared/database.py &amp;&amp; grep -q "from sqlmodel.ext.asyncio.session import AsyncSession" src/caramello/shared/database.py &amp;&amp; grep -q "from sqlalchemy.ext.asyncio import async_sessionmaker, create_async_engine" src/caramello/shared/database.py &amp;&amp; grep -q "expire_on_commit=False" src/caramello/shared/database.py &amp;&amp; grep -q "echo=False" src/caramello/shared/database.py &amp;&amp; grep -q "AsyncGenerator\[AsyncSession, None\]" src/caramello/shared/database.py &amp;&amp; ! grep -q "create_db_and_tables" src/caramello/shared/database.py &amp;&amp; ! grep -q "from sqlmodel import.*Session\b" src/caramello/shared/database.py &amp;&amp; ! grep -q "create_engine\b" src/caramello/shared/database.py &amp;&amp; uv run ruff check src/caramello/shared/ &amp;&amp; uv run mypy src/caramello/shared/</automated>
  </verify>
  <acceptance_criteria>
    - `test -f src/caramello/shared/__init__.py` exits 0 — arquivo existe
    - `wc -c < src/caramello/shared/__init__.py` retorna `0` — arquivo está vazio
    - `test -f src/caramello/shared/database.py` exits 0 — arquivo existe
    - `grep -c "from sqlmodel.ext.asyncio.session import AsyncSession" src/caramello/shared/database.py` retorna `1` — import correto do AsyncSession do SQLModel
    - `grep -c "from sqlalchemy.ext.asyncio import async_sessionmaker, create_async_engine" src/caramello/shared/database.py` retorna `1` — engine e factory do SQLAlchemy
    - `grep "from sqlalchemy.ext.asyncio import.*AsyncSession" src/caramello/shared/database.py` não retorna nada — NÃO importa AsyncSession do SQLAlchemy (Pitfall 1)
    - `grep -c "expire_on_commit=False" src/caramello/shared/database.py` retorna `1` — flag obrigatória presente
    - `grep -c "echo=False" src/caramello/shared/database.py` retorna `1` — mitigação T-2-01 presente
    - `grep -c "AsyncGenerator\[AsyncSession, None\]" src/caramello/shared/database.py` retorna `1` — type hint correto no get_session
    - `grep -c "async def get_session" src/caramello/shared/database.py` retorna `1` — função async existe
    - `grep "create_db_and_tables" src/caramello/shared/database.py` não retorna nada — função removida per D-05
    - `grep "from sqlmodel import.*Session\b" src/caramello/shared/database.py` não retorna nada — sem Session sync
    - `grep "create_engine\b" src/caramello/shared/database.py | grep -v create_async_engine` não retorna nada — sem `create_engine` síncrono
    - `uv run ruff check src/caramello/shared/` exits 0
    - `uv run mypy src/caramello/shared/` exits 0 (passa em `disallow_untyped_defs=true`)
    - `uv run python -c "from caramello.shared.database import engine, async_session_factory, get_session; from sqlmodel.ext.asyncio.session import AsyncSession; print('ok')"` imprime `ok` (assumindo `.env` presente)
  </acceptance_criteria>
  <done>
    Diretório `src/caramello/shared/` existe com `__init__.py` vazio e `database.py` expondo `engine` (via `create_async_engine`), `async_session_factory` (via `async_sessionmaker` com `expire_on_commit=False`) e `get_session()` (async generator com type hint `AsyncGenerator[AsyncSession, None]`). Imports usam `sqlmodel.ext.asyncio.session.AsyncSession`. Sem `create_db_and_tables`, sem `Session` sync, sem `create_engine` sync. ruff e mypy passam.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Settings → engine | `DATABASE_URL` (contendo `DB_PASSWORD`) é injetada no engine — não deve vazar para logs |
| Engine → query layer | Toda query passa por SQLAlchemy/SQLModel ORM — sem string-formatting de SQL no app |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2-01 | Information Disclosure | `shared/database.py` engine creation | mitigate | `echo=False` explicitamente no `create_async_engine` — impede que o SQLAlchemy logue queries com a connection string. Validado por `grep -c "echo=False"`. |
| T-2-02 | Tampering (SQL injection) | Routers consumindo `get_session` | accept | SQLModel/SQLAlchemy ORM gera queries parametrizadas por padrão; nenhum router faz string formatting de SQL bruto. Sem mudança necessária nesta fase. |
| T-2-03 | Denial of Service | engine async + sessionmaker | accept | Pool default do SQLAlchemy async (5 conexões + 10 overflow) é adequado para 1-5 usuários do grupo família. Tuning de pool fica para Phase 5 (deploy). |
</threat_model>

<verification>
- `uv run ruff check src/` passa (todo o pacote, não apenas shared/)
- `uv run mypy src/` passa
- `uv run python -c "from caramello.shared.database import get_session; print(get_session.__annotations__)"` mostra `{'return': AsyncGenerator[AsyncSession, NoneType]}` (ou equivalente)
- `src/caramello/database/session.py` AINDA existe e funciona — não foi deletado neste plano. Plan 04 cuida disso.
- O app ainda inicia (os routers atuais importam de `database.session` que segue intacto): `uv run python -c "from caramello.main import app; print(len(app.routes))"` retorna número > 0
</verification>

<success_criteria>
- D-03 implementada: `src/caramello/shared/database.py` existe
- D-04 implementada: engine + async_session_factory + get_session com tipos corretos
- D-05 implementada: `create_db_and_tables()` ausente do novo módulo
- D-13 implementada: tipos explícitos passam em ruff/mypy strict
- Prefixo `postgresql+asyncpg://` aplicado em `config.py`
- Wave 3 (Plan 04 — generator update + regen) está desbloqueada
</success_criteria>

<output>
After completion, create `.planning/phases/02-stack-async/02-02-SUMMARY.md`
</output>
