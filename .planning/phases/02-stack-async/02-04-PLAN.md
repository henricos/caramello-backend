---
phase: 02-stack-async
plan: 04
type: execute
wave: 3
depends_on:
  - 02-02
  - 02-03
files_modified:
  - scripts/generate_code.py
  - src/caramello/api/generated/user_router.py
  - src/caramello/api/generated/family_router.py
  - src/caramello/api/generated/familymember_router.py
  - src/caramello/api/generated/familyinvitation_router.py
  - src/caramello/database/session.py
  - src/caramello/main.py
autonomous: true
requirements:
  - INFRA-01
tags:
  - dsl
  - codegen
  - async
  - fastapi
  - cleanup
must_haves:
  truths:
    - "DSL generator emite routers async (async def, await session.exec, AsyncSession)"
    - "Os 4 routers em src/caramello/api/generated/ estão regenerados em modo async"
    - "Arquivo src/caramello/database/session.py não existe mais (módulo inteiro removido)"
    - "Nenhum arquivo .py do projeto importa de caramello.database.session ou usa create_engine sync"
    - "main.py inicia sem ImportError; routers continuam registrados em app.include_router"
    - "Routers regenerados passam em ruff/mypy se aplicável (eles estão excluídos do check, mas a regeneração não pode quebrar import-time)"
  artifacts:
    - path: "scripts/generate_code.py"
      provides: "generate_router() emite template async com import de caramello.shared.database"
      contains: "from caramello.shared.database import get_session"
    - path: "src/caramello/api/generated/user_router.py"
      provides: "Router CRUD async para User"
      contains: "async def create_user"
    - path: "src/caramello/api/generated/family_router.py"
      provides: "Router CRUD async para Family"
      contains: "async def create_family"
    - path: "src/caramello/api/generated/familymember_router.py"
      provides: "Router CRUD async para FamilyMember"
      contains: "async def"
    - path: "src/caramello/api/generated/familyinvitation_router.py"
      provides: "Router CRUD async para FamilyInvitation"
      contains: "async def"
  key_links:
    - from: "src/caramello/api/generated/*_router.py"
      to: "src/caramello/shared/database.py"
      via: "Depends(get_session) consome get_session async"
      pattern: "from caramello.shared.database import get_session"
    - from: "src/caramello/api/generated/*_router.py"
      to: "sqlmodel.ext.asyncio.session.AsyncSession"
      via: "type hint da dependência de session"
      pattern: "session: AsyncSession = Depends\\(get_session\\)"
    - from: "src/caramello/main.py"
      to: "src/caramello/api/generated/*_router.py"
      via: "app.include_router(...)"
      pattern: "include_router"
---

<objective>
Fechar a Phase 2: atualizar o template de `generate_router()` em `scripts/generate_code.py` para emitir routers async (D-08), regenerar os 4 routers existentes via `bin/generate_code` (D-10, D-11), deletar `src/caramello/database/session.py` (D-03 — só agora pode ser removido, com segurança) e validar que `src/caramello/main.py` continua íntegro (D-12 — verificação confirma que não importa de `database.session`).

Purpose: Encerrar a migração para async — garantir que NENHUM arquivo .py no projeto importa o módulo sync removido ou usa `create_engine` síncrono. Validar com ruff/mypy/grep.
Output:
- generator atualizado com template async (D-08, D-13)
- 4 routers regenerados em async
- `database/` removido
- `main.py` confirmado intacto (sem imports a remover, conforme PATTERNS)
- gates de Phase 2: `grep -r "create_engine" src/` vazio; `grep -r "from caramello.database" src/` vazio (exceto, se houver, em alembic — não é src/)

Per D-09: campo `domain` no YAML e mudança de output para `domains/{domain}/` NÃO entram aqui. O generator continua emitindo para `src/caramello/api/generated/` e `src/caramello/models/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-stack-async/02-CONTEXT.md
@.planning/phases/02-stack-async/02-RESEARCH.md
@.planning/phases/02-stack-async/02-PATTERNS.md
@.planning/phases/02-stack-async/02-02-PLAN.md

@scripts/generate_code.py
@src/caramello/api/generated/user_router.py
@src/caramello/api/generated/family_router.py
@src/caramello/api/generated/familymember_router.py
@src/caramello/api/generated/familyinvitation_router.py
@src/caramello/main.py
@src/caramello/database/session.py
@bin/generate_code

<interfaces>
<!-- Símbolos consumidos pelos routers regenerados — fornecidos pelo Plan 02 -->

```python
# src/caramello/shared/database.py — já existe após Plan 02
from caramello.shared.database import get_session
# get_session: async def get_session() -> AsyncGenerator[AsyncSession, None]
```

```python
# AsyncSession import canônico — DEVE ser este nos routers gerados
from sqlmodel.ext.asyncio.session import AsyncSession
```

Padrão do template async (per D-08 + RESEARCH Pattern 4 + PATTERNS):
```python
async def create_<entity>(<entity>_in: <Entity>Create, session: AsyncSession = Depends(get_session)):
    db_obj = <Entity>.model_validate(<entity>_in)
    session.add(db_obj)
    await session.commit()
    await session.refresh(db_obj)
    return db_obj

# Lista
result = await session.exec(select(<Entity>).offset(offset).limit(limit))
return result.all()

# Detail / Update / Delete — sempre dois passos:
statement = select(<Entity>).where(<Entity>.uuid == uuid)
result = await session.exec(statement)
obj = result.first()
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Atualizar template em scripts/generate_code.py::generate_router() para emitir routers async</name>
  <files>scripts/generate_code.py</files>
  <read_first>
    - scripts/generate_code.py (453 linhas) — focar na função `generate_router(entity_data)` linhas 256-320; demais funções (`load_yaml`, `map_type_to_python`, `get_field_definition`, `generate_relationships`, `generate_models`, `generate_test`, `main`) NÃO mudam per D-09
    - .planning/phases/02-stack-async/02-PATTERNS.md §"scripts/generate_code.py — função generate_router()" — código alvo exato (linhas 218-292 do PATTERNS)
    - .planning/phases/02-stack-async/02-RESEARCH.md §"Pattern 4" — template canônico e §"Diferenças críticas em relação ao template sync" (lista numerada 1-8 no PATTERNS linha 295-303)
    - src/caramello/shared/database.py — confirma o path de import do `get_session`
  </read_first>
  <action>
    Substituir APENAS o corpo da função `generate_router(entity_data)` em `scripts/generate_code.py` (linhas 256-320). NÃO mexer em mais nada do arquivo — `generate_models`, `generate_test`, `main`, `load_yaml`, `map_type_to_python`, `get_field_definition`, `generate_relationships`, as constantes de path no topo, NADA disso muda nesta fase (per D-09).

    Estado atual da função (linhas 256-320):
    ```python
    def generate_router(entity_data: Dict[str, Any]) -> str:
        name = entity_data['name']
        var_name = name.lower()
        table_name = entity_data['table_name']

        return f"""from fastapi import APIRouter, Depends, HTTPException, Query
    from sqlmodel import Session, select
    from typing import List
    from uuid import UUID
    from caramello.database.session import get_session
    from caramello.models.{var_name} import {name}, {name}Read, {name}Create, {name}Update
    ...
    """
    ```

    Estado alvo — corpo da função (per D-08 + RESEARCH Pattern 4):
    ```python
    def generate_router(entity_data: Dict[str, Any]) -> str:
        name = entity_data['name']
        var_name = name.lower()
        table_name = entity_data['table_name']

        return f"""from fastapi import APIRouter, Depends, HTTPException, Query
    from sqlmodel import select
    from uuid import UUID
    from sqlmodel.ext.asyncio.session import AsyncSession
    from caramello.shared.database import get_session
    from caramello.models.{var_name} import {name}, {name}Read, {name}Create, {name}Update

    router = APIRouter(prefix="/{table_name}", tags=["{name}"])


    @router.post("/", response_model={name}Read)
    async def create_{var_name}({var_name}_in: {name}Create, session: AsyncSession = Depends(get_session)):
        db_obj = {name}.model_validate({var_name}_in)
        session.add(db_obj)
        await session.commit()
        await session.refresh(db_obj)
        return db_obj


    @router.get("/", response_model=list[{name}Read])
    async def read_{var_name}s(
        session: AsyncSession = Depends(get_session),
        offset: int = 0,
        limit: int = Query(default=100, le=100),
    ):
        result = await session.exec(select({name}).offset(offset).limit(limit))
        return result.all()


    @router.get("/{{uuid}}", response_model={name}Read)
    async def read_{var_name}(uuid: UUID, session: AsyncSession = Depends(get_session)):
        statement = select({name}).where({name}.uuid == uuid)
        result = await session.exec(statement)
        {var_name} = result.first()
        if not {var_name}:
            raise HTTPException(status_code=404, detail="{name} not found")
        return {var_name}


    @router.patch("/{{uuid}}", response_model={name}Read)
    async def update_{var_name}(uuid: UUID, {var_name}_in: {name}Update, session: AsyncSession = Depends(get_session)):
        statement = select({name}).where({name}.uuid == uuid)
        result = await session.exec(statement)
        db_obj = result.first()
        if not db_obj:
            raise HTTPException(status_code=404, detail="{name} not found")

        hero_data = {var_name}_in.model_dump(exclude_unset=True)
        for key, value in hero_data.items():
            setattr(db_obj, key, value)

        session.add(db_obj)
        await session.commit()
        await session.refresh(db_obj)
        return db_obj


    @router.delete("/{{uuid}}")
    async def delete_{var_name}(uuid: UUID, session: AsyncSession = Depends(get_session)):
        statement = select({name}).where({name}.uuid == uuid)
        result = await session.exec(statement)
        db_obj = result.first()
        if not db_obj:
            raise HTTPException(status_code=404, detail="{name} not found")

        await session.delete(db_obj)
        await session.commit()
        return {{"ok": True}}
    """
    ```

    **Diferenças críticas em relação ao template atual (todas obrigatórias):**
    1. Remover `from typing import List` — passa a usar `list[...]` nativo (Python 3.10+)
    2. Remover `Session` do import de sqlmodel → manter só `select`
    3. Adicionar `from sqlmodel.ext.asyncio.session import AsyncSession`
    4. Trocar `from caramello.database.session import get_session` por `from caramello.shared.database import get_session`
    5. Todos os endpoints viram `async def`
    6. Tipo da `session` muda de `Session` para `AsyncSession`
    7. `response_model=List[X]` → `response_model=list[X]`
    8. `session.exec(...).all()` → `result = await session.exec(...)` + `return result.all()` (dois passos)
    9. `session.exec(...).first()` → `result = await session.exec(...)` + `result.first()` (dois passos)
    10. `session.commit()` → `await session.commit()`
    11. `session.refresh(...)` → `await session.refresh(...)`
    12. `session.delete(...)` → `await session.delete(...)`

    **NÃO MUDAM (per D-09):**
    - Imports e constantes no topo do arquivo (linhas 1-16) — incluindo `MODELS_OUTPUT_DIR`, `API_OUTPUT_DIR`, `TESTS_OUTPUT_DIR`
    - `generate_models()` (linhas 128-254)
    - `generate_test()` (linhas 322-401)
    - `main()` (linhas 403-449)
    - Path de saída dos routers permanece `src/caramello/api/generated/<entity>_router.py`

    **Observação sobre escapes em f-string:** No template f-string da função, chaves dentro de strings literais (não interpoladas) devem aparecer duplicadas: `"/{{uuid}}"` no path, `{{"ok": True}}` no retorno. Isso já está correto no template atual e deve permanecer no novo.
  </action>
  <verify>
    <automated>grep -q "from sqlmodel.ext.asyncio.session import AsyncSession" scripts/generate_code.py &amp;&amp; grep -q "from caramello.shared.database import get_session" scripts/generate_code.py &amp;&amp; ! grep -q "from caramello.database.session import get_session" scripts/generate_code.py &amp;&amp; grep -q "async def create_" scripts/generate_code.py &amp;&amp; grep -q "async def read_" scripts/generate_code.py &amp;&amp; grep -q "async def update_" scripts/generate_code.py &amp;&amp; grep -q "async def delete_" scripts/generate_code.py &amp;&amp; grep -q "await session.exec" scripts/generate_code.py &amp;&amp; grep -q "await session.commit()" scripts/generate_code.py &amp;&amp; grep -q "await session.refresh" scripts/generate_code.py &amp;&amp; grep -q "await session.delete" scripts/generate_code.py &amp;&amp; grep -q "session: AsyncSession = Depends(get_session)" scripts/generate_code.py &amp;&amp; ! grep -q "from sqlmodel import Session, select" scripts/generate_code.py &amp;&amp; ! grep -q "from typing import List" scripts/generate_code.py &amp;&amp; grep -q "response_model=list\[" scripts/generate_code.py &amp;&amp; uv run python -c "import ast; ast.parse(open('scripts/generate_code.py').read()); print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "from sqlmodel.ext.asyncio.session import AsyncSession" scripts/generate_code.py` retorna `1` — import async no template
    - `grep -c "from caramello.shared.database import get_session" scripts/generate_code.py` retorna `1` — path novo de get_session
    - `grep "from caramello.database.session import get_session" scripts/generate_code.py` não retorna nada — path antigo eliminado
    - `grep -c "async def create_" scripts/generate_code.py` retorna `1`; idem para `read_`, `update_`, `delete_`
    - `grep -c "session: AsyncSession = Depends(get_session)" scripts/generate_code.py` >= `1` (aparece em todos os endpoints)
    - `grep -c "await session.commit()" scripts/generate_code.py` >= `1`
    - `grep -c "await session.refresh" scripts/generate_code.py` >= `1`
    - `grep -c "await session.delete" scripts/generate_code.py` >= `1`
    - `grep -c "await session.exec" scripts/generate_code.py` >= `1`
    - `grep "from sqlmodel import Session, select" scripts/generate_code.py` não retorna nada — Session sync removida do import
    - `grep "from typing import List" scripts/generate_code.py` não retorna nada — `List` substituído por `list` nativo
    - `grep "response_model=List\[" scripts/generate_code.py` não retorna nada — sem `List` em response_model
    - `grep -c "response_model=list\[" scripts/generate_code.py` >= `1` — `list` nativo presente
    - `uv run python -c "import ast; ast.parse(open('scripts/generate_code.py').read()); print('ok')"` imprime `ok` — sintaxe válida
    - `generate_models`, `generate_test`, `main` preservados: `grep -c "^def generate_models" scripts/generate_code.py` retorna `1`; `grep -c "^def generate_test" scripts/generate_code.py` retorna `1`; `grep -c "^def main" scripts/generate_code.py` retorna `1`
  </acceptance_criteria>
  <done>
    `scripts/generate_code.py::generate_router()` emite template async (per D-08): `async def` em todos os endpoints, `AsyncSession`, `await session.exec/commit/refresh/delete`, `from caramello.shared.database import get_session`, `list[...]` nativo. Demais funções intocadas. Sintaxe válida.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Regenerar os 4 routers via bin/generate_code, deletar database/session.py, validar main.py</name>
  <files>src/caramello/api/generated/user_router.py, src/caramello/api/generated/family_router.py, src/caramello/api/generated/familymember_router.py, src/caramello/api/generated/familyinvitation_router.py, src/caramello/database/session.py, src/caramello/main.py</files>
  <read_first>
    - bin/generate_code (3 linhas) — script wrapper de `uv run python scripts/generate_code.py`
    - dsl/manifest.yaml — confirma as 4 entidades (user, family, family_member, family_invitation) que serão regeneradas
    - src/caramello/api/generated/user_router.py — estado atual sync; após regen deve ficar async
    - src/caramello/main.py (36 linhas) — confirmar que NÃO há `from caramello.database.session import ...` nem chamada a `create_db_and_tables()` (per PATTERNS §"src/caramello/main.py"); se houver, remover
    - src/caramello/database/session.py — arquivo a ser removido por completo após regen
    - .planning/phases/02-stack-async/02-PATTERNS.md §"src/caramello/main.py" — diff esperado (provavelmente noop)
  </read_first>
  <action>
    **Etapa A — Regenerar routers (per D-10, D-11):**

    Executar (na raiz do repo, com `.env` presente):
    ```bash
    bash bin/generate_code
    ```

    Resultado esperado:
    - `src/caramello/api/generated/user_router.py` sobrescrito com template async
    - `src/caramello/api/generated/family_router.py` sobrescrito
    - `src/caramello/api/generated/familymember_router.py` sobrescrito
    - `src/caramello/api/generated/familyinvitation_router.py` sobrescrito
    - `src/caramello/models/*.py` também são reescritos pelo `generate_models` — esses NÃO devem ter mudança funcional (Phase 1 produziu os modelos corretos; o generator de models não foi tocado nesta fase per D-09). Verificar com `git diff src/caramello/models/` — se houver diff substantivo (mudança de campos, tipos, etc.), investigar; mudanças apenas de whitespace/formatação são aceitáveis.
    - Pode ser que `tests/generated/__init__.py` seja criado pelo `main()` do generator (`mkdir(parents=True, exist_ok=True)` + `.touch()`). A criação desse diretório é efeito colateral aceitável — Phase 5 trata dos testes.

    **Etapa B — Deletar `src/caramello/database/session.py` e o diretório `database/` (per D-03):**

    Após regen, os routers passam a importar de `caramello.shared.database`, então o módulo legado pode ser removido com segurança.

    ```bash
    rm src/caramello/database/session.py
    rm src/caramello/database/__init__.py
    rmdir src/caramello/database/__pycache__ 2>/dev/null || rm -rf src/caramello/database/__pycache__
    rmdir src/caramello/database
    ```

    Resultado: o diretório `src/caramello/database/` deixa de existir. O Python deixará de importá-lo (não há mais arquivos lá).

    **Etapa C — Validar `src/caramello/main.py` (per D-12):**

    Conferir o conteúdo atual (36 linhas). Segundo PATTERNS, o arquivo NÃO importa `database.session` nem chama `create_db_and_tables()` — apenas importa os routers gerados e configura FastAPI/CORS.

    Verificação:
    ```bash
    grep -n "database.session\|create_db_and_tables" src/caramello/main.py
    ```

    - Se o resultado é vazio: NÃO mexer em `main.py`. Adicionar `main.py` à lista `files_modified` do plano é precaução defensiva — efetivamente este task pode ser noop nesse arquivo.
    - Se houver alguma linha: remover os imports/chamadas correspondentes. Estado alvo: o arquivo continua incluindo os 4 routers e configurando CORS, mas sem nenhuma referência a `caramello.database.*` ou a `create_db_and_tables`.

    **Etapa D — Validar a integridade do app após regen:**

    Executar:
    ```bash
    uv run python -c "from caramello.main import app; print(len(app.routes))"
    ```

    Deve imprimir um número > 5 (pelo menos: root `/`, openapi, docs, e os 5 endpoints CRUD × 4 routers = >= 20 rotas).

    Executar (gates finais da Phase 2):
    ```bash
    # Gate 1: nenhum create_engine no código-fonte (success criteria #1 do ROADMAP)
    grep -rn "create_engine\b" src/ scripts/ alembic/ | grep -v "create_async_engine"
    # esperado: vazio

    # Gate 2: nenhum import de caramello.database em src/
    grep -rn "from caramello.database" src/
    # esperado: vazio

    # Gate 3: lint passa
    uv run ruff check src/

    # Gate 4: type-check passa (excluindo generated/models)
    uv run mypy src/
    ```
  </action>
  <verify>
    <automated>grep -q "async def create_user" src/caramello/api/generated/user_router.py &amp;&amp; grep -q "async def read_users" src/caramello/api/generated/user_router.py &amp;&amp; grep -q "from sqlmodel.ext.asyncio.session import AsyncSession" src/caramello/api/generated/user_router.py &amp;&amp; grep -q "from caramello.shared.database import get_session" src/caramello/api/generated/user_router.py &amp;&amp; grep -q "await session.exec" src/caramello/api/generated/user_router.py &amp;&amp; grep -q "async def create_family" src/caramello/api/generated/family_router.py &amp;&amp; grep -q "async def" src/caramello/api/generated/familymember_router.py &amp;&amp; grep -q "async def" src/caramello/api/generated/familyinvitation_router.py &amp;&amp; ! test -e src/caramello/database/session.py &amp;&amp; ! test -d src/caramello/database &amp;&amp; ! grep -rn "from caramello.database" src/ &amp;&amp; ! grep -rn "create_engine\b" src/ | grep -v "create_async_engine" &amp;&amp; uv run ruff check src/ &amp;&amp; uv run mypy src/ &amp;&amp; uv run python -c "from caramello.main import app; assert len(app.routes) &gt; 5, len(app.routes); print('ok', len(app.routes))"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "async def create_user" src/caramello/api/generated/user_router.py` retorna `1`
    - `grep -c "async def read_users" src/caramello/api/generated/user_router.py` retorna `1`
    - `grep -c "async def read_user\b" src/caramello/api/generated/user_router.py` retorna `1`
    - `grep -c "async def update_user" src/caramello/api/generated/user_router.py` retorna `1`
    - `grep -c "async def delete_user" src/caramello/api/generated/user_router.py` retorna `1`
    - `grep -c "from sqlmodel.ext.asyncio.session import AsyncSession" src/caramello/api/generated/user_router.py` retorna `1`
    - `grep -c "from caramello.shared.database import get_session" src/caramello/api/generated/user_router.py` retorna `1`
    - `grep -c "await session.exec" src/caramello/api/generated/user_router.py` >= `3` (read_user, update_user, delete_user usam exec; list também)
    - `grep -c "await session.commit()" src/caramello/api/generated/user_router.py` >= `2`
    - `grep "Session = Depends" src/caramello/api/generated/user_router.py | grep -v AsyncSession` não retorna nada — sem `Session` sync
    - Mesmo conjunto de critérios aplicado a `family_router.py`, `familymember_router.py`, `familyinvitation_router.py` substituindo o nome da entidade
    - `test -e src/caramello/database/session.py` exits 1 — arquivo deletado
    - `test -d src/caramello/database` exits 1 — diretório removido
    - `grep -rn "from caramello.database" src/` não retorna nada — nenhum import órfão
    - `grep -rn "from caramello.database" alembic/ scripts/` não retorna nada (apenas confirmação extra; alembic já usa `caramello.models` e `caramello.core.config`)
    - `grep -rn "create_engine\b" src/ | grep -v create_async_engine` não retorna nada — sem `create_engine` sync residual (ROADMAP success criteria §1)
    - `grep -c "create_async_engine" src/caramello/shared/database.py` retorna `1` — confirma que o único engine no código é async
    - `uv run ruff check src/` exits 0
    - `uv run mypy src/` exits 0
    - `uv run python -c "from caramello.main import app; assert len(app.routes) > 5; print('ok', len(app.routes))"` imprime `ok N` onde N > 5
    - `grep -c "include_router" src/caramello/main.py` retorna `4` — 4 routers ainda registrados
    - `grep "from caramello.database" src/caramello/main.py` não retorna nada — main.py sem referências ao módulo legado
    - `grep "create_db_and_tables" src/caramello/main.py` não retorna nada — chamada não está em main.py
    - `git status src/caramello/api/generated/` mostra 4 arquivos modificados (os 4 routers); demais arquivos podem ou não estar modificados (modelos só se houve regen no `generate_models` — aceitável se sem mudança funcional)
  </acceptance_criteria>
  <done>
    Os 4 routers em `src/caramello/api/generated/` estão regenerados em modo async (`async def`, `await session.exec/commit/refresh/delete`, `AsyncSession` from sqlmodel, `get_session` from `caramello.shared.database`). O módulo `src/caramello/database/` foi removido por completo. `src/caramello/main.py` continua incluindo os 4 routers e não importa nada de `caramello.database`. Gates de Phase 2 passam: nenhum `create_engine` sync, nenhum import de `caramello.database` em `src/`, ruff e mypy passam em `src/`, app inicia com `len(app.routes) > 5`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Cliente HTTP → routers | Inputs validados via Pydantic schemas (UserCreate, UserUpdate, etc.) |
| Routers → ORM/Database | `await session.exec(select(...))` parametriza queries; sem string-formatting de SQL |
| Geração de código → arquivos `.py` | `generate_code.py` sobrescreve routers — única forma sancionada de alterar `api/generated/` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2-07 | Tampering | SQL via routers gerados | accept | Template emite `await session.exec(select(Entity).where(Entity.uuid == uuid))` — ORM parametrizado, sem concatenação. Confirmado por inspeção do template. |
| T-2-08 | Information Disclosure | response_model dos routers | accept | Schemas `*Read` foram desenhados na Phase 1 para excluir campos sensíveis (não há `hashed_password` no User per MODEL-01). Generator preserva esse comportamento. |
| T-2-09 | Repudiation | Mutação async sem auditoria | accept | Phase 2 não introduz audit log; logging estruturado é deferido (OPS-02, v2). Sem regressão vs. estado anterior. |
| T-2-10 | Denial of Service | endpoints async sem rate limit | accept | Rate limiting é responsabilidade do Cloudflare Tunnel (Phase 5 deploy). Grupo família tem 1-5 usuários — sem risco prático nesta fase. |
| T-2-11 | Tampering (codegen) | `scripts/generate_code.py` template | mitigate | Template é estático no código-fonte (não dinâmico) — não há injeção de YAML→Python além dos nomes de entidade (já validados na Phase 1). f-string interpola apenas `name`, `var_name`, `table_name` — tokens DSL controlados. |
</threat_model>

<verification>
**Gates finais da Phase 2 (executar em sequência após Task 2):**

1. `grep -rn "create_engine\b" src/ scripts/ alembic/ | grep -v "create_async_engine"` retorna vazio (ROADMAP Phase 2 success criteria #1)
2. `grep -rn "from caramello.database" src/ scripts/` retorna vazio (módulo legado eliminado)
3. `grep -c "AsyncSession" src/caramello/shared/database.py` retorna >= 2 (import + uso) — ROADMAP Phase 2 success criteria #2
4. `grep -c "async_engine_from_config" alembic/env.py` retorna 1 — ROADMAP Phase 2 success criteria #3
5. `grep -rn "async def" src/caramello/api/generated/` retorna >= 20 linhas (5 endpoints × 4 routers) — ROADMAP Phase 2 success criteria #4
6. `uv run ruff check src/` exits 0
7. `uv run mypy src/` exits 0
8. `uv run python -c "from caramello.main import app; print(len(app.routes))"` imprime número > 5
9. Phase gate manual (executar com `.env` apontando para `familia_dev` em PostgreSQL acessível): `uv run alembic upgrade head` conclui sem hang e sem warning de conexão não fechada do asyncpg

**Validação de regressão:**
- Os modelos em `src/caramello/models/` podem ter sido re-emitidos pelo `generate_models` — se `git diff src/caramello/models/` mostrar mudanças funcionais (campos diferentes, tipos diferentes), investigar antes de commitar. Mudanças apenas de whitespace são aceitáveis.
</verification>

<success_criteria>
- D-08 implementada: generator emite template async (`async def`, `AsyncSession`, `await session.exec`, `from caramello.shared.database import get_session`)
- D-09 respeitada: campo `domain` no YAML e path de saída para `domains/{domain}/` NÃO foram introduzidos
- D-10 implementada: 4 routers regenerados via `bin/generate_code`, passam em ruff/mypy
- D-11 implementada: nenhum arquivo importa `Session` síncrona nem `create_engine` sync
- D-12 implementada: `main.py` sem referências a `caramello.database.session`; deleção de `src/caramello/database/` confirmada
- D-13 implementada: código gerado e código humano passam em ruff/mypy sem erros
- INFRA-01 satisfeita (cobertura total): toda a stack de queries de banco passou a ser async
- ROADMAP Phase 2 success criteria 1, 2, 3, 4 verificados via grep e execução
</success_criteria>

<output>
After completion, create `.planning/phases/02-stack-async/02-04-SUMMARY.md`
</output>
