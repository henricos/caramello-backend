---
phase: 03-estrutura-por-dom-nios-e-autentica-o
plan: "06"
type: execute
wave: 1
gap_closure: true
depends_on: []
files_modified:
  - scripts/generate_code.py
  - src/caramello/user/models.py
  - src/caramello/family/router.py
  - tests/test_user_operations.py
  - tests/test_auth.py
  - .planning/ROADMAP.md
autonomous: true
requirements: [STRUCT-02, AUTH-02, USER-01]

must_haves:
  truths:
    - "configure_mappers() para User passa sem erro — precondição para qualquer query real (Gap 1)"
    - "select(User).where(User.idp_sub == idp_sub) executa sem InvalidRequestError em runtime"
    - "Generator emite Relationship cross-domain late-bound com `# noqa: UP037` — ruff --unsafe-fixes não remove aspas"
    - "Re-execução de bin/generate_code mantém aspas em list['Family'] e list['FamilyInvitation']"
    - "family/router.py tem `_: User = Depends(get_current_user)` em vez de `_: Family|FamilyInvitation`"
    - "test_get_me_returns_user_fields e test_jit_provisioning podem rodar sem xfail/skip (uma vez que o mapper bug é corrigido)"
    - "ROADMAP.md SC2 da Phase 3 reflete a realidade (models.py contém ORM + schemas)"
  artifacts:
    - path: "src/caramello/user/models.py"
      provides: "User table model com relationships late-bound resolvíveis"
      contains: 'list["Family"]'
    - path: "src/caramello/user/models.py"
      provides: "User com sent_invitations late-bound"
      contains: 'list["FamilyInvitation"]'
    - path: "scripts/generate_code.py"
      provides: "generate_relationships emite # noqa: UP037 para tipos late-bound"
      contains: "noqa: UP037"
    - path: "src/caramello/family/router.py"
      provides: "Router family com auth tipada corretamente"
      contains: "_: User = Depends(get_current_user)"
    - path: ".planning/ROADMAP.md"
      provides: "SC2 ajustado para refletir modelo unificado"
      contains: "models.py"
  key_links:
    - from: "src/caramello/user/models.py"
      to: "Mapper SQLAlchemy"
      via: 'list["Family"] (string literal preservada por # noqa: UP037)'
      pattern: 'list\["Family"\]'
    - from: "scripts/generate_code.py"
      to: "src/caramello/user/models.py"
      via: "regeneração via bin/generate_code preserva aspas"
      pattern: "noqa: UP037"
    - from: "src/caramello/shared/auth.py"
      to: "Mapper SQLAlchemy"
      via: "select(User) em get_current_user"
      pattern: "select\\(User\\)"
---

<objective>
Corrigir o bug de mapper SQLAlchemy no modelo `User` que bloqueia `configure_mappers()` e qualquer query real em runtime (Gap 1 de VERIFICATION.md), corrigir o warning de tipo `_: Family = Depends(get_current_user)` em `family/router.py` (deveria ser `_: User`), remover marcas `@pytest.mark.xfail`/`@pytest.mark.skip` dos testes que dependiam do bug, e atualizar `ROADMAP.md` SC2 da Phase 3 para refletir que o generator produz apenas `models.py` (Gap 3 — documental).

Purpose: Sem este fix, `get_current_user` falha com `InvalidRequestError: relationship('list[Family]')` em qualquer request ao banco, bloqueando AUTH-02 (JIT provisioning) e USER-01 (GET /user/me). O bug ocorre porque o `_run_ruff_fix()` no pipeline do generator usa `ruff --unsafe-fixes`, e a rule **UP037 (quoted-annotation)** remove as aspas de `list["Family"]` -> `list[Family]`, sem perceber que `Family` está apenas sob `TYPE_CHECKING` (não disponível em runtime para o SQLAlchemy class registry).

Output: Generator corrigido para emitir `# noqa: UP037` em relationships cuja anotação referencia entidades late-bound; arquivos regenerados; testes destravados; ROADMAP atualizado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-estrutura-por-dom-nios-e-autentica-o/03-CONTEXT.md
@.planning/phases/03-estrutura-por-dom-nios-e-autentica-o/03-VERIFICATION.md
@.planning/phases/03-estrutura-por-dom-nios-e-autentica-o/03-RESEARCH.md
@.planning/phases/03-estrutura-por-dom-nios-e-autentica-o/03-PATTERNS.md
@.planning/phases/03-estrutura-por-dom-nios-e-autentica-o/03-05-SUMMARY.md

<interfaces>
Causa raiz confirmada por reprodução experimental antes do planejamento:

```
$ uv run python -c "from sqlalchemy.orm import configure_mappers; from caramello.user.models import User; configure_mappers()"
sqlalchemy.exc.InvalidRequestError: When initializing mapper Mapper[User(user)],
expression "relationship('list[Family]')" seems to be using a generic class as
the argument to relationship(); please state the generic argument using an annotation,
e.g. "families: Mapped[list['Family']] = relationship()"
```

Estado atual de `src/caramello/user/models.py` (relevante):

```python
from __future__ import annotations
from typing import TYPE_CHECKING
# ...
if TYPE_CHECKING:
    from caramello.family.models import Family, FamilyInvitation

class User(SQLModel, table=True):
    # ... fields ...
    families: list[Family] = Relationship(back_populates="members")           # BUG: precisa ser list["Family"]
    sent_invitations: list[FamilyInvitation] = Relationship(back_populates="inviter")  # BUG idem
```

Trace do generator (`scripts/generate_code.py` linhas 39-47) — emite corretamente:

```python
def map_type_to_python(dsl_type: str) -> str:
    if clean_type.lower().startswith("list["):
        inner = clean_type[5:-1].strip()
        special = {"UUID", "datetime", "EmailStr"}
        if inner not in STANDARD_TYPES and inner not in special:
            return f'list["{inner}"]'   # COM aspas
        return f"list[{inner}]"
```

Comprovação experimental do bug do ruff UP037 (causa real):

```
Entrada do generator:  families: list["Family"] = Relationship(...)
Após `ruff check --fix --unsafe-fixes`: families: list[Family] = Relationship(...)
```

UP037 (quoted-annotation) assume que com `from __future__ import annotations` as aspas
são redundantes — não sabe que SQLModel/SQLAlchemy parseia a string literal da anotação
em runtime via class registry, e `Family` está apenas sob TYPE_CHECKING.

`_run_ruff_fix()` em `scripts/generate_code.py` linhas 804-820:

```python
def _run_ruff_fix(src_dir: Path) -> None:
    dirs = [str(src_dir / d) for d in ("user", "family") if (src_dir / d).exists()]
    if not dirs: return
    subprocess.run(
        ["python", "-m", "ruff", "check", "--fix", "--unsafe-fixes", *dirs],
        capture_output=True,
    )
    subprocess.run(["python", "-m", "ruff", "format", *dirs], capture_output=True)
```

Solução: `generate_relationships` (linhas 107-132) emite `# noqa: UP037` ao final de cada
linha de Relationship cujo `type` é uma entidade late-bound (TYPE_CHECKING). O `_consolidate_models`
(linhas 463-630) já calcula `late_bind_imports` e `skip_link_models` por entidade via
`_would_create_cycle` — basta extrair os nomes de classes e propagar.

Estado atual do template de router (`generate_router`, linhas 295-380):

```python
@router.get("/{{uuid}}", response_model={name}Read)
async def read_{var_name}(
    uuid: UUID,
    session: AsyncSession = Depends(get_session),
    _: {name} = Depends(get_current_user),   # BUG: emite "_: Family" — deveria ser "_: User"
) -> {name}:
```

`get_current_user` retorna `User`, sempre. FastAPI ignora o tipo na variável `_` (não usada),
então não causa erro de runtime — mas é incorreto semanticamente e foi sinalizado como
WARNING em VERIFICATION.md (5 ocorrências em family/router.py).

Estado atual de `tests/test_user_operations.py` linhas 11-13:

```python
@pytest.mark.xfail(reason="Wave 4 (Plan 05) implementa GET /user/me", strict=False)
def test_get_me_returns_user_fields():
```

Estado atual de `tests/test_auth.py`:
- linha 12: `@pytest.mark.xfail(reason="Wave 3 (Plan 04) cria shared/auth.py", strict=False)` em `test_auth_module` — pode ser removida (módulo já existe).
- linhas 21-24: `@pytest.mark.xfail(reason="Wave 4 (Plan 05) registra router /user/me e habilita auth", strict=False)` em `test_me_unauthenticated` — pode ser removida.
- linhas 34-37: `@pytest.mark.xfail(reason="Wave 4 (Plan 05) habilita auth em todos os routers CRUD", strict=False)` em `test_user_crud_requires_auth` — pode ser removida.
- linhas 44-56: `@pytest.mark.integration` + `@pytest.mark.xfail` + corpo com `pytest.skip` em `test_jit_provisioning` — remover `@pytest.mark.xfail` (não há mais bug bloqueante, apenas dependência de banco real). Manter `@pytest.mark.integration` e o `pytest.skip` interno com motivo claro.
- linhas 59-62: `@pytest.mark.xfail(reason="Wave 3 (Plan 04) define algorithm=['RS256']", strict=False)` em `test_jwt_decode_only_accepts_rs256` — pode ser removida.

Estado atual de ROADMAP.md SC2 da Phase 3 (linha 61 do arquivo, JÁ CORRIGIDO em commit anterior):

```
2. DSL generator com campo `domain` no YAML produz `models.py` (ORM + schemas Read/Create/Update), `router.py` e `operations.py` dentro de `src/caramello/{domain}/` sem editar arquivos gerados
```

Importante: o texto antigo "produz `models.py` e `schemas.py` dentro de `domains/{domain}/`" JÁ FOI corrigido em commit anterior; portanto o Passo 7 desta task é, na prática, uma verificação de idempotência — confirma que SC2 já reflete a realidade e que nenhuma edição adicional é necessária.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Corrigir generator — emitir # noqa: UP037 em Relationship de tipos late-bound</name>
  <files>scripts/generate_code.py</files>
  <read_first>
    - scripts/generate_code.py (estado atual — funções map_type_to_python linhas 39-64, generate_relationships linhas 107-132, generate_models linhas 135-292, _consolidate_models linhas 463-630)
    - src/caramello/user/models.py (estado atual com bug — linhas 31-32)
    - .planning/phases/03-estrutura-por-dom-nios-e-autentica-o/03-VERIFICATION.md (descrição da causa raiz, gaps)
  </read_first>
  <action>
    Alterar `scripts/generate_code.py` para que `generate_relationships` aceite um parâmetro `late_bound_types: set[str] | None` (nomes de classes que serão late-bound via TYPE_CHECKING) e emita `# noqa: UP037` ao final das linhas de Relationship cujo tipo referencia uma dessas classes. Propagar `late_bound_types` a partir de `_consolidate_models` para `generate_models` para `generate_relationships`.

    Edit 1 — Atualizar `generate_relationships` (linhas 107-132). Substituir EXATAMENTE:

    ```python
    def generate_relationships(
        relationships: list[dict[str, Any]],
        entity_name: str,
        entity_domain: dict[str, str],
        skip_link_models: set[str] | None = None,
    ) -> list[str]:
        """Gera as linhas de Relationship() para uma entidade.

        skip_link_models: set de nomes de link_model que serão late-bound (não emitir
        no Relationship() inline para evitar NameError por import circular).
        """
        skip = skip_link_models or set()
        lines = []
        for rel in relationships:
            rname = rel["name"]
            rtype = map_type_to_python(rel["type"])

            args: list[str] = []
            if rel.get("back_populates"):
                args.append(f"back_populates={rel['back_populates']!r}")
            lm = rel.get("link_model")
            if lm and lm not in skip:
                args.append(f"link_model={lm}")

            lines.append(f"    {rname}: {rtype} = Relationship({', '.join(args)})")
        return lines
    ```

    Por:

    ```python
    def generate_relationships(
        relationships: list[dict[str, Any]],
        entity_name: str,
        entity_domain: dict[str, str],
        skip_link_models: set[str] | None = None,
        late_bound_types: set[str] | None = None,
    ) -> list[str]:
        """Gera as linhas de Relationship() para uma entidade.

        skip_link_models: set de nomes de link_model que serão late-bound (não emitir
        no Relationship() inline para evitar NameError por import circular).
        late_bound_types: set de classes referenciadas no rel.type que estão apenas
        sob TYPE_CHECKING (não disponíveis em runtime). Necessário para emitir
        `# noqa: UP037` e impedir que `ruff --unsafe-fixes` remova as aspas da
        anotação `list["Foo"]`, o que quebraria o mapper SQLAlchemy.
        """
        skip = skip_link_models or set()
        late_bound = late_bound_types or set()
        lines = []
        for rel in relationships:
            rname = rel["name"]
            rtype = map_type_to_python(rel["type"])

            args: list[str] = []
            if rel.get("back_populates"):
                args.append(f"back_populates={rel['back_populates']!r}")
            lm = rel.get("link_model")
            if lm and lm not in skip:
                args.append(f"link_model={lm}")

            # Detectar se rtype referencia classe late-bound (sob TYPE_CHECKING).
            # Nesse caso anotar `# noqa: UP037` para impedir que ruff
            # `--unsafe-fixes` (rule UP037) remova as aspas. `Family` em
            # `list["Family"]` precisa permanecer string para o mapper SQLAlchemy
            # resolver via class registry em runtime.
            rtype_raw = rel["type"].strip()
            if rtype_raw.lower().startswith("list["):
                inner = rtype_raw[5:-1].strip().strip('"')
            else:
                inner = rtype_raw.strip('"')
            needs_noqa = inner in late_bound
            line = f"    {rname}: {rtype} = Relationship({', '.join(args)})"
            if needs_noqa:
                line += "  # noqa: UP037"
            lines.append(line)
        return lines
    ```

    Edit 2 — Atualizar `generate_models` (linhas 135-227). Substituir a assinatura na linha 135:

    ```python
    def generate_models(
        entity_data: dict[str, Any],
        entity_domain: dict[str, str],
        skip_link_models: set[str] | None = None,
    ) -> str:
    ```

    Por:

    ```python
    def generate_models(
        entity_data: dict[str, Any],
        entity_domain: dict[str, str],
        skip_link_models: set[str] | None = None,
        late_bound_types: set[str] | None = None,
    ) -> str:
    ```

    Edit 3 — Atualizar a chamada de `generate_relationships` dentro de `generate_models` (linhas 225-227). Substituir:

    ```python
        rel_lines = generate_relationships(
            relationships, name, entity_domain, skip_link_models
        )
    ```

    Por:

    ```python
        rel_lines = generate_relationships(
            relationships, name, entity_domain, skip_link_models, late_bound_types
        )
    ```

    Edit 4 — Atualizar `_consolidate_models` (loop por entidade nas linhas 501-515). Localizar o trecho:

    ```python
        for entity_data in entities:
            # Pré-calcular quais link_models desta entidade serão late-bound
            entity_skip_link_models: set[str] = set()
            for rel in entity_data.get("relationships", []):
                lm = rel.get("link_model")
                if not lm:
                    continue
                lm_domain = entity_domain.get(lm, "")
                if lm_domain and lm_domain != domain and _would_create_cycle(lm_domain):
                    entity_skip_link_models.add(lm)
                    ent_name = entity_data["name"]
                    rel_name = rel["name"]
                    late_bind_assignments.append((ent_name, rel_name, lm))

            block = generate_models(entity_data, entity_domain, entity_skip_link_models)
    ```

    Substituir somente a chamada `block = generate_models(...)` (última linha do trecho acima) por:

    ```python
            # Extrair nomes de classes que entrarão em TYPE_CHECKING (late-bound).
            # Necessário para que generate_relationships emita `# noqa: UP037` nas
            # linhas cujo tipo referencia essas classes (UP037 removeria as aspas
            # de `list["Family"]` e quebraria o mapper SQLAlchemy).
            entity_late_bound: set[str] = set()
            for rel in entity_data.get("relationships", []):
                rtype_raw = rel["type"].strip()
                if rtype_raw.lower().startswith("list["):
                    inner = rtype_raw[5:-1].strip().strip('"')
                else:
                    inner = rtype_raw.strip('"')
                special3 = {"UUID", "datetime", "EmailStr"}
                if inner and inner not in STANDARD_TYPES and inner not in special3:
                    rel_domain = entity_domain.get(inner, "")
                    if (
                        rel_domain
                        and rel_domain != domain
                        and _would_create_cycle(rel_domain)
                    ):
                        entity_late_bound.add(inner)

            block = generate_models(
                entity_data,
                entity_domain,
                entity_skip_link_models,
                entity_late_bound,
            )
    ```

    Nenhuma outra função muda. Não tocar em `_run_ruff_fix()` — manter `--unsafe-fixes` para que outras rules (UP006, I001, etc.) continuem sendo aplicadas. O `# noqa: UP037` é cirúrgico.
  </action>
  <verify>
    <automated>cd /home/claude/work/caramello-api && uv run python -c "
import sys; sys.path.insert(0, 'scripts')
from generate_code import generate_relationships

rels = [
    {'name': 'families', 'type': 'list[Family]', 'link_model': 'FamilyMember', 'back_populates': 'members'},
    {'name': 'sent_invitations', 'type': 'list[FamilyInvitation]', 'back_populates': 'inviter'},
]
entity_domain = {'Family': 'family', 'User': 'user', 'FamilyMember': 'family', 'FamilyInvitation': 'family'}
lines = generate_relationships(rels, 'User', entity_domain, {'FamilyMember'}, {'Family', 'FamilyInvitation'})
for line in lines:
    print(line)
    assert 'noqa: UP037' in line, f'Faltou noqa em: {line}'
print('OK')
"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "late_bound_types" scripts/generate_code.py` retorna >= 4 (assinaturas + lógica)
    - `grep -c "noqa: UP037" scripts/generate_code.py` retorna >= 1
    - O script de verificação acima imprime 2 linhas terminadas em `# noqa: UP037` e finaliza com `OK` (exit 0)
    - `grep -c "entity_late_bound" scripts/generate_code.py` >= 3 (dentro de `_consolidate_models`)
    - `uv run ruff check scripts/generate_code.py` retorna `All checks passed`
    - `uv run mypy scripts/generate_code.py` retorna `Success: no issues found`
  </acceptance_criteria>
  <done>
    Generator alterado para emitir `# noqa: UP037` em relationships com tipos late-bound; lógica isolada e verificada via script Python; ruff/mypy do generator limpos.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Corrigir template de router — emitir `_: User = Depends(get_current_user)` em vez de `_: {name}`</name>
  <files>scripts/generate_code.py</files>
  <read_first>
    - scripts/generate_code.py linhas 295-380 (função `generate_router` — template f-string com `_: {name}`)
    - src/caramello/family/router.py (referência: estado atual com 10 ocorrências de `_: Family` e `_: FamilyInvitation`)
    - src/caramello/shared/auth.py (referência: `get_current_user` retorna `User`)
  </read_first>
  <action>
    Alterar `generate_router` (linhas 295-380) para emitir SEMPRE `_: User = Depends(get_current_user)` em vez de `_: {name} = Depends(get_current_user)`, e adicionar import de `User` no template quando o domain do router for diferente de `user`.

    Edit 1 — Adicionar bloco de import condicional ANTES do `return f"""..."""` (no início do template após capturar `name`, `var_name`, `table_name`, `domain`):

    Localizar o início da função `generate_router` (linha 295). Após a linha `domain = entity_data.get("domain", "")` (linha 300), e ANTES do `return f"""...` (linha 302), inserir:

    ```python
        # Import de User é necessário para a anotação `_: User = Depends(get_current_user)`.
        # Se o domain do router for 'user', a classe já é importada via models do próprio domínio.
        if domain == "user":
            user_import_line = ""
        else:
            user_import_line = "from caramello.user.models import User\n"
    ```

    Edit 2 — Substituir o bloco de imports do f-string para incluir `{user_import_line}`. Localizar dentro do `return f"""..."""` (linhas 302-313):

    ```
    return f"""from __future__ import annotations

    from uuid import UUID

    from fastapi import APIRouter, Depends, HTTPException, Query
    from sqlmodel import select
    from sqlmodel.ext.asyncio.session import AsyncSession

    from caramello.shared.auth import get_current_user
    from caramello.shared.database import get_session
    from caramello.{domain}.models import {name}, {name}Read, {name}Create, {name}Update
    ```

    Trocar a última linha do bloco de imports por (inserindo `{user_import_line}` ANTES da linha de import dos modelos do domínio):

    ```
    from caramello.shared.auth import get_current_user
    from caramello.shared.database import get_session
    {user_import_line}from caramello.{domain}.models import {name}, {name}Read, {name}Create, {name}Update
    ```

    Observação: `{user_import_line}` JÁ termina com `\n` quando preenchido, ou é vazio quando domain == "user". Isso preserva isort posteriormente (ruff format).

    Edit 3 — Substituir TODAS as 5 ocorrências de `_: {name} = Depends(get_current_user),` no f-string por `_: User = Depends(get_current_user),`.

    Identifique cada handler dentro do f-string template (POST `/`, GET `/`, GET `/{{uuid}}`, PATCH `/{{uuid}}`, DELETE `/{{uuid}}`). Em cada um, troque literalmente o texto:

    ```
        _: {name} = Depends(get_current_user),
    ```

    Por:

    ```
        _: User = Depends(get_current_user),
    ```

    Após a alteração, o trecho deve aparecer 5 vezes no template como `_: User = Depends(get_current_user),`.

    Nenhuma outra função muda.
  </action>
  <verify>
    <automated>cd /home/claude/work/caramello-api && uv run python -c "
import sys; sys.path.insert(0, 'scripts')
from generate_code import generate_router
entity_data_family = {'name': 'Family', 'table_name': 'family', 'domain': 'family'}
code = generate_router(entity_data_family)
assert '_: User = Depends(get_current_user)' in code, 'Falta _: User no template'
assert '_: Family = Depends(get_current_user)' not in code, 'Ainda tem _: Family'
assert 'from caramello.user.models import User' in code, 'Falta import de User'
assert code.count('_: User = Depends(get_current_user)') == 5, f'Esperado 5 ocorrencias, achou {code.count(chr(95) + chr(58) + chr(32) + chr(85))}'

entity_data_user = {'name': 'User', 'table_name': 'user', 'domain': 'user'}
code_user = generate_router(entity_data_user)
assert 'from caramello.user.models import User' in code_user, 'User domain ainda precisa importar User (via linha principal)'
assert code_user.count('from caramello.user.models import User') == 1, 'No domain user, deve ter exatamente 1 import (na linha principal de imports do domain)'
print('OK')
"</automated>
  </verify>
  <acceptance_criteria>
    - O script de verificação acima imprime `OK` (exit 0)
    - `grep -c "_: User = Depends(get_current_user)" scripts/generate_code.py` >= 5
    - `grep -c "_: {name} = Depends(get_current_user)" scripts/generate_code.py` == 0 (padrão antigo eliminado)
    - `grep -c "user_import_line" scripts/generate_code.py` >= 2 (definição + uso no f-string)
    - `uv run ruff check scripts/generate_code.py` retorna `All checks passed`
  </acceptance_criteria>
  <done>
    Template de router emite anotação correta `_: User` em todos os endpoints CRUD, com import condicional de `User` quando o domain != 'user'. Verificado isoladamente sem regenerar arquivos.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Regenerar código + validar fix do mapper + atualizar testes + atualizar ROADMAP</name>
  <files>
    src/caramello/user/models.py,
    src/caramello/family/router.py,
    src/caramello/family/models.py,
    src/caramello/user/router.py,
    tests/test_user_operations.py,
    tests/test_auth.py,
    .planning/ROADMAP.md
  </files>
  <read_first>
    - bin/generate_code (wrapper shell)
    - scripts/generate_code.py (estado após Tasks 1 e 2)
    - src/caramello/user/models.py (estado atual com bug)
    - src/caramello/family/router.py (estado atual com `_: Family`)
    - tests/test_user_operations.py (estado atual com xfail)
    - tests/test_auth.py (estado atual com xfail/skip)
    - .planning/ROADMAP.md (estado atual — SC2 JÁ contém "models.py (ORM + schemas Read/Create/Update)" após commit anterior)
    - src/caramello/user/operations.py (verificar que anotação `# CARAMELLO-GENERATED: implemented` é preservada)
  </read_first>
  <action>
    Executar `bin/generate_code` para regenerar arquivos a partir do generator corrigido (Tasks 1 e 2). Verificar que o fix do mapper funciona. Remover marcas `@pytest.mark.xfail` e `@pytest.mark.skip` que dependiam do bug. Atualizar ROADMAP.md SC2 (no-op idempotente — texto já correto).

    Passo 1 — Limpar caches Python:

    ```bash
    find /home/claude/work/caramello-api -type d -name __pycache__ -exec rm -rf {} + 2>/dev/null || true
    ```

    Passo 2 — Regenerar código a partir do DSL:

    ```bash
    cd /home/claude/work/caramello-api && bin/generate_code
    ```

    Verificação imediata pós-regeneração (NÃO commitar ainda):

    ```bash
    grep -n 'list\["Family"\]\|list\["FamilyInvitation"\]\|noqa: UP037' src/caramello/user/models.py
    grep -c "_: User = Depends(get_current_user)" src/caramello/family/router.py
    head -1 src/caramello/user/operations.py
    ```

    Resultado esperado:
    - `user/models.py` contém EXATAMENTE 2 linhas com `noqa: UP037` (uma para `families`, uma para `sent_invitations`), e ambas mantêm aspas em `list["Family"]` e `list["FamilyInvitation"]`.
    - `family/router.py` tem 10 ocorrências de `_: User = Depends(get_current_user)` (5 endpoints x 2 routers no arquivo consolidado).
    - `user/operations.py` linha 1 é `# CARAMELLO-GENERATED: implemented` (generator deve ter pulado este arquivo).

    Se algum desses checks falhar, voltar ao generator e corrigir antes de prosseguir.

    Passo 3 — Validar o fix do mapper SQLAlchemy (PRECONDIÇÃO crítica para Gap 1):

    ```bash
    cd /home/claude/work/caramello-api && uv run python -c "
    from sqlalchemy.orm import configure_mappers
    from caramello.user.models import User
    from caramello.family.models import Family, FamilyInvitation, FamilyMember
    configure_mappers()
    print('OK')
    "
    ```

    Resultado esperado: imprime `OK` em stdout e exit code 0. Se ainda falhar com `InvalidRequestError: relationship('list[Family]')`, há algum caminho residual onde o `noqa: UP037` não foi emitido — voltar para Task 1.

    Passo 4 — Verificar qualidade do código gerado:

    ```bash
    cd /home/claude/work/caramello-api && uv run ruff check src/
    cd /home/claude/work/caramello-api && uv run mypy src/
    ```

    Ambos devem terminar com sucesso (All checks passed / Success: no issues found in 14 source files).

    Passo 5 — Remover marcas `@pytest.mark.xfail` em `tests/test_user_operations.py`:

    Editar `tests/test_user_operations.py`:

    1. Linhas 11-14: remover o decorator `@pytest.mark.xfail(...)` de `test_get_me_returns_user_fields` (manter assinatura da função).
    2. Linhas 53-56: remover o decorator `@pytest.mark.xfail(...)` de `test_operations_annotation_is_implemented` (manter assinatura).
    3. Avaliar se `import pytest` ainda é necessário — se não houver mais decorators, remover a linha. Caso contrário, manter.

    Resultado esperado: o arquivo tem 2 funções de teste sem marcas `xfail`.

    Passo 6 — Atualizar marcas em `tests/test_auth.py`:

    1. Linha 12: remover `@pytest.mark.xfail(reason="Wave 3 (Plan 04) cria shared/auth.py", strict=False)` de `test_auth_module`.
    2. Linhas 21-24: remover `@pytest.mark.xfail(...)` de `test_me_unauthenticated`.
    3. Linhas 34-37: remover `@pytest.mark.xfail(...)` de `test_user_crud_requires_auth`.
    4. Linhas 44-46: em `test_jit_provisioning`, REMOVER o `@pytest.mark.xfail(reason="Wave 4 (Plan 05) implementa JIT provisioning — requer banco real", strict=False)`. MANTER `@pytest.mark.integration`. MANTER o corpo `pytest.skip(...)` mas atualizar o motivo para refletir o estado atual:

       Substituir:
       ```python
           pytest.skip("Requer Keycloak real e banco isolado (Phase 5)")
       ```

       Por:
       ```python
           pytest.skip(
               "Requer Keycloak real e banco PostgreSQL configurado via .env "
               "(executado manualmente pelo operador no plano 03-07; "
               "banco isolado vem na Phase 5)"
           )
       ```
    5. Linhas 59-62: remover `@pytest.mark.xfail(reason="Wave 3 (Plan 04) define algorithm=['RS256']", strict=False)` de `test_jwt_decode_only_accepts_rs256`.

    Resultado esperado: `tests/test_auth.py` tem 5 funções de teste — `test_jit_provisioning` mantém apenas `@pytest.mark.integration` (não há mais `xfail`); as outras 4 não têm marca alguma.

    Passo 7 — Verificar idempotência de `.planning/ROADMAP.md` SC2 da Phase 3 (no-op esperado):

    O texto antigo `"produz `models.py` e `schemas.py` dentro de `domains/{domain}/`"` JÁ foi corrigido em commit anterior e o SC2 hoje contém o texto desejado. Esta task apenas confirma o estado e edita CONDICIONALMENTE caso o texto antigo retorne.

    Verificação:
    ```bash
    grep -n "schemas.py" .planning/ROADMAP.md
    grep -n "models.py (ORM + schemas Read/Create/Update)" .planning/ROADMAP.md
    ```

    Resultado esperado:
    - O primeiro grep retorna 0 linhas (não há mais referência a `schemas.py` no ROADMAP).
    - O segundo grep retorna pelo menos 1 linha (texto novo presente).

    Se o primeiro grep retornar linhas com `schemas.py` em contexto de Phase 3 SC2 (regressão inesperada), localizar o trecho e substituir por:

    ```
      2. DSL generator com campo `domain` no YAML produz `models.py` (ORM + schemas Read/Create/Update), `router.py` e `operations.py` dentro de `src/caramello/{domain}/` sem editar arquivos gerados
    ```

    Caso contrário (caminho esperado), Passo 7 é no-op: nenhuma edição em `.planning/ROADMAP.md`. Documentar no SUMMARY que o ROADMAP já estava correto.

    Passo 8 — Rodar a suite de testes para confirmar que nada quebrou:

    ```bash
    cd /home/claude/work/caramello-api && uv run pytest tests/test_generator.py tests/test_auth.py tests/test_user_operations.py -v
    ```

    Resultado esperado: nenhum teste com `xfailed` ou `xpassed` (porque removemos as marcas). Testes que requerem banco real (`@pytest.mark.integration`) ainda passam como `skipped`. Demais testes devem passar (PASSED) ou — se algum teste falhar por dependência de banco real que antes estava sob xfail — investigar e ajustar antes de fechar a task.

    Observação importante sobre `test_get_me_returns_user_fields`: este teste usa `app.dependency_overrides[get_current_user]` para mockar a auth e NÃO depende de banco real. Após remover o `xfail`, deve passar como `PASSED` (porque o mapper agora resolve).
  </action>
  <verify>
    <automated>cd /home/claude/work/caramello-api && uv run python -c "
from sqlalchemy.orm import configure_mappers
from caramello.user.models import User
from caramello.family.models import Family, FamilyInvitation, FamilyMember
configure_mappers()
print('MAPPER_OK')
" && [ "$(grep -c "noqa: UP037" src/caramello/user/models.py)" = "2" ] && [ "$(grep -c "_: User = Depends(get_current_user)" src/caramello/family/router.py)" = "10" ] && ! grep -q "@pytest.mark.xfail" tests/test_auth.py tests/test_user_operations.py && ! grep -q "schemas.py" .planning/ROADMAP.md && uv run ruff check src/ && uv run mypy src/ && uv run pytest tests/test_generator.py tests/test_auth.py tests/test_user_operations.py -v</automated>
  </verify>
  <acceptance_criteria>
    - O comando `uv run python -c "from sqlalchemy.orm import configure_mappers; from caramello.user.models import User; configure_mappers(); print('OK')"` imprime `OK` e termina com exit 0
    - `grep -c "noqa: UP037" src/caramello/user/models.py` == 2 (exatamente uma em families, uma em sent_invitations)
    - `grep -c 'list\["Family"\]' src/caramello/user/models.py` >= 1
    - `grep -c 'list\["FamilyInvitation"\]' src/caramello/user/models.py` >= 1
    - `grep -c "_: User = Depends(get_current_user)" src/caramello/family/router.py` == 10
    - `grep -c "_: Family = Depends(get_current_user)" src/caramello/family/router.py` == 0
    - `grep -c "_: FamilyInvitation = Depends(get_current_user)" src/caramello/family/router.py` == 0
    - `head -1 src/caramello/user/operations.py` retorna `# CARAMELLO-GENERATED: implemented` (anotação preservada)
    - `! grep -q "@pytest.mark.xfail" tests/test_auth.py tests/test_user_operations.py` (exit 0 — nenhuma marca xfail restante)
    - `grep -c "@pytest.mark.integration" tests/test_auth.py` == 1 (test_jit_provisioning mantém marca integration)
    - `grep -c "models.py" .planning/ROADMAP.md` >= 1 (texto SC2 atualizado)
    - `! grep -q "schemas.py" .planning/ROADMAP.md` (exit 0 — referência a schemas.py ausente do SC2)
    - `uv run ruff check src/` retorna `All checks passed!`
    - `uv run mypy src/` retorna `Success: no issues found in 14 source files`
    - `uv run pytest tests/test_generator.py tests/test_auth.py tests/test_user_operations.py -v` termina com 0 failures e 0 errors (skipped por integration é aceito)
  </acceptance_criteria>
  <done>
    Mapper SQLAlchemy resolve sem erro; `family/router.py` tem anotação correta `_: User` em todos os endpoints; testes destravados; ROADMAP.md SC2 já reflete a realidade (Passo 7 é no-op por idempotência); ruff/mypy/pytest passam.
  </done>
</task>

</tasks>

<verification>
Após executar as 3 tasks, confirmar manualmente:

1. **Mapper passa:**
   ```bash
   uv run python -c "
   from sqlalchemy.orm import configure_mappers
   from caramello.user.models import User
   configure_mappers()
   print('OK')
   "
   ```
   Deve imprimir `OK`.

2. **Idempotência do generator:** rodar `bin/generate_code` uma segunda vez não deve alterar arquivos:
   ```bash
   bin/generate_code
   git status --porcelain src/caramello/user/ src/caramello/family/
   ```
   Saída esperada: vazia (nenhum arquivo modificado).

3. **Suite de testes verde:**
   ```bash
   uv run pytest tests/ -v
   ```
   Deve mostrar 0 failures, 0 errors. `test_jit_provisioning` aparece como `SKIPPED` (espera operador do plano 03-07).
</verification>

<success_criteria>
- `configure_mappers()` para User executa sem `InvalidRequestError`
- `src/caramello/user/models.py` contém `list["Family"]  # noqa: UP037` e `list["FamilyInvitation"]  # noqa: UP037`
- `src/caramello/family/router.py` tem 10 ocorrências de `_: User = Depends(get_current_user)`
- `scripts/generate_code.py` propaga `late_bound_types` por `_consolidate_models -> generate_models -> generate_relationships`
- `tests/test_auth.py` e `tests/test_user_operations.py` não têm mais `@pytest.mark.xfail` (apenas `@pytest.mark.integration` em `test_jit_provisioning`)
- `.planning/ROADMAP.md` SC2 Phase 3 menciona apenas `models.py` (não `schemas.py`)
- `uv run ruff check src/` e `uv run mypy src/` continuam passando
- `bin/generate_code` é idempotente (segunda execução não muda arquivos)
- Gap 1 (mapper) e Gap 3 (ROADMAP) de `03-VERIFICATION.md` fechados — abre caminho para o operador executar o checklist E2E do plano 03-07
</success_criteria>

<output>
After completion, create `.planning/phases/03-estrutura-por-dom-nios-e-autentica-o/03-06-SUMMARY.md` listing commits, tasks completed, and verification results.
</output>
