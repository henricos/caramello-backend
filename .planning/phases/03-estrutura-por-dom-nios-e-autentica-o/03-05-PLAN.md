---
phase: 03-estrutura-por-dom-nios-e-autentica-o
plan: 05
type: execute
wave: 3
depends_on: [03, 04]
files_modified:
  - alembic/env.py
  - src/caramello/main.py
  - pyproject.toml
  - src/caramello/user/__init__.py
  - src/caramello/user/models.py
  - src/caramello/user/router.py
  - src/caramello/user/operations.py
  - src/caramello/family/__init__.py
  - src/caramello/family/models.py
  - src/caramello/family/router.py
  - src/caramello/models/
  - src/caramello/api/
  - src/caramello/repositories/
  - src/caramello/services/
  - tests/generated/
autonomous: false
requirements: [STRUCT-01, STRUCT-02, AUTH-01, AUTH-02, AUTH-03, USER-01]

must_haves:
  truths:
    - "src/caramello/user/ contém models.py, router.py, operations.py"
    - "src/caramello/family/ contém models.py e router.py"
    - "src/caramello/models/ não existe mais"
    - "src/caramello/api/ não existe mais"
    - "tests/generated/ não existe mais"
    - "alembic/env.py importa de caramello.user.models e caramello.family.models"
    - "main.py registra lifespan que chama fetch_jwks no startup"
    - "main.py inclui routers de user.router, user.operations e family.router"
    - "user/operations.py tem anotação `# CARAMELLO-GENERATED: implemented` no topo"
    - "user/operations.py GET /user/me retorna current_user (não NotImplementedError)"
    - "pyproject.toml [tool.ruff] e [tool.mypy] removem exclude para paths antigos"
    - "ruff check src/ e mypy src/ passam sem erros"
    - "alembic upgrade head conclui sem erro em banco familia_dev"
  artifacts:
    - path: "src/caramello/user/models.py"
      provides: "Classes User, UserRead, UserCreate, UserUpdate"
      contains: "class User(SQLModel, table=True):"
    - path: "src/caramello/user/router.py"
      provides: "Router CRUD de User com Depends(get_current_user)"
      contains: "Depends(get_current_user)"
    - path: "src/caramello/user/operations.py"
      provides: "GET /user/me implementado"
      contains: "# CARAMELLO-GENERATED: implemented"
    - path: "src/caramello/family/models.py"
      provides: "Classes Family, FamilyMember, FamilyInvitation + Read/Create/Update"
      contains: "class FamilyMember"
    - path: "src/caramello/family/router.py"
      provides: "Routers CRUD de Family e FamilyInvitation com auth"
      contains: "Depends(get_current_user)"
    - path: "src/caramello/main.py"
      provides: "App FastAPI com lifespan + routers dos domínios"
      contains: "lifespan"
    - path: "alembic/env.py"
      provides: "Imports atualizados para caramello.user.models / caramello.family.models"
      contains: "from caramello.user.models"
  key_links:
    - from: "src/caramello/main.py"
      to: "fetch_jwks()"
      via: "lifespan asynccontextmanager"
      pattern: "await fetch_jwks"
    - from: "src/caramello/main.py"
      to: "user/family routers"
      via: "app.include_router"
      pattern: "include_router"
    - from: "src/caramello/user/router.py"
      to: "shared/auth.get_current_user"
      via: "import + Depends"
      pattern: "Depends\\(get_current_user\\)"
    - from: "src/caramello/user/operations.py"
      to: "get_current_user"
      via: "Depends — endpoint /me retorna o user diretamente"
      pattern: "return current_user"
---

<objective>
Integração final da Phase 3: (1) atualizar `alembic/env.py` para os novos paths ANTES de remover diretórios antigos, (2) rodar `bin/generate_code` para regenerar TODO o código em `src/caramello/{user,family}/`, (3) implementar o stub `/user/me` (mudar anotação para `implemented`, remover `NotImplementedError`), (4) atualizar `main.py` com lifespan + novos imports, (5) remover diretórios obsoletos (`models/`, `api/`, `repositories/`, `services/`, `tests/generated/`), (6) limpar `pyproject.toml` removendo exclusões de ruff/mypy obsoletas, (7) checkpoint humano para verificação E2E com Keycloak real.

Purpose: Esta é a wave de wiring que torna todos os artefatos das waves anteriores funcionais. Sem ela, a Phase 3 está incompleta — temos generator pronto mas código não regenerado, auth pronta mas não integrada ao FastAPI, e diretórios antigos coexistindo com os novos.

Output: Codebase reorganizada por domínio, app FastAPI bootando com lifespan + JWKS fetch, GET /user/me funcional, e migrations Alembic alinhadas com os novos paths. Sucesso desta wave fecha a Phase 3 completa.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-estrutura-por-dom-nios-e-autentica-o/03-CONTEXT.md
@.planning/phases/03-estrutura-por-dom-nios-e-autentica-o/03-RESEARCH.md
@.planning/phases/03-estrutura-por-dom-nios-e-autentica-o/03-PATTERNS.md
@.planning/phases/03-estrutura-por-dom-nios-e-autentica-o/03-02-SUMMARY.md
@.planning/phases/03-estrutura-por-dom-nios-e-autentica-o/03-03-SUMMARY.md
@.planning/phases/03-estrutura-por-dom-nios-e-autentica-o/03-04-SUMMARY.md

<interfaces>
# Estado esperado ao iniciar este plano:
#  - dsl/entities/*.yaml com campo domain
#  - dsl/operations/user.yaml com operação get_me
#  - scripts/generate_code.py reescrito (Plan 03)
#  - src/caramello/shared/auth.py implementado (Plan 04)
#  - src/caramello/models/ AINDA EXISTE (será removido nesta wave)
#  - src/caramello/api/generated/ AINDA EXISTE (será removido)
#  - main.py importa de caramello.api.generated.*

# Estado alvo ao final deste plano:
#  - src/caramello/user/{__init__.py, models.py, router.py, operations.py}
#  - src/caramello/family/{__init__.py, models.py, router.py}
#  - Sem src/caramello/models/, src/caramello/api/, src/caramello/repositories/, src/caramello/services/, tests/generated/
#  - alembic/env.py importa de caramello.{user,family}.models
#  - main.py com lifespan + fetch_jwks + include_router dos domínios
#  - pyproject.toml sem exclude para paths obsoletos
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Atualizar alembic/env.py para importar dos novos paths (executar ANTES de regenerar)</name>
  <files>alembic/env.py</files>
  <read_first>
    - alembic/env.py (estado atual: linha 22 `from caramello.models import *`)
    - .planning/phases/03-estrutura-por-dom-nios-e-autentica-o/03-RESEARCH.md (Pitfall 2)
    - .planning/phases/03-estrutura-por-dom-nios-e-autentica-o/03-PATTERNS.md (seção alembic/env.py)
  </read_first>
  <action>
    Editar `alembic/env.py`. Substituir a linha 22:

    Antes:
    ```python
    from caramello.models import *  # noqa: E402, F403 # Import all models for autogenerate
    ```

    Depois:
    ```python
    from caramello.user.models import User  # noqa: E402, F401
    from caramello.family.models import (  # noqa: E402, F401
        Family,
        FamilyMember,
        FamilyInvitation,
    )
    ```

    **Importante**: este passo precede a regeneração (Task 3) porque alembic/env.py é importado por qualquer comando alembic. Se rodarmos a regeneração antes de atualizar env.py, qualquer chamada alembic durante a regeneração falha.

    Mas há um problema: nesta task, os módulos `caramello.user.models` e `caramello.family.models` AINDA NÃO EXISTEM. Eles serão criados na Task 3 (via generate_code). Portanto, esta task deve ser ESTRITAMENTE textual — editar o arquivo, NÃO executar alembic. A verificação de import só roda após a Task 3.

    Para detectar regressões durante o ciclo: a Task 4 (Verificação alembic) confirma que `alembic check` funciona após os imports virarem reais.

    Nenhuma outra modificação em alembic/env.py. Manter exatamente:
    - `from sqlmodel import SQLModel  # noqa: E402`
    - `from caramello.core.config import settings  # noqa: E402`
    - `target_metadata = SQLModel.metadata`
    - Toda a lógica de `run_migrations_offline`, `do_run_migrations`, `run_async_migrations`, `run_migrations_online`
  </action>
  <verify>
    <automated>cd /home/claude/work/caramello-api &amp;&amp; grep -c "from caramello.user.models import User" alembic/env.py &amp;&amp; grep -c "from caramello.family.models import" alembic/env.py &amp;&amp; ! grep "from caramello.models import \*" alembic/env.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "from caramello.user.models import User" alembic/env.py` retorna 1
    - `grep -c "from caramello.family.models import" alembic/env.py` retorna 1
    - `grep -c "Family," alembic/env.py` retorna 1
    - `grep -c "FamilyMember," alembic/env.py` retorna 1
    - `grep -c "FamilyInvitation" alembic/env.py` retorna 1
    - `grep -c "from caramello.models import" alembic/env.py` retorna 0 (linha antiga removida)
    - Resto do arquivo intacto: `grep -c "target_metadata = SQLModel.metadata" alembic/env.py` retorna 1
    - `grep -c "async def run_async_migrations" alembic/env.py` retorna 1
  </acceptance_criteria>
  <done>
    alembic/env.py edita textualmente os imports para os novos paths. Validação funcional fica para a Task 4.
  </done>
</task>

<task type="auto">
  <name>Task 2: Limpar pyproject.toml — remover exclude obsoletos antes de regenerar</name>
  <files>pyproject.toml</files>
  <read_first>
    - pyproject.toml (estado: linhas 50-54 [tool.ruff] exclude; linhas 67-71 [tool.mypy] exclude)
    - .planning/phases/03-estrutura-por-dom-nios-e-autentica-o/03-RESEARCH.md (seção "Atualizar Exclusões de Ruff/Mypy no pyproject.toml")
  </read_first>
  <action>
    Editar `pyproject.toml` para remover as exclusões dos paths que serão deletados na Task 5.

    No bloco `[tool.ruff]` (linhas 46-54 atuais), substituir:
    ```toml
    exclude = [
        "src/caramello/api/generated",
        "src/caramello/models",
        "src/caramello/schemas/generated",
    ]
    ```
    por:
    ```toml
    exclude = []
    ```

    No bloco `[tool.mypy]` (linhas 60-71 atuais), substituir:
    ```toml
    exclude = [
        "src/caramello/api/generated/.*",
        "src/caramello/models/.*",
        "src/caramello/schemas/generated/.*",
    ]
    ```
    por:
    ```toml
    exclude = []
    ```

    Não modificar outras chaves do pyproject (target-version, line-length, select, ignore, strict, etc.).

    **Importante**: Nesta task, ruff/mypy podem falhar se rodados imediatamente porque os paths antigos ainda existem com código violator. A verificação completa de ruff/mypy fica na Task 6 (após os paths antigos serem removidos). Esta task apenas edita o config.
  </action>
  <verify>
    <automated>cd /home/claude/work/caramello-api &amp;&amp; uv run python -c "
import tomllib
from pathlib import Path
data = tomllib.loads(Path('pyproject.toml').read_text())
ruff_exclude = data['tool']['ruff'].get('exclude', [])
mypy_exclude = data['tool']['mypy'].get('exclude', [])
assert 'src/caramello/api/generated' not in ruff_exclude, f'ruff exclude ainda tem api/generated: {ruff_exclude}'
assert 'src/caramello/models' not in ruff_exclude, f'ruff exclude ainda tem models: {ruff_exclude}'
assert 'src/caramello/api/generated/.*' not in mypy_exclude, f'mypy exclude ainda tem api/generated/.*: {mypy_exclude}'
assert 'src/caramello/models/.*' not in mypy_exclude, f'mypy exclude ainda tem models/.*: {mypy_exclude}'
print('OK')
"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "src/caramello/api/generated" pyproject.toml` retorna 0
    - `grep -c "src/caramello/models" pyproject.toml` retorna 0
    - `grep -c "src/caramello/schemas/generated" pyproject.toml` retorna 0
    - O bloco `[tool.ruff]` ainda existe com `target-version`, `line-length`, `src`
    - O bloco `[tool.mypy]` ainda existe com `python_version`, `disallow_untyped_defs`
    - O verify python imprime "OK"
  </acceptance_criteria>
  <done>
    pyproject.toml não exclui mais nenhum path para ruff ou mypy. Validação funcional na Task 6.
  </done>
</task>

<task type="auto">
  <name>Task 3: Rodar bin/generate_code para regenerar código nos novos diretórios</name>
  <files>src/caramello/user/__init__.py, src/caramello/user/models.py, src/caramello/user/router.py, src/caramello/user/operations.py, src/caramello/family/__init__.py, src/caramello/family/models.py, src/caramello/family/router.py</files>
  <read_first>
    - scripts/generate_code.py (Plan 03 entregou — confirmar que está atualizado)
    - bin/generate_code (script wrapper)
    - dsl/entities/user.yaml (com domain: user)
    - dsl/entities/family.yaml (com domain: family)
    - dsl/operations/user.yaml (com operação get_me)
  </read_first>
  <action>
    1. Verificar que pré-requisitos estão satisfeitos:
       ```bash
       grep -q "def generate_operations" scripts/generate_code.py || echo "FAIL: Plan 03 generator não atualizado"
       grep -q "from caramello.shared.auth import get_current_user" scripts/generate_code.py || echo "FAIL: Plan 03 generator não emite auth"
       grep -q "^domain: user" dsl/entities/user.yaml || echo "FAIL: Plan 02 não rodou"
       test -f dsl/operations/user.yaml || echo "FAIL: Plan 02 não rodou"
       ```

       Se algum FAIL aparecer, parar e investigar — não rodar o generator com pré-requisitos quebrados.

    2. Executar `bin/generate_code` na raiz do repo. Isso produz:
       - `src/caramello/user/__init__.py` (vazio)
       - `src/caramello/user/models.py` (classe User + UserRead + UserCreate + UserUpdate)
       - `src/caramello/user/router.py` (CRUD com Depends(get_current_user))
       - `src/caramello/user/operations.py` (stub de get_me com `# CARAMELLO-GENERATED: stub`)
       - `src/caramello/family/__init__.py` (vazio)
       - `src/caramello/family/models.py` (Family + FamilyMember + FamilyInvitation + Read/Create/Update; com `from caramello.user.models import User`)
       - `src/caramello/family/router.py` (CRUD de Family + FamilyInvitation com auth)

       Comando exato:
       ```bash
       bin/generate_code
       ```

    3. Validar que os arquivos foram criados:
       ```bash
       ls -la src/caramello/user/ src/caramello/family/
       ```

    4. Validar imports cross-domain:
       ```bash
       grep "from caramello.user.models import User" src/caramello/family/models.py
       ```
       Deve retornar uma linha (FamilyMember tem FK para User).

    5. Validar código gerado é ruff-compliant:
       ```bash
       uv run ruff check src/caramello/user/ src/caramello/family/
       ```
       Deve passar sem erros.

    6. Validar imports estáticos funcionam:
       ```bash
       uv run python -c "
       from caramello.user.models import User, UserRead, UserCreate, UserUpdate
       from caramello.family.models import Family, FamilyMember, FamilyInvitation
       from caramello.user import router as user_router
       from caramello.user import operations as user_operations
       from caramello.family import router as family_router
       print('OK')
       "
       ```

    Notas:
    - Se `bin/generate_code` falhar com erro, NÃO commitar nada — investigar primeiro o output do script. Possíveis causas: domain field ausente em algum YAML, ou bug no generator reescrito em Plan 03.
    - O arquivo `operations.py` será criado com anotação `# CARAMELLO-GENERATED: stub` e corpo `raise NotImplementedError`. A Task 5 muda isso para `implemented` + retorno do `current_user`.
  </action>
  <verify>
    <automated>cd /home/claude/work/caramello-api &amp;&amp; bin/generate_code &amp;&amp; test -f src/caramello/user/models.py &amp;&amp; test -f src/caramello/user/router.py &amp;&amp; test -f src/caramello/user/operations.py &amp;&amp; test -f src/caramello/family/models.py &amp;&amp; test -f src/caramello/family/router.py &amp;&amp; grep -q "class User" src/caramello/user/models.py &amp;&amp; grep -q "class FamilyMember" src/caramello/family/models.py &amp;&amp; grep -q "from caramello.user.models import User" src/caramello/family/models.py &amp;&amp; grep -q "Depends(get_current_user)" src/caramello/user/router.py &amp;&amp; head -1 src/caramello/user/operations.py | grep -q "CARAMELLO-GENERATED" &amp;&amp; uv run ruff check src/caramello/user/ src/caramello/family/ &amp;&amp; uv run python -c "from caramello.user.models import User; from caramello.family.models import Family, FamilyMember, FamilyInvitation; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `test -d src/caramello/user` exit 0
    - `test -d src/caramello/family` exit 0
    - `test -f src/caramello/user/models.py` exit 0
    - `test -f src/caramello/user/router.py` exit 0
    - `test -f src/caramello/user/operations.py` exit 0
    - `test -f src/caramello/family/models.py` exit 0
    - `test -f src/caramello/family/router.py` exit 0
    - `grep -q "class User(SQLModel, table=True):" src/caramello/user/models.py` exit 0
    - `grep -q "class Family(SQLModel, table=True):" src/caramello/family/models.py` exit 0
    - `grep -q "class FamilyMember(SQLModel, table=True):" src/caramello/family/models.py` exit 0
    - `grep -q "class FamilyInvitation(SQLModel, table=True):" src/caramello/family/models.py` exit 0
    - `grep -q "from __future__ import annotations" src/caramello/user/models.py` exit 0 (tipos modernos ativados)
    - `grep -c "Optional\[" src/caramello/user/models.py` retorna 0 (sem typing.Optional)
    - `grep -c "from typing import Optional" src/caramello/user/models.py` retorna 0
    - `grep -q "from caramello.shared.auth import get_current_user" src/caramello/user/router.py` exit 0
    - `grep -c "Depends(get_current_user)" src/caramello/user/router.py` retorna >= 5 (5 endpoints CRUD)
    - `grep -q "^# CARAMELLO-GENERATED: stub$" src/caramello/user/operations.py` exit 0 (anotação stub inicialmente)
    - `grep -q "from caramello.user.models import User" src/caramello/family/models.py` exit 0 (import cross-domain correto)
    - `uv run ruff check src/caramello/user/ src/caramello/family/` exit 0
    - `uv run python -c "from caramello.user.models import User; from caramello.family.models import Family, FamilyMember, FamilyInvitation; print('OK')"` exit 0 e imprime OK
    - `uv run pytest tests/test_generator.py -x -q` — testes que estavam xfail agora passam (xpass)
  </acceptance_criteria>
  <done>
    bin/generate_code executou com sucesso; src/caramello/user/ e src/caramello/family/ contêm models, router e (para user) operations stub; código gerado passa ruff; imports funcionam.
  </done>
</task>

<task type="auto">
  <name>Task 4: Validar alembic com novos imports + smoke test mypy</name>
  <files>alembic/env.py, src/caramello/user/models.py, src/caramello/family/models.py (read-only — validação)</files>
  <read_first>
    - alembic/env.py (atualizado na Task 1)
    - src/caramello/user/models.py (criado na Task 3)
    - src/caramello/family/models.py (criado na Task 3)
  </read_first>
  <action>
    Validar que a integração alembic + novos paths está funcional. NÃO mexe em arquivos — apenas executa checks.

    1. Importar models via Python para verificar que SQLModel.metadata é populado:
       ```bash
       uv run python -c "
       from caramello.user.models import User
       from caramello.family.models import Family, FamilyMember, FamilyInvitation
       from sqlmodel import SQLModel
       tables = list(SQLModel.metadata.tables.keys())
       assert 'user' in tables, f'user table not in metadata: {tables}'
       assert 'family' in tables, f'family table not in metadata: {tables}'
       assert 'family_member' in tables
       assert 'family_invitation' in tables
       print('metadata OK:', tables)
       "
       ```

    2. Verificar que alembic env.py carrega sem ImportError:
       ```bash
       uv run python -c "
       import sys
       sys.path.insert(0, 'alembic')
       # Simular a execução do env.py em modo offline (sem precisar de DB real)
       import importlib.util
       spec = importlib.util.spec_from_file_location('alembic_env', 'alembic/env.py')
       # Não executar — apenas carregar imports
       # (executar dispara configuração completa de alembic)
       # Em vez disso, apenas verificar via grep + import dos módulos importados
       from caramello.user.models import User
       from caramello.family.models import Family, FamilyMember, FamilyInvitation
       print('alembic imports OK')
       "
       ```

    3. Smoke test mypy (apenas nos arquivos do projeto que importamos, NÃO no diretório antigo que ainda existe):
       ```bash
       uv run mypy src/caramello/shared/ src/caramello/core/ src/caramello/user/ src/caramello/family/
       ```
       Deve passar. Se falhar, investigar:
       - mypy reclama de tipos em código gerado? → ajustar templates do generator (volta para Plan 03)
       - mypy reclama de auth.py? → ajustar Plan 04

    4. (Opcional, se DB acessível) `alembic check`:
       ```bash
       uv run alembic check 2>&1 | tail -5
       ```
       Se .env tem credenciais válidas, deve reportar "Target database is up to date" ou diferença detectada. Não interromper se DB inacessível — apenas se houver ImportError.

    Notas:
    - Nesta task, ruff geral do `src/` ainda pode falhar porque os diretórios antigos (`src/caramello/models/`, `src/caramello/api/`) ainda existem e o exclude foi removido (Task 2). A Task 5 remove esses diretórios e a Task 6 valida ruff geral.
  </action>
  <verify>
    <automated>cd /home/claude/work/caramello-api &amp;&amp; uv run python -c "
from caramello.user.models import User
from caramello.family.models import Family, FamilyMember, FamilyInvitation
from sqlmodel import SQLModel
tables = list(SQLModel.metadata.tables.keys())
assert 'user' in tables, tables
assert 'family' in tables, tables
assert 'family_member' in tables, tables
assert 'family_invitation' in tables, tables
print('OK', sorted(tables))
" &amp;&amp; uv run mypy src/caramello/shared/ src/caramello/core/ src/caramello/user/ src/caramello/family/</automated>
  </verify>
  <acceptance_criteria>
    - O comando python imprime "OK" com lista de tabelas incluindo user, family, family_member, family_invitation
    - `uv run mypy src/caramello/shared/ src/caramello/core/ src/caramello/user/ src/caramello/family/` exit 0
    - Nenhum ImportError ao importar User, Family, FamilyMember, FamilyInvitation
    - SQLModel.metadata.tables contém as 4 tabelas
  </acceptance_criteria>
  <done>
    Os novos paths estão funcionais: SQLModel metadata populada, imports limpos, mypy passa nos novos diretórios + shared + core.
  </done>
</task>

<task type="auto">
  <name>Task 5: Implementar GET /user/me — mudar anotação para implemented + retornar current_user</name>
  <files>src/caramello/user/operations.py</files>
  <read_first>
    - src/caramello/user/operations.py (gerado pela Task 3 como stub com NotImplementedError)
    - .planning/phases/03-estrutura-por-dom-nios-e-autentica-o/03-RESEARCH.md (Pattern 4 — GET /user/me)
    - .planning/phases/03-estrutura-por-dom-nios-e-autentica-o/03-CONTEXT.md (D-09, D-10)
    - .planning/phases/03-estrutura-por-dom-nios-e-autentica-o/03-PATTERNS.md (seção src/caramello/user/operations.py — versão "Após implementação")
  </read_first>
  <action>
    Editar `src/caramello/user/operations.py` (arquivo gerado pela Task 3 com NotImplementedError) para:

    1. Mudar a primeira linha de `# CARAMELLO-GENERATED: stub` para `# CARAMELLO-GENERATED: implemented` (D-09 + D-10 — protege o arquivo contra sobrescrita futura).

    2. Substituir o corpo da função `get_me` removendo `raise NotImplementedError` e retornando o `current_user` diretamente.

    O arquivo final deve ser EXATAMENTE:

    ```python
    # CARAMELLO-GENERATED: implemented
    from __future__ import annotations

    from fastapi import APIRouter, Depends

    from caramello.shared.auth import get_current_user
    from caramello.user.models import User, UserRead

    router = APIRouter(prefix="/user", tags=["User"])


    @router.get("/me", response_model=UserRead)
    async def get_me(current_user: User = Depends(get_current_user)) -> User:
        """Retorna o perfil do usuário autenticado."""
        return current_user
    ```

    Validar:
    - Primeira linha é exatamente `# CARAMELLO-GENERATED: implemented`
    - Não há `raise NotImplementedError` no corpo
    - `return current_user` está presente
    - ruff/mypy passam neste arquivo
  </action>
  <verify>
    <automated>cd /home/claude/work/caramello-api &amp;&amp; head -1 src/caramello/user/operations.py | grep -q "^# CARAMELLO-GENERATED: implemented$" &amp;&amp; ! grep -q "NotImplementedError" src/caramello/user/operations.py &amp;&amp; grep -q "return current_user" src/caramello/user/operations.py &amp;&amp; uv run ruff check src/caramello/user/operations.py &amp;&amp; uv run mypy src/caramello/user/operations.py</automated>
  </verify>
  <acceptance_criteria>
    - `head -1 src/caramello/user/operations.py` retorna `# CARAMELLO-GENERATED: implemented`
    - `grep -c "NotImplementedError" src/caramello/user/operations.py` retorna 0
    - `grep -c "return current_user" src/caramello/user/operations.py` retorna 1
    - `grep -c "Depends(get_current_user)" src/caramello/user/operations.py` retorna 1
    - `grep -c "response_model=UserRead" src/caramello/user/operations.py` retorna 1
    - `grep -c '@router.get("/me"' src/caramello/user/operations.py` retorna 1
    - `uv run ruff check src/caramello/user/operations.py` exit 0
    - `uv run mypy src/caramello/user/operations.py` exit 0
    - Re-rodar `bin/generate_code` NÃO sobrescreve este arquivo (D-09): `bin/generate_code && head -1 src/caramello/user/operations.py | grep -q implemented`
  </acceptance_criteria>
  <done>
    GET /user/me implementado; anotação muda para implemented; re-execução do generator preserva o arquivo (anotação protege).
  </done>
</task>

<task type="auto">
  <name>Task 6: Remover diretórios obsoletos + atualizar main.py com lifespan e novos imports</name>
  <files>src/caramello/main.py, src/caramello/models/, src/caramello/api/, src/caramello/repositories/, src/caramello/services/, tests/generated/, src/caramello/__init__.py</files>
  <read_first>
    - src/caramello/main.py (estado atual: importa de caramello.api.generated.*)
    - .planning/phases/03-estrutura-por-dom-nios-e-autentica-o/03-PATTERNS.md (seção src/caramello/main.py — Padrão alvo)
    - .planning/phases/03-estrutura-por-dom-nios-e-autentica-o/03-RESEARCH.md (Pattern 3: Lifespan FastAPI)
  </read_first>
  <action>
    **Parte A — Reescrever main.py com lifespan e novos imports:**

    Substituir TODO o conteúdo de `src/caramello/main.py` por:

    ```python
    """Entrypoint da aplicação Caramello.

    - Lifespan: popula cache JWKS via shared.auth.fetch_jwks no startup
    - CORS: configurado para o frontend React/Capacitor
    - Routers: registrados a partir dos domínios user/ e family/
    """
    from __future__ import annotations

    from contextlib import asynccontextmanager
    from typing import AsyncIterator

    from fastapi import FastAPI
    from fastapi.middleware.cors import CORSMiddleware

    from caramello.core.config import settings
    from caramello.family import router as family_router
    from caramello.shared.auth import fetch_jwks
    from caramello.user import operations as user_operations
    from caramello.user import router as user_router


    @asynccontextmanager
    async def lifespan(app: FastAPI) -> AsyncIterator[None]:
        """Popula o cache JWKS na inicialização da app."""
        await fetch_jwks()
        yield


    app = FastAPI(
        title="Caramello Backend",
        description="Backend API for Caramello",
        version="0.1.0",
        lifespan=lifespan,
    )

    app.add_middleware(
        CORSMiddleware,
        allow_origins=settings.CORS_ORIGINS,
        allow_credentials=True,
        allow_methods=["*"],
        allow_headers=["*"],
    )

    # Routers por domínio
    app.include_router(user_router.router)
    app.include_router(user_operations.router)
    app.include_router(family_router.router)


    @app.get("/")
    def root() -> dict[str, str]:
        return {"message": "Welcome to Caramello API"}
    ```

    **Parte B — Verificar que main.py importa sem erro:**
    ```bash
    uv run python -c "import caramello.main; print('main imports OK')"
    ```
    Esse comando NÃO chama o lifespan (não há server rodando), só verifica que os imports estáticos resolvem.

    **Parte C — Remover diretórios obsoletos:**

    Executar:
    ```bash
    rm -rf src/caramello/models
    rm -rf src/caramello/api
    rm -rf src/caramello/repositories
    rm -rf src/caramello/services
    rm -rf tests/generated
    find . -type d -name __pycache__ -exec rm -rf {} + 2>/dev/null || true
    ```

    Justificativa de cada remoção:
    - `src/caramello/models/` — substituído por `caramello.user.models` e `caramello.family.models`
    - `src/caramello/api/` — substituído por `caramello.user.router` e `caramello.family.router`
    - `src/caramello/repositories/` — stub vazio (apenas `.gitkeep` ou `__init__.py` vazio), nunca foi usado
    - `src/caramello/services/` — idem
    - `tests/generated/` — testes auto-gerados não são mais produzidos pelo generator (Plan 03)
    - `__pycache__/` — invalidar caches stale após renomeação

    **Parte D — Verificar que `src/caramello/__init__.py` ainda existe** (não remover; é o pacote raiz):
    ```bash
    test -f src/caramello/__init__.py
    ```

    **Parte E — Validações finais:**

    ```bash
    # 1. Imports
    uv run python -c "import caramello.main; print('OK')"

    # 2. Ruff geral
    uv run ruff check src/

    # 3. Mypy geral
    uv run mypy src/

    # 4. Pytest dos testes que JÁ passavam + os novos
    uv run pytest tests/test_generator.py tests/test_auth.py tests/test_user_operations.py -x -q
    ```

    Se ruff ou mypy falharem aqui após remoção, investigar — pode haver código gerado ainda violando regras. Possíveis ajustes:
    - Ruff `F401` em imports cross-domain de family/models.py? Adicionar `# noqa: F401` ou usar `__all__`.
    - Mypy `name-defined` em forward refs? Adicionar `if TYPE_CHECKING` apropriados.

    Esses ajustes podem demandar voltar e ajustar o generator (Plan 03). Se for o caso, abrir gap para o orchestrator.
  </action>
  <verify>
    <automated>cd /home/claude/work/caramello-api &amp;&amp; ! test -d src/caramello/models &amp;&amp; ! test -d src/caramello/api &amp;&amp; ! test -d src/caramello/repositories &amp;&amp; ! test -d src/caramello/services &amp;&amp; ! test -d tests/generated &amp;&amp; test -f src/caramello/__init__.py &amp;&amp; uv run python -c "import caramello.main; print('OK')" &amp;&amp; uv run ruff check src/ &amp;&amp; uv run mypy src/ &amp;&amp; uv run pytest tests/test_generator.py tests/test_auth.py tests/test_user_operations.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `test -d src/caramello/models` exit != 0 (NÃO existe mais)
    - `test -d src/caramello/api` exit != 0
    - `test -d src/caramello/repositories` exit != 0
    - `test -d src/caramello/services` exit != 0
    - `test -d tests/generated` exit != 0
    - `test -f src/caramello/__init__.py` exit 0 (pacote raiz preservado)
    - `grep -c "from caramello.api.generated" src/caramello/main.py` retorna 0 (sem import antigo)
    - `grep -c "@asynccontextmanager" src/caramello/main.py` retorna 1
    - `grep -c "await fetch_jwks" src/caramello/main.py` retorna 1
    - `grep -c "from caramello.user import router" src/caramello/main.py` retorna 1
    - `grep -c "from caramello.user import operations" src/caramello/main.py` retorna 1
    - `grep -c "from caramello.family import router" src/caramello/main.py` retorna 1
    - `app.include_router(user_operations.router)` aparece no main (verifica via grep)
    - `uv run python -c "import caramello.main"` exit 0
    - `uv run ruff check src/` exit 0 (sem exclusões! todo o código passa)
    - `uv run mypy src/` exit 0
    - `grep -r "from caramello.models" src/` retorna 0 linhas (truth do success criteria do roadmap)
    - `grep -r "from caramello.api" src/` retorna 0 linhas
    - `uv run pytest tests/test_generator.py tests/test_auth.py tests/test_user_operations.py -x -q` — pelo menos os testes não-integration passam ou ficam em xpass
  </acceptance_criteria>
  <done>
    main.py com lifespan + routers de domínios; diretórios obsoletos removidos; ruff e mypy passam em `src/` inteiro; nenhuma referência aos paths antigos.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 7: Verificação humana E2E — boot da app, alembic, GET /user/me com token real</name>
  <files>(nenhum arquivo — apenas validação humana com Keycloak e banco reais)</files>
  <what-built>
    A Phase 3 inteira está implementada:
    - Camada de auth Keycloak em `shared/auth.py` (JWT RS256, JWKS cache, JIT provisioning)
    - DSL generator com suporte a `domain` + `dsl/operations/{domain}.yaml`
    - Código reorganizado em `src/caramello/user/` e `src/caramello/family/`
    - Diretórios `models/`, `api/`, `repositories/`, `services/`, `tests/generated/` removidos
    - `GET /user/me` implementado retornando o usuário autenticado
    - `main.py` com lifespan + CORS + 3 routers
    - alembic/env.py importa dos novos paths
    - ruff e mypy passam em `src/` completo

    Os automated checks confirmaram tudo isso. Esta verificação humana valida:
    1. Boot da app funciona end-to-end com Keycloak real (impossível mockar)
    2. JWKS fetch no lifespan não trava nem timeouta
    3. `GET /user/me` com Bearer token Keycloak válido retorna 200 com id/email/name
    4. JIT provisioning cria registro na tabela `users` (verificável no banco)
    5. Decisão sobre claim `aud` (D-02): confirmar valor real do token

    O AI agent NÃO consegue executar isso porque:
    - Não tem acesso ao Keycloak real (infra existente)
    - Não tem token JWT válido emitido por humano logado
    - Não tem banco PostgreSQL com .env configurado nesta máquina (apenas no host do operador)
  </what-built>
  <action>
    Pausar a execução automática e apresentar ao operador as instruções de verificação descritas em `<how-to-verify>`. Não modificar arquivos do repo — exceto se, no Passo 7, o operador instruir a alterar `shared/auth.py` para ativar `audience=settings.KEYCLOAK_CLIENT_ID` (commit separado).
  </action>
  <how-to-verify>
    Execute todas as etapas abaixo na sua máquina local. Reporte cada resultado.

    **Passo 1 — `.env` configurado:**
    ```bash
    grep -E "^KEYCLOAK_(URL|REALM|CLIENT_ID)=" .env
    ```
    Deve mostrar três linhas com valores reais (não vazios). Se algum estiver vazio, preencher com:
    - `KEYCLOAK_REALM=caramello` (fixo, D-01)
    - `KEYCLOAK_URL=<URL da sua infra>`
    - `KEYCLOAK_CLIENT_ID=<client de dev>`

    **Passo 2 — Banco preparado:**
    ```bash
    bin/manage_db reset    # CUIDADO: destrutivo, recria familia_dev do zero
    bin/manage_db upgrade  # aplica migrations
    ```
    Esperado: sem erros. Se falhar, reportar o output completo.

    **Passo 3 — Boot da app:**
    ```bash
    uv run uvicorn caramello.main:app --reload
    ```
    Esperado:
    - Mensagens normais do uvicorn
    - Sem traceback de erro
    - Sem timeout no JWKS fetch (deve completar em < 10s)
    - App escuta em http://localhost:8000

    Se aparecer `httpx.ConnectError` ou similar: o Keycloak não está acessível — confirmar URL e rede.

    **Passo 4 — Endpoint público funciona:**
    Em outro terminal:
    ```bash
    curl -s http://localhost:8000/
    ```
    Esperado: `{"message":"Welcome to Caramello API"}`

    **Passo 5 — /user/me sem token retorna 401/403 (AUTH-01):**
    ```bash
    curl -s -o /dev/null -w "%{http_code}\n" http://localhost:8000/user/me
    ```
    Esperado: `401` ou `403`. Qualquer outro código = falha.

    **Passo 6 — Obter Bearer token Keycloak real:**
    Use o método que você preferir — login via Keycloak admin/account ou o frontend dev. Salve o token em uma variável:
    ```bash
    export TOKEN="<seu-token-aqui>"
    ```

    **Passo 7 — Inspecionar claim `aud` do token (D-02):**
    ```bash
    echo "$TOKEN" | cut -d. -f2 | base64 -d 2>/dev/null | python -m json.tool | grep -E "aud|sub|email|name|preferred_username"
    ```
    Anote o valor de `aud` (pode ser string, array, ou client_id). Decisão a tomar:
    - Se `aud` contém `settings.KEYCLOAK_CLIENT_ID` → editar `shared/auth.py` para `options={"verify_aud": False}` → `audience=settings.KEYCLOAK_CLIENT_ID` (sem options). Commit separado: `feat(auth): valida claim aud contra KEYCLOAK_CLIENT_ID`.
    - Se `aud=["account"]` ou outro valor padrão Keycloak → manter `verify_aud=False`. Documentar a decisão em STATE.md.

    **Passo 8 — GET /user/me com token válido (AUTH-02, USER-01):**
    ```bash
    curl -s -H "Authorization: Bearer $TOKEN" http://localhost:8000/user/me | python -m json.tool
    ```
    Esperado: JSON com `uuid`, `email`, `name`, `idp_sub`, `created_at`, `updated_at`. Status 200.

    Se retornar 401/403: o token é inválido ou expirou — gerar novo.
    Se retornar 500: investigar logs do uvicorn. Pode ser JIT provisioning falhando.

    **Passo 9 — JIT provisioning criou o registro:**
    ```bash
    psql -h $DB_HOST -U $DB_USER -d $DB_NAME -c "SELECT id, idp_sub, email, name FROM \"user\";"
    ```
    Esperado: 1 linha com seus dados (`idp_sub` = claim `sub` do token).

    **Passo 10 — Segunda chamada não duplica (idempotência):**
    Rodar o curl do Passo 8 outra vez. Confirmar que ainda há apenas 1 linha em `user` (não duplicou).

    **Passo 11 — CRUD genérico exige auth (D-11):**
    ```bash
    curl -s -o /dev/null -w "%{http_code}\n" http://localhost:8000/user/
    ```
    Esperado: 401/403 (sem token). Confirma que TODOS os endpoints exigem auth.

    **Passo 12 — CRUD com token funciona:**
    ```bash
    curl -s -H "Authorization: Bearer $TOKEN" http://localhost:8000/user/ | head -c 200
    ```
    Esperado: lista JSON com pelo menos o seu user.
  </how-to-verify>
  <verify>
    <automated>echo "Manual verification — operador executa os 12 passos descritos em <how-to-verify> e reporta resultado via <resume-signal>"</automated>
  </verify>
  <done>
    Operador respondeu `<resume-signal>` com "aprovado" ou "aprovado com ressalva: aud=<valor>". Se respondeu "falhou no passo X", a fase NÃO está fechada — abrir ciclo gap-closure.
  </done>
  <resume-signal>
    Reporte uma das opções:
    - "aprovado" — tudo funcionou conforme esperado
    - "aprovado com ressalva: aud=<valor>" — funcionou; anote o claim aud para decidir se ativa validação
    - "falhou no passo X: <descrição do erro>" — pare aqui; abrir ciclo de gap-closure
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Cliente HTTP → /user/me | Bearer token externo; validação por get_current_user |
| API → Keycloak | HTTPS para JWKS no startup; falha = app não sobe |
| API → PostgreSQL | JIT provisioning faz INSERT; bug aqui = race conditions ou tabela corrompida |
| Generator → src/ | Reescreve arquivos versionados; anotação CARAMELLO-GENERATED protege /user/me |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-10 | DoS | JWKS fetch no lifespan timeout | mitigate | `httpx.AsyncClient(timeout=10.0)` em shared/auth.py (Plan 04). Se falhar, app NÃO sobe — fail-fast. |
| T-3-11 | Tampering | Diretórios antigos coexistindo com novos | mitigate | Task 6 remove explicitamente todos os diretórios obsoletos; verify confirma ausência. |
| T-3-12 | Elevation of Privilege | /user/me sem `Depends(get_current_user)` por engano de geração | mitigate | Operations.py (Task 5) explicitamente declara `Depends(get_current_user)`; criteria valida via grep. |
| T-3-13 | Tampering | bin/generate_code sobrescreve /user/me implementado | mitigate | Anotação `# CARAMELLO-GENERATED: implemented` na Task 5; acceptance criteria re-executa generator e confirma que o arquivo continua intacto. |
| T-3-14 | Spoofing | aud claim aceito sem validar | accept (temporário) | D-02 + Task 7 Passo 7 — operador inspeciona token real e decide. Risco aceitável durante Phase 3; será fechado quando operador reportar `aud` no resume-signal. |
| T-3-15 | Information Disclosure | alembic/env.py importa models — pode vazar schema interno | accept | env.py roda apenas em dev/migrate, não em runtime de API. |
</threat_model>

<verification>
- Task 1: `grep -c "from caramello.user.models import User" alembic/env.py` retorna 1; sem `from caramello.models`
- Task 2: `grep -c "src/caramello/api/generated" pyproject.toml` retorna 0
- Task 3: bin/generate_code criou 7 arquivos em src/caramello/{user,family}/; ruff passa
- Task 4: mypy passa em src/caramello/{shared,core,user,family}/
- Task 5: head -1 user/operations.py == `# CARAMELLO-GENERATED: implemented`; re-rodar generator não sobrescreve
- Task 6: paths obsoletos removidos; ruff/mypy passam em src/ inteiro; main.py importa de user/family
- Task 7 (checkpoint): operador reporta aprovação; aud claim conhecido
</verification>

<success_criteria>
Phase 3 fechada:

1. **STRUCT-01:** `grep -r "from caramello.models" src/` retorna 0; `grep -r "from caramello.api" src/` retorna 0; `find src/caramello -maxdepth 1 -type d` lista apenas `core`, `shared`, `user`, `family` (+ pacote raiz)
2. **STRUCT-02:** `bin/generate_code` produz código em `src/caramello/{user,family}/` a partir do campo `domain` nos YAMLs
3. **AUTH-01:** `curl http://localhost:8000/user/me` retorna 401/403; `curl http://localhost:8000/user/` retorna 401/403
4. **AUTH-02:** Primeira chamada com Bearer token Keycloak válido cria registro em `user`; segunda chamada não duplica
5. **AUTH-03:** `from caramello.shared.auth import get_current_user` funciona; nenhum outro módulo tem lógica de validação JWT
6. **USER-01:** `GET /user/me` com Bearer token válido retorna 200 com `uuid`, `email`, `name`, `idp_sub`
7. ruff check src/ passa sem exclusões; mypy src/ passa
8. Operador reportou checkpoint aprovado no Task 7
</success_criteria>

<output>
Após completion, criar `.planning/phases/03-estrutura-por-dom-nios-e-autentica-o/03-05-SUMMARY.md` documentando:
- Resultado do checkpoint humano (especialmente: valor do claim `aud`)
- Status final de cada requirement (STRUCT-01, STRUCT-02, AUTH-01, AUTH-02, AUTH-03, USER-01)
- Atualizações em STATE.md e ROADMAP.md marcando Phase 3 como completa
</output>
