---
phase: 03-estrutura-por-dom-nios-e-autentica-o
plan: "07"
type: execute
wave: 2
gap_closure: true
depends_on: ["06"]
files_modified:
  - scripts/smoke_e2e.py
  - .planning/phases/03-estrutura-por-dom-nios-e-autentica-o/03-07-EVIDENCE.md
autonomous: false
requirements: [AUTH-01, AUTH-02, AUTH-03, USER-01]
user_setup:
  - service: keycloak
    why: "Validação JWT no lifespan + decode de tokens reais (AUTH-01, AUTH-03)"
    env_vars:
      - name: KEYCLOAK_URL
        source: "URL base da instância Keycloak da infra existente (ex: https://kc.caramello.cloud)"
      - name: KEYCLOAK_REALM
        source: "Realm Keycloak (conforme D-01: caramello)"
      - name: KEYCLOAK_CLIENT_ID
        source: "Client ID do app no realm caramello (conferir no Keycloak Admin Console)"
    dashboard_config:
      - task: "Emitir token de acesso para usuário de teste"
        location: "Keycloak Admin Console -> realm caramello -> Users -> Impersonate, OU usar fluxo de password grant via curl/Postman para obter access_token"
  - service: postgresql
    why: "JIT provisioning escreve em tabela `user` real (AUTH-02, USER-01)"
    env_vars:
      - name: DB_HOST
        source: "Host do PostgreSQL (ex: localhost ou hostname interno)"
      - name: DB_PORT
        source: "Porta (padrão: 5432)"
      - name: DB_USER
        source: "Usuário PostgreSQL com permissão de CREATE/INSERT/SELECT em familia_dev"
      - name: DB_PASSWORD
        source: "Senha do DB_USER"
      - name: DB_NAME
        source: "familia_dev (conforme MODEL-03)"
    dashboard_config:
      - task: "Criar banco familia_dev e aplicar migrations"
        location: "Usar `bin/setup_db` para criar usuário/banco, depois `bin/manage_db upgrade` para aplicar `alembic upgrade head`"

must_haves:
  truths:
    - "App boota sem traceback com Keycloak real configurado — `uv run uvicorn caramello.main:app --reload` arranca limpo"
    - "JWKS fetch no lifespan completa em < 10s e popula _jwks_cache com pelo menos uma chave"
    - "GET /user/me sem token retorna 401 ou 403 (AUTH-01)"
    - "GET /user/me com Bearer token Keycloak válido retorna HTTP 200 com JSON contendo uuid, email, name, idp_sub (USER-01)"
    - "Primeira chamada com token válido para novo usuário cria registro na tabela `user` (AUTH-02)"
    - "Segunda chamada com mesmo token NÃO duplica o registro — idempotência confirmada via SELECT COUNT(*)"
    - "alembic upgrade head em familia_dev cria tabelas user, family, family_member, family_invitation sem erro"
    - "Operador registra evidências em 03-07-EVIDENCE.md (saída de comandos, respostas HTTP, queries SQL)"
  artifacts:
    - path: "scripts/smoke_e2e.py"
      provides: "Script de smoke test automatizável quando .env está configurado"
      exports: "Executável via `uv run python scripts/smoke_e2e.py`"
    - path: ".planning/phases/03-estrutura-por-dom-nios-e-autentica-o/03-07-EVIDENCE.md"
      provides: "Arquivo de evidências preenchido pelo operador com saídas reais dos comandos"
      contains: "GET /user/me"
  key_links:
    - from: "Operador (humano)"
      to: ".env configurado"
      via: "preencher KEYCLOAK_URL, KEYCLOAK_REALM, KEYCLOAK_CLIENT_ID, DB_*"
      pattern: "Settings carrega sem erro de variável obrigatória"
    - from: "uvicorn lifespan"
      to: "_jwks_cache populado"
      via: "fetch_jwks() via httpx.AsyncClient"
      pattern: "len(_jwks_cache) >= 1 após boot"
    - from: "Bearer token Keycloak"
      to: "INSERT INTO user (idp_sub, email, name)"
      via: "get_current_user -> pg_insert ON CONFLICT DO NOTHING -> SELECT"
      pattern: "SELECT COUNT(*) FROM user WHERE idp_sub = '<sub>' retorna 1 após duas chamadas"
---

<objective>
Executar a verificação end-to-end (E2E) que foi DIFERIDA na Task 7 do plano 03-05, fechando o Gap 2 de `03-VERIFICATION.md`. Esta verificação requer infraestrutura externa (Keycloak real + PostgreSQL `familia_dev`) que não está disponível no ambiente de execução do agente — por isso este plano é `autonomous: false` e exige que o **operador** execute manualmente o checklist passo-a-passo, registrando evidências em um arquivo `03-07-EVIDENCE.md`.

Purpose: Sem esta execução, os critérios de sucesso SC3 e SC5 do ROADMAP (Phase 3) não têm evidência real — apenas verificação de código. Os requisitos AUTH-01, AUTH-02, AUTH-03 e USER-01 dependem deste teste para sair do estado "BLOCKED" no relatório de verificação.

Output:
- `scripts/smoke_e2e.py` — script Python que automatiza a sequência de verificações quando .env está configurado, para o operador rodar de uma vez (substitui múltiplos curls manuais).
- `.planning/phases/03-estrutura-por-dom-nios-e-autentica-o/03-07-EVIDENCE.md` — arquivo de evidências preenchido pelo operador com saídas reais dos comandos.
- Pré-requisito do plano 06 (mapper bug corrigido) consumido — se 06 não estiver mergeado, este plano não pode rodar.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-estrutura-por-dom-nios-e-autentica-o/03-CONTEXT.md
@.planning/phases/03-estrutura-por-dom-nios-e-autentica-o/03-VERIFICATION.md
@.planning/phases/03-estrutura-por-dom-nios-e-autentica-o/03-RESEARCH.md
@.planning/phases/03-estrutura-por-dom-nios-e-autentica-o/03-05-SUMMARY.md
@.env.example
@bin/setup_db
@bin/manage_db

<interfaces>
Pré-condições antes de iniciar este plano:
- Plano 03-06 mergeado e seus testes passando (mapper bug corrigido)
- `.env` preenchido com valores reais de Keycloak e PostgreSQL (NÃO commitar — é gitignored)
- Acesso ao Keycloak da infra existente: realm `caramello`, capacidade de obter access_token
- PostgreSQL acessível com permissão de CREATE DATABASE e CREATE USER (ou banco/usuário já criados)

Estado atual da app:
- `src/caramello/main.py` tem `@asynccontextmanager lifespan` que chama `await fetch_jwks()` no startup
- `src/caramello/shared/auth.py` implementa `get_current_user` com fluxo: decode JWT (RS256) -> extrair `sub`/`email`/`name` -> `pg_insert(User).on_conflict_do_nothing(index_elements=['idp_sub'])` -> `SELECT User WHERE idp_sub`
- `src/caramello/user/operations.py` linha 1 é `# CARAMELLO-GENERATED: implemented`, expõe `GET /user/me` que retorna `current_user`
- `src/caramello/user/router.py` tem 5 endpoints CRUD com `Depends(get_current_user)`
- `src/caramello/family/router.py` tem 10 endpoints CRUD com `Depends(get_current_user)` (após 03-06)
- `alembic/env.py` importa de `caramello.user.models` e `caramello.family.models`

`bin/setup_db` (referência): cria role e banco no PostgreSQL a partir de `.env`.
`bin/manage_db upgrade`: roda `alembic upgrade head` com o `.env` carregado.

Variáveis de ambiente esperadas em `.env`:
```
ENVIRONMENT=development
DB_HOST=...
DB_PORT=5432
DB_USER=...
DB_PASSWORD=...
DB_NAME=familia_dev
CORS_ORIGINS=http://localhost:3000,http://localhost:5173
KEYCLOAK_URL=https://kc.caramello.cloud  # ou equivalente da infra
KEYCLOAK_REALM=caramello
KEYCLOAK_CLIENT_ID=<client_id real do app>
```

Observação D-02 do CONTEXT.md: `verify_aud=False` no `shared/auth.py` é intencional até o operador confirmar o claim `aud` real emitido pelo Keycloak. Durante este plano, o operador deve inspecionar um token (via jwt.io ou `python -c "import jwt; print(jwt.decode(token, options={'verify_signature': False}))"`) e DECIDIR:
- Se `aud` contém `KEYCLOAK_CLIENT_ID` -> recomendar mudar `verify_aud=True` (mudança fora do escopo deste plano, registrar como recomendação)
- Caso contrário (`aud=['account']` ou outro) -> manter `verify_aud=False` e documentar no EVIDENCE.md

Formato de evidência esperado em `03-07-EVIDENCE.md`:
```markdown
# Phase 3 — E2E Evidence (Gap 2)

**Executado por:** <nome>
**Data:** YYYY-MM-DD
**Ambiente:** development / .env local

## Step 1 — Setup do banco
<saída de bin/setup_db>

## Step 2 — Alembic upgrade
<saída de bin/manage_db upgrade>
<saída de psql -c "\dt" familia_dev>

## Step 3 — Boot da app
<saída de uvicorn arranque até "Application startup complete">

## Step 4 — GET /user/me sem token
<status code, response body>

## Step 5 — Obtenção de token Keycloak
<comando usado para obter token; primeiros 30 chars do token + payload decodificado>

## Step 6 — GET /user/me com token
<status code, response body, hora>

## Step 7 — Verificar JIT no banco
<saída de SELECT id, uuid, idp_sub, email, name FROM user WHERE idp_sub = '<sub>'>

## Step 8 — Idempotência: segunda chamada
<status code, response body, COUNT(*) após>

## Step 9 — Análise do claim aud
<payload do token, decisão sobre verify_aud>

## Conclusão
[ ] PASS — todas as verificações passaram
[ ] FAIL — descrever falhas
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Criar scripts/smoke_e2e.py — automatiza a sequência de verificações</name>
  <files>scripts/smoke_e2e.py</files>
  <read_first>
    - src/caramello/main.py (referência: lifespan + routers registrados)
    - src/caramello/shared/auth.py (referência: fluxo de get_current_user)
    - src/caramello/core/config.py (referência: Settings expostas)
    - .env.example (variáveis necessárias)
  </read_first>
  <action>
    Criar `scripts/smoke_e2e.py` — script Python autocontido que, dado um token Keycloak passado via env var `SMOKE_TOKEN`, executa as verificações E2E principais e imprime PASS/FAIL para cada uma. NÃO é um pytest — é um script que o operador roda manualmente após boot da app.

    Conteúdo de `scripts/smoke_e2e.py`:

    ```python
    """Smoke test E2E para Phase 3 — Gap 2 (verificação humana).

    Pré-requisitos:
    - App rodando em http://localhost:8000 (uvicorn caramello.main:app --reload)
    - .env configurado com KEYCLOAK_URL, KEYCLOAK_REALM, KEYCLOAK_CLIENT_ID, DB_*
    - Banco familia_dev com migrations aplicadas (bin/manage_db upgrade)
    - Token Keycloak válido exportado como SMOKE_TOKEN

    Uso:
        export SMOKE_TOKEN=$(...obter token via fluxo de password grant...)
        uv run python scripts/smoke_e2e.py

    Saída: imprime PASS/FAIL para cada verificação e exit code 0 se tudo passou.
    """

    from __future__ import annotations

    import json
    import os
    import sys
    from typing import Any

    import httpx

    BASE_URL = os.environ.get("SMOKE_BASE_URL", "http://localhost:8000")
    TOKEN = os.environ.get("SMOKE_TOKEN", "")


    def _print_result(label: str, passed: bool, detail: str = "") -> None:
        status = "PASS" if passed else "FAIL"
        print(f"[{status}] {label}")
        if detail:
            print(f"       {detail}")


    def check_unauthenticated() -> bool:
        """AUTH-01: GET /user/me sem token retorna 401 ou 403."""
        r = httpx.get(f"{BASE_URL}/user/me")
        ok = r.status_code in (401, 403)
        _print_result(
            "AUTH-01 (sem token -> 401/403)",
            ok,
            f"status={r.status_code} body={r.text[:80]}",
        )
        return ok


    def check_authenticated_get_me() -> tuple[bool, dict[str, Any]]:
        """USER-01: GET /user/me com Bearer token retorna 200 + uuid/email/name."""
        if not TOKEN:
            _print_result("USER-01 (com token)", False, "SMOKE_TOKEN não definido")
            return False, {}
        r = httpx.get(
            f"{BASE_URL}/user/me",
            headers={"Authorization": f"Bearer {TOKEN}"},
        )
        body: dict[str, Any] = {}
        try:
            body = r.json()
        except Exception:
            pass
        has_fields = (
            r.status_code == 200
            and "uuid" in body
            and "email" in body
            and "name" in body
        )
        _print_result(
            "USER-01 (com token -> 200 + uuid/email/name)",
            has_fields,
            f"status={r.status_code} body_keys={list(body.keys()) if body else 'n/a'}",
        )
        return has_fields, body


    def check_idempotent_jit() -> bool:
        """AUTH-02: segunda chamada com mesmo token NÃO altera resposta nem causa erro."""
        if not TOKEN:
            _print_result("AUTH-02 (idempotência)", False, "SMOKE_TOKEN não definido")
            return False
        r1 = httpx.get(
            f"{BASE_URL}/user/me",
            headers={"Authorization": f"Bearer {TOKEN}"},
        )
        r2 = httpx.get(
            f"{BASE_URL}/user/me",
            headers={"Authorization": f"Bearer {TOKEN}"},
        )
        ok = (
            r1.status_code == 200
            and r2.status_code == 200
            and r1.json().get("uuid") == r2.json().get("uuid")
        )
        _print_result(
            "AUTH-02 (idempotência: duas chamadas retornam mesmo uuid)",
            ok,
            (
                f"r1.status={r1.status_code} r2.status={r2.status_code} "
                f"uuids_equal={r1.json().get('uuid') == r2.json().get('uuid') if r1.status_code == 200 and r2.status_code == 200 else 'n/a'}"
            ),
        )
        return ok


    def check_crud_requires_auth() -> bool:
        """AUTH-01 D-11: endpoints CRUD sem token também rejeitam."""
        endpoints = ["/user/", "/family/", "/family_invitation/"]
        results = []
        for ep in endpoints:
            r = httpx.get(f"{BASE_URL}{ep}")
            results.append((ep, r.status_code in (401, 403), r.status_code))
        ok = all(passed for _, passed, _ in results)
        detail = "; ".join(f"{ep}->{code}" for ep, _, code in results)
        _print_result("AUTH-01 D-11 (CRUD sem token -> 401/403)", ok, detail)
        return ok


    def inspect_token_audience() -> None:
        """D-02: inspecionar claim 'aud' do token para decisão sobre verify_aud."""
        if not TOKEN:
            print("[INFO] D-02 análise de 'aud' pulada — sem SMOKE_TOKEN")
            return
        try:
            import jwt
            payload = jwt.decode(TOKEN, options={"verify_signature": False})
            aud = payload.get("aud")
            iss = payload.get("iss")
            sub = payload.get("sub")
            print(f"[INFO] D-02 análise do token:")
            print(f"       sub={sub}")
            print(f"       iss={iss}")
            print(f"       aud={aud!r} (tipo: {type(aud).__name__})")
            client_id = os.environ.get("KEYCLOAK_CLIENT_ID", "")
            if client_id:
                if isinstance(aud, str) and aud == client_id:
                    print(f"       RECOMENDAÇÃO: ativar verify_aud=True (aud == KEYCLOAK_CLIENT_ID)")
                elif isinstance(aud, list) and client_id in aud:
                    print(f"       RECOMENDAÇÃO: ativar verify_aud=True (KEYCLOAK_CLIENT_ID in aud)")
                else:
                    print(f"       RECOMENDAÇÃO: manter verify_aud=False (aud não bate com KEYCLOAK_CLIENT_ID)")
        except Exception as exc:
            print(f"[WARN] D-02 análise falhou: {exc}")


    def main() -> int:
        print(f"=== Smoke E2E Phase 3 — base_url={BASE_URL} ===")
        print(f"Token presente: {bool(TOKEN)}")
        print()

        results: list[bool] = []
        results.append(check_unauthenticated())
        results.append(check_crud_requires_auth())
        ok_get_me, _body = check_authenticated_get_me()
        results.append(ok_get_me)
        results.append(check_idempotent_jit())
        print()
        inspect_token_audience()
        print()

        passed = sum(1 for r in results if r)
        total = len(results)
        print(f"=== Resultado: {passed}/{total} checks passaram ===")
        return 0 if passed == total else 1


    if __name__ == "__main__":
        sys.exit(main())
    ```

    Adicionar shebang? Não é necessário — uso é `uv run python scripts/smoke_e2e.py`.

    O script intencionalmente NÃO toca o banco diretamente (não tem `psycopg2`/`asyncpg` import) — verificações de banco vão no checklist da Task 2 com SQL explícito que o operador roda via `psql`. O smoke valida apenas o caminho HTTP.

    Garantir que o arquivo passa ruff e mypy:

    ```bash
    uv run ruff check scripts/smoke_e2e.py
    uv run mypy scripts/smoke_e2e.py
    ```
  </action>
  <verify>
    <automated>cd /home/claude/work/caramello-api && test -f scripts/smoke_e2e.py && uv run ruff check scripts/smoke_e2e.py && uv run python -c "
import ast
src = open('scripts/smoke_e2e.py').read()
tree = ast.parse(src)
funcs = {node.name for node in ast.walk(tree) if isinstance(node, ast.FunctionDef)}
required = {'check_unauthenticated', 'check_authenticated_get_me', 'check_idempotent_jit', 'check_crud_requires_auth', 'inspect_token_audience', 'main'}
missing = required - funcs
assert not missing, f'Faltam funcoes: {missing}'
print('OK')
"</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/smoke_e2e.py` existe (`test -f scripts/smoke_e2e.py` exit 0)
    - `uv run ruff check scripts/smoke_e2e.py` retorna `All checks passed`
    - O script Python AST contém as 6 funções: `check_unauthenticated`, `check_authenticated_get_me`, `check_idempotent_jit`, `check_crud_requires_auth`, `inspect_token_audience`, `main`
    - `grep -c "SMOKE_TOKEN" scripts/smoke_e2e.py` >= 2 (uso e referência na docstring)
    - `grep -c "verify_signature.*False" scripts/smoke_e2e.py` >= 1 (inspeção de aud)
    - `grep -c "401\\|403" scripts/smoke_e2e.py` >= 2 (validação de unauthenticated)
  </acceptance_criteria>
  <done>
    Script `scripts/smoke_e2e.py` criado, lint-clean, com as funções esperadas e pronto para o operador rodar depois do boot da app.
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Operador executa checklist E2E e preenche 03-07-EVIDENCE.md</name>
  <files>.planning/phases/03-estrutura-por-dom-nios-e-autentica-o/03-07-EVIDENCE.md</files>
  <read_first>
    - scripts/smoke_e2e.py (criado na Task 1)
    - .env.example (estrutura das variáveis)
    - .planning/phases/03-estrutura-por-dom-nios-e-autentica-o/03-VERIFICATION.md (gaps detalhados)
  </read_first>
  <what-built>
    O agente preparou:
    - Plano 03-06 mergeado: bug de mapper SQLAlchemy corrigido (configure_mappers() passa)
    - `scripts/smoke_e2e.py`: script automatizado que valida o caminho HTTP de auth + JIT provisioning quando .env e SMOKE_TOKEN estão preenchidos
    - Template para evidência em 03-07-EVIDENCE.md
  </what-built>
  <action>
    OBJETIVO: Validar end-to-end que a Phase 3 funciona com infraestrutura real (Keycloak + PostgreSQL), preencher um arquivo de evidência, e marcar AUTH-01/02/03 e USER-01 como SATISFIED no relatório de verificação.

    PRÉ-REQUISITOS antes de começar:
    - [ ] Plano 03-06 mergeado (commit em main) — verificar com `git log --oneline -5`
    - [ ] `.env` na raiz do repo preenchido com valores reais de Keycloak e PostgreSQL (DB_HOST, DB_PORT, DB_USER, DB_PASSWORD, DB_NAME, KEYCLOAK_URL, KEYCLOAK_REALM, KEYCLOAK_CLIENT_ID)
    - [ ] PostgreSQL acessível: `psql -h $DB_HOST -p $DB_PORT -U $DB_USER -d postgres -c "SELECT 1"` retorna `1`
    - [ ] Capacidade de obter token Keycloak (via Postman, curl com password grant, ou Impersonate no Admin Console)

    PASSO 1 — Criar banco familia_dev (caso ainda não exista):

    ```bash
    cd /home/claude/work/caramello-api
    bin/setup_db
    ```

    Esperado: usuário e banco `familia_dev` criados. Capturar saída completa para o EVIDENCE.

    PASSO 2 — Aplicar migrations:

    ```bash
    bin/manage_db upgrade
    ```

    Esperado: `INFO  [alembic.runtime.migration] Running upgrade -> <hash>, initial_schema` (ou similar). Sem traceback. Capturar saída.

    Verificar tabelas criadas:

    ```bash
    psql -h "$DB_HOST" -p "$DB_PORT" -U "$DB_USER" -d "$DB_NAME" -c "\dt"
    ```

    Esperado: tabelas `user`, `family`, `family_member`, `family_invitation`, `alembic_version` presentes. Capturar saída.

    PASSO 3 — Bootar a app:

    Em um terminal dedicado:

    ```bash
    cd /home/claude/work/caramello-api
    uv run uvicorn caramello.main:app --reload --port 8000
    ```

    Esperado: stdout contém:
    - `INFO:     Started server process [...]`
    - `INFO:     Application startup complete.`
    - SEM traceback de `InvalidRequestError` ou `httpx.ConnectError`
    - JWKS fetch completa em < 10 segundos (geralmente < 2s)

    Se o boot falhar com erro de JWKS, verificar:
    - `KEYCLOAK_URL` está correto e acessível da máquina (testar com `curl $KEYCLOAK_URL/realms/$KEYCLOAK_REALM/.well-known/openid-configuration`)
    - Realm `caramello` existe no Keycloak

    Capturar primeiras 30 linhas do log no EVIDENCE.

    PASSO 4 — Em OUTRO terminal, validar /user/me sem token:

    ```bash
    curl -i http://localhost:8000/user/me
    ```

    Esperado: HTTP 401 ou 403, body com `{"detail":"Not authenticated"}` ou similar. Capturar saída completa.

    PASSO 5 — Obter token Keycloak válido. Opção mais simples — password grant via curl (substituir placeholders):

    ```bash
    TOKEN=$(curl -s -X POST \
      "$KEYCLOAK_URL/realms/$KEYCLOAK_REALM/protocol/openid-connect/token" \
      -d "client_id=$KEYCLOAK_CLIENT_ID" \
      -d "username=<user_teste>" \
      -d "password=<senha_teste>" \
      -d "grant_type=password" \
      | jq -r '.access_token')
    export SMOKE_TOKEN="$TOKEN"
    echo "Primeiros 30 chars do token: ${TOKEN:0:30}..."
    ```

    Esperado: token JWT obtido (começa com `eyJ...`). Capturar primeiros 30 chars no EVIDENCE (NÃO o token completo).

    Decodificar payload sem validar (apenas para registro):

    ```bash
    uv run python -c "
    import jwt, os, json
    payload = jwt.decode(os.environ['SMOKE_TOKEN'], options={'verify_signature': False})
    print(json.dumps({k: payload[k] for k in ('sub', 'iss', 'aud', 'email', 'name', 'preferred_username', 'exp') if k in payload}, indent=2))
    "
    ```

    Capturar saída — registrar especialmente o valor de `aud` para análise D-02. Capturar `sub` para verificações de banco.

    PASSO 6 — Rodar smoke E2E automatizado:

    ```bash
    cd /home/claude/work/caramello-api
    uv run python scripts/smoke_e2e.py
    ```

    Esperado:
    - Cada check imprime `[PASS]` ou `[FAIL]`
    - Última linha: `=== Resultado: N/N checks passaram ===`
    - Exit code 0 se tudo passou

    Capturar saída completa no EVIDENCE.

    Se algum check falhar, ANTES de marcar Gap 2 como fechado:
    - Recolher resposta exata e código HTTP
    - Inspecionar logs do uvicorn no terminal 1
    - Documentar a falha em EVIDENCE com a hipótese de causa

    PASSO 7 — Verificar JIT provisioning no banco:

    ```bash
    SUB=$(uv run python -c "import jwt, os; print(jwt.decode(os.environ['SMOKE_TOKEN'], options={'verify_signature': False})['sub'])")
    psql -h "$DB_HOST" -p "$DB_PORT" -U "$DB_USER" -d "$DB_NAME" \
      -c "SELECT id, uuid, idp_sub, email, name FROM \"user\" WHERE idp_sub = '$SUB';"
    ```

    Esperado: EXATAMENTE 1 linha retornada após a primeira chamada bem-sucedida de `/user/me`. Capturar saída.

    PASSO 8 — Verificar idempotência (chamar /user/me uma terceira vez e contar):

    ```bash
    curl -s -H "Authorization: Bearer $SMOKE_TOKEN" http://localhost:8000/user/me > /dev/null
    psql -h "$DB_HOST" -p "$DB_PORT" -U "$DB_USER" -d "$DB_NAME" \
      -c "SELECT COUNT(*) FROM \"user\" WHERE idp_sub = '$SUB';"
    ```

    Esperado: COUNT = 1 (não duplicou). Capturar saída.

    PASSO 9 — Análise do claim `aud` (D-02):

    A saída do script `smoke_e2e.py` no PASSO 6 já imprime uma recomendação sobre `verify_aud`. Registrar a decisão final em EVIDENCE:
    - Se o token tem `aud` que inclui `KEYCLOAK_CLIENT_ID` -> escrever no EVIDENCE: "Recomendo abrir issue para ativar verify_aud=True em shared/auth.py — fora do escopo deste plano"
    - Senão -> escrever: "Manter verify_aud=False conforme D-02; aud=<valor> não inclui client_id"

    PASSO 10 — Criar `.planning/phases/03-estrutura-por-dom-nios-e-autentica-o/03-07-EVIDENCE.md`:

    Estrutura mínima:

    ```markdown
    # Phase 3 — E2E Evidence (Gap 2 — autonomous: false)

    **Executado por:** <seu nome>
    **Data:** YYYY-MM-DD
    **Ambiente:** development
    **Plano:** 03-07-PLAN.md
    **Fecha gap:** Gap 2 de 03-VERIFICATION.md

    ## Step 1 — bin/setup_db
    ```
    <saída completa>
    ```

    ## Step 2 — bin/manage_db upgrade + \dt
    ```
    <saída de upgrade>
    <saída de \dt>
    ```

    ## Step 3 — uvicorn startup
    ```
    <primeiras 30 linhas do log>
    ```

    ## Step 4 — GET /user/me sem token
    ```
    <saída de curl -i>
    ```

    ## Step 5 — Obtenção e decodificação do token
    - Primeiros 30 chars: `eyJ...`
    - Payload decodificado (claims principais):
    ```json
    <saída de jwt.decode sem verificar assinatura>
    ```

    ## Step 6 — Smoke E2E automatizado
    ```
    <saída completa de uv run python scripts/smoke_e2e.py>
    ```

    ## Step 7 — JIT provisioning no banco
    ```
    <saída da query SELECT após primeira chamada>
    ```

    ## Step 8 — Idempotência (COUNT após segunda chamada)
    ```
    <saída de SELECT COUNT(*)>
    ```

    ## Step 9 — Análise do claim aud (D-02)

    Valor de aud no token: `<...>`
    Tipo: `<str|list>`
    KEYCLOAK_CLIENT_ID atual: `<...>`
    Decisão: [ ] Ativar verify_aud=True (criar follow-up) / [ ] Manter verify_aud=False

    Justificativa: <...>

    ## Conclusão

    - [ ] PASS — todas as verificações dos passos 1-8 passaram. Gap 2 fechado. AUTH-01, AUTH-02, AUTH-03 e USER-01 podem ser marcados SATISFIED em 03-VERIFICATION.md.
    - [ ] FAIL — descrever falhas e abrir gap em verificação seguinte.

    ## Follow-ups recomendados (fora do escopo)
    - <ex: ativar verify_aud=True após confirmação do claim aud>
    - <ex: documentar fluxo de obtenção de token em docs/dev.md>
    ```

    Commitar o arquivo de evidência com mensagem em pt-BR seguindo Conventional Commits (ex: `docs(03-07): registra evidências E2E da Phase 3`).

    Não modificar `src/caramello/shared/auth.py` para `verify_aud=True` neste plano — registrar como follow-up. Mudanças de código devem entrar em plano separado para preservar auditabilidade.
  </action>
  <verify>
    <automated>test -f .planning/phases/03-estrutura-por-dom-nios-e-autentica-o/03-07-EVIDENCE.md &amp;&amp; grep -c "^## Step" .planning/phases/03-estrutura-por-dom-nios-e-autentica-o/03-07-EVIDENCE.md | awk '$1 &gt;= 9 {print "OK"; exit 0} {exit 1}'</automated>
  </verify>
  <resume-signal>
    Para retomar, responder uma destas opções:
    - "approved" / "PASS" — todos os passos completados, EVIDENCE.md commitado, Gap 2 fechado. O orquestrador vai prosseguir para o summary do plano 03-07 e atualização do VERIFICATION.md.
    - "FAIL: <descrição>" — algum passo falhou; descrever o que falhou e a hipótese de causa. O orquestrador vai criar um plano de remediação ou voltar à 03-06 conforme o caso.
    - "BLOCKED: <razão>" — não foi possível executar (ex: Keycloak inacessível, .env incompleto). Descrever o bloqueio.
  </resume-signal>
  <acceptance_criteria>
    - `test -f .planning/phases/03-estrutura-por-dom-nios-e-autentica-o/03-07-EVIDENCE.md` (arquivo existe)
    - O EVIDENCE.md contém TODAS as 9 seções de step (`grep -c "^## Step" .planning/phases/03-estrutura-por-dom-nios-e-autentica-o/03-07-EVIDENCE.md` >= 9)
    - O EVIDENCE.md contém a linha `## Conclusão` com um dos itens marcado
    - `grep -c "PASS\\|FAIL\\|BLOCKED" .planning/phases/03-estrutura-por-dom-nios-e-autentica-o/03-07-EVIDENCE.md` >= 1 (resultado registrado)
    - Se conclusão for PASS: `grep -c "GET /user/me" .planning/phases/03-estrutura-por-dom-nios-e-autentica-o/03-07-EVIDENCE.md` >= 2 (sem token + com token registrados)
    - Se conclusão for PASS: o EVIDENCE.md contém uma saída SQL mostrando 1 linha na tabela user (verificação de JIT)
    - Commit registrado no git log com mensagem em pt-BR mencionando `03-07` ou `evidências E2E`
  </acceptance_criteria>
  <done>
    Arquivo EVIDENCE.md commitado, conclusão registrada (PASS/FAIL/BLOCKED), e — se PASS — todos os requisitos AUTH-01/02/03 e USER-01 com evidência real. Gap 2 de 03-VERIFICATION.md pode ser fechado.
  </done>
</task>

</tasks>

<verification>
A verificação automatizada deste plano é parcial — a tarefa principal é manual. O agente verifica apenas que:

1. `scripts/smoke_e2e.py` existe e passa lint (Task 1)
2. `03-07-EVIDENCE.md` existe, está commitado, e contém as seções esperadas (Task 2)

A semântica "PASS / FAIL / BLOCKED" do operador é a verdade — sem ela o gap não pode ser fechado.

Após o operador concluir, atualizar `.planning/phases/03-estrutura-por-dom-nios-e-autentica-o/03-VERIFICATION.md`:
- Marcar Gap 2 como `status: closed` (se PASS)
- Marcar AUTH-02 e USER-01 como SATISFIED no quadro de Requirements Coverage
- Adicionar referência cruzada para 03-07-EVIDENCE.md
</verification>

<success_criteria>
- `scripts/smoke_e2e.py` criado, lint-clean, com as 6 funções esperadas
- `.planning/phases/03-estrutura-por-dom-nios-e-autentica-o/03-07-EVIDENCE.md` criado e commitado pelo operador
- EVIDENCE.md contém 9 steps preenchidos com saídas reais (uvicorn, curl, smoke script, psql)
- Conclusão do operador registrada: PASS / FAIL / BLOCKED
- Decisão D-02 sobre `verify_aud` documentada no Step 9
- Se PASS: Gap 2 fechado em 03-VERIFICATION.md; AUTH-02 e USER-01 marcados SATISFIED
- Phase 3 100% verificada (5/5 must-haves do ROADMAP) — pré-condição para iniciar Phase 4
</success_criteria>

<output>
After completion, create `.planning/phases/03-estrutura-por-dom-nios-e-autentica-o/03-07-SUMMARY.md` listing:
- Commits criados (scripts/smoke_e2e.py + EVIDENCE.md)
- Resultado registrado pelo operador (PASS/FAIL/BLOCKED)
- Follow-ups recomendados a partir do EVIDENCE.md Step 9 (ex: verify_aud, doc em docs/dev.md)
- Próximo passo: re-rodar `/gsd-verify-work` para fechar Gap 2 oficialmente em 03-VERIFICATION.md
</output>
