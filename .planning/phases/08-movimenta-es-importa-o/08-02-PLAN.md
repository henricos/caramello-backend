---
phase: 08-movimenta-es-importa-o
plan: "02"
type: execute
wave: 1
depends_on: ["08-01"]
files_modified:
  - dsl/entities/movement.yaml
  - src/caramello/finances/models.py
  - alembic/versions/0003_movement_schema_update.py
autonomous: true
requirements: [MOV-01, MOV-04, MOV-05]
user_setup: []

must_haves:
  truths:
    - "Movement no ORM não tem mais campos type nem is_duplicate"
    - "amount aceita valores negativos (débito) e positivos (crédito)"
    - "alembic upgrade head aplica a migration 0003 e alembic downgrade -1 reverte sem erro"
  artifacts:
    - path: "dsl/entities/movement.yaml"
      provides: "Definição DSL de Movement sem type e is_duplicate (D-01, D-02)"
      contains: "name: amount"
    - path: "src/caramello/finances/models.py"
      provides: "ORM Movement regenerado sem type/is_duplicate"
      contains: "class Movement"
    - path: "alembic/versions/0003_movement_schema_update.py"
      provides: "Migration DROP COLUMN type + DROP COLUMN is_duplicate (D-03)"
      contains: "down_revision"
  key_links:
    - from: "alembic/versions/0003_movement_schema_update.py"
      to: "0002"
      via: "down_revision aponta para a migration anterior"
      pattern: "down_revision.*0002"
---

<objective>
Atualizar o schema de Movement: remover os campos `type` (D-01 — substituído por `amount` com sinal) e `is_duplicate` (D-02 — substituído por `potential_duplicates[]` na resposta de importação) do DSL YAML, regenerar o código gerado, e criar a migration 0003 que faz `DROP COLUMN` de ambos.

Purpose: Habilitar `SUM(amount)` para saldo sem `CASE WHEN type=...` (Phase 9) e eliminar duplicatas persistidas no banco. É a fundação de schema sobre a qual os parsers (08-03) e endpoints (08-04) operam.
Output: `dsl/entities/movement.yaml` editado, `src/caramello/finances/models.py` regenerado, migration `0003_movement_schema_update.py` aplicada e revertível.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-movimenta-es-importa-o/08-CONTEXT.md
@.planning/phases/08-movimenta-es-importa-o/08-RESEARCH.md
@.planning/phases/08-movimenta-es-importa-o/08-PATTERNS.md
@dsl/entities/movement.yaml
@alembic/versions/0002_finances_schema.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Editar movement.yaml (remover type e is_duplicate) e regenerar código</name>
  <read_first>
    - dsl/entities/movement.yaml (estado atual: campo `type` linhas 29-33, campo `is_duplicate` linhas 57-61, campo `amount` linhas 40-43 com descrição "Sempre positivo")
    - dsl/entities/account.yaml (analog de estilo de YAML do mesmo domínio)
    - bin/generate_code (wrapper que roda `uv run python scripts/generate_code.py`)
    - .planning/phases/08-movimenta-es-importa-o/08-PATTERNS.md (§dsl/entities/movement.yaml e §src/caramello/finances/models.py — campos retidos e a forma final do ORM Movement)
    - src/caramello/finances/models.py (ORM gerado atual — `class Movement` para confirmar o que mudou após regenerar)
  </read_first>
  <files>dsl/entities/movement.yaml, src/caramello/finances/models.py</files>
  <action>
    Em `dsl/entities/movement.yaml`: (1) remover por completo o bloco do campo `type` (name/type/max_length/nullable/description); (2) remover por completo o bloco do campo `is_duplicate`; (3) atualizar a `description` do campo `amount` para "Valor com sinal: positivo=crédito, negativo=débito. NUMERIC(15,2)." (D-01). Manter inalterados: `id`, `uuid`, `account_id`, `date`, `description`, `import_hash` (unique, nullable=true), `created_at`, `updated_at`, e o bloco `filters: account_id`. Depois rodar `bin/generate_code` para regenerar `src/caramello/finances/models.py`. NÃO editar `models.py` à mão — é gerado (pitfall: editar gerado direto). Confirmar que `Movement.type` e `Movement.is_duplicate` deixaram de existir no ORM.
  </action>
  <verify>
    <automated>uv run python -c "from caramello.finances.models import Movement; assert not hasattr(Movement, 'type'), 'type ainda existe'; assert not hasattr(Movement, 'is_duplicate'), 'is_duplicate ainda existe'; from sqlmodel import select; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - dsl/entities/movement.yaml NÃO contém `name: type` nem `name: is_duplicate`
    - dsl/entities/movement.yaml contém `name: amount` com description mencionando "positivo=crédito, negativo=débito"
    - `python -c "from caramello.finances.models import Movement; hasattr(Movement,'type')"` retorna False
    - `python -c "from caramello.finances.models import Movement; hasattr(Movement,'is_duplicate')"` retorna False
    - src/caramello/finances/models.py contém `class Movement` e mantém `import_hash` e `amount` com Numeric(15, 2)
    - `uv run python -m pytest tests/test_generator.py -q` continua verde (gerador não quebrado)
  </acceptance_criteria>
  <done>YAML editado, models.py regenerado sem type/is_duplicate, import sem ImportError.</done>
</task>

<task type="auto">
  <name>Task 2: Criar migration 0003 (DROP type + DROP is_duplicate) e verificar upgrade/downgrade</name>
  <read_first>
    - alembic/versions/0002_finances_schema.py (analog de header e estrutura — revision="0002", down_revision="0001", padrão op.create_table; usar `op.drop_column`/`op.add_column` para 0003)
    - .planning/phases/08-movimenta-es-importa-o/08-PATTERNS.md (§alembic/versions/0003_movement_schema_update.py — header completo, upgrade() com drop_column, downgrade() com add_column server_default)
    - .planning/phases/08-movimenta-es-importa-o/08-RESEARCH.md (§Common Pitfalls Pitfall 3 — down_revision DEVE ser "0002", verificar com alembic history --verbose; §Runtime State Inventory — DROP COLUMN é irreversível se houver dados, aceitável em dev)
    - bin/manage_db (wrapper alembic: upgrade, reset)
  </read_first>
  <files>alembic/versions/0003_movement_schema_update.py</files>
  <action>
    Criar `alembic/versions/0003_movement_schema_update.py` com `revision="0003"`, `down_revision="0002"` (D-03 — pitfall P6), `branch_labels=None`, `depends_on=None`. `upgrade()`: `op.drop_column("movement", "type")` e `op.drop_column("movement", "is_duplicate")`. NÃO alterar `amount` — NUMERIC(15,2) já aceita negativos. `downgrade()`: `op.add_column("movement", sa.Column("type", sa.String(length=10), nullable=False, server_default="credito"))` e `op.add_column("movement", sa.Column("is_duplicate", sa.Boolean(), nullable=False, server_default="false"))`. Após criar, rodar `uv run alembic history --verbose` e confirmar que 0003 → 0002 (sem branch). Aplicar `uv run alembic upgrade head` e reverter `uv run alembic downgrade -1` para validar reversibilidade em banco real (caramello_dev). NÃO usar `alembic revision --autogenerate` — escrever a migration à mão para controlar exatamente as duas operações DROP.
  </action>
  <verify>
    <automated>uv run alembic history --verbose 2>&1 | grep -E "0003 -> |Rev: 0003" && uv run alembic upgrade head && uv run alembic downgrade -1 && uv run alembic upgrade head</automated>
  </verify>
  <acceptance_criteria>
    - alembic/versions/0003_movement_schema_update.py existe
    - O arquivo contém `revision: str = "0003"` e `down_revision` com valor `"0002"`
    - upgrade() contém `op.drop_column("movement", "type")` e `op.drop_column("movement", "is_duplicate")`
    - downgrade() contém `op.add_column` para `type` e `is_duplicate`
    - `uv run alembic history --verbose` mostra 0003 apontando para 0002 (sem branch / sem down_revision None inesperado)
    - `uv run alembic upgrade head` aplica 0003 sem erro
    - `uv run alembic downgrade -1` reverte 0003 sem erro
  </acceptance_criteria>
  <done>Migration 0003 aplicada e revertida em banco real; grafo alembic linear 0001→0002→0003.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration → banco caramello | DDL irreversível (DROP COLUMN) sobre tabela existente |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-02 | Tampering | DROP COLUMN type/is_duplicate | accept | Phase 8 é a primeira implementação real de movement; sem dados de produção. Em dev, dados são de teste (Runtime State Inventory RESEARCH.md). downgrade() reconstrói as colunas com server_default |
| T-08-03 | Denial of Service | down_revision incorreto | mitigate | Verificação obrigatória via `alembic history --verbose` confirmando 0003→0002 (pitfall P6) antes de aceitar a task |
</threat_model>

<verification>
- ORM Movement sem `type`/`is_duplicate` (`hasattr` retorna False)
- `alembic history --verbose` mostra grafo linear 0001→0002→0003
- upgrade/downgrade/upgrade ciclo completo sem erro em caramello_dev
</verification>

<success_criteria>
- movement.yaml sem type/is_duplicate; amount com descrição de sinal
- models.py regenerado refletindo o YAML
- Migration 0003 aplicável e revertível, down_revision="0002"
</success_criteria>

<output>
Create `.planning/phases/08-movimenta-es-importa-o/08-02-SUMMARY.md` when done
</output>
