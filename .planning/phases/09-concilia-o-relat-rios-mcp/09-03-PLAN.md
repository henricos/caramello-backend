---
phase: 09-concilia-o-relat-rios-mcp
plan: 03
type: execute
wave: 2
depends_on: ["09-01", "09-02"]
files_modified:
  - src/caramello/finances/operations.py
  - src/caramello/finances/services.py
autonomous: true
requirements: [LAN-01, LAN-02, LAN-03, LAN-04, LAN-05]
user_setup: []

must_haves:
  truths:
    - "Usuário pode conciliar uma movimentação criando um lançamento financeiro com subcategoria e competência"
    - "Segunda conciliação da mesma movimentação retorna 409"
    - "Usuário recebe top-5 sugestões de subcategoria ordenadas por score"
    - "Usuário pode detalhar e atualizar um lançamento existente (subcategoria, competência, notas, responsável, recorrência)"
  artifacts:
    - path: "src/caramello/finances/operations.py"
      provides: "Endpoints reconcile, suggest-category, GET/PATCH entries, GET entries list + schema rico"
      contains: "FinancialEntryRichPublic"
    - path: "src/caramello/finances/services.py"
      provides: "suggest_category() via rapidfuzz"
      contains: "def suggest_category"
  key_links:
    - from: "src/caramello/finances/operations.py"
      to: "FinancialEntry (UNIQUE movement_id)"
      via: "session.add + except IntegrityError → 409"
      pattern: "IntegrityError"
    - from: "src/caramello/finances/operations.py"
      to: "src/caramello/finances/services.py:suggest_category"
      via: "import + chamada do endpoint suggest-category"
      pattern: "suggest_category"
    - from: "src/caramello/finances/services.py"
      to: "rapidfuzz.fuzz.token_set_ratio"
      via: "import rapidfuzz"
      pattern: "token_set_ratio"
---

<objective>
Implementar o núcleo de conciliação da fase 9: criação de lançamento financeiro 1:1 a partir de uma movimentação (LAN-01/02/04), sugestão semi-automática de subcategoria via fuzzy match (LAN-03), e detalhe/atualização/listagem de lançamentos com schema rico (LAN-05). Estende `operations.py` (endpoints) e `services.py` (`suggest_category`).

Purpose: É a entrega central da fase — transforma movimentações brutas em lançamentos classificados, com a sugestão de categoria que torna a classificação semi-automática.
Output: 5 endpoints novos em `operations.py` (POST reconcile, GET suggest-category, GET entry, PATCH entry, GET entries list), schema rico `FinancialEntryRichPublic`, e função `suggest_category()` em `services.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-concilia-o-relat-rios-mcp/09-CONTEXT.md
@.planning/phases/09-concilia-o-relat-rios-mcp/09-RESEARCH.md
@.planning/phases/09-concilia-o-relat-rios-mcp/09-PATTERNS.md
@src/caramello/finances/operations.py
@src/caramello/finances/services.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: suggest_category() em services.py + endpoint GET suggest-category</name>
  <files>src/caramello/finances/services.py, src/caramello/finances/operations.py</files>
  <behavior>
    - `test_suggest_category_service`: histórico não-vazio → lista de dicts com score (int 0-100), max-1 por subcategoria, ordenada desc, no máx 5 itens
    - `test_suggest_category_empty_history`: sem histórico → `[]`
    - `test_suggest_category` (endpoint): GET retorna lista com `subcategory_uuid`, `subcategory_name`, `category_uuid`, `category_name`, `score`
  </behavior>
  <read_first>
    - src/caramello/finances/services.py (cabeçalho do módulo, assinatura de `import_movements` como referência de service async sem deps FastAPI; uso de `session.execute()`)
    - src/caramello/finances/operations.py (padrão de resolução movement_uuid → movement → account → family + `_require_family_access`; padrão de endpoint GET)
    - .planning/phases/09-concilia-o-relat-rios-mcp/09-PATTERNS.md (Pattern 5 suggest_category com rapidfuzz; seção "Shared Patterns: Authentication" cadeia movement→account→family)
    - tests/test_services/test_finances_service.py + tests/test_finances_operations.py (stubs criados no plano 01)
  </read_first>
  <action>
    Em `src/caramello/finances/services.py`, implementar `async def suggest_category(movement_uuid: UUID, family_id: int, session: AsyncSession) -> list[dict]` (D-CAT-04). Lógica (D-CAT-01/02/03):
    1. Buscar `Movement` pelo `movement_uuid` via `session.execute(select(Movement).where(Movement.uuid == movement_uuid))`; se não existe, retornar `[]`.
    2. Buscar histórico da família: `select(Movement.description, Subcategory.id/uuid/name, Category.uuid/name)` com joins `FinancialEntry`→`Subcategory`→`Category`→(`Movement`→`Account`), filtrado por `Account.family_id == family_id`. Usar `session.execute().fetchall()`.
    3. Se vazio, retornar `[]`.
    4. Para cada linha, `score = int(fuzz.token_set_ratio(target_desc, row.description))` (cast para int — A1 do RESEARCH). Manter o maior score por `subcategory_id` num dict.
    5. Ordenar desc por score, retornar top-5 com chaves `subcategory_uuid`, `subcategory_name`, `category_uuid`, `category_name`, `score`. Sem threshold mínimo.
    Importar `from rapidfuzz import fuzz`. Não usar `run_in_executor` (volume pequeno — Open Question 2 do RESEARCH). Não usar `session.exec()` para os JOINs — sempre `session.execute()` (pitfall P3).

    Em `src/caramello/finances/operations.py`, adicionar `GET /finances/movements/{movement_uuid}/suggest-category`. Resolver `movement_uuid → movement → account.family_id`, chamar `_require_family_access(family_id, current_user, session)` (404 se movement inexistente, 403 não-membro), depois `suggest_category(movement_uuid, family_id, session)` e retornar a lista. Adicionar `suggest_category` ao bloco de import de `caramello.finances.services` (LAN-03).
  </action>
  <verify>
    <automated>uv run pytest tests/test_services/test_finances_service.py::test_suggest_category_service tests/test_services/test_finances_service.py::test_suggest_category_empty_history tests/test_finances_operations.py::test_suggest_category -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'def suggest_category' src/caramello/finances/services.py` retorna 0
    - `grep -q 'token_set_ratio' src/caramello/finances/services.py` retorna 0
    - `src/caramello/finances/operations.py` contém o path `/movements/{movement_uuid}/suggest-category`
    - `uv run pytest tests/test_services/test_finances_service.py::test_suggest_category_service tests/test_services/test_finances_service.py::test_suggest_category_empty_history -x` sai 0 (testes deixam de skipar e passam)
    - `uv run pytest tests/test_finances_operations.py::test_suggest_category -x` sai 0
  </acceptance_criteria>
  <done>`suggest_category` implementada com rapidfuzz retornando top-5 por score; endpoint GET suggest-category com auth por família; testes de serviço e endpoint verdes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Schema rico + POST reconcile (LAN-01/02/04)</name>
  <files>src/caramello/finances/operations.py</files>
  <behavior>
    - `test_reconcile_movement`: POST reconcile retorna 201 + schema rico (uuid, movement embutido, subcategory_uuid/name, category_uuid/name, competencia, is_recorrente, responsible_user_uuid)
    - `test_reconcile_409_duplicate`: IntegrityError no commit → 409
  </behavior>
  <read_first>
    - src/caramello/finances/operations.py (schemas públicos locais BaseModel linhas 44-135; padrão de POST create; construção explícita de schema de resposta; cadeia movement→account→family; resolução subcategory_uuid→subcategory_id)
    - src/caramello/finances/models.py (FinancialEntry com responsible_user_id já adicionado no plano 02; Subcategory, Category, Movement)
    - .planning/phases/09-concilia-o-relat-rios-mcp/09-PATTERNS.md (schemas `ReconcileCreatePublic`, `MovementSummaryPublic`, `FinancialEntryRichPublic`; Pattern 1 reconcile com IntegrityError→409; bloco de resolução responsible_user_uuid + membership)
    - .planning/phases/09-concilia-o-relat-rios-mcp/09-CONTEXT.md (D-REC-01/02, D-ATTR-01/02/03)
  </read_first>
  <action>
    Em `operations.py`, definir os schemas públicos locais (BaseModel, antes dos endpoints, seguindo o padrão de 09-PATTERNS.md): `MovementSummaryPublic` (uuid, date, amount: Decimal, description), `FinancialEntryRichPublic` (uuid, movement: MovementSummaryPublic, subcategory_uuid, subcategory_name, category_uuid, category_name, competencia_year, competencia_month, notes, is_recorrente, responsible_user_uuid: UUID|None, created_at, updated_at), e `ReconcileCreatePublic` (subcategory_uuid, competencia_year, competencia_month, notes?, is_recorrente=False, responsible_user_uuid?). Schema rico único reutilizado nos 4 endpoints de entry (Open Question 1: model separado).

    Adicionar imports `from sqlalchemy.exc import IntegrityError` e `from caramello.users.models import User` (se ausente).

    Implementar `POST /finances/movements/{movement_uuid}/reconcile` (status 201, response_model=FinancialEntryRichPublic) per D-REC-01:
    1. Resolver `movement_uuid → Movement` (404 se None).
    2. Resolver `account` via `Movement.account_id` → `account.family_id`; `_require_family_access(family_id, current_user, session)`.
    3. Resolver `subcategory_uuid → subcategory_id` (404 se None); obter Category via `subcategory.category_id`.
    4. Se `responsible_user_uuid` presente: resolver `User.uuid → id` (422 se inválido, D-ATTR-01); validar membership via `select(FamilyMember).where(family_id, user_id)` (422 se não-membro, D-ATTR-02).
    5. `session.add(FinancialEntry(...))`; `await session.commit()`; envolver em `try/except IntegrityError → rollback + HTTPException(409, "Movimentação já possui lançamento financeiro")` (LAN-02, constraint UNIQUE do banco — não check Python).
    6. Construir e retornar `FinancialEntryRichPublic` explicitamente a partir dos objetos já carregados (sem lazy load — evitar pitfall selectinload).
    `is_recorrente` vem do payload (LAN-04). Decimal nunca convertido para float.
  </action>
  <verify>
    <automated>uv run pytest tests/test_finances_operations.py::test_reconcile_movement tests/test_finances_operations.py::test_reconcile_409_duplicate -x</automated>
  </verify>
  <acceptance_criteria>
    - `src/caramello/finances/operations.py` contém `class FinancialEntryRichPublic`
    - `src/caramello/finances/operations.py` contém `class ReconcileCreatePublic`
    - `src/caramello/finances/operations.py` contém o path `/movements/{movement_uuid}/reconcile`
    - `grep -q 'IntegrityError' src/caramello/finances/operations.py` retorna 0
    - `uv run pytest tests/test_finances_operations.py::test_reconcile_movement -x` sai 0
    - `uv run pytest tests/test_finances_operations.py::test_reconcile_409_duplicate -x` sai 0
  </acceptance_criteria>
  <done>POST reconcile cria FinancialEntry 1:1 com schema rico; 409 em duplicata via IntegrityError; responsável validado por membership; is_recorrente aceito.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: GET entry, PATCH entry (sentinela), GET entries list (LAN-05)</name>
  <files>src/caramello/finances/operations.py</files>
  <behavior>
    - `test_update_entry`: PATCH atualiza subcategory_uuid, competencia, notes → 200 + schema rico, updated_at alterado
    - `test_entry_responsible_user_uuid`: PATCH com responsible_user_uuid atribui; PATCH com null limpa
  </behavior>
  <read_first>
    - src/caramello/finances/operations.py (schemas e endpoint reconcile criados na Task 2; padrão PATCH com updated_at manual; padrão GET detail)
    - .planning/phases/09-concilia-o-relat-rios-mcp/09-PATTERNS.md (schema `FinancialEntryUpdatePublic`; padrão PATCH com `model_fields_set` para responsible_user_uuid; nota: NÃO usar model_dump(exclude_none=True))
    - .planning/phases/09-concilia-o-relat-rios-mcp/09-CONTEXT.md (D-REC-03/04/05; tratamento de sentinela na seção Claude's Discretion)
    - .planning/phases/09-concilia-o-relat-rios-mcp/09-RESEARCH.md (Pattern 6 sentinela; pitfall P2 exclude_none; pitfall P3 updated_at)
  </read_first>
  <action>
    Definir `FinancialEntryUpdatePublic` (BaseModel): subcategory_uuid?, competencia_year?, competencia_month?, notes?, is_recorrente?, responsible_user_uuid? — todos com default. Reutilizar `FinancialEntryRichPublic` como response.

    Implementar `GET /finances/entries/{entry_uuid}` (D-REC-03): resolver entry por uuid (404), resolver movement→account→family + `_require_family_access`, construir e retornar `FinancialEntryRichPublic` (joins explícitos para subcategory/category/responsible_user).

    Implementar `PATCH /finances/entries/{entry_uuid}` (D-REC-04, LAN-05): resolver entry + auth por família. Atualizar campos presentes. Para `responsible_user_uuid` usar `model_fields_set` (NÃO `model_dump(exclude_none=True)` — pitfall P2): se o campo está em `model_fields_set` e é `None` → `responsible_user_id = None` (limpar); se presente e não-None → resolver User.uuid→id + membership check (422); se ausente → não tocar. Resolver `subcategory_uuid → subcategory_id` quando enviado (404 se inválido). Definir `db_entry.updated_at = datetime.now(timezone.utc)` manualmente antes do commit (pitfall P3). Retornar schema rico.

    Implementar `GET /finances/entries?family_uuid=&year=&month=` (D-REC-05): resolver `family_uuid → family.id` + `_require_family_access`. Query de lançamentos da família filtrando por `competencia_year`/`competencia_month` quando fornecidos (opcionais). Adicionar `limit` default 100 + `offset` (Open Question 3). Retornar `list[FinancialEntryRichPublic]`.
  </action>
  <verify>
    <automated>uv run pytest tests/test_finances_operations.py::test_update_entry tests/test_finances_operations.py::test_entry_responsible_user_uuid -x</automated>
  </verify>
  <acceptance_criteria>
    - `src/caramello/finances/operations.py` contém `class FinancialEntryUpdatePublic`
    - `src/caramello/finances/operations.py` contém o path `/entries/{entry_uuid}`
    - `src/caramello/finances/operations.py` contém o path `/entries` (GET list)
    - `grep -q 'model_fields_set' src/caramello/finances/operations.py` retorna 0
    - `grep -q 'updated_at = datetime.now' src/caramello/finances/operations.py` retorna 0
    - `uv run pytest tests/test_finances_operations.py::test_update_entry tests/test_finances_operations.py::test_entry_responsible_user_uuid -x` sai 0
  </acceptance_criteria>
  <done>GET detail, PATCH (com sentinela responsible_user_uuid + updated_at manual) e GET list de entries implementados com schema rico e auth por família.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cliente HTTP → API /finances | UUIDs e payloads não confiáveis atravessam para resolução de recursos |
| API → DB | Inserts/updates de FinancialEntry com constraint UNIQUE(movement_id) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-04 | Information Disclosure | IDOR em reconcile/GET/PATCH/list entry de outra família | mitigate | Resolver `movement_uuid → movement → account → family_id` (ou `family_uuid`/`entry_uuid → family_id`) + `_require_family_access` antes de qualquer leitura/escrita; retorna 403 para não-membro |
| T-09-05 | Tampering | atribuição de responsible_user de outra família | mitigate | Após resolver `responsible_user_uuid → id`, validar `select(FamilyMember).where(family_id, user_id)`; 422 se não-membro (D-ATTR-02) |
| T-09-06 | Tampering | conciliação dupla via race condition (TOCTOU) | mitigate | Constraint `UNIQUE(movement_id)` no banco + `except IntegrityError → 409`; sem check Python pré-insert (race-safe) |
| T-09-07 | Spoofing | acesso sem token | mitigate | `get_current_user` em todos os endpoints → 401 sem Bearer válido (AUTH-FIN-01, reutilizado) |
| T-09-08 | Input Validation | UUID/competencia inválidos | mitigate | Pydantic BaseModel com tipos explícitos (UUID, int); 422 automático; 404 para UUID inexistente |
</threat_model>

<verification>
- `uv run pytest tests/test_finances_operations.py tests/test_services/test_finances_service.py -x` sai 0 (todos os testes de LAN-01..05 verdes)
- `uv run pytest` (suite completa) verde
</verification>

<success_criteria>
- POST reconcile cria lançamento 1:1, 409 em duplicata (LAN-01/02/04)
- GET suggest-category retorna top-5 por score (LAN-03)
- GET/PATCH/GET-list entries com schema rico; PATCH com sentinela e updated_at manual (LAN-05)
- Membership de responsável validada (D-ATTR-02); IDOR mitigado em todos os endpoints
</success_criteria>

<output>
Create `.planning/phases/09-concilia-o-relat-rios-mcp/09-03-SUMMARY.md` when done
</output>
