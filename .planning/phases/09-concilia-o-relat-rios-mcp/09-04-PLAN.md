---
phase: 09-concilia-o-relat-rios-mcp
plan: 04
type: execute
wave: 3
depends_on: ["09-03"]
files_modified:
  - src/caramello/finances/operations.py
  - src/caramello/finances/services.py
autonomous: true
requirements: [REL-01, REL-02, REL-03, REL-04, REL-05]
user_setup: []

must_haves:
  truths:
    - "Usuário consulta saldo atual de uma conta (créditos − débitos, Decimal)"
    - "Usuário consulta saldo consolidado de todas as contas ativas da família"
    - "Usuário consulta breakdown mensal por categoria pai com detalhe por subcategoria"
    - "Relatórios filtram por competência (year/month), nunca por data da movimentação"
    - "Listagem de movimentações expõe entry_uuid e aceita filtro reconciled"
  artifacts:
    - path: "src/caramello/finances/services.py"
      provides: "account_balance, family_balance, monthly_breakdown, by_member_breakdown"
      contains: "def account_balance"
    - path: "src/caramello/finances/operations.py"
      provides: "Endpoints balance/reports + MovementReadPublic.entry_uuid + filtro reconciled"
      contains: "entry_uuid"
  key_links:
    - from: "src/caramello/finances/services.py"
      to: "sqlalchemy func.sum / group_by"
      via: "session.execute"
      pattern: "func.sum"
    - from: "src/caramello/finances/operations.py"
      to: "FinancialEntry (LEFT JOIN)"
      via: "outerjoin para entry_uuid em list_movements"
      pattern: "outerjoin"
    - from: "src/caramello/finances/operations.py"
      to: "FinancialEntry.competencia_year/month"
      via: "filtro de relatório por competência"
      pattern: "competencia_year"
---

<objective>
Implementar a camada analítica da fase 9: saldos de conta e família (REL-01/02), breakdown mensal por categoria/subcategoria (REL-03/04), breakdown por membro (D-REP-02), todos filtrando por competência e não por data (REL-05). Inclui a extensão de `MovementReadPublic` com `entry_uuid` e o filtro `?reconciled` (D-MOV-01/02). Estende `operations.py` (endpoints) e `services.py` (funções de agregação).

Purpose: Os relatórios são a razão de existir do domínio financeiro — transformam lançamentos classificados em insight. O indicador de conciliação em Movement fecha o loop entre importação (fase 8) e conciliação (plano 03).
Output: 4 funções de agregação em `services.py`, 4 endpoints de relatório/saldo + 2 endpoints de relatório por membro/mensal em `operations.py`, `MovementReadPublic.entry_uuid` e filtro `reconciled` no `list_movements` existente.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-concilia-o-relat-rios-mcp/09-CONTEXT.md
@.planning/phases/09-concilia-o-relat-rios-mcp/09-RESEARCH.md
@.planning/phases/09-concilia-o-relat-rios-mcp/09-PATTERNS.md
@src/caramello/finances/operations.py
@src/caramello/finances/services.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Funções de saldo + endpoints balance (REL-01/02)</name>
  <files>src/caramello/finances/services.py, src/caramello/finances/operations.py</files>
  <behavior>
    - `test_account_balance_empty` (service): SUM None → Decimal("0.00")
    - `test_family_balance` (service): soma de contas ativas
    - `test_account_balance` (endpoint): GET retorna {account_uuid, balance, currency}
    - `test_family_balance` (endpoint): GET retorna {family_uuid, total_balance, accounts}
  </behavior>
  <read_first>
    - src/caramello/finances/services.py (padrão de service async; import_movements como referência)
    - src/caramello/finances/operations.py (cadeia account_uuid→family / family_uuid→family + _require_family_access; schemas públicos locais; padrão GET)
    - .planning/phases/09-concilia-o-relat-rios-mcp/09-PATTERNS.md (Pattern 3 account_balance; guard scalar_one_or_none P6; seção "Decimal para Valores Monetários")
    - .planning/phases/09-concilia-o-relat-rios-mcp/09-CONTEXT.md (D-BAL-01/02/03)
  </read_first>
  <action>
    Em `services.py` implementar (funções independentes, async, sem deps FastAPI — Claude's Discretion):
    - `async def account_balance(account_id: int, session: AsyncSession) -> Decimal`: `session.execute(select(func.sum(Movement.amount)).where(Movement.account_id == account_id))`, `scalar_one_or_none()`, retorna `total if total is not None else Decimal("0.00")` (pitfall P6). NUNCA `session.exec()` (pitfall P3). Créditos positivos / débitos negativos já é convenção do amount (Phase 8 D-01).
    - `async def family_balance(family_id: int, session: AsyncSession) -> list[tuple]` (ou dict por conta): para cada `Account` ativa (`is_active=true`) da família, computar `account_balance`; retornar dados suficientes para o endpoint montar `{account_uuid, name, currency, balance}` + total. Manter Decimal.

    Em `operations.py` definir schemas públicos locais `AccountBalancePublic` (account_uuid, balance: Decimal, currency) e `FamilyBalancePublic` (family_uuid, total_balance: Decimal, accounts: list[...]). Implementar:
    - `GET /finances/accounts/{account_uuid}/balance` (REL-01, D-BAL-01): resolver account→family + `_require_family_access`; chamar `account_balance`; retornar schema.
    - `GET /finances/families/{family_uuid}/balance` (REL-02, D-BAL-02): resolver family + `_require_family_access`; chamar `family_balance`; somar `total_balance`; retornar schema. Apenas contas ativas.
    Adicionar `account_balance, family_balance` ao import de `caramello.finances.services`. Decimal serializado como string pelo Pydantic (não float).
  </action>
  <verify>
    <automated>uv run pytest tests/test_services/test_finances_service.py::test_account_balance_empty tests/test_services/test_finances_service.py::test_family_balance tests/test_finances_operations.py::test_account_balance tests/test_finances_operations.py::test_family_balance -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'def account_balance' src/caramello/finances/services.py` retorna 0
    - `grep -q 'def family_balance' src/caramello/finances/services.py` retorna 0
    - `grep -q 'func.sum' src/caramello/finances/services.py` retorna 0
    - `src/caramello/finances/operations.py` contém o path `/accounts/{account_uuid}/balance`
    - `src/caramello/finances/operations.py` contém o path `/families/{family_uuid}/balance`
    - `uv run pytest tests/test_services/test_finances_service.py::test_account_balance_empty tests/test_finances_operations.py::test_account_balance tests/test_finances_operations.py::test_family_balance -x` sai 0
  </acceptance_criteria>
  <done>account_balance/family_balance via func.sum com guard P6; endpoints de saldo de conta e família com auth e Decimal; testes verdes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Breakdown mensal + por membro (REL-03/04/05, D-REP)</name>
  <files>src/caramello/finances/services.py, src/caramello/finances/operations.py</files>
  <behavior>
    - `test_monthly_breakdown` / `test_by_member_breakdown` (service): GROUP BY retorna rows; by-member inclui grupo user_uuid=None
    - `test_monthly_report` (endpoint): GET retorna {period, total, rows} com category/subcategory por row
    - `test_report_uses_competencia` (endpoint): aceita year/month, filtra por competência
  </behavior>
  <read_first>
    - src/caramello/finances/services.py (account_balance/family_balance criadas na Task 1; uso de session.execute)
    - src/caramello/finances/operations.py (cadeia family_uuid→family + _require_family_access; schemas públicos)
    - .planning/phases/09-concilia-o-relat-rios-mcp/09-PATTERNS.md (Pattern 4 GROUP BY monthly report; query com joins FinancialEntry→Subcategory→Category→Movement→Account filtrando competencia_year/month)
    - .planning/phases/09-concilia-o-relat-rios-mcp/09-CONTEXT.md (D-REP-01/02/03/04; formato JSON de resposta de monthly e by-member; linha "Não atribuído")
  </read_first>
  <action>
    Em `services.py` implementar (session.execute + func.sum + group_by — pitfall P3/P4):
    - `async def monthly_breakdown(family_id, year, month, session, member_id=None) -> dict`: query com `func.sum(Movement.amount)` + `func.count(FinancialEntry.id)`, joins `FinancialEntry→Subcategory→Category` e `FinancialEntry→Movement→Account`, WHERE `Account.family_id == family_id` + `FinancialEntry.competencia_year == year` + `FinancialEntry.competencia_month == month` (filtra por COMPETÊNCIA, nunca por Movement.date — REL-05/D-REP-03), GROUP BY category+subcategory. `member_id` opcional filtra por `responsible_user_id`. Retornar `{period, total, rows}` onde rows têm category_uuid/name, subcategory_uuid/name, total (Decimal), count. `fetchall()` (não scalars) — pitfall P5.
    - `async def by_member_breakdown(family_id, year, month, session) -> dict` (D-REP-02): GROUP BY `responsible_user_id` sobre FinancialEntry da família no período. Linhas com `responsible_user_id IS NULL` agrupadas como `{user_uuid: null, name: "Não atribuído"}` — incluídas no total, não descartadas (pitfall P7). Retornar `{period, total, rows}` com user_uuid, name, total, count.

    Em `operations.py` definir schemas de resposta `MonthlyReportPublic` e `ByMemberReportPublic` (com sub-schemas de row). Implementar:
    - `GET /finances/reports/monthly?family_uuid=&year=&month=[&member_uuid=]` (REL-03/04): resolver family + `_require_family_access`; resolver `member_uuid → user.id` se fornecido; chamar `monthly_breakdown`; retornar. `year`/`month` parâmetros de competência.
    - `GET /finances/reports/by-member?family_uuid=&year=&month=` (D-REP-02): `year`/`month` obrigatórios; resolver family + auth; chamar `by_member_breakdown`; retornar.
    Adicionar `monthly_breakdown, by_member_breakdown` ao import de services. Decimal como string.
  </action>
  <verify>
    <automated>uv run pytest tests/test_services/test_finances_service.py::test_monthly_breakdown tests/test_services/test_finances_service.py::test_by_member_breakdown tests/test_finances_operations.py::test_monthly_report tests/test_finances_operations.py::test_report_uses_competencia -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'def monthly_breakdown' src/caramello/finances/services.py` retorna 0
    - `grep -q 'def by_member_breakdown' src/caramello/finances/services.py` retorna 0
    - `grep -q 'competencia_year' src/caramello/finances/services.py` retorna 0
    - `src/caramello/finances/operations.py` contém o path `/reports/monthly`
    - `src/caramello/finances/operations.py` contém o path `/reports/by-member`
    - `uv run pytest tests/test_finances_operations.py::test_monthly_report tests/test_finances_operations.py::test_report_uses_competencia -x` sai 0
  </acceptance_criteria>
  <done>monthly_breakdown e by_member_breakdown via group_by sobre competência; endpoints de relatório mensal e por membro com auth; "Não atribuído" preserva total; testes verdes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: entry_uuid em MovementReadPublic + filtro reconciled (D-MOV-01/02)</name>
  <files>src/caramello/finances/operations.py</files>
  <behavior>
    - `test_movement_entry_uuid_field`: GET movements inclui entry_uuid (UUID|None) por item
    - `test_movement_reconciled_filter`: ?reconciled=false retorna apenas pendentes (entry_uuid None)
  </behavior>
  <read_first>
    - src/caramello/finances/operations.py (`MovementReadPublic` linhas ~115-122; `list_movements` linhas ~697-740 com session.exec atual)
    - .planning/phases/09-concilia-o-relat-rios-mcp/09-PATTERNS.md (extensão MovementReadPublic com entry_uuid; Pattern 2 LEFT JOIN com outerjoin + filtro reconciled; nota fetchall não scalars — pitfall P5)
    - .planning/phases/09-concilia-o-relat-rios-mcp/09-CONTEXT.md (D-MOV-01/02)
  </read_first>
  <action>
    Adicionar o campo `entry_uuid: UUID | None = None` a `MovementReadPublic` (D-MOV-01), inserido após `import_hash` e antes de `created_at`.

    Reescrever a query de `list_movements` (`GET /finances/accounts/{account_uuid}/movements`) para usar `session.execute()` com LEFT JOIN (Pattern 2): `select(Movement, FinancialEntry.uuid.label("entry_uuid")).select_from(outerjoin(Movement, FinancialEntry, FinancialEntry.movement_id == Movement.id)).where(Movement.account_id == db_account.id)`. Adicionar parâmetro de query `reconciled: bool | None = None` (D-MOV-02): se `False` → `.where(FinancialEntry.id.is_(None))`; se `True` → `.where(FinancialEntry.id.is_not(None))`; se None → sem filtro. Manter `order_by(Movement.date.desc())` + offset/limit existentes. Usar `result.fetchall()` e acessar `row[0]` (Movement) + `row[1]` (entry_uuid) por posição (pitfall P5 — NÃO usar `.scalars()`). Construir cada `MovementReadPublic` com `entry_uuid=row[1]`.

    Importar `from sqlalchemy import func, outerjoin` (e select de sqlalchemy se necessário para o multi-entity select). NÃO quebrar os campos existentes do endpoint nem alterar import de movimentações.

    Após implementar, remover qualquer `_skip_if_stub`/guard que mantinha `test_finances_router_paths` skipado no plano 01, de modo que o teste valide o set completo de 8 paths novos contra as rotas agora existentes.
  </action>
  <verify>
    <automated>uv run pytest tests/test_finances_operations.py::test_movement_entry_uuid_field tests/test_finances_operations.py::test_movement_reconciled_filter tests/test_finances_operations.py::test_finances_router_paths -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'entry_uuid' src/caramello/finances/operations.py` retorna 0
    - `grep -q 'outerjoin' src/caramello/finances/operations.py` retorna 0
    - `MovementReadPublic` contém `entry_uuid: UUID | None`
    - `list_movements` aceita parâmetro `reconciled`
    - `uv run pytest tests/test_finances_operations.py::test_movement_entry_uuid_field tests/test_finances_operations.py::test_movement_reconciled_filter -x` sai 0
    - `uv run pytest tests/test_finances_operations.py::test_finances_router_paths -x` sai 0 (8 paths novos presentes no router)
  </acceptance_criteria>
  <done>MovementReadPublic expõe entry_uuid via LEFT JOIN; filtro ?reconciled funciona; test_finances_router_paths valida os 8 paths da fase 9.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cliente HTTP → API /finances | family_uuid/account_uuid/member_uuid não confiáveis em endpoints de relatório/saldo |
| API → DB | Agregações func.sum/group_by sobre FinancialEntry/Movement de uma família |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-09 | Information Disclosure | IDOR em relatórios/saldos (dados de outra família) | mitigate | Resolver `family_uuid`/`account_uuid → family_id` + `_require_family_access` antes de qualquer query de agregação; 403 para não-membro |
| T-09-10 | Information Disclosure | member_uuid de outra família em /reports/monthly | mitigate | `member_uuid` filtra apenas dentro da família já autorizada; resolução para id ocorre após `_require_family_access` da família |
| T-09-11 | Spoofing | acesso sem token | mitigate | `get_current_user` em todos os endpoints → 401 (AUTH-FIN-01) |
| T-09-12 | Tampering | precisão monetária (float em saldo/total) | mitigate | `Decimal` em todas as agregações; Pydantic serializa como string; zero float (pitfall P1) |
</threat_model>

<verification>
- `uv run pytest tests/test_finances_operations.py tests/test_services/test_finances_service.py -x` sai 0 (REL-01..05 + D-MOV verdes)
- `uv run pytest` (suite completa) verde — todos os stubs da fase 9 agora implementados ou propositalmente cobertos
</verification>

<success_criteria>
- Saldo de conta e família via func.sum/Decimal (REL-01/02)
- Breakdown mensal por categoria/subcategoria filtrando por competência (REL-03/04/05)
- Breakdown por membro com "Não atribuído" preservando total (D-REP-02)
- MovementReadPublic.entry_uuid + filtro reconciled (D-MOV-01/02)
- test_finances_router_paths valida os 8 paths novos; suite completa verde
</success_criteria>

<output>
Create `.planning/phases/09-concilia-o-relat-rios-mcp/09-04-SUMMARY.md` when done
</output>
