---
phase: 09-concilia-o-relat-rios-mcp
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/caramello/finances/models.py
  - dsl/entities/financial_entry.yaml
  - alembic/versions/0004_financial_entry_responsible_user.py
autonomous: true
requirements: [LAN-01, LAN-05]
user_setup: []

must_haves:
  truths:
    - "Coluna responsible_user_id existe em financial_entry após migration (D-SCHEMA-02)"
    - "FinancialEntry ORM expõe responsible_user_id nullable FK → user.id; responsible_user_uuid presente em todos os schemas de resposta como null quando não atribuído (D-ATTR-03)"
    - "alembic downgrade -1 remove a coluna sem erro"
  artifacts:
    - path: "alembic/versions/0004_financial_entry_responsible_user.py"
      provides: "Migration ADD COLUMN responsible_user_id"
      contains: "responsible_user_id"
    - path: "src/caramello/finances/models.py"
      provides: "Campo responsible_user_id em FinancialEntry"
      contains: "responsible_user_id"
  key_links:
    - from: "alembic/versions/0004_financial_entry_responsible_user.py"
      to: "0003_movement_schema_update"
      via: "down_revision"
      pattern: "down_revision.*0003"
    - from: "src/caramello/finances/models.py"
      to: "user.id"
      via: "foreign_key"
      pattern: "foreign_key=.user.id"
---

<objective>
Estender o schema de `financial_entry` com a coluna `responsible_user_id` (FK nullable → `user.id`) que sustenta a atribuição de responsável nos lançamentos (D-SCHEMA-01/02). Isto cobre a base de dados necessária para o payload de reconcile/PATCH (LAN-01, LAN-05) e para o relatório por membro do plano 04.

Purpose: Sem esta coluna, os endpoints de conciliação não conseguem persistir o responsável e o relatório por membro não tem como agrupar. A migration é simples (uma coluna nullable), mas o `down_revision` precisa de verificação para não criar fork no histórico Alembic (pitfall P4/P6).
Output: Migration `0004_financial_entry_responsible_user.py`, campo `responsible_user_id` em `FinancialEntry`, e YAML DSL atualizado para manter o gerador consistente.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-concilia-o-relat-rios-mcp/09-CONTEXT.md
@.planning/phases/09-concilia-o-relat-rios-mcp/09-PATTERNS.md
@.planning/phases/09-concilia-o-relat-rios-mcp/09-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Adicionar responsible_user_id ao modelo FinancialEntry + atualizar YAML DSL</name>
  <files>src/caramello/finances/models.py, dsl/entities/financial_entry.yaml</files>
  <behavior>
    - `from caramello.finances import models` importa sem erro
    - `FinancialEntry.responsible_user_id` existe como campo opcional (default None)
  </behavior>
  <read_first>
    - src/caramello/finances/models.py (classe FinancialEntry, linhas ~107-136; cabeçalho `# CARAMELLO-GENERATED: implemented`; padrão de Field com foreign_key)
    - dsl/entities/financial_entry.yaml (definição DSL atual da entidade)
    - .planning/phases/09-concilia-o-relat-rios-mcp/09-PATTERNS.md (seção "models.py" — posição exata do campo: inserir após `is_recorrente`, antes de `created_at`; NÃO mexer em `__table_args__`)
  </read_first>
  <action>
    Editar `src/caramello/finances/models.py` (edição manual permitida pois o arquivo tem `# CARAMELLO-GENERATED: implemented`). Dentro de `class FinancialEntry(SQLModel, table=True)`, inserir após o campo `is_recorrente` e antes de `created_at` um campo `responsible_user_id` do tipo `int | None`, com `default=None`, `foreign_key="user.id"`, `nullable=True` (per D-SCHEMA-01/02). NÃO alterar `__table_args__` nem nenhum campo existente.

    Atualizar `dsl/entities/financial_entry.yaml` adicionando o campo correspondente `responsible_user` / `responsible_user_id` nullable FK para `user`, mantendo o YAML como fonte de verdade do gerador (consistência DSL-first, regra do AGENTS.md). Não regenerar o arquivo de models automaticamente (a edição manual acima já reflete a anotação `implemented`); o YAML é atualizado para que uma regeneração futura não perca o campo.
  </action>
  <verify>
    <automated>uv run python -c "from caramello.finances.models import FinancialEntry; assert 'responsible_user_id' in FinancialEntry.model_fields, 'campo ausente'; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'responsible_user_id' src/caramello/finances/models.py` retorna 0
    - `grep -q 'foreign_key=' src/caramello/finances/models.py` (linha do novo campo aponta para user.id)
    - `uv run python -c "from caramello.finances.models import FinancialEntry; assert 'responsible_user_id' in FinancialEntry.model_fields"` sai com código 0
    - `dsl/entities/financial_entry.yaml` menciona `responsible_user`
    - `__table_args__` de FinancialEntry permanece inalterado (sem novos Index)
  </acceptance_criteria>
  <done>Campo `responsible_user_id` presente no ORM como FK nullable para `user.id`; YAML DSL atualizado; import do módulo sem erro.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Criar migration 0004 ADD COLUMN responsible_user_id</name>
  <files>alembic/versions/0004_financial_entry_responsible_user.py</files>
  <read_first>
    - alembic/versions/0003_movement_schema_update.py (estrutura completa de migration: header docstring, `revision`/`down_revision`/`branch_labels`/`depends_on`, upgrade/downgrade; confirmado como head atual com revision="0003")
    - .planning/phases/09-concilia-o-relat-rios-mcp/09-PATTERNS.md (seção migration 0004 — estrutura exata; nota sobre coluna nullable não precisar de server_default)
    - .planning/phases/09-concilia-o-relat-rios-mcp/09-RESEARCH.md (Code Examples: migration 0004; pitfall P4 sobre down_revision)
  </read_first>
  <action>
    Antes de escrever a migration, executar `uv run alembic history --verbose` para confirmar que `0003` é a head atual (pitfall P6/P4). Criar `alembic/versions/0004_financial_entry_responsible_user.py` seguindo a estrutura de `0003`:
    - `revision: str = "0004"`
    - `down_revision: str | Sequence[str] | None = "0003"` (DEVE apontar para `0003`, nunca `0002`)
    - `branch_labels = None`, `depends_on = None`
    - `upgrade()`: `op.add_column("financial_entry", sa.Column("responsible_user_id", sa.Integer(), sa.ForeignKey("user.id"), nullable=True))`
    - `downgrade()`: `op.drop_column("financial_entry", "responsible_user_id")`

    A coluna é nullable — NÃO usar `server_default` (linhas existentes ficam NULL automaticamente; sem data migration). Como o sandbox pode não ter PostgreSQL disponível, validar a sintaxe e a cadeia de revisões via `alembic history`; a aplicação real (`alembic upgrade head` / `downgrade -1`) será verificada em banco quando disponível.
  </action>
  <verify>
    <automated>uv run python -c "import importlib.util; spec=importlib.util.spec_from_file_location('m','alembic/versions/0004_financial_entry_responsible_user.py'); m=importlib.util.module_from_spec(spec); spec.loader.exec_module(m); assert m.revision=='0004'; assert m.down_revision=='0003', m.down_revision; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `alembic/versions/0004_financial_entry_responsible_user.py` existe
    - Arquivo contém `revision: str = "0004"`
    - Arquivo contém `down_revision` com valor `"0003"`
    - Arquivo contém `op.add_column` e `responsible_user_id`
    - Arquivo contém `op.drop_column` em `downgrade`
    - `uv run alembic history` mostra cadeia 0001→0002→0003→0004 com head único (sem fork)
  </acceptance_criteria>
  <done>Migration 0004 criada com down_revision="0003"; cadeia Alembic linear sem fork; ADD/DROP COLUMN nullable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration → DB schema | DDL executado contra o banco de produção/dev altera estrutura persistente |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-02 | Denial of Service | migration 0004 (fork de histórico Alembic) | mitigate | `down_revision="0003"` verificado com `alembic history --verbose` antes de gerar; coluna nullable sem server_default evita lock de tabela em reescrita |
| T-09-03 | Tampering | FK responsible_user_id → user.id | accept | Validação de membership feita no service (D-SCHEMA-02), não no banco; FK garante integridade referencial básica |
</threat_model>

<verification>
- `uv run python -c "from caramello.finances.models import FinancialEntry"` sai 0
- `uv run alembic history` mostra head único `0004`
- `uv run pytest` permanece verde (stubs da fase 9 ainda skipados)
</verification>

<success_criteria>
- `responsible_user_id` no ORM `FinancialEntry` como FK nullable → user.id
- Migration `0004` com `down_revision="0003"`, ADD COLUMN nullable + DROP no downgrade
- YAML DSL atualizado com o campo
- Cadeia Alembic linear sem fork
</success_criteria>

<output>
Create `.planning/phases/09-concilia-o-relat-rios-mcp/09-02-SUMMARY.md` when done
</output>
