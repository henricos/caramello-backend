---
phase: 06-funda-o-dsl-schema
plan: 02
type: execute
wave: 2
depends_on:
  - "06-01"
files_modified:
  - dsl/entities/account.yaml
  - dsl/entities/movement.yaml
  - dsl/entities/financial_entry.yaml
  - dsl/entities/category.yaml
  - dsl/entities/subcategory.yaml
  - dsl/manifest.yaml
  - dsl/operations/finances.yaml
  - src/caramello/finances/__init__.py
  - src/caramello/finances/models.py
  - src/caramello/finances/router.py
  - src/caramello/finances/operations.py
  - tests/test_generator.py
autonomous: true
requirements:
  - "Nenhum requisito funcional direto — habilita SC-4, SC-5, SC-6 do ROADMAP Phase 6; modela ACC/CAT/MOV/LAN/REL (implementados em Phases 7-9)"
must_haves:
  truths:
    - "Existem 5 YAMLs DSL em dsl/entities/ com `domain: finances`: account, movement, financial_entry, category, subcategory"
    - "Movement tem campo `amount: Decimal` e `import_hash` com `unique: true`; FinancialEntry tem `movement_id` com `unique: true`, `competencia_year`, `competencia_month`, `subcategory_id`"
    - "Subcategory tem FK `category_id → category.id` e NÃO tem campo apontando para outra Subcategory (CAT-03 estrutural / D-09)"
    - "FinancialEntry NÃO tem campo de valor próprio — sem `amount`/`type` (D-05)"
    - "src/caramello/finances/models.py importa sem ImportError e contém as 5 classes table=True"
  artifacts:
    - path: "dsl/entities/movement.yaml"
      provides: "Definição da entidade Movement com amount Decimal + import_hash unique"
      contains: "type: Decimal"
    - path: "dsl/entities/financial_entry.yaml"
      provides: "Definição de FinancialEntry: classificação sem valor próprio"
      contains: "competencia_year"
    - path: "src/caramello/finances/models.py"
      provides: "Modelos gerados das 5 entidades financeiras"
      contains: "Numeric(15, 2)"
    - path: "dsl/manifest.yaml"
      provides: "Registro dos 5 YAMLs financeiros"
      contains: "account.yaml"
  key_links:
    - from: "dsl/entities/subcategory.yaml"
      to: "category.id"
      via: "foreign_key"
      pattern: "foreign_key:.*category\\.id"
    - from: "src/caramello/finances/models.py"
      to: "Decimal / Numeric(15, 2)"
      via: "gerador (campo amount)"
      pattern: "Numeric\\(15, 2\\)"
---

<objective>
Criar os 5 YAMLs DSL do domínio financeiro (`account`, `movement`, `financial_entry`, `category`, `subcategory`, todos `domain: finances`), registrá-los em `dsl/manifest.yaml`, criar o stub `dsl/operations/finances.yaml`, executar o gerador, e validar o código gerado em `src/caramello/finances/` com testes unitários (Wave 0).

Purpose: Os YAMLs são a fonte de verdade do schema financeiro. A hierarquia de categorias usa duas entidades separadas (`Category` + `Subcategory`, D-06/D-07) — eliminando o self-referencial e qualquer pós-processamento manual de models.py. `FinancialEntry` herda valor/tipo de `Movement` (D-05). Esta é a base sobre a qual a migration 0002 (Plano 03) será gerada.
Output: 5 YAMLs + manifest atualizado + operations stub + `src/caramello/finances/{__init__,models,router,operations}.py` gerados + testes Wave 0.

Nota de escopo: este plano substitui a abordagem do ROADMAP original (4 entidades, `category.yaml` self-referencial) por 5 entidades (Category + Subcategory) conforme D-06. CAT-01/02/03/04 do REQUIREMENTS mapeiam agora para duas entidades; a implementação funcional dessas regras ocorre na Phase 7.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-funda-o-dsl-schema/06-CONTEXT.md
@.planning/phases/06-funda-o-dsl-schema/06-RESEARCH.md
@.planning/phases/06-funda-o-dsl-schema/06-PATTERNS.md
@.planning/phases/06-funda-o-dsl-schema/06-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar os 5 YAMLs financeiros + manifest + operations stub</name>
  <files>dsl/entities/account.yaml, dsl/entities/movement.yaml, dsl/entities/financial_entry.yaml, dsl/entities/category.yaml, dsl/entities/subcategory.yaml, dsl/manifest.yaml, dsl/operations/finances.yaml</files>
  <read_first>
    - dsl/entities/family.yaml (template canônico de entidade: campos padrão id/uuid/created_at/updated_at, bloco relationships)
    - dsl/entities/family_invitation.yaml (padrão de entidade com foreign_key cross-domain para family.id e user.id)
    - dsl/manifest.yaml (lista atual x-caramello-entities com 4 entradas)
    - dsl/operations/family.yaml (template de arquivo de operações: domain + operations[name/method/path/description])
    - .planning/phases/06-funda-o-dsl-schema/06-RESEARCH.md (Code Examples: Account YAML completo; Pattern 1 campo Decimal; Pattern 4 operations stub)
    - .planning/phases/06-funda-o-dsl-schema/06-PATTERNS.md (seções account/movement/financial_entry/category/subcategory com fragmentos de FK e unique)
    - .planning/REQUIREMENTS.md (Glossário do Domínio + ACC/CAT/MOV/LAN para campos corretos)
  </read_first>
  <action>
    Criar os 5 YAMLs em dsl/entities/, todos com `domain: finances` e os 4 campos padrão (`id` int primary_key; `uuid` UUID unique default_factory uuid4 nullable false; `created_at`/`updated_at` datetime default_factory now_utc nullable false):
    - account.yaml (table_name `account`): `family_id` int FK `family.id` nullable false; `name` str max_length 100 nullable false; `type` str max_length 20 nullable false (corrente/poupanca/cartao/investimento); `currency` str max_length 3 default "BRL" nullable false; `is_active` bool default true nullable false. Bloco `filters: [{fields: [family_id]}]`.
    - movement.yaml (table_name `movement`): `account_id` int FK `account.id` nullable false; `type` str max_length 10 nullable false (credito/debito); `date` datetime nullable false; `amount` Decimal nullable false; `description` str max_length 255 nullable false; `import_hash` str unique true nullable true (D-10 dedup); `is_duplicate` bool default false nullable false (MOV-05). Bloco `filters: [{fields: [account_id]}]`.
    - financial_entry.yaml (table_name `financial_entry`): `movement_id` int FK `movement.id` unique true nullable false (D-05 relação 1:1, P5); `subcategory_id` int FK `subcategory.id` nullable false (D-08); `competencia_year` int nullable false; `competencia_month` int nullable false (D-04, 1-12); `notes` str max_length 500 nullable true; `is_recorrente` bool default false nullable false (LAN-04). SEM campo `amount`/`type` próprio (D-05). Bloco `filters: [{fields: [competencia_year, competencia_month]}, {fields: [subcategory_id]}]`.
    - category.yaml (table_name `category`): `family_id` int FK `family.id` nullable false; `name` str max_length 100 nullable false. Bloco `filters: [{fields: [family_id]}]`.
    - subcategory.yaml (table_name `subcategory`): `category_id` int FK `category.id` nullable false (D-06 hierarquia 2 níveis); `name` str max_length 100 nullable false. SEM campo apontando para outra Subcategory (D-09 — CAT-03 enforced pelo schema). Bloco `filters: [{fields: [category_id]}]`.
    Em dsl/manifest.yaml, adicionar as 5 entradas após `family_invitation.yaml`: `account.yaml`, `movement.yaml`, `financial_entry.yaml`, `category.yaml`, `subcategory.yaml`.
    Criar dsl/operations/finances.yaml com `domain: finances` e uma operação placeholder (ex: `list_accounts` GET `/finances/account`) seguindo o template de family.yaml.
    NÃO declarar relationships cross-domain de finances → users/families nem o inverso (Pitfall 4 — evita ciclo de import). Usar apenas `foreign_key:` por campo.
  </action>
  <verify>
    <automated>uv run python -c "import yaml; files=['account','movement','financial_entry','category','subcategory']; assert all(yaml.safe_load(open(f'dsl/entities/{f}.yaml'))['domain']=='finances' for f in files), 'domain finances ausente'; print('5 YAMLs finances OK')"</automated>
  </verify>
  <acceptance_criteria>
    - Os 5 arquivos existem em dsl/entities/ e cada um tem `domain: finances`
    - movement.yaml contém `type: Decimal` no campo amount e `unique: true` no campo import_hash
    - financial_entry.yaml contém `competencia_year`, `competencia_month`, `subcategory_id`, e `unique: true` no campo movement_id; NÃO contém um campo `amount` nem um campo `type`
    - subcategory.yaml contém `foreign_key: "category.id"` e nenhum campo com `foreign_key: "subcategory.id"`
    - dsl/manifest.yaml lista as 5 novas entradas
    - dsl/operations/finances.yaml existe com `domain: finances`
  </acceptance_criteria>
  <done>5 YAMLs financeiros válidos, registrados no manifest, com operations stub criado.</done>
</task>

<task type="auto">
  <name>Task 2: Executar gerador + validar código de finances (Wave 0 tests)</name>
  <files>src/caramello/finances/__init__.py, src/caramello/finances/models.py, src/caramello/finances/router.py, src/caramello/finances/operations.py, tests/test_generator.py</files>
  <read_first>
    - scripts/generate_code.py (função `main` linhas 838+; entrada de geração e gravação por domínio)
    - bin/generate_code (wrapper shell — comando de geração)
    - src/caramello/families/models.py (formato esperado das 4 classes por entidade; imports gerados)
    - src/caramello/families/operations.py (anotação `# CARAMELLO-GENERATED` na primeira linha)
    - tests/test_generator.py (padrões `test_family_yamls_have_domain_field` linhas 26-33 e `test_family_models_consolidated` linhas 82-89)
    - .planning/phases/06-funda-o-dsl-schema/06-VALIDATION.md (Wave 0 Requirements — testes a criar)
    - docs/dev.md (comando exato de geração e validação)
  </read_first>
  <action>
    Executar o gerador via `bin/generate_code` (ou `uv run python scripts/generate_code.py` conforme docs/dev.md). Confirmar que `src/caramello/finances/` é criado com `__init__.py`, `models.py`, `router.py`, `operations.py`. Validar com `uv run python -c "from caramello.finances import models; print('OK')"` (SC-4). Rodar `bin/validate_generation` se disponível.
    Adicionar a `tests/test_generator.py` três testes Wave 0 (seguindo os padrões de teste existentes):
    - `test_finances_yamls_have_domain_finances`: para cada um dos 5 YAMLs, `data.get("domain") == "finances"` (SC-5).
    - `test_finances_models_no_float`: o conteúdo de `src/caramello/finances/models.py` NÃO contém a substring `: float` nem `Float(` em campos monetários — o campo amount usa `Numeric(15, 2)` (SC-6).
    - `test_finances_models_import_ok`: importar `from caramello.finances import models` não levanta ImportError (SC-4).
    Verificar que `src/caramello/finances/models.py` contém `class Movement(SQLModel, table=True)`, `Numeric(15, 2)`, `from decimal import Decimal`, e `__table_args__` com pelo menos um `Index(`. Rodar a suite completa do gerador.
  </action>
  <verify>
    <automated>uv run python -c "from caramello.finances import models; print('OK')" && uv run pytest tests/test_generator.py::test_finances_yamls_have_domain_finances tests/test_generator.py::test_finances_models_no_float tests/test_generator.py::test_finances_models_import_ok -v</automated>
  </verify>
  <acceptance_criteria>
    - `from caramello.finances import models` imprime OK sem ImportError (SC-4)
    - src/caramello/finances/models.py contém `class Movement(SQLModel, table=True):`, `Numeric(15, 2)`, `from decimal import Decimal`, e `__table_args__`
    - src/caramello/finances/models.py NÃO contém `: float` em campos de valor (SC-6)
    - src/caramello/finances/operations.py primeira linha == `# CARAMELLO-GENERATED: stub`
    - Os 3 testes Wave 0 passam: test_finances_yamls_have_domain_finances, test_finances_models_no_float, test_finances_models_import_ok
    - `uv run pytest tests/test_generator.py -v` integralmente verde
  </acceptance_criteria>
  <done>Código de finances gerado, importável sem erro, com Decimal/Numeric/__table_args__; 3 testes Wave 0 verdes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| YAML DSL → código gerado | Definição de schema confiável (build-time); FK cross-domain finances→users/families é unidirecional |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03 | Tampering | amount gerado como FLOAT | mitigate | YAML usa `type: Decimal`; teste `test_finances_models_no_float` bloqueia FLOAT em models.py |
| T-06-04 | Denial of Service | Import circular finances↔users/families quebra startup | mitigate | YAMLs usam apenas `foreign_key:` por campo, sem relationships cross-domain (Pitfall 4); import-check SC-4 prova ausência de ciclo |
| T-06-05 | Elevation of Privilege | Subcategoria filha de subcategoria (>2 níveis) | mitigate | D-09: Subcategory sem FK para subcategory — restrição estrutural (CAT-03) verificada por acceptance criterion |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | Nenhum pacote externo novo nesta fase (RESEARCH §Package Legitimacy Audit: não aplicável) |
</threat_model>

<verification>
- `uv run python -c "from caramello.finances import models; print('OK')"` (SC-4)
- `uv run pytest tests/test_generator.py -v` verde (Wave 0 completa: 5 testes novos + preexistentes)
- Nenhum pacote novo instalado
</verification>

<success_criteria>
SC-4 (import sem ImportError), SC-5 (`domain: finances` nos 5 YAMLs), SC-6 (sem float) do ROADMAP §Phase 6 verificados. Código gerado pronto para a migration 0002 no Plano 03.
</success_criteria>

<output>
Create `.planning/phases/06-funda-o-dsl-schema/06-02-SUMMARY.md` when done
</output>
