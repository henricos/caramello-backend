---
phase: 06-funda-o-dsl-schema
plan: 03
type: execute
wave: 3
depends_on:
  - "06-02"
files_modified:
  - alembic/env.py
  - alembic/versions/0002_finances_schema.py
autonomous: false
requirements:
  - "Nenhum requisito funcional direto — habilita SC-1, SC-2, SC-3 do ROADMAP Phase 6 (schema financeiro no banco)"
must_haves:
  truths:
    - "`SQLModel.metadata.naming_convention = {...}` está definido em alembic/env.py ANTES de qualquer import de modelo (Pitfall 6)"
    - "alembic/env.py importa Account, Category, FinancialEntry, Movement, Subcategory de caramello.finances.models"
    - "A migration 0002 tem `down_revision = \"0001\"` e a cadeia history é linear None→0001→0002 (head) (Pitfall P6)"
    - "`alembic upgrade head` cria as 5 tabelas com NUMERIC(15,2) em amount, UNIQUE em movement_id e import_hash, e os índices declarados"
    - "`alembic downgrade -1` remove as 5 tabelas completamente sem erro"
  artifacts:
    - path: "alembic/env.py"
      provides: "naming_convention + imports dos modelos finances para autogenerate"
      contains: "naming_convention"
    - path: "alembic/versions/0002_finances_schema.py"
      provides: "Migration do schema financeiro completo"
      contains: "Numeric(precision=15, scale=2)"
  key_links:
    - from: "alembic/env.py"
      to: "caramello.finances.models"
      via: "import para autogenerate"
      pattern: "from caramello.finances.models import"
    - from: "alembic/versions/0002_finances_schema.py"
      to: "0001"
      via: "down_revision"
      pattern: "down_revision.*=.*0001"
---

<objective>
Preparar o Alembic e aplicar o schema financeiro no banco: adicionar `naming_convention` em `alembic/env.py` ANTES dos imports de modelo (D-11/Pitfall 6), importar os 5 modelos de `caramello.finances.models` para o autogenerate, gerar a migration `0002_finances_schema.py`, verificar a cadeia de revisão (Pitfall P6), e validar `upgrade`/`downgrade` em banco real.

Purpose: A ordem é crítica — se a migration for gerada antes da `naming_convention`, as constraints recebem nomes automáticos do PostgreSQL e corrigi-los exige DROP+CREATE. Esta é a entrega final da fase: o schema financeiro no banco, base de toda a lógica de negócio das Phases 7-9.
Output: `alembic/env.py` modificado, `alembic/versions/0002_finances_schema.py` aplicado, schema financeiro materializado e reversível.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-funda-o-dsl-schema/06-CONTEXT.md
@.planning/phases/06-funda-o-dsl-schema/06-RESEARCH.md
@.planning/phases/06-funda-o-dsl-schema/06-PATTERNS.md
@.planning/phases/06-funda-o-dsl-schema/06-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: naming_convention + imports finances em alembic/env.py; gerar migration 0002</name>
  <files>alembic/env.py, alembic/versions/0002_finances_schema.py</files>
  <read_first>
    - alembic/env.py (INTEGRAL — ordem atual dos imports linhas 19-29: `from sqlmodel import SQLModel`, imports de families/users, `target_metadata = SQLModel.metadata`)
    - alembic/versions/0001_initial_schema.py (header de revisão: `revision = "0001"`, `down_revision = None`; padrão de upgrade/downgrade)
    - .planning/phases/06-funda-o-dsl-schema/06-RESEARCH.md (Pattern 3 naming_convention exato; Pitfall 1 down_revision; Pitfall 6 ordem; exemplo `alembic/env.py com naming_convention`)
    - .planning/phases/06-funda-o-dsl-schema/06-PATTERNS.md (seção alembic/env.py — ordem obrigatória; seção 0002_finances_schema.py — padrão upgrade/downgrade, NUMERIC, op.create_index)
    - bin/manage_db (wrapper dos comandos alembic — migrate/upgrade/downgrade)
    - docs/dev.md (comandos de migration e variáveis de conexão ao banco)
  </read_first>
  <action>
    Em alembic/env.py, inserir IMEDIATAMENTE após `from sqlmodel import SQLModel` (e ANTES de qualquer `from caramello.*.models import`, Pitfall 6) o dict `SQLModel.metadata.naming_convention` com as chaves: `ix = "ix_%(column_0_label)s"`, `uq = "uq_%(table_name)s_%(column_0_name)s"`, `ck = "ck_%(table_name)s_%(constraint_name)s"`, `fk = "fk_%(table_name)s_%(column_0_name)s_%(referred_table_name)s"`, `pk = "pk_%(table_name)s"` (valores exatos do Pattern 3 do RESEARCH).
    Depois dos imports existentes de families/users, adicionar `from caramello.finances.models import (Account, Category, FinancialEntry, Movement, Subcategory)` com `# noqa: E402, F401`.
    Gerar a migration via `bin/manage_db migrate` (ou `uv run alembic revision --autogenerate -m "0002_finances_schema"`). Garantir o nome do arquivo `alembic/versions/0002_finances_schema.py`. Editar o header gerado para `revision = "0002"` e `down_revision = "0001"`.
    Verificar `uv run alembic history --verbose` — a cadeia DEVE ser linear `None → 0001 → 0002 (head)`; se aparecerem dois heads ou `down_revision` errado, corrigir o valor no arquivo (Pitfall 1/P6).
    Inspecionar o `upgrade()` autogerado: confirmar que `amount` é `sa.Numeric(precision=15, scale=2)`, que há unique em `movement_id` (1:1, P5) e em `import_hash` (dedup), e que os índices `ix_*` declarados nos `filters:` aparecem como `op.create_index`. Confirmar que `downgrade()` faz drop na ordem reversa de FK (financial_entry → subcategory → category, e financial_entry/movement → account).
  </action>
  <verify>
    <automated>uv run python -c "src=open('alembic/env.py').read(); assert src.index('naming_convention') < src.index('from caramello.finances.models import'), 'naming_convention deve vir antes do import de finances.models'; print('ordem env.py OK')" && grep -q '0001' alembic/versions/0002_finances_schema.py && grep -Eq 'Numeric\(precision=15, scale=2\)|Numeric\(15, 2\)' alembic/versions/0002_finances_schema.py && echo "migration 0002 OK"</automated>
  </verify>
  <acceptance_criteria>
    - alembic/env.py: a linha `naming_convention` aparece antes da linha `from caramello.finances.models import` (verificado por índice de string)
    - alembic/env.py contém `from caramello.finances.models import` com Account, Category, FinancialEntry, Movement, Subcategory
    - alembic/versions/0002_finances_schema.py contém `down_revision` com valor `"0001"`
    - alembic/versions/0002_finances_schema.py contém `Numeric(precision=15, scale=2)` (ou `Numeric(15, 2)`)
    - `uv run alembic history --verbose` mostra cadeia linear None→0001→0002 (head) sem múltiplos heads
    - upgrade() cria as 5 tabelas; downgrade() as remove em ordem reversa de FK
  </acceptance_criteria>
  <done>env.py com naming_convention na posição correta + imports finances; migration 0002 gerada com down_revision=0001, NUMERIC(15,2) e índices; history linear.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verificar upgrade/downgrade da migration 0002 em banco real (SC-1/SC-2/SC-3)</name>
  <files>alembic/versions/0002_finances_schema.py (aplicada ao banco caramello_dev — sem edição de arquivo)</files>
  <action>
    Aplicar e reverter a migration 0002 no banco PostgreSQL real `caramello_dev` para provar SC-1/SC-2/SC-3. O executor roda os comandos CLI (`uv run alembic upgrade head`, inspeção psql, `uv run alembic downgrade -1`, reaplicar upgrade) e então pausa para confirmação humana do resultado conforme <how-to-verify>. Nenhum arquivo é editado nesta task — é verificação de runtime de banco.
  </action>
  <what-built>
    Migration `0002_finances_schema.py` gerada e validada estruturalmente na Task 1. Esta verificação exige conexão PostgreSQL real (`caramello_dev`) — não há fallback (SQLite não é suportado). O executor DEVE primeiro tentar aplicar via CLI (`bin/manage_db upgrade` / `uv run alembic upgrade head`); este checkpoint confirma o resultado após a automação.
  </what-built>
  <how-to-verify>
    1. Garantir que o banco `caramello_dev` está acessível (variáveis DB_* em `.env`; ver docs/dev.md e bin/setup_db).
    2. Executar `uv run alembic upgrade head` — deve concluir sem erro (SC-1).
    3. Inspecionar o schema via psql: `\d+ account`, `\d+ movement`, `\d+ financial_entry`, `\d+ category`, `\d+ subcategory`. Confirmar (SC-3):
       - `movement.amount` é `NUMERIC(15,2)`
       - `UNIQUE` em `movement.import_hash` e em `financial_entry.movement_id`
       - índices `ix_account_family_id`, `ix_movement_account_id`, `ix_financial_entry_competencia_year_competencia_month` (ou equivalentes pela naming_convention) existem
       - `financial_entry` NÃO tem coluna `amount` nem `type` (D-05)
    4. Executar `uv run alembic downgrade -1` — deve reverter sem erro e remover as 5 tabelas (SC-2).
    5. Reaplicar `uv run alembic upgrade head` para deixar o banco no estado final.
  </how-to-verify>
  <verify>
    <human-check>upgrade head sem erro (SC-1); tabelas/constraints/tipos corretos via psql (SC-3); downgrade -1 reverte limpo (SC-2); reaplicação final OK</human-check>
  </verify>
  <acceptance_criteria>
    - `uv run alembic upgrade head` retorna código 0 (SC-1)
    - `\d+ movement` mostra `amount` como `numeric(15,2)` (SC-3)
    - `\d+ movement` e `\d+ financial_entry` mostram constraint UNIQUE em import_hash e movement_id (SC-3)
    - `\d+ financial_entry` NÃO lista coluna amount/type (D-05/SC-3)
    - `uv run alembic downgrade -1` remove as 5 tabelas sem erro (SC-2)
  </acceptance_criteria>
  <resume-signal>Digite "approved" após confirmar upgrade/downgrade OK no banco, ou descreva o erro encontrado.</resume-signal>
  <done>Migration 0002 aplica (SC-1), tabelas/colunas/constraints corretas via psql (SC-3) e reverte limpa (SC-2) no banco caramello_dev; banco deixado em head após reaplicação.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Migration Alembic → PostgreSQL | DDL aplicado ao banco; constraints e tipos definem a integridade dos dados financeiros downstream |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-06 | Tampering | Branch no histórico Alembic (down_revision errado) | mitigate | `down_revision = "0001"` verificado por grep + `alembic history --verbose` linear (Pitfall 1/P6) |
| T-06-07 | Repudiation | Migration não reversível (downgrade quebra) | mitigate | Checkpoint valida `alembic downgrade -1` em banco real (SC-2); drop em ordem reversa de FK |
| T-06-08 | Tampering | amount persistido sem precisão (FLOAT) corrompe valores | mitigate | NUMERIC(15,2) verificado via `\d+ movement` no checkpoint (SC-3) |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | Nenhum pacote externo novo nesta fase (RESEARCH §Package Legitimacy Audit: não aplicável) |
</threat_model>

<verification>
- Task 1 (automático): ordem env.py + down_revision + NUMERIC verificados por script/grep
- Task 2 (checkpoint humano): SC-1 upgrade, SC-2 downgrade, SC-3 inspeção de schema em banco real
- Phase gate: `uv run pytest -v` verde + `uv run alembic upgrade head` + `uv run alembic downgrade -1`
</verification>

<success_criteria>
SC-1 (upgrade head sem erro), SC-2 (downgrade -1 reverte), SC-3 (tabelas/colunas/constraints corretas) do ROADMAP §Phase 6 verificados em banco real. Schema financeiro materializado e reversível — fase concluída.
</success_criteria>

<output>
Create `.planning/phases/06-funda-o-dsl-schema/06-03-SUMMARY.md` when done
</output>
